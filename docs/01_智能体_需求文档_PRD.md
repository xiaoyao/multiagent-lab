# eino-multiagent-lab · 智能体平台 需求文档（PRD）

> 状态：Draft v0.53
> 日期：2026-09-26
> 负责人：董奎
> 文档性质：**活文档**。每个迭代开始前更新「迭代记录」与「需求池」，已确认需求不要直接删除，改为在条目后标注 `（已变更/已移交，见 vX.X）`。
> 配套文档：《02_智能体_技术方案设计.md》界面口径事实源见《17_产品_信息架构与界面设计.md》（原 prototype/ 交互原型已移除，2026-09-23 整理轮；历史见 git）；本体模块：《04_本体_方案设计.md》（**独立维护**，v0.6，构建/运行解耦）；知识库模块：《11_知识库_需求文档.md》《12_知识库_方案设计.md》（**独立维护**，2026-09-21 起自本文档/02 拆分）。
> **会话分工（v0.6，2026-09-20 确认）**：本会话专注**多智能体平台**（智能体管理/知识库/技能/项目/**本体对接**）；本体模块的**实现**（构建平面、运行平面、运行方案、可视化浏览等）由本体专属会话维护，见上述本体文档；**对接本体的方案保留在本文档**（§3.8、REQ-97/98）。
> v0.1~v0.18 变更要点已压缩（2026-09-23 整理轮，主人授权）——逐版变更全文见文末「§10 迭代记录」与 git 历史；关键历史决策：v0.5 模块再梳理（沙箱/技能/折叠树立项）、v0.7 OO 独立集成、v0.9 前端全蚂蚁生态、v0.13 知识库需求独立成档（11 号）。
> v0.19 变更要点（2026-09-23，模型管理预设增强实现回写，REQ-104/105/106 全项交付；版本号续接 §3.4 表 v0.18 回写注记）：①**REQ-104**——后端迁移 011 预置百度千帆 `embeddings-v1` 向量连接候选（仅当库内无 embedding 连接时插入；Key 留空、停用态标记 is_default=1，`GetDefaultConnection` 过滤停用连接故不阻断知识库，填 Key 启用即为默认向量连接，呼应 REQ-46）；②**REQ-105**——前端静态厂商预设清单 `web/src/api/providerPresets.ts`（DeepSeek/阿里百炼/百度千帆/智谱 GLM/Kimi/硅基流动/MiniMax/讯飞星火 + 千帆向量共 9 条，预填协议与 BaseURL 及建议模型名；实现形式按需求说明取前端静态，清单纯数据驱动可增删）；③**REQ-106**——添加提供商弹窗顶部「从厂商预设快速填充」（自动填充名称/协议/BaseURL/建议模型/类型 + Key 申请入口），保存成功后自动展开该提供商行并挂 REQ-48 自动发现面板（串联拉取可用模型批量建连）；添加模型弹窗无提供商时空态引导。§3.4、§6 同步；技术方案见 02 v0.34。
> v0.20 变更要点（2026-09-23，REQ-102 Git 视图深度版实现回写，M12 全项收尾）：Git 视图从状态概览升级为**分支 + 提交历史 + 变更明细**——①分支 chips（本地高亮/远程虚线，点击切换 log 范围）；②提交历史时间线（rail 圆点、合并提交菱形标识、refs 装饰章、作者与时间，limit≤200）；③点击提交展开变更文件明细（numstat +/-，二进制标注）；④点击文件/「查看完整 diff」→ patch 预览（≤200KB）；⑤工作区未提交变更明细（porcelain 状态 + numstat）。后端新增五端点 git-log / git-branches / git-commit-files / git-commit-patch / git-working（修订参数防注入校验；顺带修复 gitOutput TrimSpace 吃掉 porcelain 首行状态空格、core.quotepath 中文路径转义两处既有缺陷）。§3.1 表、§6 同步；技术方案见 02 v0.35。
> v0.21 变更要点（2026-09-23，产品规划补强——主人认可缺口分析并授权全项落实）：①**需求编号治理**——REQ-90~93（技能/挂载/沙箱/Agent 级配置）及 REQ-94~96（折叠树/项目文件/对话产物）与本体学习增强段（90~96）冲突，按《18_REQ编号注册表》G-1 裁决迁移至 **REQ-120~126**（§3.3 误记的"REQ-90 执行后端"随迁移并入 REQ-122；本体侧 90~96 保留不变——全部 57 处代码引用均为本体语义，零迁移）；②**导航口径修订（REQ-111 转正）**——REQ-99 由"六模块"修订为**五项导航 + OpenOntologies 并入本体模块两入口**（构建路径 + 运行引导页；REQ-100 双轨可摘除不变；现状 TopNav 实装即五项）；③**REQ-111~115 产品横切需求立项**（导航收敛与首屏规范/模块空态引导/数据导出与生命周期/绑定目录安全口径/三态与错误文案规范，新增 §3.9）；④REQ-98 补注 KB GraphRAG KG 抽取复用本端点（11 v0.3/17 §3.1 口径回写）；⑤§7 安全行更新为"绑定目录即模型读写授权范围"（引 REQ-114）；⑥§9 需求池治理（触发条件/最后评估列；人工审批三处登记合并至 REQ-14 恢复语义）；⑦原型引用路径统一 `prototype/05`；编号治理详情见 18 号注册表。
> v0.23 变更要点（2026-09-23，REQ-14(恢复) 中断恢复实现交付，M11 收尾项落地）：**ask_human 人机协作中断恢复**——①新增内置工具 `ask_human`（勾选后 Agent 可发起"向用户提问"）：经 eino ADK `compose.StatefulInterrupt` 挂起运行并自动存 checkpoint，SSE 新事件 `run.interrupted`（question/choices/checkpoint_id），`run.finished.reason=interrupted`；②挂起状态落 `conversation.interrupt_state`（迁移 012），恢复端点 `POST /api/conversations/{id}/resume` 以答复按 `InterruptCtx.ID` 定向续跑（`ResumeWithParams`），工具重入经 `GetInterruptState`/`GetResumeContext` 取回状态与答复；③前端对话窗口中断答复卡（问题 + choices 快选 + 自由输入），事件流与运行同构；④新消息运行自动放弃挂起中断（checkpoint 清理 + 警告事件）。诚实边界：checkpoint 进程内存储，后端重启后挂起失效（可重新提问）。单测（stub 模型全链路）+ 真实模型端到端冒烟（中断→UI 答复→恢复续跑）全过；技术方案见 02 v0.38。

---

## 0. 阅读指引

- 本文档只回答 **"做什么、为什么做、做到什么程度算完成"**，不写具体代码实现（实现见技术方案）。
- 需求优先级：**P0**（本迭代必须）/ **P1**（重要，紧跟其后）/ **P2**（进阶）/ **P3**（有余力再做）。v0.6 起新增 P3 档。
- 每条需求带编号 `REQ-xx`：**全局编号分配以《18_REQ编号注册表》为唯一权威**。速览——REQ-01~59 平台基础（01）；REQ-50~59 知识库（11 维护）；REQ-60~88 本体（03 维护，60~73 为冻结快照）；REQ-90~96 本体学习增强（03）；REQ-99~110 两模块（见注册表逐条归属）；REQ-111~119 智能体侧横切与产品规范（01）；REQ-120~126 智能体侧技能/沙箱/Agent 配置/折叠树/项目文件/产物（自 90~96 迁移，G-1 裁决）；REQ-127~130 知识库 KG 增强（11）；REQ-131/132 Agent 对外服务化与配置页（01）；REQ-133+ 未分配，新需求先查注册表。
- 第 8 章为**关键决策记录**：已拍板项按结论执行，调整须在「迭代记录」留痕。
- **开源优先原则**：每个模块先评估现成开源方案再决定自研；自研仅保留学习价值无可替代的部分（详见 §8 Q-17/Q-18 与技术方案自研边界清单 §2.3）。

---

## 1. 项目背景与目标

### 1.1 一句话定位

用 **Go + CloudWeGo Eino（ADK）** 亲手做一个"迷你版 Coze"的多智能体协作平台，通过**从单 Agent 到多 Agent、从进程内运行到沙箱化、从纯对话到知识/技能/本体增强**的渐进式开发，系统掌握多智能体应用的工程化开发。

### 1.2 为什么做（学习目标，优先级高于功能堆砌）

| 编号 | 学习目标 | 对应功能落点 |
| --- | --- | --- |
| LG-1 | 理解 Eino Component 抽象（ChatModel / Tool / Prompt）与一次模型调用 | 最简对话 |
| LG-2 | 掌握 ChatModelAgent 的 ReAct 循环、Runner、事件流（AgentEvent） | Agent 对话、工具调用过程展示 |
| LG-3 | 掌握会话记忆与持久化（Memory / Session / 历史回放） | 多轮对话、对话历史保存与重开 |
| LG-4 | 掌握自定义 Tool 的开发与挂载 | Agent 配置工具、工具调用可视化 |
| LG-5 | 理解 Middleware / Callback 横切能力 | 耗时日志、token 统计、统一约束、Trace |
| LG-6 | **掌握多智能体协作**：AgentAsTool（推荐）与 Transfer（SetSubAgents）两种模式 | 项目配置多 Agent、路由/委派 |
| LG-7 | 理解确定性编排：Sequential / Parallel / Loop Agent | 项目工作流模式（P1） |
| LG-8 | 掌握人机协同 Interrupt / Resume + Checkpoint | 危险操作审批（P2） |
| LG-9 | 掌握流式输出到前端的完整链路（SSE + 事件协议） | 右侧对话详情的打字机/事件渲染 |
| LG-10 | 具备"配置驱动地动态装配 Agent 运行时"的工程能力 | 创建 Agent、项目配置 Agent、模型连接配置后实时生效 |
| LG-11 | **掌握 RAG 最小闭环**：文档加载/切分/Embedding/检索/上下文注入——v0.6 起**基于开源组件搭建**而非自研 | 知识库模块（P1） |
| LG-12 | 掌握结构化知识（本体）的按需查询与工具化检索 | 本体对接（细节见本体文档） |
| LG-13（v0.4） | **掌握 MCP 服务化与跨进程工具调用**：MCP Server/Client、多协议、跨进程一致性 | 本体对接 MCP facade、Agent 级 MCP 配置 |
| LG-14（v0.6，归属本体文档） | LLM 结构化生成与校验循环（本体 AI 创建） | 经主平台模型代理支撑（REQ-98），详见本体文档 REQ-82 |
| LG-15（v0.6） | **Agent 沙箱化执行**：Docker 容器 / K8s Pod 后端、镜像构建、配置下发、生命周期与资源限制 | REQ-122 执行后端（P2） |
| LG-16（v0.6） | **技能化能力封装**：技能包模型、提示词注入、工具合并、动态装配 | 技能管理（REQ-120/121） |

> **取舍原则**：功能为学习服务。每个功能都应能对应至少一个学习目标；不为了"像 Coze"而引入与学习目标无关的复杂度（如计费、多租户、插件市场）。

### 1.3 与真实 Coze 的边界（明确不做什么）

本项目是**学习项目**，刻意裁剪以下企业级能力，避免陷入业务细节：

- **不做**用户注册登录、账户/多租户、权限体系、组织架构（Q-5：单用户本地运行，聚焦核心功能）。
- **不做**插件市场、付费、审核发布流程、模板商店。
- **不做**可视化拖拽画布（Q-7：多 Agent 关系用**表单配置 + JSON**表达）。
- **不做**计费、审计、合规等企业运营能力。
- 知识库做**简单 RAG（v0.6 开源优先）**：**不做** PDF/OCR/表格抽取、Rerank/混合检索等进阶能力（留需求池）；向量存储与检索**优先用开源组件**（eino-ext 现成适配 + 开源向量库），自研实现仅作对照 fallback。
- 本体模块：**构建/运行实现移交本体文档**（REQ-80~88）；本文档只保留**对接契约**（§3.8）——对话挂载运行方案、MCP 接入、降级、模型代理。
- Agent 沙箱：**不做**多机集群调度、镜像仓库管理、自动扩缩容（本地 Docker/K8s 单机即可）。
- **不做**高并发、分布式、多副本等生产级非功能目标（见 §7）。

---

## 2. 目标用户与典型场景

**唯一用户：开发者本人（本地学习使用）。**

| 场景编号 | 场景 | 描述 |
| --- | --- | --- |
| SC-1 | 直接对话单个 Agent（v0.7 导航形态） | Agent 视图左栏 Agent 节点展开显示该 Agent 的**多个对话**（折叠树管理）；新建对话或在既有对话中继续提问，验证 Prompt/工具/技能/Agent 行为 |
| SC-2 | 创建项目并组建 Agent 团队 | 新建项目（如"K8s 运维助手"），为项目配置多个 Agent 与协作方式；项目节点下折叠显示其多个对话 |
| SC-3 | 在项目对话中沟通项目事项 | 在项目下新建对话，与项目配置好的 Agent 团队沟通，观察多 Agent 路由与协作过程 |
| SC-4 | 回看与复盘 | 重开历史对话，查看消息、工具调用、Agent 切换轨迹 |
| SC-5 | 配置模型连接与全局默认（P1） | 在「设置」维护多个 OpenAI 兼容连接（预置 DeepSeek），测试连通；可设**全局默认模型**，Agent 未单独指定时跟随默认（Agent 级覆盖为 P2） |
| SC-6 | 建知识库并在对话中引用（开源 RAG） | 新建知识库、导入文本、完成切分向量化（开源向量存储/检索组件）；对话选择该知识库提问，看到召回片段与来源 |
| SC-7 | 创建技能并供 Agent 使用（v0.6 新增） | 在「技能管理」创建技能（名称/描述/指令段/可带工具集）；Agent 配置中勾选技能（P2），运行时技能指令注入 + 工具合并生效 |
| SC-8 | 创建/导入本体并在对话中挂载运行方案（v0.6 对接契约） | 在本体模块（独立维护）构建本体、启动运行方案（Runtime Manager）；对话输入区打开"使用本体"并选择**运行方案**，Agent 经统一 MCP facade 查询本体（细节见本体文档 O-6） |
| SC-9 | 项目文件与对话产物（P3） | 项目内上传/管理文件；对话中 Agent 生成的文件在右侧边栏「文件」面板查看/下载 |
| SC-10 | 对话对比验证（v0.9 新增，P1） | 同一问题开启「对比」后由 2~4 个窗格并行回答：各窗格独立选择模型（如 DeepSeek-V3 vs DeepSeek-R1 vs Qwen2.5-72B）、知识库或本体运行方案，横向对比回答质量、召回来源与过程事件差异——学习"不同配置对同一问题的影响"的核心场景 |

---

## 3. 信息架构与页面需求

整体为**顶部导航五模块 + 独立设置 + 各模块"左列表 + 右主区"**布局（界面口径以《17_产品_信息架构与界面设计》为准，原交互原型已移除；OpenOntologies 不设独立导航、经本体模块两入口进入，REQ-111/G-3；对标简化版 Coze 工作台）。

```
┌────────────────────────────────────────────────────────────────────────┐
│ 顶栏导航：Agent │ 项目 │ 本体 │ 知识库 │ 技能            ⚙设置        │
├──────────────┬─────────────────────────────────────────────────────────┤
│ 左列表(随模块) │ 右主区                                                  │
│ Agent 视图：  │ Agent/项目视图 = 对话窗口：                               │
│  ▾ agent-A   │  消息流（用户/Agent/工具/技能/子Agent/召回/本体事件卡）    │
│    · 对话1    │  输入框（发送/停止）                                      │
│    · 对话2    │  快捷切换条：知识库▾ ☑启用 · 运行方案▾ · 技能chips · ⚙参数 │
│  ▸ agent-B   │  ▸ 文件面板(P3，可收起)                                  │
│ 项目视图：    │                                                        │
│  ▾ 项目-1    │ 本体视图：五栏侧边栏（学习中心/构建×6路径/资产/运行/消费审计）│
│    · 对话1    │ 知识库视图：库列表 → 库详情（RAG/GraphRAG 双Tab + 试运行）  │
│ 本体视图：    │ 技能视图：左筛选(全部/已挂载) → 技能列表 + 编辑抽屉          │
│  侧边栏五栏   │ OO 双轨(P2)：本体模块内「OO 流程/引导页」→ 工作台           │
│              │   （装载/校验·推理·影响分析 + 报告/MCP 服务启停）           │
└──────────────┴─────────────────────────────────────────────────────────┘
   本体构建平面/运行平面为独立进程（见本体文档）；本平台经代理与MCP对接
```

### 3.1 模块导航与各视图左栏（v0.7 顶部导航架构）

| 编号 | 模块 | 需求 | 优先级 |
| --- | --- | --- | --- |
| REQ-99（v0.8 更新；**v0.21 按 REQ-111 修订导航口径**） | 顶部导航五模块 + 设置 | **平台按顶部导航组织五个业务模块**（Agent/项目/本体/知识库/技能）+ **参考资料**（REQ-116）+ 独立设置页，点击切换下方模块视图；各模块统一**左列表 + 右主区**两栏；本体视图界面由本体模块提供、经主平台反代同源嵌入（见 REQ-08a）；交互基线（折叠树、配置抽屉、事件卡渲染、toast 反馈等）界面口径以《17_产品_信息架构与界面设计》为准（原型已移除）。**历史口径**：v0.7~v0.20 为"六模块"（含 OpenOntologies 独立导航，从未实装）；v0.21 起 OpenOntologies 不设独立导航，其双轨能力由本体模块两处入口承载——构建栏「Open Ontologies 流程」+ 运行栏「Open Ontologies 引导页」（17 REQ-111/G-3，REQ-100 双轨可摘除原则不变） | P0 |
| REQ-01 | 智能体模块 | Agent 视图左栏列出所有已创建的 Agent；支持「新建 Agent」「编辑」「删除（二次确认）」 | P0 |
| REQ-02 | 智能体模块 | 每个 Agent 节点**可折叠展开其名下多个对话**；Agent 节点支持「＋新建对话」直接进入与该 Agent 的会话；节点齿轮入口打开 Agent 配置抽屉（SC-1） | P0 |
| REQ-03 | 项目模块 | 项目视图左栏列出所有项目；支持「新建项目」「编辑」「删除」；项目节点可折叠展开其下对话 | P0 |
| REQ-04 | 项目模块 | 项目详情/设置中可**配置该项目使用的一个或多个 Agent 及协作模式** | P0 |
| REQ-101（v0.14 新增；v0.17 增补；v0.18 实现回写） | 项目模块 | **新建项目选择本地目录**：项目创建支持绑定本地文件系统目录；创建时检测目录是否为 git 仓库并展示 git 状态信息（分支/最新提交/工作区脏状态）。**v0.17 增补**：绑定目录后，项目对话中生成的文档（对话产物）写入该本地目录，且可读取目录内文件（与 M11 files API/save_file 打通——绑定目录作为项目文件根的作用路径，直写或同步由方案定）；数据模型为后期 Agent 挂载本地/远程目录（对标扣子 Coze 的资源目录模式，见需求池）预留｜✅ 已交付（v0.23 完整迁入，智能体侧同步统一） | P3 |
| REQ-05 | 对话管理 | 对话归属于归属主体（Agent 或项目）；支持重命名、删除 | P0 |
| REQ-06 | 对话管理（v0.7 随导航形态更新） | **Agent 视图与项目视图左栏分别为两级折叠树**：Agent 树（Agent → 其对话）与项目树（项目 → 对话）；树的展开/折叠状态本地记忆 | P0 |
| REQ-07 | 知识库模块（v0.7 同屏详情） | 知识库视图：左栏库列表（名称/状态徽标），右栏为选中库详情（文档/片段管理），同屏切换；支持「新建知识库」「重命名」「删除」 | P1 |
| REQ-08 | 技能模块（v0.7 布局确认） | 技能视图：左栏筛选（全部/已挂载/未挂载，真实过滤）+ 分组/标签预留区（标注"规划中"）；右栏技能列表；支持「新建技能」「编辑」「删除」 | P1 |
| REQ-08a | 本体导航入口（v0.7 形态更新） | 顶部导航「本体」进入本体视图：本体流水线列表（进度 n/7 + 状态）与 S1~S7 七阶段视图（界面由本体模块实现，经主平台反代同源嵌入，见本体文档 D-O7）；运行方案只读信息经主平台代理展示（名称/运行时类型/状态/端点）；本平台的职责是**对接**（见 §3.8） | P1 |
| REQ-100（v0.8 新增；v0.21 入口口径随 REQ-111 修订） | OpenOntologies 集成（**独立路径**，P2） | **入口（v0.21 起）**：本体模块构建栏「Open Ontologies 流程」+ 运行栏「Open Ontologies 引导页」（不设顶部独立导航）。**工作台能力**：左栏数据集列表（其 data-dir 内 TTL 本体），右栏工作台——**装载**（上传/粘贴 TTL/OWL）、**操作**（校验 / 推理 / 变更影响分析，报告展示）、**MCP 服务**（启停/状态/端点）。经 **oo-worker** 薄服务托管 [open-ontologies](https://github.com/fabio-rovai/open-ontologies)（Rust 单二进制，MIT，Oxigraph 0.5 后端）实现；Agent 对话**直接挂载其 MCP**（onto_* 工具：查询/推理/校验/变更，复用 REQ-123 MCP 多 server 能力），guide 注入使用说明（推理为装载时物化，变更后需重载）。与主线路径（spec_json 仓库 + facade，REQ-97）**双轨并行、P1 数据不互通、可整体启停摘除**，不影响主线 O1~O9；依据《open-ontologies借鉴与引入分析.md》v0.2 §8 | P2 |

> 说明：Agent 直聊（SC-1）与项目对话（SC-3）在数据上用 `scope` 区分；知识库、技能是主平台内的全局资源；**本体是本体模块（构建平面/运行平面）管理的全局资源**，本平台只存"对话挂载了哪个运行方案"的外部引用。

### 3.2 右侧对话详情区

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| REQ-10（v0.11 更新） | 展示对话标题、所属主体（Agent/项目）、当前参与的 Agent；实际使用的模型连接移至智能体属性查看（头部不再展示） | P0 |
| REQ-11 | 消息气泡区分角色：用户 / 主 Agent / 子 Agent；多 Agent 场景标明消息由哪个 Agent 产生 | P0 |
| REQ-12 | **流式输出**：模型回复逐字/逐块呈现（打字机效果） | P0 |
| REQ-13 | **过程可观测**：展示工具调用（工具名、入参摘要、结果摘要、耗时）、Agent 切换/委派事件、技能注入情况 | P0 |
| REQ-14 | 输入框支持发送、**停止生成**；中断后可继续/恢复（含工具调用前人工审批）｜停止 P0 ✅ M5；恢复+审批 P2 ✅ v0.24——ask_human 答复 / tool_approval=all 审批挂起，approve/deny 定向恢复 | P0（停止）/ P2（恢复，随 LG-8） |
| REQ-15 | 历史对话重开后，完整还原消息与过程事件 | P0 |
| REQ-16 | 错误态可见：模型调用失败、工具报错（**含本体运行方案不可用/未运行，v0.6 语义**）在界面有明确提示，不静默吞掉 | P0 |
| REQ-17 | **知识召回可见**：选择知识库后，回答过程展示"从哪个知识库召回了哪些片段"（来源 + 摘要），可折叠查看 | P1 |
| REQ-18 | **本体查询可见**：挂载运行方案后，Agent 经 MCP 调用 onto_* 工具的入参/结果出现在过程时间线 | P1 |
| REQ-19（v0.6 新增，P3） | **右侧文件面板**：对话详情右侧可展开"文件"边栏——展示当前项目空间内文件（项目文件 + 对话产物），支持预览/下载；未开启项目时面板隐藏 | P3 |

### 3.2.1 对话输入区工具条（v0.12：输入框内配置 chips，豆包式）

对话级开关承载于**输入框内部底缘的三枚切换 chip**（知识库 / 本体 / 技能；v0.11 的"输入框下方快捷切换条"语义移除）：未启用为浅色描边、启用为品牌色深底，点击切换、可同时开启多项；配置随对话保存、仅影响更改之后发出的消息（沿用 Q-6 不追溯原则）。交互约定：①chip 只表达**是否使用**；首次启用且会话无绑定时弹**轻量单选列表**（知识库列表 / running 运行方案）完成绑定并记住，此后开合不再重选；②无可用项（如无 running 方案、无知识库、智能体未挂载技能）时 chip 置灰 + Tooltip 说明；③检索参数（TopK/min_score）等具体配置在**专属页面**维护（知识库页检索参数卡），不占用对话空间；④技能为**会话级开关**（REQ-19g `enable_skills`，默认开；后端列与装配门控由后端车道跟进），技能挂载本身在智能体属性维护。模型连接的配置与展示在**智能体属性表单**（REQ-20）：

| 编号 | 控件 | 需求 | 优先级 |
| --- | --- | --- | --- |
| REQ-19a | 知识库选择 | 下拉选择「不使用知识库 / 某个知识库」，单选；选中后该对话后续提问走知识库检索增强 | P1 |
| REQ-19b | 知识库状态提示 | 若尚未配置任何"向量模型连接"，下拉旁给出引导："向量化需要先在设置中配置向量连接（DeepSeek 不含 embedding）" | P1 |
| REQ-19c（v0.6 语义平移） | 本体开关 + 运行方案选择 | 开关「使用本体」+ **运行方案下拉**（列出 Runtime Manager 中 running 状态的方案；对话挂载存 `runtime_profile_id`，见本体文档 O-6）；方案未运行 → 禁选 + 提示 | P1 |
| REQ-19d（v0.6 新增） | 本体服务状态提示 | Runtime Manager / facade 不可达或所挂方案停止时，开关旁给出明确提示并禁用选择，不影响普通对话与知识库（降级语义见 REQ-97） | P1 |
| REQ-19e（v0.9 新增） | 对话对比开关与等分布局 | 对话详情可**手动开启「对比」**（快捷切换条开关）；开启后消息区按对比列数**横向等分**为多个窗格（2~4 列，可增删），**底部输入框保持不变（共用）**；发送一次提问，各窗格独立生成并展示各自的回答与过程事件（一次提问 → N 路并行调用）；关闭对比回到单流视图；**停止语义（v0.11 拍板）：对比模式停止按整组处理——共用输入框的停止操作终止该次运行的全部 N 路，各列已生成内容保留**｜✅ 已实现（v0.37；勘误：v0.35 前 02 §6.19 误记"已实现"，实际 2026-09-24 交付） | P1 |
| REQ-19f（v0.9 新增） | 对比窗格独立配置 | 每个窗格**下方**有独立配置区：模型（从「已配置的对话模型连接」中引用，口径同 REQ-23）、知识库（同 REQ-19a）、本体运行方案（同 REQ-19c，仅 running 可选）；未单独设置的项**继承对话当前配置**；窗格配置仅影响该窗格后续消息（Q-6 不追溯）；演示基线见 17 号 J4 与对比模式口径（原型已移除）｜✅ 已实现（v0.37，窗格头覆盖徽标 + 三项继承式选择器；模型覆盖经克隆 Agent 生效） | P1 |
| REQ-150（v0.39 新增；✅ 已交付 2026-09-26，f280135+收尾，见 v0.49） | **对比界面系统优化**（19e/19f 基座的体验重构，主人指出"当前界面太粗糙"）：①**选项承载迁移**——对比模式下共享输入区的 知识库/本体/技能 chips（现状无条件渲染，与窗格选择器双轨并存、职责混淆）不再承担配置职责（收起为"跟随窗格/继承"摘要），**迁移至上方各窗格头配置区**：每窗格自带 模型/知识库/本体运行方案/技能 全量覆盖项（**技能为新增覆盖项**，19f 现仅模型/知识库/运行方案三项），并显性显示"继承对话/本窗格覆盖"状态；②**窗格视觉系统化**——窗格头（窗格名 + 模型徽标 + 配置摘要，预留 REQ-143 的 Agent 名称/logo 位）、窗格分隔与宽度可调（Splitter）、各窗格流式/完成状态对齐（窗格头状态徽标：待提问/生成中/完成/出错/已停止）、停止归属明确（停止按钮 title 注明整组语义）；③与 REQ-143/144 合并实施避免二次重排（143/144 已交付，本项为 M19 阶段三收尾）。**验收**：对比模式下共享输入区无 知识库/本体/技能 配置 chips；每窗格可独立完成全量配置且继承/覆盖状态一目了然；SC-10 同问对照演示不回退；整体视觉符合 17 号产品口径——**全达成**（CDP 冒烟：徽标覆盖/继承、SC-10 双 Agent 对照、停止→已停止、Splitter 拖宽、刷新配置持久化；截图 smoke/req150/） | P2 |
| REQ-160（v0.44 新增，✅ 阶段一/二已交付 2026-09-25） | **DeepSeek Harness（dsh）推理后端适配器**：第 4 个外部推理后端（M13 cliAdapter 骨架第 4 例）；dsh headless 形态（`--profile headless <task>` answer-once-exit）+ Minimal 裁剪 bash/edit + 锁版本 0.1.5-rc.3；接线冒烟通过（run.started backend=deepseek-harness + 结构化 MISSING_CREDENTIAL 透出）；阶段三 agentd 镜像内置随沙箱合流；调研详见 research/DeepSeek_Harness接入可行性_20260925.md | P2 |
| REQ-161（v0.45 新增；✅ 结构基座已交付 2026-09-25：文档体系迁移+页面目录驱动二级树+相对引用点击 Drawer；交互完善/构建验证/内容扩充矩阵主人本地开发） | **参考资料升级「平台知识」——二级主题树与内容扩充**（REQ-116 v2）：①**更名**——模块名「参考资料」→「**平台知识**」（导航 label/侧边栏标题/17 口径同步；PageKey 不变）；定位=平台知识与技术的**学习视图层**（17 §1.4）。②**二级主题树**——左栏升级为可收缩两级菜单（AntD Menu inline SubMenu 原生折叠，零自研）：L1 模块 / L2 主题页；内容文件沿用 frontmatter module+topic 组织，前端按模块分组建树。③**内容扩充（首批矩阵，素材源=research/ 调研与 docs/ 模块档，文档线可先行产出）**——智能体：[总览 / Eino ADK 与 ChatModelAgent / 推理后端与 DeepSeek Harness 适配器 / 沙箱方案（10a 实测与边界治理）/ 多 Agent 协作装配]；本体：[总览 / 六条构建路径 / 运行方案与引擎（Oxigraph/Fuseki）/ 开源项目调研集（OntoChat/OntoExtend/open-ontologies/WebVOWL/Protégé）/ 消费与审计（KG/PROV-O）]；知识库：[总览 / RAG 管道与向量后端 / GraphRAG 与知识图谱 / 开源选型对比（WeKnora/RAGFlow/Qdrant）]；项目/技能/设置/平台总览：既有页保留+按需增补（Git 视图/SKILL.md 对照/多实例与别名/推理后端探测）。**验收**：左栏两级可收缩树；每 L2 页保持四段骨架+源文档地图；模块更名后导航/口径一致；20 号 S1.4 更新 | P2 |
| REQ-162（v0.46 新增，待开发） | **本体学习中心七阶段方块化 + 外部资源迁移**：①学习中心「七阶段学习路径（S1→S7）」由 Collapse 折叠面板改为**一排方块 + 从左到右箭头连接**的步骤条形态（每阶段一方块：编号+名称+状态色，点击展开该阶段方法论卡与任务卡）；②**外部资源迁移**——学习中心「外部资源」Tab（REQ-109）藏得过深，迁移至**平台知识模块**（作为「平台知识」下独立主题页，内容仍为 seeds/learning/external-resources.md 构建期内联；REQ-109 交付物不废弃只挪位）；本体模块左侧边栏相应瘦身 | P2 |
| REQ-163（v0.46 新增，待开发） | **「消费与审计」第五栏定位厘清与 KG 内容边界**：现状混淆点——第五栏同时呈现 KG 增强检索展示与决策审计，而知识库 GraphRAG 子模块（REQ-107/M16）也管理 KG（实体/关系/claims），两处 KG 来源（本体 TTL 装载 vs KB 文本抽取）与受众未在界面区分。**梳理结论（文档线 2026-09-25）**：第五栏定位=「本体消费侧观测台」——仅展示**本体 TTL 装载来源的 KG** 检索与 onto_* 消费审计；KB 文本抽取 KG 的展示归知识库 GraphRAG 子模块。改造：①第五栏 KG 检索区数据源限定本体 TTL 装载（来源徽标注 TTL/文本）；②界面文案与 03/17 口径同步「本体消费侧」；③不删除能力只厘清边界 | P2 |
| REQ-164（v0.46 新增，待开发） | **智能体配置补齐技能 UI + 分区调整**（REQ-132 分区细化）：①**补齐技能配置界面**——「能力」分区现状只有工具白名单与 MCP servers，缺技能勾选（save 仅透传 agent.skills 无 UI）；补多选技能列表（注册表来源，含已挂载状态）；②**分区调整**——「运行后端」「推理后端」从「模型与参数」移至**「基本」分区**（后端=Agent 身份属性而非模型参数，主人指示）；③同步 REQ-132 四分区定义（基本含后端身份项） | P2 |
| REQ-165（v0.46 新增，待开发） | **推理后端官方 Logo 自动匹配**（REQ-137 增强）：①为已支持的推理后端（claude-code/opencode/aider/deepseek-harness/eino-adk）内置官方 logo 资源；②Agent 选择某后端 → 对话/详情等运行上下文中 Agent 图标自动切换为该后端 logo（现为 glyph 占位）；③**智能体导航树保持智能体默认标识**（glyph 或登记 logo_url，不随后端变化）——导航树表达 Agent 身份，运行上下文表达执行身份；AgentLogo 组件扩展双态渲染 | P3 |
| REQ-19g（v0.12 新增） | 会话级技能开关 | 输入框内「技能」chip 控制该对话是否注入智能体已挂技能（`enable_skills`，默认开；关闭仅影响后续消息，Q-6 不追溯）；智能体未挂载技能时置灰并引导至智能体属性配置；后端已实现（迁移 006 + 装配 skillGated 门控） | P1 |

### 3.3 Agent 配置表单（创建/编辑）

| 编号 | 字段 | 说明 | 优先级 |
| --- | --- | --- | --- |
| REQ-20 | 名称 name | 唯一、可读；运行时作为 Agent 标识 | P0 |
| REQ-21 | 描述 description | **多 Agent 路由的关键语义依据**（Eino 依此决定是否委派/转移） | P0 |
| REQ-22 | 系统提示词 instruction | 该 Agent 的人设/职责/约束 | P0 |
| REQ-23 | 模型与参数（基础） | 从「已配置的对话模型连接」中选择，配置 temperature、max tokens；**未选择时跟随全局默认连接（P1，REQ-42）** | P0 |
| REQ-24 | 工具列表 tools | 从系统已注册工具中勾选 | P1（第一期可先内置固定工具） |
| REQ-25 | 最大迭代次数 max_iteration | 防止 ReAct 死循环 | P0 |
| REQ-26 | 头像/颜色 | 仅用于 UI 区分 | P2 |
| REQ-122（v0.6 立项；v0.21 自 REQ-90/92 合并迁移，见 18 号注册表 G-1） | **执行后端 runtime_backend（沙箱化）**：字段口径见 §3.7 同名条；✅ M10 阶段 10a/10b 已交付（96210e6/9b0d45c，2026-09-25——docker 后端 SANDBOX_IMAGE 全链实测跑通 + 沙箱生命周期可见化与资源限制参数化，见 01 v0.50；10c~10f 待领取，见 02 §12 M10 与 research/智能体沙箱方案调研_20260925.md） | P0（inprocess 默认）/ P2（docker/k8s） |
| REQ-123（原 REQ-93，v0.21 号段迁移） | **Agent 级独立配置** | 在 REQ-20~26 基础上支持单独覆盖：**模型连接**（覆盖全局默认）、**技能集合**（勾选技能库中的技能）、**MCP servers**（配置一个或多个 MCP 服务端点，运行时经 MCP Client 拉取工具合并） | P2 |
| REQ-131（v0.28 新增） | **Agent 对外 MCP 服务化（agent-as-MCP-server）** | Agent 可开启「对外服务」：平台暴露统一 MCP Server 端点 `/mcp`（Streamable HTTP，默认回环监听），将启用的 Agent 各暴露为一个 MCP 工具 `agent_{id}`（入参 input 文本 + 可选 session_id）供外部服务/Agent 调用；复用既有装配管线（模型/工具/技能/MCP/知识库全自动生效）；Agent 级静态 Token 鉴权；需审批工具（REQ-14 tool_approval≠off）在 server 会话内默认拒绝并说明；禁止自引用 + 调用深度上限（防循环）；被调用过程事件照常入库、对话窗口可回放（REQ-117 装配快照含 serve 状态）；工具式（无状态）先行，会话式（session_id 映射平台会话多轮延续）为增强项 | P2 |
| REQ-132（v0.28 新增） | **Agent 配置页改版（SidePanel 分类 + 创建分级）** | ①AgentSidePanel 由单视图平铺改为**四分类分区**（面板内页签/锚点导航）——「基本」（名称/描述/头像颜色 REQ-26/系统提示词/审批策略）、「模型与参数」（模型连接覆盖 REQ-42、温度/最大 tokens/最大迭代 REQ-25、执行后端 REQ-122、推理后端 D-O13）、「能力」（工具 REQ-24、技能 REQ-121、MCP servers REQ-123）、「对外服务」（REQ-131，默认关闭收起）；②创建弹窗 AgentModal 分级——基本项（名称/描述/系统提示词/模型连接）填写即可创建（其余取默认：跟随全局模型/inprocess/审批 off），**高级配置默认收起**，创建后一律经 SidePanel 修改（弹窗仅保留新建，17 §4.1 口径不变）；界面口径见 17 v0.7 §4.3｜✅ 已排期 M18 | P2 |
| REQ-133（v0.29 新增，v0.30 按主场景修订） | **项目配置本地目录改造（检测状态修复 + 选目录方案分层）** | **主场景（主人指正）：后端运行在 Linux，Web 页面在 Windows 打开，所配目录是 Windows 本机目录**。①修复状态显示错位：validate-dir 检测通过时界面仍显示「不存在/非目录」——前端展示与后端检测结果**分字段直连**（路径格式合法/存在/是目录/后端可达），前端不得按 POSIX 规则本地推断 Windows 盘符路径；validate-dir 语义明确：远程部署下对用户本机目录只能做格式校验，存在性检查仅对后端可达路径生效；②选目录方案分层：远程部署（主场景）下后端无法在用户侧弹系统对话框、浏览器安全模型也不暴露绝对路径（showDirectoryPicker 仅回句柄不含路径，远程部署下句柄对后端无用）——以**手输 + 后端按目录所在主机 OS 语义校验并直连反馈**为主，增强路径（可选后续立项）为本地伴生组件选目录回传或目录挂载/共享后选挂载点；**同机部署形态保留点击唤起系统文件管理器选目录**（Windows 资源管理器 / macOS Finder / Linux zenity），回填绝对路径；跨 Windows/Mac 可用｜✅ 已交付（v0.31：validate-dir 增 format_ok/reachable 分字段 + DirCheckResult 共享组件直连渲染；pick-dir 三平台系统对话框仅同机可用，前端失败提示手输；02 v0.50 §6.17） | P2 |
| REQ-134（v0.29 新增） | **智能体/项目配置界面组件化改版** | AgentModal/AgentSidePanel 及项目配置界面当前为手搓实现，观感与交互简陋；改为**优先复用成熟开源组件**重构（@ant-design/pro-components ProForm/ProCard 等蚂蚁生态既有组件，对齐 02 §10 全蚂蚁生态拍板），分区/折叠/表单/布局均采用现成组件形态，不自行手搓控件；与 REQ-132 四分类分区改版合并实施 | P2 |

### 3.4 模型设置（顶栏「⚙ 设置」入口）

> 以下 REQ-135~142 为 v0.31 批次立项（2026-09-24 主人指令，方案 only，不实现）。

| REQ-135（v0.31 新增；v0.35 实现交付） | **对话窗口配置分级与入口整合** | 总要求：通过**分级的配置开关控制 Agent 运行过程**（对话级覆盖 Agent 级默认），优化细节展示避免杂乱无序。①对话创建右上方「配置按钮」与「配置图标」重复——去掉配置按钮，配置入口改为**右侧边栏收放按钮**（与 SidePanel 收放交互一致）；②运行过程分级配置开关：对话级配置面板控制过程事件展示粒度/审批交互/细节可见性等，合并顺序=对话级 > Agent 级 > 全局默认，默认收起；③过程细节展示降噪（折叠、分组、按需展开） | P2 |
| REQ-136（v0.31 新增；v0.35 实现交付） | **对话自动命名** | 新建对话默认名（如「新对话」）——首轮用户问题发出后，用默认大模型将问题内容**提炼为对话名称**（短标题），异步更新不阻塞对话；失败/超时回退默认名；保留手动改名 | P3 |
| REQ-137（v0.31 新增；v0.35 实现交付） | **非内置后端智能体沿用原 logo** | 智能体后端为非系统内置（自定义/外部部署后端）时，该智能体在会话列表/对话界面/选择器等处展示其**原本的 logo**（配置登记的图标资源），未配置回退默认图标；系统内置后端维持现有图标体系 | P3 |
| REQ-138（v0.31 新增，v0.33 口径修正+交付） | **平台主标题更名** | 界面主标题「多智能体学习平台」改为「**智能体构建平台**」（主人 2026-09-24 实时指示定名，修正立项文本误记的「多智能体构建平台」）——页面标题/顶栏副标题/参考资料定位行同步；品牌名 AgentLab 不变，仅中文主标题调整｜✅ 已交付（2026-09-24，9bf8ca2：TopNav brand-sub + index.html title + overview.md + README 顺带补齐 AgentLab 品牌） | P2 |
| REQ-139（v0.31 新增；v0.35 实现交付） | **本体学习中心卡片内容优化** | 学习中心每个卡片内部内容展示进一步优化：深度版本默认收起保留，但收起态摘要与展开态内容**再详细一些**——目标界面更直观、内容更详实（要点结构化、示例/来源补全、排版降噪） | P3 |
| REQ-140（v0.31 新增；v0.35 实现交付） | **内部方案文档链接点击查看** | 界面中链接的内部方案文档（docs/ 下需求/方案/产品设计等）当前不支持查看——增加**点击查看**：只读渲染 Markdown（弹层或新开页），不要求在线编辑 | P4 |
| REQ-141（v0.31 新增） | **知识库与知识图谱产品能力增强（调研启动）**｜✅ 已排期 M19 调研轨道 | 当前知识库实现较单薄——**启动调研任务**：对标主流产品补齐知识库与知识图谱的产品能力（多格式摄入/切分策略/混合检索/图谱构建与问答联动等），产出调研结论与立项建议后再排期；本轮仅立项待办不实施 | P3（调研） |
| REQ-142（v0.31 新增） | **多类型智能体支持方案研究（发展方向）**｜✅ 已排期 M19 研究轨道 | 当前智能体均为对话型——**研究支持其他类型智能体的方案**（任务型/工作流编排/自主规划/多 Agent 协作形态等），作为将来发展方向；产出方案研究后另行立项，本轮仅登记待办 | P4（待办） |

> 以下 REQ-143/144 为 v0.35 立项（2026-09-24 主人确认，对话对比扩展，方案 only，不实现；约束：**在已实现的 REQ-19e/19f 对比模式上增量扩展，改动克制**）。

| REQ-143（v0.35 新增；✅ 已交付 v0.41，M19 阶段一） | **对话对比扩展——跨智能体窗格** | 对比窗格头部增加「智能体」选择器：默认锁定当前对话所属 Agent，可切换为**其他 Agent**（项目内 + 全局，窗格头显示名称与 logo，logo 口径沿用 REQ-137 资源字段）；切换后该窗格以所选 Agent 的**完整配置**执行（系统提示词/技能/工具/知识库），既有窗格级单项覆盖（模型/知识库/运行方案）仍可叠加；上下文口径：默认**共享对话历史**（与现状一致），窗格级新增「不携带历史」开关做干净对照；停止语义不变（对比组一次运行、整组停止）；实现约束：**不改动 SSE 契约与入库结构**，仅每路运行增加 Agent 解析与配置快照记录 | P2 |
| REQ-144（v0.35 新增；✅ 已交付 v0.42，M19 阶段二） | **对话对比扩展——配置剖面与复用** | REQ-19f 窗格配置区（模型/知识库/运行方案）之上增加可折叠「更多配置」：推理参数（温度等）、系统提示词临时改写、技能开关（口径同 REQ-19g）；剖面可**命名保存至对话级**复用（不做全局剖面库）；提供「复制上一窗格配置」快捷操作便于单变量 A/B；覆盖合并序：**剖面 > 窗格单项 > Agent 默认 > 全局默认**（与 REQ-135 分级口径一致）；可选增强（同优先级子项）：对比结束后可「采纳」某窗格——以该窗格的 Agent/剖面写回对话配置继续对话 | P3 |

> 以下 REQ-155~158 为 v0.43 立项（2026-09-25 主人指令：本体开源实现方案借鉴研究（docs/23，7 方向 18 方案）全部排期无损推进；三原则：①开源方案选型**全都要**——同一功能点多方案并存不做二选一；②**Go 优先排期**——Go 后端原生可实现优先，Java/Python CLI 依赖以 sidecar 隔离后置；③**结构化交互界面**——多路径多方案分组化呈现、一目了然）

| REQ-155（v0.43 新增） | **AI-native 本体工具链**（调研报告 Top1，docs/23） | 参考 Open Ontologies（Rust/Oxigraph，70+ MCP 工具；实验依据：结构化 MCP 工具访问 F1=0.717 ≫ LLM 直读 OWL 的 0.323）——LLM 生成→工具链验证→迭代修复闭环；工具集按 Go 栈裁剪（mcp-go 实现 validate/query/diff/lint/version），query 衔接 REQ-151 facade sparql_query；Terraform 式生命周期（plan/enforce/apply/monitor/drift）映射运行栏「方案生命周期面板」（Phase 3）；映射构建+资产+运行三栏 | P1 |
| REQ-156（v0.43 新增） | **本体资产质量门禁**（调研报告 Top2） | 参考 OLIVAW（保存钩子/导入门禁/CLI 批检三模式）+ Semantica QualityGate——Go 规则引擎实现检查器（TTL 可解析性/IRI 引用一致性/概念区分度/覆盖率阈值/基础 SHACL 约束；完整 SHACL 走 pyshacl 类 sidecar 后置可选）；结果落资产详情「质量卡」页签；保存/导入时 pass/fail 门禁可配置 | P1 |
| REQ-157（v0.43 新增） | **本体导入审查 UI**（调研报告 Top3） | 参考 OrionBelt——补齐 oo/ontoextend 路径「敢导入」体验：导入合并三策略（replace/merge/merge-overwrite）+ 冲突检测（类/属性 IRI 冲突、前缀冲突）+ 前缀自动协调 + 变更报告 diff 预览（结构化报告先行，语义 diff 后置）；Go 合并逻辑 + React 审查向导 | P1 |
| REQ-158（v0.43 新增） | **本体技术研究专栏与参考资料接入** | 学习中心参考资料页开辟「技术研究」专栏（REQ-109 模式：内容单源 seeds/learning/external-resources.md §六，18 方案卡片化，改 md 即生效）+ 本体栏专栏入口；卡片结构化呈现（方向分组/落地栏徽标/工作量 S·M·L），多方案并存一目了然；其余 14 方案（ROBOT Template/ODP/Domain-OntoGen/LinkML/OLS/BioPortal/OntoChat 完善/OntoGenix/LLM4ACOE/Data Pipeline 增强/语义嵌入/WebProtégé 协作/语义 Diff/OntoGraf）全量纳入**吸收池**按三阶段路线推进（Phase1 Go 原生夯实：质量门禁+LOV 词表+自修复循环；Phase2：AI 工具链核心+导入审查 UI；Phase3：生命周期面板+plan/apply/drift+ODP 推荐+可视化——WebVOWL 已随 REQ-154/M21 排期不重复立项），推进时逐一立项，详见 docs/23 §9 | P2 |


| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| REQ-40 | 维护一个或多个**模型连接**：名称、类型（对话 chat / 向量 embedding）、BaseURL、协议（OpenAI 兼容）、模型名（model）、API Key、备注；支持新增/编辑/删除 | P0（chat）/ P1（embedding） |
| REQ-41 | 首次启动**预置一条 DeepSeek 对话连接**（BaseURL、model 预填，key 留空待填） | P0 |
| REQ-42 | **全局默认模型（v0.6 用户口径 P1）**：可将某条**对话连接设为全局默认**；Agent/项目未单独指定模型时使用默认连接；每类型（chat/embedding）各一条默认 | P1 |
| REQ-43 | 提供「**测试连接**」：用当前填写的 BaseURL/key/model 发一次最小请求，回显成功/失败与错误信息；对话连接走 chat、向量连接走 embeddings | P0（chat）/ P1（embedding） |
| REQ-44 | API Key 在库中**加密存储**；设置列表中以掩码显示（如 `sk-****ab12`），不回传明文 | P0 |
| REQ-45 | 支持连接的启用/停用；删除被 Agent/知识库引用的连接时给出提示 | P1 |
| REQ-46 | **默认向量连接**：知识库向量化使用一条 embedding 连接，可标记默认；DeepSeek 官方不提供 embedding，需配置 OpenAI 兼容向量服务（OpenAI、DashScope 兼容模式、硅基流动 BGE 等） | P1 |
| REQ-47（v0.13 新增） | **供应商-模型合并视图**：设置页模型管理以供应商为可折叠行、模型嵌套其下（替代提供商/模型双页签）；供应商行承载测试/编辑/删除/＋添加模型/自动获取模型入口 | P1 |
| REQ-48（v0.13 新增） | **模型自动发现**：按供应商（复用其已存 Key）调用 OpenAI 兼容 `GET {base_url}/models` 列出可用模型，多选后批量创建为该供应商下的模型连接（Key 经 copy_key_from 共享、命名防撞）；接口未就绪时降级为手动添加 | P1 |
| REQ-49（v0.13 新增） | **模型使用统计**：按模型/智能体/项目维度聚合 token 消耗（调用次数、prompt/completion/total tokens）+ 轻量可视化横条；数据来自运行事件 usage（后端聚合端点已实现：`GET /api/stats/usage`，02 §8；v0.16 增按供应商维度与 from/to 时间段筛选） | P2 |
| REQ-104（v0.15 新增；v0.19 实现回写） | **预置百度千帆向量连接**：知识库嵌入模型默认使用百度千帆 `embeddings-v1`——预置千帆 embedding 连接（OpenAI 兼容接入点预填，Key 留空）并标记为默认向量连接（呼应 REQ-46）｜✅ 已交付（v0.19，迁移 011 幂等种子） | P2 |
| REQ-105（v0.15 新增；v0.19 实现回写） | **内置国内常见模型供应商预设**：模型管理页内置国内常见厂商访问配置预设（DeepSeek、阿里百炼/DashScope、百度千帆、智谱 GLM、月之暗面 Kimi、硅基流动、MiniMax、讯飞星火等——预填协议与 BaseURL）；预设清单形式（前端静态/后端种子）实现时定｜✅ 已交付（v0.19，前端静态清单 9 条） | P2 |
| REQ-106（v0.15 新增；v0.19 实现回写） | **添加模型快速引用厂商配置**：添加提供商/模型时从预设清单快速选择某厂商，自动填充协议与 BaseURL 等访问配置，仅需补 API Key 与模型名；与模型自动发现（REQ-48）串联可直接拉取该厂商可用模型｜✅ 已交付（v0.19，预设填充 + REQ-48 串联） | P2 |
| REQ-148（v0.38 新增；✅ 已交付 v0.39） | **供应商多实例与别名/改名**：①**同一供应商可创建多个独立实例**（同 BaseURL 不同账号/Key/用途，如两个 DeepSeek 账号）——现状分组按 `protocol::base_url` 聚合，同 BaseURL 强制并入一组无法拆分；数据模型需将"分组标识"与 BaseURL 解耦（连接表增 provider_group_id 或组元数据，老数据按 BaseURL 迁移初始化）；②**供应商别名**：显示别名独立于连接名前缀（别名仅展示层，不改连接真名），列表/模型下拉/引用处显示别名；③**改名**：保留既有能力（编辑弹窗改名按前缀批量重生成连接名）并与别名语义统一（改名=真名变更，别名=显示层）；实现提示：SettingsPage 分组键、ProviderModal、模型下拉引用处联动 | P2 |

> REQ-172 为 v0.51 立项（2026-09-26 主人提出，模型接入增强；立项即交付）。

| REQ-172（v0.51 新增；✅ 立项即交付 2026-09-26） | **模型管理 Anthropic 协议支持 + 二级配置界面优化** | ①**Anthropic 协议**：模型连接协议（REQ-40）新增 `anthropic` 取值（Anthropic Messages API）——添加/编辑提供商与模型界面均可选；后端按连接协议构造模型实例：openai_compat → OpenAI 兼容通道（现状），anthropic → Messages API 通道（网关根地址自动拼接 `/v1/messages`，兼容 DeepSeek/智谱 GLM/Kimi 等厂商的 Anthropic 兼容端点；温度域钳制 [0,1]、MaxTokens 为该 API 必填故兜底 4096）；测试连接（REQ-43）与自动发现（REQ-48）增 anthropic 分支（`x-api-key` + `anthropic-version` 头，`/v1/messages` 与 `/v1/models`）；Agent 对话/结构化生成（REQ-98）/平台助手（REQ-166）全链路按连接协议生效；诚实约束：Anthropic 无官方向量接口，anthropic+embedding 组合在创建/保存时拒绝并提示；②**二级配置界面优化**（开源组件 + 布局）：添加/编辑提供商、添加/编辑模型弹窗与自动发现面板自 SettingsPage 抽取为 `settings/` 独立组件文件（延续 REQ-145 B1 拆分口径），表单分组化布局（厂商预设/基本信息/接入配置/首个模型），协议感知动态表单（BaseURL 标签/占位/说明、类型限制、模型名占位随协议联动）；预设清单（REQ-105）增 Anthropic 系 4 条（Anthropic 官方 + DeepSeek/智谱 GLM/Kimi 的 Anthropic 兼容端点）；控件全部使用既有开源组件（AntD 6），不引入手搓控件 | P1 |

> 安全边界：虽无登录，API Key 仍属敏感信息。加密密钥来自本地环境变量（见技术方案 §9），数据库文件与密钥文件都不进仓库。

### 3.5 项目配置表单

| 编号 | 字段 | 说明 | 优先级 |
| --- | --- | --- | --- |
| REQ-30 | 项目名称、简介 | | P0 |
| REQ-31 | 协作模式 collaboration_mode | `single`（单 Agent）/ `agent_as_tool`（主 Agent 把子 Agent 当工具，**推荐**）/ `transfer`（主 Agent 转移控制权，SetSubAgents） | P0 |
| REQ-32 | 成员 Agent | 从 Agent 库选 1~N 个；标记其中一个为主 Agent/协调者 | P0 |
| REQ-33 | 工作流模式 workflow_mode | 仅 P1：`free`（自主）/`sequential`/`parallel`/`loop`，选后按固定编排执行 | P1 |
| REQ-34 | 项目级系统约束 | 追加给该项目所有 Agent 的统一指令（如"始终中文、先结论后依据"） | P1 |
| REQ-125（原 REQ-95，v0.21 号段迁移） | **项目文件管理** | 项目详情提供"文件"页：上传/列表/下载/删除项目文件（本地项目目录存储，目录按项目隔离）；文件元数据入库 | P3 |
| REQ-126（原 REQ-96，v0.21 号段迁移） | **对话产物文件** | Agent 运行中生成的文件（由具备文件写入能力的工具产出）保存到项目空间（归属对话），并在右侧文件面板（REQ-19）可见可下载；产物事件进过程时间线 | P3 |

### 3.6 知识库对接（v0.12 收编为对话侧契约；模块需求与设计见知识库文档）

**分工**：知识库模块自身需求（REQ-07 界面、REQ-50/51 库与内容、REQ-52/53 切分与向量化、REQ-54/55 文档与索引状态、REQ-57 试运行）移交《11_知识库_需求文档.md》（**唯一需求事实源**，REQ-50~59 编号冻结延续）；设计与开源方案对比（含 WeKnora 0.8.0 评估：不替换组件级方案）见《12_知识库_方案设计.md》。**本文档只约定对话侧的引用需求**（REQ-19a/19b 见 §3.2，REQ-45/46 模型连接见 §3.4）：

| 编号 | 需求（对话侧） | 优先级 |
| --- | --- | --- |
| REQ-17 | **知识召回可见**：选择知识库后，回答过程展示"从哪个知识库召回了哪些片段"（来源 + 摘要），可折叠查看 | P1 |
| REQ-56 | 对话引用：由 REQ-19a 选择知识库；提问时检索 TopK 片段（K 可配置，默认 4）注入上下文，Agent 据此回答（检索与注入设计见 12 §3） | P1 |
| REQ-58 | 召回片段带来源（文档名/片段序号），回答过程可展开查看（REQ-17） | P1 |
| REQ-59 | 无向量连接或索引为空时，选择/提问给出明确引导，不静默用空知识作答 | P1 |

> 模块边界与需求池（原"明确不做"：PDF/Word/PPT/网页解析、表格/OCR、Rerank、混合检索等）已升级为 11 KB-2 进阶能力需求池；进阶能力的独立服务集成备选（WeKnora/RAGFlow）见 11 KB-3（P2，对齐 REQ-100 双轨模式）。

### 3.7 技能管理模块（v0.6 新增）

**定位**：技能 = 可复用的能力包，让"一段领域指令 + 一组工具"能被创建一次、多个 Agent 复用。**开源参考**：Claude Skills（SKILL.md 结构化能力包）、Eino 提示词模板机制；不自研复杂技能运行时（Q-19）。

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| REQ-120（原 REQ-90，v0.21 号段迁移） | **技能 CRUD**：创建/编辑/删除技能，字段——名称、描述、**技能指令段 instruction**（将注入使用者的系统提示词）、可选**工具白名单 tools**（从系统工具注册表勾选）、可选资源说明；技能为全局资源；提供 1~2 个内置示例技能（如"结构化输出约束""K8s 排障指引"） | P1 |
| REQ-121（原 REQ-91，v0.21 号段迁移） | **技能供 Agent 使用**：Agent 配置中勾选技能（REQ-123）；运行时装配——技能 instruction 追加进 Agent 系统提示词、技能工具并入 Agent 工具集；对话中可见"本 Agent 启用了哪些技能"（过程可观测 REQ-13） | P2（随 Agent 级配置） |
| REQ-122（v0.6 立项；v0.21 自 REQ-90/92 合并迁移，见 18 号注册表 G-1） | **执行后端 runtime_backend（沙箱化）** | 每个 Agent 可选择运行方式：`inprocess`（默认：主平台进程内 ADK 装配，快速实现路径）/ `docker`（每 Agent 一个容器，通用 agent-runtime 镜像 + 配置下发）/ `k8s`（每 Agent 一个 Pod）；后端接口可插拔（Q-17）；沙箱资源限制（CPU/内存）与生命周期（启停/状态）可见。§3.3 原"REQ-90 执行后端"行为本条之重复，随号段迁移并入。✅ 资源限制与生命周期可见化已随 10b 交付（9b0d45c，01 v0.50）：agent.sandbox_memory/sandbox_cpus（迁移 020，空/0=默认 512m/1CPU）+ `GET /api/agents/{id}/sandbox` 状态与 `start`/`stop` 端点 + SidePanel 运行后端 Select 与沙箱面板 | P0（inprocess 默认）/ P1（docker 后端 + agent-runtime 镜像）/ P2（k8s Pod） |
| REQ-123（原 REQ-93，v0.21 号段迁移） | **Agent 级独立配置**（见 §3.3）：模型连接覆盖、技能勾选、MCP servers 多端点配置——三者均支持"全局默认 + Agent 覆盖"两级 | P2 |

> 优先级说明：用户口径"Agent 单独配置模型/技能/MCP 为 P2"。因此 P1 阶段技能**先建库**（REQ-120），P2 阶段接入 Agent（REQ-121/123）并配合沙箱化（REQ-122）形成"独立配置 + 独立运行"的完整能力。

### 3.8 本体对接（v0.6 收编为对接契约；实现见本体文档）

**分工**：本体的构建（导入/编辑/AI 创建/浏览）与运行（多运行方案编排）由本体模块实现，见《04_本体_方案设计.md》v0.6（REQ-80~88；本体界面以 D-O7 七阶段流水线呈现）。**本文档只约定主平台侧的对接需求**（原 REQ-60~73 冻结为快照，编号继续有效）：

| 编号 | 对接需求 | 优先级 |
| --- | --- | --- |
| REQ-97 | **本体运行方案对接**：①主平台经统一 MCP facade（Runtime Manager，`ONTOLOGY_MCP_URL`）以 MCP Client 接入 onto_* 工具（Eino `GetTools`），断线自动重连；②对话挂载**运行方案**（REQ-19c，存 `runtime_profile_id`），工具入参带 `ontology_id`；③所挂方案未运行/停止 → `ONTOLOGY_SERVICE_UNAVAILABLE` 降级事件（带 profile_id），不影响普通对话与知识库；④指引注入：装配时取方案 guide（概念/关系清单）拼进 Agent 指引；⑤双反向代理：`/api/ontologies*` → 构建平面、`/api/runtime-profiles*` → Runtime Manager（同源透传，前端免跨域） | P1 |
| REQ-98 | **模型能力代理（支撑本体 AI 创建）**：主平台提供 `/api/ontology-llm/generate` 端点——本体构建平面复用主平台已配置的模型连接做结构化生成（Eino ChatModel + spec_json Schema 约束），构建平面只传 prompt/schema 并接收草稿；**模型能力归主平台，校验归构建平面**（本体文档 REQ-82/O5 依赖本端点）。**v0.21 注**：KB GraphRAG 的 KG 抽取（11 REQ-107/D-O15 自研口径）同样复用本端点做 LLM 抽取——本端点实际范围已扩展为"本体构建平面 + KB KG 抽取"共用的模型代理，特此回写口径（11 v0.3/17 §3.1 已按此引用） | P1 |

> 对接要点（详见技术方案 §6.10/§6.11 与本体文档 §5）：主平台**不实现**本体查询/存储/编排，只做 MCP 客户端、代理与降级；Runtime Manager facade 对 Agent 侧契约稳定（4 个 onto_* 工具），主平台无需感知各引擎运行时差异。本体 v0.6 D-O5 定案：**P1 唯一运行引擎为 Oxigraph**（onto_*→SPARQL 翻译，自建本体经 spec_json→TTL 导出装载），memory_graph 延后 P2 可选（集成开源 Cayley fork）——该演进不改变主平台对接契约。

### 3.9 产品体验横切需求（v0.21 新增；编号分配见 18 号注册表）

> 背景：2026-09-23 产品规划缺口分析（17 v0.1 起草 + 主人授权全项落实）识别的横切空白，集中登记于此；实现形态随 P2 里程碑排期。

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| REQ-111（自 17 v0.1 转正） | **全局导航收敛与模块首屏规范**：①顶部导航维持五项，OpenOntologies 不设独立导航，双轨能力由本体构建路径 + 运行引导页承载（REQ-99 已随之修订，REQ-100 双轨原则不变）；②各模块首屏空态统一引导卡规范——无数据时不留白，引导至学习中心/对应创建动作；全产品唯一 onboarding 主线是学习中心任务卡 | P2 |
| REQ-112（自 17 v0.1 转正） | **模块空态与首用引导**：各模块首用引导矩阵——智能体无 Agent → 引导建第一个 Agent（附示例配置）；本体无资产 → 引导学习中心示例；知识库无库 → 引导建库+示例文档；对话 chip 无可用项 → 置灰+Tooltip（REQ-19b/d 范式推广）｜✅ v0.26 已实现：共享 EmptyGuide 引导卡组件接入智能体/项目/知识库三左栏（17 §9.1 实现注记） | P2 |
| REQ-113 | **数据导出与生命周期管理**：①对话导出为 Markdown（消息 + 可选过程事件附录，供学习笔记归档）；②设置页「数据与安全」激活最小版——按对话/项目删除并提示级联范围（现仅"停服删库"一种手段，见 16）；③SQLite 增长声明：会话/事件/KG 数据量级与清理建议写入 16 号文档｜✅ v0.25 已实现：①`GET /api/conversations/{id}/export?events=1` + 对话头部「导出」下拉；②`GET /api/stats/storage` + 设置「数据与安全」激活（DB 体积/各表行数概览 + 会话/项目列表带级联规模与删除确认）；③16 号 §9 增长声明（各表来源/量级/清理建议） | P2 |
| REQ-114 | **绑定目录安全口径**：项目绑定本地目录即授予模型在该目录范围内的读写权限（list_files/read_file/save_file + Git 只读）——①绑定/修改目录时界面明示授权范围与风险；②模型文件操作在过程时间线可审计（工具事件已覆盖）；③路径越界由 fsutil.SafeJoin 强制（02 v0.33）；§7 安全行随之更新 | P2 |
| REQ-115 | **三态与错误文案规范**：全产品统一空态/加载态/错误态的组件与文案规范；错误文案三要素——发生了什么 / 可能原因 / 下一步动作（REQ-16/19b/19d/59 升级为全站规范；落地时沉淀到 17 号界面口径档）｜✅ v0.26 规范已沉淀：17 §9.2 三态规范表（空态 EmptyGuide/加载/错误三要素），存量页面按此渐进对齐 | P2 |
| REQ-117 | **执行调试模式**：对话头部三档观测级别（0 简洁 / 1 详细 / 2 调试，每会话记忆）——**详细档**：run.started 附装配快照（协作模式、各 Agent 模型/工具及来源/技能/MCP）、新增 model.step 事件（每轮模型调用：agent/轮次/耗时/分步 usage/finish_reason/输入规模与逐条预览/绑定工具名）、tool.result 增执行耗时；**调试档**：另附每轮模型输入 messages 全文（截断 4KB/条）与工具 schema（含描述）。详细/调试档事件仅实时透传不入库（存储膨胀控制，入库开关与事件流导出为阶段二）。定位：商业产品不展示的执行细节（最终提示词/模型输入/分步代价）正是学习 ReAct 与 prompt 工程的核心教具（LG-2/5/10） | P1 |
| REQ-116 | **参考资料中心**：顶部导航新增「参考资料」入口（REQ-99 口径随之更新为五业务模块 + 参考资料 + 设置）；页面按**模块为单位**左侧分类（平台总览/智能体/项目/本体/知识库/技能/设置），每类展示该模块的**产品定位 / 设计原理 / 相关资料**（内部文档编号指针 + 外部开源/厂商链接）；内容资产随仓库维护（seeds/learning/reference/*.md，构建期内联，编辑后重建即生效，REQ-109 同模式），定位=把散在 docs/research 的设计语境带入产品内，服务学习动线｜✅ 已交付（2026-09-24，ReferencePage + seeds/learning/reference/ 8 篇（v0.44 增 DeepSeek Harness 专题页）；**v2 升级（二级主题树+更名平台知识）→ REQ-161/M25** | P1 |
| REQ-166（v0.47 新增；✅ 已交付 2026-09-26，bd8cb50） | **内置系统级 Agent「平台助手」（Plata）**：平台内置一个专用 Agent——inprocess 后端、默认模型连接与采样配置、固定系统提示词（角色=平台使用助手：解释模块机制/优化文本内容/执行配置操作），不占用户 Agent 列表、不可删除；配置界面（提示词微调/模型覆盖/温度）加到**设置模块**新增「平台助手」分区 | P1 |
| REQ-167（v0.47 新增；✅ 已交付 2026-09-26，bd8cb50） | **AI 内容优化按钮**：复杂输入框通用组件——在 Agent 系统提示词等编辑区上方/旁显示「AI 优化」按钮，点击由**内置系统级 Agent（REQ-166）**优化当前内容（优化前后 diff 预览，用户确认后回填）；首批挂载：Agent 系统提示词、项目级约束；组件化设计供后续输入框复用 | P1 |
| REQ-168（预留，暂不排期） | **悬浮 AI 对话泡泡**：页面右下角悬浮入口，点击展开对话面板——输入操作指令由内置系统级 Agent 执行配置（如"帮我把 X 智能体切到 dsh 后端"→ 结构化操作建议+确认执行），输入问题由内置系统级 Agent 解释回答 | P3 预留 |
| REQ-149（v0.38 新增；✅ 已交付 v0.40） | **展示级别作用于历史回放**（REQ-117 配套）：①**回放按当前级别过滤渲染**——历史事件时间线依据会话当前展示级别呈现（简洁档隐藏 model.step/装配快照细节；详细/调试档展开已入库的调试事件），纯前端过滤可先行；②**数据前提联动**——级别 ≥1 而运行时 debug_persist 关闭时，调试细节不会留存：调整级别时明确提示"历史生效范围"或提供自动开启 debug_persist（对话级记忆）选项；③**诚实标注**——入库时已截断（单条 4KB）与存量未入库运行不可追溯，界面须明示"该运行未记录调试细节"而非静默留白；实现提示：回放过滤为 ChatWindow 前端逻辑，联动为设置语义，不涉后端新端点 | P2 |
| REQ-111~117 | **产品体验横切**：导航收敛与首屏规范 / 空态引导 / 数据导出与生命周期 / 绑定目录安全口径 / 三态与错误文案规范 / 参考资料中心 / **执行调试模式（M17，已交付）**（§3.9） | P1~P2 | 产品化质量 |
| REQ-148 | **供应商多实例与别名/改名**（§3.4） | P2 | LG-10 |
| REQ-149 | **展示级别作用于历史回放**（§3.9，REQ-117 配套） | P2 | LG-5 |

---

## 4. 功能需求清单（汇总与优先级）

| 编号 | 功能 | 优先级 | 学习目标 |
| --- | --- | --- | --- |
| REQ-01~06, REQ-124, REQ-99 | 智能体/项目/对话 CRUD 与**顶部导航五模块 + 设置**（各视图左列表+右主区；Agent/项目视图折叠树；REQ-124=折叠树，自 94 迁移） | P0 | LG-3, LG-10 |
| REQ-10~16 | 对话详情：多角色消息、流式、过程事件、历史还原、错误态 | P0 | LG-2, LG-9 |
| REQ-20~23,25 | 创建/编辑 Agent 核心配置 | P0 | LG-1, LG-10 |
| REQ-30~32 | 创建项目、配置多 Agent 与协作模式 | P0 | LG-6 |
| REQ-40/41/43/44 | 模型连接管理（预置 DeepSeek、测试连通、Key 加密） | P0 | LG-1, LG-10 |
| REQ-14(停止) | 停止生成 | P0 | LG-9 |
| REQ-42/45/46 | 全局默认模型、连接停用与引用保护、默认向量连接 | P1 | LG-10 |
| REQ-24 | 自定义工具勾选挂载 | P1 | LG-4 |
| REQ-33 | Sequential/Parallel/Loop 工作流 | P1 | LG-7 |
| REQ-34 | 项目级统一约束（Middleware 注入） | P1 | LG-5 |
| REQ-155~158（v0.43） | **本体开源方案吸收落地**：AI-native 本体工具链（Top1）/ 资产质量门禁（Top2）/ 导入审查 UI（Top3）/ 技术研究专栏+吸收池 14 方案登记（方案 only，docs/23 全量推进，Go 优先+全都要+结构化交互） | P1~P2 | | LG-3, LG-8 |
| REQ-07, REQ-50~59 | **知识库（简单 RAG，开源优先）**——v0.12 模块需求独立成档《11_知识库_需求文档.md》，本文档保留对话侧引用（REQ-17/56/58/59，§3.6） | P1 | LG-11 |
| REQ-08, REQ-120 | **技能管理（技能 CRUD + 内置示例）** | P1 | LG-16 |
| REQ-19a/19b/19c/19d, REQ-17/18 | 对话级知识库/本体运行方案选择与过程可视化 | P1 | LG-11, LG-12, LG-13 |
| REQ-19e/19f（v0.9） | **对话对比模式**（开关 + 2~4 列横向等分窗格 + 窗格独立配置：模型/知识库/运行方案） | P1 | | LG-3, LG-8 |
| REQ-150（v0.39；✅ 已交付 2026-09-26） | **对比界面系统优化**：选项承载迁至窗格头（含技能新增覆盖项）+ 窗格视觉系统化（状态徽标/Splitter） | P2 | LG-3 |
| REQ-160（v0.44） | **dsh 推理后端适配器**：第 4 个外部推理后端 | P2 | LG-13 |
| REQ-166 | **内置系统级 Agent「平台助手」**与设置页配置界面 | P1 | LG-10 |
| REQ-167 | **AI 内容优化按钮**（复杂输入框通用组件，由内置系统级 Agent 驱动） | P1 | LG-2 |
| REQ-168 | **悬浮 AI 对话泡泡**（操作配置+解释回答，预留） | P3 | 产品化质量 |
| REQ-161（v0.45） | **参考资料升级「平台知识」**：二级主题树 + 内容扩充（§3.9/17） | P2 | 产品化质量 |
| REQ-162 | **本体学习中心七阶段方块化 + 外部资源迁平台知识** | P2 | LG-16 |
| REQ-163 | **「消费与审计」定位厘清与 KG 边界**（第五栏=本体消费侧观测台） | P2 | LG-12 |
| REQ-164 | **智能体配置补技能 UI + 后端字段移基本分区**（REQ-132 细化） | P2 | LG-16 |
| REQ-165 | **推理后端官方 Logo 自动匹配**（REQ-137 增强） | P3 | 产品化质量 |
| REQ-172（v0.51） | **模型管理 Anthropic 协议支持 + 二级配置界面优化**（协议双通道/预设扩充/弹窗组件化） | P1 | LG-1, LG-10 |
| REQ-143/144（v0.35） | **对话对比扩展**：跨智能体窗格（窗格可选其他 Agent）+ 配置剖面与复用｜✅ 已排期 M19 | P2~P3 | | LG-3, LG-8 |
| REQ-97 | **本体对接**（MCP facade 接入、挂载运行方案、降级、双反代） | P1 | LG-12, LG-13 |
| REQ-98 | **模型能力代理**（支撑本体 LLM 辅助创建） | P1 | LG-14 |
| REQ-100 | **OpenOntologies 集成**（独立双轨：工作台 + oo-worker + Agent 挂载其 MCP 推理） | P2 | LG-12, LG-13 |
| REQ-13 进阶 | 子 Agent 内部事件实时透传展示（EmitInternalEvents） | P1 | LG-6, LG-9 |
| REQ-122 | **Agent 执行后端**：inprocess（P0 默认）/ Docker 沙箱 / K8s Pod | P0 / P1 / P2 | LG-15 |
| REQ-123, REQ-121 | **Agent 级独立配置**（模型/技能/MCP）与技能挂载生效 | P2 | LG-16, LG-13 |
| REQ-131, REQ-132 | **Agent 对外 MCP 服务化（/mcp 端点 + 工具式先行）与配置页改版（SidePanel 四分区 + 创建分级）**——v0.28 立项；已排期 M18；**🚧 WIP（b5de3ac）：/mcp 端点链与四分类页签已交付；阻塞于工具执行环回归（先修）** | P2 | LG-13, LG-16 |
| REQ-133, REQ-134 | **项目配置本地目录改造与配置界面组件化（pro-components 重构，不手搓）**——133 ✅ 已交付（v0.31）；134 ✅ 已排期 M18（与 REQ-132 合并实施） | P2 | — |
| REQ-135~142 | **对话窗口配置分级与平台细节优化批次**——135/136/137/138/139/140 ✅ 已交付（v0.33~v0.35）；141（知识库能力调研）/142（多类型智能体研究）✅ 已排期 M19 调研轨道 | P2~P4 | — |
| REQ-14(恢复) | Interrupt/Resume + 审批 | P2 | LG-8 |
| — | Callback 落 Trace 视图（耗时/token/调用链） | P2 | LG-5 |
| — | DeepAgents 预构建模式体验（write_todos/task） | P2 | 多 Agent 对照 |
| REQ-125/126, REQ-19 | **项目文件管理 + 对话产物右侧文件面板** | P3 | LG-15 |

| — | 知识库进阶：Rerank/混合检索/PDF 解析（开源组件接入） | P2/P3 | LG-11 |
| — | 本体：见本体文档需求池（热加载/fork/外部复用等） | 见本体文档 | LG-12/14 |

---

## 5. 核心数据概念（业务视角，非建表语句）

> 完整字段与建表见技术方案 §5。**v0.6 数据按服务拆分**：主平台库（智能体/对话/知识库/技能/文件）+ 本体模块自有存储（构建平面仓库、运行方案库，见本体文档）。

- **ModelConnection（模型连接，主平台库）**：OpenAI 兼容端点配置，带类型 `chat` / `embedding`；各类型可有一条**全局默认**（P1）；Key 加密存储。
- **Skill（技能，主平台库，v0.6 新增）**：可复用能力包——名称、描述、指令段（注入系统提示词）、工具白名单（可选）、资源（可选）；全局资源，供 Agent 勾选（P2）。
- **Agent（智能体，主平台库）**：可复用配置实体（名称/描述/提示词/模型引用/采样参数/工具/技能/MCP servers/执行后端）；**被多个项目引用，也可单独直聊**；v0.6 起 Agent 名下可挂多个对话（左侧树）。
- **Project（项目，主平台库）**：一组 Agent + 协作模式 + 项目级约束 + **项目文件空间（P3）**的组合。
- **KnowledgeBase / KnowledgeDoc / KnowledgeChunk（知识库三件套，主平台库）**：文档集合（绑定向量连接）→ 文档 → 切分片段；向量存开源向量存储（Q-18），主平台记录索引引用与状态。
- **ProjectFile / ConversationArtifact（项目文件与对话产物，P3）**：项目空间文件元数据 + 对话运行产物归属记录（实际文件存本地项目目录）。
- **Ontology / RuntimeProfile（本体与运行方案）**：**存储与实现在本体模块**（构建平面仓库 + Runtime Manager，见本体文档 §3.1/§4.1）；主平台库**不存本体内容**，只在对话里记录所挂**运行方案的外部 id**（`runtime_profile_id`，O-6 口径）。
- **Conversation（对话，主平台库）**：两种作用域 `agent`（Agent 直聊，Agent 名下多对话）/ `project`（项目下对话）；可关联至多一个知识库（主平台 id）、一个**本体运行方案**（外部 `runtime_profile_id` + 启用开关）、（P2）技能选择随 Agent 配置走；配置随对话保存、不追溯（Q-6）。
- **Message / RunEvent（消息与运行事件，主平台库）**：多角色消息 + 过程事件（工具调用、Agent 切换、知识召回、本体查询、技能注入、产物文件 P3、错误）。
- **Tool（工具注册表，主平台）**：系统内可用工具元信息；**动态工具来源扩展（v0.6）**：①本体运行方案经 MCP facade 下发（onto_*）；②Agent 级配置的 MCP servers 下发（P2）；③技能白名单合并。

关系基数：一个 Project 引用 N 个 Agent（多对多）；一个 Agent 引用 0~1 条对话模型连接（或跟随全局默认）；一个 Agent 名下 N 个直聊对话；一个对话最多引用 1 个知识库、1 个运行方案；技能与 Agent 多对多（P2）。

---

## 6. 验收标准（Definition of Done）

**P0 满足即 v1.0 学习闭环达成：**

1. 能在「设置」预置/填写 DeepSeek 对话连接、**测试连通成功**；列表中 key 为掩码、不明文回显。
2. 平台按**顶部导航五模块 + 设置**组织（Agent/项目/本体/知识库/技能；以《17_产品_信息架构与界面设计》为基线（原型已移除）；OpenOntologies 不设独立导航——v0.21 REQ-111 修订，其工作台经本体模块构建/运行入口进入）；能创建 Agent（名称/描述/提示词/显式选择模型连接），保存后立即可用；**Agent 视图左栏折叠树可展开该 Agent 的多个对话**，重开历史对话完整还原。
3. 单 Agent 多轮对话**流式**呈现；停止生成可用。
4. 能创建项目、配置 ≥2 个 Agent 与协作模式；项目树下新建对话并沟通，界面能看到**主 Agent 委派/切换到子 Agent**与工具调用过程；`agent_as_tool` 与 `transfer` 至少各跑通一个示例（基于 Eino ADK 真实装配）。
5. 配置变更后**下一次运行/新对话**按新配置运行（配置驱动装配，无需改代码重启）。
6. Agent 默认以 **inprocess 后端**运行（执行后端抽象已就位，接口可插拔）。
7. 模型或工具报错时，界面有明确错误提示，后台有日志。

**P1 达成（v1.1）：**

8. **全局默认模型**：可设全局默认对话连接，Agent 未显式指定时跟随；连接停用/删除有引用保护提示。
9. **知识库闭环（开源 RAG）**：配置向量连接后，新建知识库、导入文本完成切分与向量化（**向量存储/检索使用开源组件**——本地向量库或 eino-ext 现成适配）；对话引用知识库提问，回答基于召回片段且界面可见召回来源（REQ-17）；"试运行检索"返回 TopK 与得分；未配向量连接有明确引导。
10. **技能库**：能创建/编辑/删除技能（指令段+可选工具白名单），内置示例技能可预览其将注入的内容。
11. **本体对接闭环**：本体模块（独立会话交付）中已有 running 运行方案 → 主平台对话输入区可选运行方案并启用；Agent 经统一 MCP facade 正确回答"某实例与谁有何关系"类问题，`ontology.query` 事件带 `profile_id/version/via=mcp`；**所挂方案停止 → 明确降级提示且不影响其他功能**；本体构建平面可经主平台代理访问（前端同源）。
12. **模型能力代理**：`/api/ontology-llm/generate` 可用——本体"AI 创建"经主平台模型连接生成 spec_json 草稿（本体文档 O5 联调通过）。
12a. **对话对比（P1，REQ-19e/19f，v0.9 新增）**：对话可开启对比模式——消息区 2~4 列横向等分、底部输入框共用不变；各列独立选择模型/知识库/本体运行方案（未设置的继承对话配置）；一次提问各列并行流式输出；至少跑通"同问不同模型"与"同问同模型不同知识库"两个对比示例（SC-10）。——✅ 已达成（v0.37 交付冒烟：双窗格同问独立流式、模型覆盖生效、整组停止；KB 对照待有可用知识库时按 20 号 2.6 复验）

**P2 达成（v1.2）：**

13. **Agent 级独立配置**：单个 Agent 可覆盖模型连接、勾选技能、配置多个 MCP servers；运行时技能指令注入 + 工具合并生效，过程时间线可见技能/MCP 工具来源。
14. **Docker 沙箱后端**：将 Agent 执行后端切换为 `docker`——每 Agent 一个容器（通用 agent-runtime 镜像），配置下发正确，生命周期（启停/状态/资源限制）可见；inprocess 与 docker 结果一致。
15. K8s Pod 后端（可选进阶）；Interrupt/Resume 审批；Trace 视图；DeepAgents 对照。
15a. **OpenOntologies 集成（P2，可选，REQ-100）**：工作台可装载 TTL 本体并跑通校验/推理/影响分析报告；MCP 服务可启停且状态可见；Agent 挂载「OpenOntologies MCP」后能查询该本体并回答依赖推理的问题（如继承类归属）；停用 oo-worker 后主线功能不受影响（双轨隔离验证）。**入口（v0.21 起）经本体模块构建/运行两入口，不经顶部导航（REQ-111）。**
17. **产品体验横切（P2，REQ-111~115）**：导航五项 + OO 本体模块入口可走通 J2/J3 动线；各模块空态有引导卡；对话可导出 Markdown；绑定目录时可见授权提示；错误提示符合三要素规范（逐项随实现验收，见 20 号冒烟清单）。
18. **Agent 对外服务与配置页（P2，REQ-131/132）**：开启对外服务的 Agent 经统一 /mcp 端点可被外部 MCP 客户端发现并调用（工具名/描述与配置一致；未启用 Agent 不可见）；Token 错误拒答；tool_approval≠off 的工具在 server 会话内默认拒绝并说明；SidePanel 四分区配置保存生效；创建弹窗仅基本必填项即可创建且高级配置默认收起。
19. **本地目录配置（P2，REQ-133）**：validate-dir 检测通过时界面状态显示正常态（不再出现「不存在/非目录」误报）；点击选择按钮可在 Windows 与 macOS 唤起系统文件管理器并回填所选目录绝对路径；手输路径兜底可用。
20. **配置界面组件化（P2，REQ-132/134，已排期 M18）**：智能体与项目配置界面重构后无自研表单/布局控件，均由开源组件（pro-components/AntD）承载；REQ-132 四分区结构保留；构建与冒烟通过。
21. **对话窗口与平台细节优化（REQ-135~140）✅ 已交付（v0.33~v0.35）**：对话窗口配置入口唯一（右侧边栏收放按钮）+对话级分级开关控制运行过程、时间线折叠降噪；对话按首轮问题自动命名；非内置后端智能体展示原 logo；主标题显示「智能体构建平台」（REQ-138 定名）；学习中心卡片内容详实。剩余 141/142（调研/研究）已排期 M19直观；内部方案文档可点击只读查看。

22. **对话对比扩展（P2~P3，REQ-143/144，已排期 M19）**：对比窗格可选不同智能体（窗格头带名称/logo，项目内+全局），默认共享对话历史、可按窗格关闭；窗格配置剖面覆盖推理参数/系统提示词/技能开关并可命名保存复用；「采纳某窗格配置继续对话」为可选增强

**P3 达成（v1.3+）：**

16. **项目文件**：项目内上传/管理文件；对话中 Agent 生成文件落项目空间，右侧「文件」面板可见可下载，产物事件进时间线。

---

## 7. 非功能需求（学习尺度，非生产标准）

| 维度 | 目标 |
| --- | --- |
| 运行环境 | 本地单机；进程形态：**主平台 + 本体模块两平面（Runtime Manager + 构建平面，本体文档定义）+ oo-worker（OpenOntologies 集成，P2 可选）+ 可选开源向量库容器 + P1+ 的 Agent 沙箱容器**；提供一键脚本/`docker-compose` 拉起；单机 Docker / 单集群 K8s（学习尺度） |
| 性能 | 单用户低并发；知识库为学习尺度小数据量；本体查询走本地 MCP 毫秒级；沙箱冷启动秒级可接受（不追求生产级弹性）；流式不卡顿 |
| 可维护性 | 前后端分离；**执行后端/向量存储/本体运行时均为可插拔抽象**（接口先行，实现可替换）；技能/MCP/工具统一走"能力装配层"；开源组件升级不穿透业务代码（适配层隔离） |
| 成本 | 对话默认低价 DeepSeek；向量化按需低价/本地 embedding；本体查询与沙箱运行不消耗模型 token |
| 可观测 | 结构化日志 + 前端过程事件（含技能注入、MCP 工具来源标注）；沙箱容器日志可查看；为后续 Trace 留接口 |
| 可用性 | 本体运行方案、向量库、沙箱均为**可选增强依赖**：不可用时对应功能明确降级，主对话链路不受影响 |
| 安全 | 本地运行、服务默认回环监听、不暴露公网；**API Key 加密存储、界面掩码、DB 与密钥不进仓库**；MCP 工具默认只读；沙箱资源限制（CPU/内存）+ 无特权容器；**文件工具路径约束**：写入类工具限项目目录内（P3 路径校验，fsutil.SafeJoin 防目录穿越/符号链接逃逸，02 v0.33）；**项目绑定本地目录 = 授予模型该目录内读写权限**，绑定/变更时界面明示授权范围，文件操作经过程时间线可审计（REQ-114） |

---

## 8. 关键决策记录

| ID | 议题 | 最终决策 | 状态 |
| --- | --- | --- | --- |
| Q-1 | 前端是否自己写？ | **自己写** React + TS + Vite；v0.10 拍板采用**全蚂蚁生态**（Ant Design 5 + Pro Components + Ant Design X，结论主档 02 v0.9 §10） | ✅ 已定 |
| Q-2 | 用哪个模型、怎么配？ | 默认 **DeepSeek**（OpenAI 兼容）；**模型连接页面内配置**（多连接、测试、key 加密）；v0.6 口径：**全局默认模型 P1**（REQ-42），Agent 级覆盖 P2（REQ-123） | ✅ 已定（v0.6 更新优先级） |
| Q-3 | 数据存哪？ | **SQLite**（主平台）；**知识库向量 v0.6 起优先开源存储（Q-18）**，SQLite 方案降级为 fallback；本体数据归本体模块自有存储（独立维护） | ✅ 已定（v0.6 更新） |
| Q-4 | 多 Agent 第一期主推哪种？ | 主推 **AgentAsTool**（官方推荐、上下文隔离、可并行），Transfer 对照 | ✅ 已定 |
| Q-5 | 要不要登录/账户？ | **不做**，单用户本地 | ✅ 已定 |
| Q-6 | 配置改了，历史对话是否追溯生效？ | **不追溯**，仅对之后的新消息/新对话生效 | ✅ 已定 |
| Q-7 | 是否需要画布拖拽？ | 多 Agent 编排**不做画布**，表单 + JSON | ✅ 已定 |
| Q-8 | 知识库做到什么程度？ | **简单 RAG**：txt/md + 固定切分 + embedding + TopK 检索；不做 PDF/Rerank。P1 | ✅ 已定 |
| Q-9 | embedding 用什么？ | 模型连接扩展 `embedding` 类型，用户配置 OpenAI 兼容向量服务（DeepSeek 无 embedding） | ✅ 已定 |
| Q-10 | 本体做到什么程度、如何接 Agent？ | 精确查询工具化、不做 OWL 推理——**实现细节移交本体文档**（v0.2 构建平面/运行平面）；主平台保留对接契约（REQ-97/98） | ✅ 已定（v0.6 拆分） |
| Q-11 | 知识库/本体在哪一级选择？ | **对话级**：对话输入区选择；随对话保存、不追溯；全局资源跨项目/直聊复用 | ✅ 已定 |
| Q-12 | 知识库如何注入模型？ | **检索增强注入**（先检索 TopK 拼上下文 + `retrieval` 事件）；"检索做成 Agent 自主工具"放 P2 | ✅ 已定 |
| Q-13 | 本体查询放进程内还是独立服务？ | 独立服务 + MCP——**已演进为"构建平面 + 运行平面多运行方案"（本体文档 v0.2）**；主平台只对接 | ✅ 已定（演进至本体文档） |
| Q-14 | MCP 传输方式？ | 本地默认 **Streamable HTTP**，stdio 对照 P2 | ✅ 已定 |
| Q-15 | 外部本体导入格式分层？ | **移交本体文档**（REQ-80：OWL-RDF/Turtle/CSV/GraphML/spec_json，导入即运行） | ✅ 已定（移交） |
| Q-16 | 本体图形化编辑用什么开源库？ | React Flow + RJSF + dagre——**移交本体文档**（构建平面 §3.4，排 O8） | ✅ 已定（移交） |
| Q-17（v0.6） | **Agent 在哪里运行？（沙箱化策略）** | **执行后端可插拔**：`inprocess`（主平台进程内 ADK 装配，P0 默认，快速实现）→ `docker`（每 Agent 一容器跑通用 agent-runtime 镜像，P1）→ `k8s`（Pod，P2）。**动机**：Agent 级独立配置（REQ-123，P2）要求运行环境隔离——独立沙箱才能让模型/技能/MCP 配置互不污染。**反转条件**：若本地无 Docker/K8s 环境则长期使用 inprocess；若 agent 数量少且无隔离诉求，docker 后端可延后 | ✅ 已定 |
| Q-18（v0.6） | **知识库向量存储/检索用什么？** | **开源优先**：①首选 eino-ext 现成 indexer/retriever 适配（实现时核对组件清单，按可用性选对接）；②本地向量库首选 **Qdrant**（单容器、Apache-2.0、轻量），备选 Milvus（重）；③自研 SQLiteRetriever 降级为**无外部依赖 fallback 与学习对照**（不删除，用于理解 RAG 原理）。**验证任务**：M6 启动时核对 eino-ext 可用组件后定稿对接方式。**v0.12 更新**：开源可选方案横向对比（含微信 AI WeKnora 0.8.0 / RAGFlow 0.27 等平台级成品）已完成，结论**维持组件级方案不替换**，WeKnora 列 P2 独立集成候选——对比归档于《12_知识库_方案设计.md》§5（决策 D-KB1~3） | ✅ 已定（含验证任务；选型对比见 12 §5） |
| Q-19（v0.6） | **技能是什么、怎么生效？** | **结构化技能包**（对齐 Claude Skills 思路）：`{name, description, instruction, tools[], resources?}`——instruction 注入 Agent 系统提示词、tools 并入工具集、resources（P2）按需注入；**不自研技能运行时/沙箱**（技能是配置级能力包，非代码执行环境）；开源格式参考 SKILL.md，存储用主平台 SQLite（P1）→ 后续可兼容目录式导入（P2/P3） | ✅ 已定 |
| Q-20（v0.6） | **文档与会话如何分工？** | 本会话专注**多智能体平台 + 本体对接**：文档改名加「智能体」标识（01/02 编号前缀）；本体实现（构建/运行平面，REQ-80~88）移交《04_本体_方案设计.md》独立会话维护；REQ-60~73 在本文档**冻结为快照**（编号有效，细节以本体文档为准）；本体文档预留对接契约 §5，双方向编号互认 | ✅ 已定 |

---

## 9. 需求池（Backlog，暂不承诺迭代）

> v0.21 治理：每条增加**触发条件**（满足才评估升级）与**最后评估**日期；无触发条件的条目在下次评审时处置（升级/删除）。池内条目不重复登记——同一能力只在本节出现一次并标注关联 REQ。"工具调用前的人工审批"原三处登记（本池 / §4 REQ-14 恢复 / LG-8）合并为 REQ-14 恢复语义一条。

| 条目 | 触发条件 | 最后评估 |
| --- | --- | --- |
| ~~事件流重放视图：对话内按序重放本轮 SSE 过程事件（前端调试/演示辅助）~~ **✅ 已出池交付（2026-09-24）**：并入 M17 阶段二「事件流导出与重放」（02 v0.45，EventReplayDrawer 播放/单步/速度重放 + 事件流 JSON 导出）——触发条件随 REQ-117 调试模式交付而满足 | 已交付 | 2026-09-24 |
| Agent 配置版本/草稿与"发布"概念；对话级临时覆盖 Agent 提示词 | 配置误改真实困扰学习流时 | 2026-09-23 |
~~多模型混用：不同子 Agent 用不同模型~~ **✅ 已满足**（REQ-123 Agent 级模型连接覆盖已交付，每成员可独立选型） | 2026-09-25 |
| Token/成本按项目、按对话、按模型连接统计面板（依赖 LG-5 埋点，REQ-49 已覆盖模型维度） | 需要项目级成本归因时 | 2026-09-23 |
~~工具调用前的人工审批~~ **✅ 已出池交付**（REQ-14 恢复/审批已交付：REQ-135② 对话级覆盖 + 迁移 016 + 审批卡，02 v0.38/0.39）——人工审批能力已实现，池条目关闭 | 2026-09-25 |
| 导入/导出 Agent、项目、模型连接、技能配置（JSON；key 脱敏）——与 REQ-113 数据导出互补（配置面 vs 记录面） | 换机/备份真实需要时 | 2026-09-23 |
| 技能进阶：目录式技能包（SKILL.md 兼容导入）、技能版本、技能市场式分享（本地）；技能资源文件注入（REQ-120 resources 字段激活） | 技能数量多到需要工程化管理时 | 2026-09-23 |
沙箱进阶（**启用，2026-09-25 主人指示**）：镜像仓库与版本管理、沙箱快照/恢复、多机调度 + **沙箱环境分级（REQ-159：gVisor/Kata/sysbox 运行时叠加与执行沙箱选型，调研见 research/智能体沙箱方案调研 §7）**——并入 M10 阶段化（10b 并行开发中，10f 进阶） | 触发即领取（10a 已交付） | 2026-09-25 |
| 知识库进阶：PDF/Word/网页解析、标题语义切分、BM25+向量混合、Rerank、问答反馈、更大规模向量库集群——正式登记于 11 KB-2（触发时优先评估 KB-3 双轨集成，D-KB2） | 见 11 KB-2 触发机制 | 2026-09-23 |
| 对话产物文件进阶：文件 diff/版本、产物与知识库联动（一键入库） | 产物积累到需要版本对照时 | 2026-09-23 |
| 把 ChatWithEino 的 Skill（SKILL.md 知识包）能力接进来（与技能模块天然衔接） | 与"目录式技能包"同触发 | 2026-09-23 |
| 本体相关：见 03 文档需求池（热加载、fork、外部 MCP 客户端复用等） | 见 03 §9/AGENTS.md 存量池 | 2026-09-23 |
| 两轨本体数据互通（REQ-78 登记，v0.8 池条目）：spec_json 仓库与 OpenOntologies data-dir 的 TTL 导出互通 | REQ-100/oo-worker 实际启用时 | 2026-09-23 |
| Agent 挂载本地/远程目录（对标扣子 Coze 资源目录模式，依赖 REQ-101 数据模型） | 项目目录绑定（REQ-101/114）使用成熟后评估泛化到 Agent 级 | 2026-09-23 |

---

## 10. 迭代记录

| 版本 | 日期 | 变更 | 负责人 |
| --- | --- | --- | --- |
| v0.1 | 2026-09-18 | 初版：定位、信息架构、P0/P1/P2、数据概念 | 董奎 |
| v0.2 | 2026-09-18 | 决策拍板（Q-1~Q-7）；模型连接管理 REQ-40~45、SC-5、验收扩 8 条 | 董奎 |
| v0.3 | 2026-09-18 | 新增知识库（最简化 RAG）与本体管理模块及对话级选择（LG-11/12、REQ-07/08、17/18/19 系列、46、50~69） | 董奎 |
| v0.4 | 2026-09-18 | 本体改为独立本体服务（A 方案，Q-13/Q-14）：REST + MCP、主平台 MCP Client、编辑即生效、降级（LG-13、REQ-19c、REQ-70） | 董奎 |
| v0.5 | 2026-09-18 | 本体管理类知识库两级形态；外部导入转正（格式分层 Q-15）；图形化编辑器 REQ-71（Q-16）；打开即用 REQ-73 | 董奎 |
| v0.6 | 2026-09-20 | **模块再梳理 + 会话分工**：①文档命名对齐（01/02 编号前缀 +「智能体」标识）；②本体实现移交《04_本体_方案设计.md》（REQ-60~73 冻结快照，Q-15/Q-16 移交），本平台只留对接契约 REQ-97/98（挂载运行方案、MCP facade、双反代、模型代理，O-6 口径）；③**Agent 沙箱化**：REQ-92 执行后端可插拔（inprocess P0 / docker P1 / k8s P2，Q-17）+ LG-15；④**新增技能管理** REQ-90/91（Q-19，LG-16）+ REQ-08 导航；⑤**Agent 级独立配置** REQ-93（模型/技能/MCP，P2）+ 全局默认模型 P1（Q-2 更新）；⑥**知识库开源优先**（Q-18：eino-ext + Qdrant，自研降级 fallback）；⑦**项目文件 + 对话产物文件面板** REQ-95/96/19（P3）；⑧左侧折叠树 REQ-94/06（Agent 名下多对话）；⑨总原则入档：开源优先、快速实现、避免重复造轮子；⑩新增 SC-7~9、LG-14~16、P3 优先级档；技术方案同步：执行后端抽象、技能系统、开源 RAG 选型、MCP 多 server、DDL/API/里程碑重排、自研边界清单（§2.3） | 董奎 |
| v0.7 | 2026-09-20 | **界面原型确认回写**：①界面原型 v0.2（prototype/05）经主人确认"基本满足预期"，作为信息架构与交互基线（设计说明见 prototype/06）；②§3 信息架构改**顶部导航五模块**（各视图左列表+右主区），新增 REQ-99；REQ-01/02/03/06/07/08/08a 表述随导航形态更新（Agent/项目视图左栏折叠树、知识库左列表右详情同屏、技能左筛选+预留区、本体导航入口=S1~S7 七阶段流水线界面）；③§3.2.1 对话配置条位置语义明确（顶部配置条 + 输入区工具条等价承载）；④§3.8/对接要点同步本体文档 v0.6 与 D-O5（P1 唯一引擎 Oxigraph，memory_graph 延后 P2），对接契约不变；⑤§4 汇总表、验收 2、需求池（事件重放视图）同步 | 董奎 |
| v0.8 | 2026-09-20 | **OpenOntologies 独立集成（拍板）**：①依据《open-ontologies借鉴与引入分析.md》v0.2 §8，主人拍板放弃融入式引入、改独立路径；②导航五模块→**六模块**（新增「OpenOntologies」），REQ-99 更新、§3 信息架构与 ASCII 图、§4 汇总表、验收 2 同步；③新增 **REQ-100**（工作台 + oo-worker + Agent 挂载其 MCP 推理，P2，与主线双轨并行、可整体摘除）；④验收新增 15a（双轨隔离验证）；⑤需求池增"两轨 TTL 导出互通"；技术方案同步：oo-worker 进程/§6.15/环境变量/M8.5（详见 02 v0.8） | 董奎 |

> 后续每次变更请追加一行，不要覆盖历史。
| v0.9 | 2026-09-20 | **对比模式停止语义拍板（主人：整组停止）**：REQ-19e 补充停止语义——对比组为一次运行，共用输入框的停止操作终止全部 N 路，各列已生成内容保留；后端口径入 02 v0.10（stop 端点整组取消，与现有原型交互一致：停止按钮在共用 Sender 上） | 董奎 |
| v0.10 | 2026-09-21 | **知识库需求独立成档**：①§3.6 收编为对话侧对接契约，模块需求（REQ-07、REQ-50~55/57）移交《11_知识库_需求文档.md》（v0.1，REQ-50~59 编号冻结延续，新增 KB-1 选型确认/KB-2 进阶池/KB-3 WeKnora·RAGFlow 独立集成备选 P2）；②开源可选方案对比分析归档《12_知识库_方案设计.md》§5（WeKnora 0.8.0：腾讯微信 AI、MIT、Go+Vue3+Python、anydoc/GraphRAG/Skill 沙箱/Lite 单二进制——**不替换组件级方案**，理由：学习目标 LG-11、Agent 职责重叠、进阶能力在需求池、Vue 前端与蚂蚁生态冲突；决策 D-KB1~3）；③Q-18/§4 汇总/配套文档同步；对话侧引用（REQ-17/56/58/59/19a/19b/45/46）保留本文档 | 董奎 |
| v0.11 | 2026-09-20 | **前端实现选型拍板（主人：全蚂蚁生态）**：Q-1 更新为「自己写 + 全蚂蚁生态」（React 18 + TS + Vite + Ant Design 5 + Pro Components + Ant Design X）；结论主档 02 v0.9 §10，06 §3.7 组件映射同步；对比文档已归档删除 | 董奎 |
| v0.12 | 2026-09-20 | **对话配置重组 + 可拖拽分栏（前端实现回写）**：①REQ-19a~d 承载位置改**输入框下方快捷切换条**（§3.2.1 语义更新，Q-6 不追溯原则不变）；②REQ-10 模型展示移至智能体属性（REQ-20 落点）；③REQ-99 增补：各模块左列表宽度 **Splitter 可拖拽调宽**（220~480px，localStorage 全站记忆）；④智能体标识 glyph 品牌化；技术方案同步 02 v0.10 §10 | 董奎 |
| v0.13 | 2026-09-20 | **对话配置 chips 化（前端实现回写）**：§3.2.1 承载形态改输入框内三枚开关 chip（知识库/本体/技能，豆包式交互：浅/深底状态、可多开、首次启用轻量单选绑定、置灰降级）；检索参数归知识库页；新增 REQ-19g 会话级技能开关（enable_skills，后端字段跟进）；技术方案同步 02 v0.11 §10 | 董奎 |
| v0.14 | 2026-09-21 | **模型管理合并树 + 自动发现 + 使用统计（前端实现回写）**：①REQ-47 供应商-模型单表可折叠视图（替代双页签）；②REQ-48 自动发现（假设契约 POST /api/model-connections/{id}/list-models，多选批量建连 + copy_key_from 共享 Key，未就绪降级手动）；③REQ-49 使用统计（假设契约 GET /api/stats/usage?group_by=model|agent|project，CSS 横条无图表依赖，未就绪降级）；技术方案同步 02 v0.12 §8/§10 | 董奎 |
| v0.15 | 2026-09-21 | **项目工作台增强待办排期（只排期未实现）**：①新增 REQ-101（新建项目绑定本地目录 + git 状态展示，P2/M12）；②REQ-102（项目右侧类 VSCode Git Graph 侧边栏，P2/M12）；③REQ-103（项目配置迁入右侧边栏独立页，P3/M12+）；④需求池预留 Agent 本地/远程目录挂载（扣子式）；技术方案排期 02 v0.13 §12 M12 | 董奎 |
| v0.16 | 2026-09-21 | **模型管理预设增强待办排期（只排期未实现）**：①REQ-104 预置百度千帆 embeddings-v1 为默认向量连接候选；②REQ-105 内置国内常见厂商访问配置预设（DeepSeek/百炼/千帆/智谱/Kimi/硅基流动等）；③REQ-106 添加模型快速引用厂商预设自动填充 + 串联自动发现；均 P2/M12；技术方案排期 02 v0.14 §10/§12 | 董奎 |
| v0.17 | 2026-09-22 | **项目模块需求增补（只排期未实现）**：①REQ-101 增补对话产物写入/读取绑定本地目录（M11 files API/save_file 打通，绑定目录作为项目文件根作用路径）；②REQ-102 增补侧边栏小图标条切换视图（文件视图/Git 视图/配置视图，VSCode activity bar 范式）；均 P2/M12；技术方案同步 02 v0.21 §10/§12 | 董奎 |
| v0.19 | 2026-09-23 | **模型管理预设增强实现回写（REQ-104/105/106 全项，M12 收尾）**：①迁移 011 预置千帆 embeddings-v1 向量连接候选（幂等，仅无 embedding 连接时插入；停用态默认标记，填 Key 启用即默认）；②前端静态厂商预设清单 9 条（providerPresets.ts）；③ProviderModal 预设快速填充 + 保存后串联 REQ-48 自动发现（自动展开挂面板）；ModelModal 无提供商空态引导。冒烟：headless 验证千帆预置行渲染（embedding 默认标记）、预设选择自动填充（智谱 GLM）截图留证；技术方案同步 02 v0.34 | 董奎 |
| v0.20 | 2026-09-23 | **REQ-102 Git 视图深度版实现回写（M12 全项收尾）**：Git 视图升级为分支 chips（本地/远程切换 log 范围）+ 提交历史时间线（合并标识/refs 章/作者时间）+ 每提交变更文件明细（numstat）+ 单文件与完整 diff 预览（≤200KB）+ 工作区未提交变更明细；后端五端点（git-log/git-branches/git-commit-files/git-commit-patch/git-working，修订参数防注入）；顺带修复 gitOutput TrimSpace 吃 porcelain 首行状态空格与 quotepath 中文转义两处既有缺陷。冒烟：真实仓库活体验证——分支 chips/时间线渲染、展开提交文件明细（中文路径原样）、patch 预览 3KB diff、工作区 6 文件 +/− 统计，截图留证；技术方案同步 02 v0.35 | 董奎 |
| v0.21 | 2026-09-23 | **产品规划补强（缺口分析全项落实，主人授权）**：①编号治理——REQ-90~96 冲突裁决（18 号注册表 G-1）：本体学习增强保留 90~96，01 侧技能/挂载/沙箱/Agent 配置/折叠树/项目文件/产物迁移至 REQ-120~126（§3.3/§3.5/§3.7/§4/§8/§9 全量替换，历史版本记录保留原号）；②REQ-99 修订为五项导航 + OO 并入本体两入口（REQ-111 转正，§3 图/§4/验收 2 同步）；③新增 §3.9 产品体验横切（REQ-111 导航收敛与首屏规范 / 112 空态与首用引导 / 113 数据导出与生命周期 / 114 绑定目录安全口径 / 115 三态与错误文案规范），§4/§6/§7 同步；④REQ-98 补注 KB GraphRAG KG 抽取复用；⑤§9 需求池表格化（触发条件/最后评估，人工审批三处合并）；⑥原型引用统一 prototype/05。新增《18_REQ编号注册表》《20_回归冒烟清单》 | 董奎 × 协作 Agent |
| v0.22 | 2026-09-23 | **整理压缩轮**：头部 v0.1~v0.18 变更要点归并为指针（全文在 §10）；REQ-101/102/103/104/105/106 已交付注记压缩为一行结论；原型移除后界面口径统一指向 17 号。2026-09-23 文档整理轮（主人授权）：已完成需求细节压缩、过期/退役内容清除、prototype 原型移除——完整历史见 git 与本文档迭代记录；本轮不改变任何需求语义与验收口径。 | 董奎 × 协作 Agent |
| v0.23 | 2026-09-23 | **REQ-14(恢复) 中断恢复实现交付（M11 收尾项落地）**：ask_human 工具（StatefulInterrupt 挂起 + checkpoint 自动存档）+ `run.interrupted` 事件与 `run.finished.reason=interrupted` + 迁移 012 会话挂起状态 + `POST /api/conversations/{id}/resume` 定向恢复（InterruptCtx.ID → ResumeParams.Targets）+ 前端中断答复卡（choices 快选/自由输入）；新消息运行自动放弃挂起中断。诚实边界：checkpoint 进程内存储，重启后挂起失效。冒烟：stub 模型单测全链路 + 真实模型端到端（中断→UI 答复→恢复续跑）+ headless UI 验证；技术方案同步 02 v0.38 | 董奎 × 协作 Agent |
| v0.23 | 2026-09-24 | **REQ-116 参考资料中心交付**：①导航新增「参考资料」入口（REQ-99 口径更新为五业务模块+参考资料+设置）；②页面按模块分类（平台总览/智能体/项目/本体/知识库/技能/设置），每类展示产品定位/设计原理/相关资料；③内容资产 seeds/learning/reference/*.md 7 篇（构建期内联，REQ-109 同模式，编辑后重建即生效）；18 号注册表 116 分配。冒烟：tsc/vite + headless（导航入口/7 分类切换/正文渲染/外链，截图留证） | 董奎 × 协作 Agent |
| v0.24 | 2026-09-24 | **REQ-14(恢复②) 工具调用人工审批交付（LG-8 危险操作审批）**：①Agent 配置新增 `tool_approval`（迁移 013，""=off / all=全部工具调用前审批），装配层对非 AgentTool 工具统一审批包装；②首次调用 `compose.StatefulInterrupt` 挂起（run.interrupted 事件 kind=approval 携带 tool_name/arguments），恢复数据 approve/deny 定向续跑——批准真实执行、拒绝返回拒答载荷（模型据此调整）；③前端审批卡（参数预览 + 批准/拒绝按钮），AgentModal/AgentSidePanel 表单新增审批下拉；④复用 REQ-14 恢复管线（checkpoint/InterruptCtx.ID/答复卡框架）。冒烟：单测批准/拒绝两路径 + 真实模型端到端（审批挂起→批准执行返回时间 / 拒绝后模型诚实告知）+ 20 号 S2 节口径；技术方案同步 02 v0.39 | 董奎 × 协作 Agent |
| v0.25 | 2026-09-24 | **REQ-113 数据导出与生命周期交付**：①对话导出 `GET /api/conversations/{id}/export?events=1`（Markdown：元信息头 + 按角色分节正文 + 可选过程事件附录 ≤500 条），ChatWindow 头部「导出」下拉（仅消息/含事件）；②`GET /api/stats/storage` + 设置「数据与安全」激活——DB 体积与各表行数概览、项目/会话列表带级联规模（会话数/消息数），删除 Popconfirm 明示级联范围；③16 号新增 §9 SQLite 数据增长声明（各表来源/量级/清理建议）。冒烟：真实库导出 Markdown 结构正确、storage 端点返回真实统计；技术方案同步 02 v0.40 | 董奎 × 协作 Agent |
| v0.26 | 2026-09-24 | **REQ-114①/112/115 产品横切批次交付**：①REQ-114①——项目新建/配置表单本地目录节增「绑定即授权」提示卡（授权范围：list_files/read_file/save_file + Git 只读；越界由 fsutil.SafeJoin 强制；时间线可审计）；②REQ-112——共享 EmptyGuide 空态引导卡接入智能体/项目/知识库三左栏（引导矩阵见 17 §9.1）；③REQ-115——三态与错误文案规范沉淀至 17 §9.2（错误文案三要素），存量页面渐进对齐。冒烟：tsc/vite 构建通过；界面口径沉淀 17 v0.6 | 董奎 × 协作 Agent |
| v0.27 | 2026-09-24 | **REQ-117 执行调试模式交付（M17 阶段一；版本号顺延 v0.27——v0.24~v0.26 已被并行 REQ-112/114/115 批次占用）**：①对话头部三档观测级别（简洁/详细/调试，下拉选择、每会话 localStorage 记忆、运行中禁改）；②后端新增 debugModel 装饰器（包装 BaseChatModel，Generate/Stream 全量采集——含流式旁路 tee）+ model.step 事件（agent/轮次/耗时/分步 usage/finish/输入逐条预览/绑定工具名+schema，仅实时透传不入库）；③run.started 附装配快照（协作模式 + 各 Agent 模型/工具及来源/技能/MCP，详细档含最终指令全文）；④tool.result 增执行耗时（tool.call→result 计时）；⑤前端：调试下拉、model.step 卡（调用链路详情：输入逐条/绑定工具 schema）、run.started 装配快照折叠视图、子智能体嵌套缩进（enter/exit 深度计算）。单测 TestDebugModelEmitsStep + headless 冒烟通过。阶段二（池）：外部 CLI 原始输出、调试事件入库开关、事件流导出重放 | 董奎 × 协作 Agent |
| v0.28 | 2026-09-24 | **REQ-131/132 立项（方案 only，不实现）**：①REQ-131 Agent 对外 MCP 服务化（agent-as-MCP-server）——统一 /mcp 端点（Streamable HTTP）、agent_{id} 工具暴露、Agent 级 Token 鉴权、需审批工具默认拒绝、防自引用与调用深度上限、工具式先行/会话式增强、过程事件入库可回放（LG-13 server 侧闭环）；②REQ-132 Agent 配置页改版——SidePanel 四分类分区（基本/模型与参数/能力/对外服务）+ 创建弹窗分级（基本必填即建、高级配置默认收起）；③§6 里程碑与验收 18 同步；界面口径 17 v0.7 §4.3、技术方案 02 v0.47 §6.13、注册表 18 v1.5 | 董奎 × 协作 Agent |
| v0.29 | 2026-09-24 | **REQ-133/134 立项（方案 only，不实现）+「北极星」表述修正**：①REQ-133 项目配置本地目录改造——validate-dir 检测状态展示修复（检测通过仍显示「不存在/非目录」错位）+ 点击唤起系统文件管理器选目录（Windows 资源管理器/macOS Finder，手输兜底）；②REQ-134 智能体/项目配置界面组件化——@ant-design/pro-components 等开源组件重构不手搓，与 REQ-132 合并实施；③17「北极星」表述统一修正为「产品核心目标」（主人指正）；注册表 18 v1.6、技术方案 02 v0.48 §6.17 | 董奎 × 协作 Agent |
| v0.30 | 2026-09-24 | **REQ-133 按实际异常场景修订（主人补充）**：异常出现于**后端 Linux 部署 + Web 在 Windows 打开 + 目录为 Windows 本机目录**——①状态错位修复明确为前端直连后端分字段结果（路径格式/存在/是目录/后端可达），禁止前端按 POSIX 规则推断 Windows 盘符路径；②选目录方案分层：远程部署主场景下手输+按目录所在主机 OS 语义校验为主（浏览器安全模型取不到绝对路径、后端弹不了用户侧对话框），本地伴生组件/挂载共享为可选增强，同机部署保留系统文件管理器选目录；技术方案 02 v0.49 §6.17、界面 17 v0.9、注册表 18 v1.7 | 董奎 × 协作 Agent |
| v0.31 | 2026-09-24 | **REQ-133 实现交付（检测状态分字段直连 + 选目录分层）**：①validate-dir 增 format_ok/reachable 分字段（exists/is_dir 不可达时为 null），前端 DirCheckResult 共享组件直连渲染——主场景 Windows 路径显示「路径格式合法 + 后端不可达（远程目录）」，不再误报「不存在/非目录」；②同机部署 pick-dir 端点（macOS osascript / Windows FolderBrowserDialog / Linux zenity，3 分钟超时）+ 新建/配置两处「选目录」按钮，失败 toast 提示手输；③fsutil.PathForm 形态分类单测。冒烟：CDP 全动线三场景（Windows 远程 /tmp POSIX 可达 / 相对路径拒绝）全绿。技术方案 02 v0.50 §6.17、界面 17 v0.10、注册表 18 v1.8、冒烟 20 S3.6 | 董奎 × 协作 Agent |
| v0.32 | 2026-09-24 | **REQ-135~142 批次立项（主人指令，方案 only，不实现）**：①REQ-135 对话窗口配置分级与入口整合——去重复配置按钮→右侧边栏收放 + 对话级分级开关控制运行过程 + 展示降噪，P2；②REQ-136 对话自动命名（默认大模型提炼首轮问题），P3；③REQ-137 非内置后端智能体沿用原 logo，P3；④REQ-138 主标题「多智能体学习平台」→「多智能体构建平台」，P2；⑤REQ-139 学习中心卡片内容优化（深度版收起保留、内容加详），P3；⑥REQ-140 内部方案文档点击只读查看，P4；⑦REQ-141 知识库与知识图谱产品能力增强——启动调研（待办），P3；⑧REQ-142 多类型智能体支持方案研究（发展方向待办），P4；另：参考资料智能体/知识库内容补充随 REQ-109 内容资产直接更新（第五节新增 6 条）；里程碑/验收 21 同步；技术方案 02 v0.51 §6.18、界面 17 v0.11、注册表 18 v1.9 | 董奎 × 协作 Agent |
| v0.33 | 2026-09-24 | **REQ-138 口径修正 + 交付标注（主人实时指示）**：主标题定名「**智能体构建平台**」（修正 v0.31 立项文本误记的「多智能体构建平台」——立项与实现并行间隙主人重新明确了用词）；实现已先行交付（9bf8ca2：顶栏 brand-sub/页面标题/参考资料 overview.md 定位行，README 顺带补齐上轮遗漏的 Eino→AgentLab 品牌）；界面 17 v0.12、注册表 18 v1.10 | 董奎 × 协作 Agent |
| v0.34 | 2026-09-24 | **需求池清账**：「事件流重放视图」出池——已随 M17 阶段二「事件流导出与重放」交付（02 v0.45 EventReplayDrawer + JSON 导出），触发条件（演示/教学逐步重放）随 REQ-117 调试模式满足。其余池条目触发条件未变 | 董奎 × 自动化执行者 |
| v0.35 | 2026-09-24 | **REQ-135/136/137/139/140「方案 only」批次实现交付（主人授权转入开发）**：①REQ-135——去独立配置按钮（入口=侧边栏收放，项目侧打开定位配置视图）+ 对话级过程展示面板（粒度 all/key/off + 深度思考 + 原始 JSON，localStorage 按会话记忆）+ 对话级工具审批覆盖（迁移 016 + 装配合并 对话级>Agent 级）；②REQ-136——POST /auto-name 异步提炼标题（15s 超时回退默认名/手动改名保护/二轮校验防竞态），真实模型冒烟「eino多智能体编排示例」；③REQ-137——迁移 017 logo_url + AgentLogo 组件（内置后端回退 glyph）+ 侧栏/空态展示 + 表单登记；④REQ-139——方法论卡片要点结构化渲染（自动分段列表）+ 来源行；⑤REQ-140——/api/docs/read 只读端点（限 docs/ 内 .md，SafeJoin 防越界）+ DocViewerModal + 源文档地图 docs 指针可点击；REQ-141/142 调研文档落档（21/22 号） | 董奎 × 自动化执行者 |
| v0.35 | 2026-09-24 | **REQ-143/144 立项（对话对比扩展，方案 only，不实现）**：在已实现的 REQ-19e/19f 对比模式上增量——①REQ-143 跨智能体窗格（P2）：窗格头「智能体」选择器（项目内+全局，logo 沿用 REQ-137）、按所选 Agent 完整配置执行、默认共享对话历史+窗格级「不携带历史」开关、停止语义与 SSE 契约不变；②REQ-144 配置剖面与复用（P3）：窗格「更多配置」（推理参数/系统提示词改写/技能开关）、剖面对话级命名保存、「复制上一窗格配置」、合并序剖面>窗格>Agent>全局、可选「采纳窗格配置」；主人四点确认（范围含全局/默认共享历史/剖面保存/采纳可选）+「改动克制增量优先」约束；技术方案 02 v0.52 §6.19、界面 17 v0.13、注册表 18 v1.11 | 董奎 × 协作 Agent |
| v0.36 | 2026-09-24 | **方案 only 全量排期**（主人指令）：131/132/134 → M18（M18 去重并扩范围）；143/144 + 141/142（调研轨道）→ M19；§4 与验收 20~22 措辞同步（验收 21 主标题修正为「智能体构建平台」并标注 135~140 已交付）；排期明细见 02 v0.53 | 董奎 × 协作 Agent || v0.37 | 2026-09-24 | **REQ-19e/19f 对话对比模式实现交付（P1 基座补齐）**：①ChatWindow 头部「对比」开关 + 2~4 列 Segmented（按会话记忆）；消息区横向等分窗格（头徽标：模型/库/方案 覆盖或继承 + 底部三项独立配置选择器，空=继承对话配置，Q-6 不追溯）；②后端 `RunInput.Panes` + `RunCompare`：一次提问 N 路——用户消息组级仅落一条、历史快照组级共享（先完成窗格答案不进其他窗格上下文）、每窗格独立 runID 装配（克隆 Agent/Conversation 应用单项覆盖）、助手消息 meta 记录 pane/覆盖、run_events 按路落库；③停止整组（组级 cancel，已生成内容保留）；④SSE 契约不变（meta 增窗格 run_id 映射，事件按 run_id 路由）；⑤单路管线抽取 `runOnce` 复用（Run/Resume 行为不变）；边界：docker 沙箱与外部 CLI 推理后端暂不支持对比（组级明确报错）；⑥勘误：02 §6.19 前提"REQ-19e/19f 已实现"系误记（v0.9 立项至本次前从未实现，纪律 6 以实现为准回修）；验证：go test 新增 compare_test 4 例 + 全量绿 / headless CDP 冒烟（开关/列数/覆盖选择/双窗格独立答案/组停止/开关还原）+ 截图；为 M19（REQ-143/144）提供基座 | 董奎 × 自动化执行者 |
| v0.37 | 2026-09-25 | **REQ-122 沙箱调研落档**：research/智能体沙箱方案调研_20260925.md（骨架盘点/六方案比选/阶段化 10a~10e/安全边界清单）；02 §12 M10 状态改「调研完成待领取」；15 号登记 gVisor/Kata/E2B/Podman 候选 | 董奎 × 文档会话 |
| v0.38 | 2026-09-25 | **体验细节两批次立项（待办，等统一开发）**：①REQ-148 供应商多实例与别名/改名——分组标识与 BaseURL 解耦 + 别名显示层（同供应商多账号场景，主人提出）；②REQ-149 展示级别作用于历史回放——回放按当前级别过滤 + debug_persist 数据前提联动 + 不可追溯诚实标注（REQ-117 配套）；编号经 18 v1.16 分配（148/149） | 董奎 × 文档会话 || v0.39 | 2026-09-25 | **REQ-148 供应商多实例与别名/改名实现交付（a64178f）**：①数据模型解耦——迁移 019 新增 provider_group 表（id/alias）+ model_connection.provider_group_id；启动时按 (protocol, base_url) 确定性分组幂等回填（存量同 BaseURL 仍同组行为不变）；②多实例——同一供应商（同 BaseURL）可创建多个独立实例（不同账号/Key），连接创建校验分组存在；③别名语义分离——别名仅展示层（provider_group.alias，经 ListConnections 以 provider_alias 快照联查下发）：列表/模型下拉/对比窗格统一 connDisplayName（别名·模型名）；改名=真名前缀批量重生成（既有能力保留），编辑弹窗名称字段显示真名前缀、别名字段独立保存；④设置页分组键 pg:{id}（空回退旧键）；供应商统计聚合修正为按 run.started 的 model 标签（连接名@模型名）映射展示名；⑤验证：backfill 幂等/多实例归属/别名联查 2 组单测 + 全量绿；e2e 同 BaseURL 三实例并存 + 别名下拉显示 + 真名/别名字段分离 + 400 坏分组校验 + 截图 | 董奎 × 自动化执行者 |
| v0.39 | 2026-09-25 | **REQ-150 对比界面系统优化立项（待办，等统一开发）**：主人指出对比界面粗糙——①选项承载迁移：对比模式下共享输入区 知识库/本体/技能 chips（现状无条件渲染，代码证据 ChatWindow Sender footer 无 cmp.on 分流）收起为摘要，迁移至各窗格头全量配置区（技能新增为窗格覆盖项，19f 现仅模型/知识库/运行方案三项）+ 继承/覆盖状态显性化；②窗格视觉系统化（窗格头/模型徽标/分隔可调/状态对齐，预留 143 Agent 位）；③与 REQ-143/144 合并实施。登记表 18 v1.17 分配 REQ-150 | 董奎 × 文档会话 || v0.40 | 2026-09-25 | **REQ-149 展示级别作用于历史回放实现交付（f03c1fe+5ee3ddd）**：①级别过滤渲染——levelGated 门控（简洁档隐藏 model.step 等调试细节事件）：ChatWindow 历史时间线渲染层过滤（级别切换即时生效）+ EventReplayDrawer 按 level 过滤重放流；②数据前提联动——debug_persist 首次获得 UI 面：「过程展示」面板「调试事件入库」开关（对话级记忆），run/resume/对比发送透传；级别调至≥详细且未开入库时 toast 提示 + 面板联动警示（开启即消）；③诚实标注——回放器三条：简洁档「已隐藏 N 条调试细节事件」/ 无记录「该会话历史未记录调试细节（未开入库或早于该功能，存量不可追溯）」/ model.step 含「截断」时注明输入全文约 4000 字截断；④顺修 M17 阶段二既有 bug：debug_persist 开启时 model.step 在 SSE 双发（record 与 emit 双路），改二选一；⑤验证：后端差分 e2e（persist 开→落库 1 条/关→0 条/修复后 SSE 不重复）+ UI 四态冒烟 + 截图。**补录**：v0.39 REQ-148 交付迭代行曾被并行写入覆盖（01 v0.39 行现为 REQ-150 立项），交付细节见 02 v0.56/v0.57 与 20 v1.7 | 董奎 × 自动化执行者 |
| v0.41 | 2026-09-25 | **REQ-143 跨智能体窗格实现交付（df25e82，M19 阶段一）**：①对比窗格头显示窗格 Agent（AgentLogo+名称，覆盖/继承即时反映）；②窗格级 Agent 独立装配——PaneConfig 增 agent_id（项目成员/全局候选，后端 GetAgent 按完整配置装配），组级后端守卫下沉窗格级（docker/外部 CLI/不存在 → 该窗格 run.error 不阻断整组）；③窗格级「不携带历史」开关（干净对照：仅本轮提问快照，e2e 差分 input 5 vs 2）；④run.started 增 agent_id；⑤助手消息 meta 有效配置快照增 agent_id+no_history；停止语义与 SSE 契约不变。验证：单测六场景 + 全量绿；e2e 双 Agent 对比/差分/守卫 + UI 冒烟截图。REQ-144（配置剖面）与 REQ-150（选项承载迁窗格头）待后续随 M19 合并实施 | 董奎 × 自动化执行者 |
| v0.42 | 2026-09-25 | **REQ-144 配置剖面与复用实现交付（f1fdc40，M19 阶段二）**：①窗格「更多配置」折叠区——推理参数（温度）、系统提示词临时改写（仅本窗格）、技能开关（继承/开/关，REQ-19g 口径）；②配置剖面对话级命名保存（localStorage 不做全局剖面库）与应用（覆盖合并序「剖面 > 窗格单项 > 智能体 > 全局」toast 明示）；③「复制上一窗格配置」快捷操作；④采纳转正（可选增强）：agent 直聊窗格头「采纳」按钮把智能体/知识库/方案/技能开关写回对话配置，项目会话不提供（协调者变更属项目配置职责，诚实边界）；⑤后端 PaneConfig 增 temperature/instruction/enable_skills（克隆应用 + meta 快照扩展）。**顺修 research/ChatWindow渲染与SSE链路审查 P0 两项**：B1 exportEventsJSON 文件名字面量、B3 切会话不 abort 旧流。验证：单测扩展 + 全量绿；e2e 提示词改写差分（海盗腔 vs 正常介绍）+ meta 三窗格快照 + UI 冒烟（剖面保存/应用/复制/采纳）+ 截图。M19 对话对比扩展（143/144）至此全项完成，REQ-150（选项承载迁窗格头）待后续 | 董奎 × 自动化执行者 |
| v0.40 | 2026-09-25 | **排期补全与池清理（主人指令 a+b）**：①池出池三条——事件流重放（M17 阶段二已交付）、工具审批（REQ-14 已交付）、多模型混用（REQ-123 已支持）；②沙箱进阶启用——REQ-159 沙箱环境分级（gVisor/Kata/sysbox 叠加 + 执行沙箱选型，调研 research/智能体沙箱方案调研 §7），并入 M10 阶段化；③REQ-150 对比界面系统优化已立项（M19 阶段三） | 董奎 × 文档会话 |
| v0.43 | 2026-09-25 | **REQ-155~158 立项（本体开源实现方案借鉴研究落档，方案 only）**：主人指令——调研报告（docs/23，7 方向 18 方案）全部排期为 P1/P2 候选；三原则：选型全都要、Go 优先排期、结构化交互；登记表 18 v1.21 分配 REQ-155~158（另有外部调研报告 docs/23+24 入库） | 运维本体开发助理(云端) |
| v0.44 | 2026-09-25 | **REQ-160 dsh 推理后端适配器交付（M24 阶段一/二）**：第 4 个外部推理后端（M13 cliAdapter 骨架第 4 例，dsh headless + Minimal 裁剪 + 锁版本 0.1.5-rc.3）；接线冒烟通过（backend=deepseek-harness + MISSING_CREDENTIAL 结构化透出）；阶段三 agentd 镜像内置随沙箱合流；18 v1.22 撞号治理同步 | 董奎 × 文档会话 |
| v0.45 | 2026-09-25 | **REQ-161 立项排期（参考资料升级「平台知识」，主人提出二级标题/内容扩充/更名）**：①更名参考资料→平台知识（学习视图层定位，17 §1.4）；②左栏可收缩两级主题树（AntD inline SubMenu 原生折叠）；③内容扩充首批矩阵（智能体 5 题/本体 5 题/知识库 4 题+既有 8 页保留）；登记表 18 v1.24 分配 REQ-161；排期 M25（P2） | 董奎 × 文档会话 |
| v0.46 | 2026-09-25 | **体验细节四批次立项（待办，等统一开发）**：①REQ-162 学习中心七阶段改方块+箭头步骤条、外部资源 Tab 迁平台知识（REQ-109 挪位不废弃）；②REQ-163 消费与审计定位厘清——第五栏 KG 检索限定本体 TTL 装载来源（来源徽标），KB 抽取 KG 归知识库侧（文档线梳理结论落 03 D-O19）；③REQ-164 智能体配置补技能 UI + 运行/推理后端移基本分区（主人指出技能配置缺失/后端放错分区）；④REQ-165 推理后端官方 Logo 自动匹配（导航树保持智能体默认标识）。登记表 18 v1.25 分配 REQ-162~165 | 董奎 × 文档会话 |
| v0.47 | 2026-09-25 | **内置系统级 Agent 与 AI 辅助立项（主人提出，168 预留）**：①REQ-166 内置系统级 Agent「平台助手」（inprocess+默认模型与配置，设置页配置界面，不占用户列表不可删）；②REQ-167 AI 内容优化按钮（Agent 提示词等复杂输入框旁「AI 优化」，diff 预览确认回填）；③REQ-168 悬浮 AI 对话泡泡（P3 预留：操作指令执行+解释回答）。登记表 18 v1.26 分配 REQ-166~168 | 董奎 × 文档会话 |
| v0.48 | 2026-09-25 | **REQ-161 结构基座交付 + REQ-169 立项（平台知识文档体系整理，主人指令）**：①文档分类——docs/ 保留开发辅助活文档，产品设计/技术原理/研究 7 篇（17/19/21/22/23/24/25）迁 platform-knowledge/ 按模块子目录（本体/智能体/知识库/产品设计，不平铺）；②学习导读 8 篇自 seeds/learning/reference/ 迁入 platform-knowledge/ 各模块目录；③页面目录驱动二级主题树+模块更名（参考资料→平台知识）+外部资源主题页挂载；④REQ-169 立项：互引相对路径化+点击相对路径→右侧 Drawer 阅读（复用 REQ-140 组件；后端只读白名单扩 platform-knowledge/，代码基座已交付）；⑤前端交互完善与构建验证主人本地开发（云环境依赖未装齐未构建）；需求 18 v1.27、20 v1.15、17 v0.19 | 董奎 × 文档会话 |
| v0.49 | 2026-09-26 | **REQ-150 对比界面系统优化交付收尾（M19 阶段三，代码 f280135 2026-09-25 主人本地先行 + 本轮补齐验证/缺陷修复/文档回写）**：①选项承载迁移——共享输入区 chips 收起为摘要文案（对比模式不参与配置）、模型/库/方案/技能四徽标迁窗格头（技能新增覆盖项，覆盖态品牌高亮显值名/继承态浅色+Popover 内显继承来源值）；②窗格视觉系统化——窗格头（窗格名+Agent logo/名称+徽标组+采纳）、Splitter 分隔宽度可调、停止按钮 title 注明整组语义；③本轮补齐：窗格头状态徽标（待提问/生成中/完成/出错/已停止，脉冲动画）+ 顺修停止断流缺陷（abort 后 run.finished 收不到致流式游标永挂——stopFlag 标记+收尾兜底置终态，单路/对比两路同修）；④验证：go build/go test 全绿 + vite build + CDP 冒烟（徽标覆盖 flashx 生效/SC-10 双 Agent 同问对照/停止→已停止/Splitter 拖宽 580→660/刷新配置持久化）+ 截图 2 张存 smoke/req150/；02 v0.68/17 v0.20/20 v1.18/18 v1.30 同步 | 董奎 × 自动化执行者 |
| v0.50 | 2026-09-26 | **M10 10b 交付回写领取（9b0d45c 代码 2026-09-25 先行，本轮补齐文档侧——AGENTS「当前状态」悬置项清账）**：REQ-122 §3.3 行「🔍 调研完成待领取」→ ✅ 10a/10b 已交付标注（96210e6/9b0d45c：docker 后端 SANDBOX_IMAGE 全链实测 + 沙箱生命周期可见化/资源限制参数化）；§3.7 行补交付注记（sandbox_memory/sandbox_cpus 迁移 020 + /api/agents/{id}/sandbox 状态与启停端点 + SidePanel 沙箱面板）。经核对 20 号 S2.8 的 10b 检查项已在 v1.12 随交付当日并入（无缺）；17 v0.21（LG-15 行去「M10 未激活」+ §4.3 实现注记）、02 v0.69（M10 行回写收口）同步 | 董奎 × 自动化执行者 |
| v0.51 | 2026-09-26 | **REQ-172 立项即交付（主人提出：模型管理支持 anthropic 协议 + 二级配置界面优化）**：①协议新增 `anthropic`——连接协议从仅 openai_compat 扩为双通道，后端按协议建模（internal/modelproto 纯函数包 + chat 三处建模收口 `buildChatModel`；anthropic 走 eino-ext/components/model/claude v0.1.20，15 v2.13 登记；网关根地址归一自动拼 /v1/messages；温度钳 [0,1]；anthropic+embedding 组合拒绝）；测试连接/自动发现（REQ-43/48）增 anthropic 分支；②二级配置界面——ProviderModal/ModelModal/DiscoverPanel 抽取 `web/src/pages/settings/` 组件 + 分组化布局 + 协议感知动态表单 + 预设清单 +4 条 Anthropic 系。02 v0.76（§12 M29 行）/15 v2.13/20 v1.24（S6.6）/18 v1.37 同步 | 董奎 × 协作 Agent |
| v0.52 | 2026-09-26 | **REQ-169 交付（主人四点体验反馈驱动收尾，平台知识文档互引阅读）**：①平台知识页侧栏菜单区自身滚动；②侧栏 L1 分组改规划序（平台总览置顶 → 五业务模块按导航栏顺序 → 产品设计/DeepSeek Harness/外部资源 → 设置殿后；组内「模块导读」置顶、专题按文档编号升序、展示标题去编号前缀——不再照搬目录存放顺序）；③右侧阅读抽屉默认关、点击互引才展开、可手动关闭；④互引链接点击全链修复——根因：DocViewerModal 误作 Splitter 直接子元素被 AntD Splitter 吞成空白面板（「右侧默认空白栏」与「01_PRD 链接点击无反应」共同根因），移出 Splitter；抽屉内 XMarkdown 补 openLinksInNewTab（默认渲染器对中文 href 做 encodeURI 致 docRead 按字面路径找不到）+ resolveRef 百分号编码防御解码（抽共享 web/src/lib/docref.ts）+ 抽屉内互引可续点（按当前文档目录解析，跨目录跳转实测 docs/03→platform-knowledge/智能体）+ 抽屉剥 frontmatter 头。20 v1.25（S1.4）/18 v1.39/17 v0.23 同步；截图 smoke/req169/ | 董奎 × 协作 Agent |
| v0.53 | 2026-09-26 | **REQ-169 二轮微调（主人指定三处挂载）**：平台知识页 L1 由十组收敛为七组（平台总览/智能体/项目/本体/知识库/技能/设置）——产品设计（17 号）挂平台总览二级、DeepSeek Harness 挂智能体组、本体学习外部资源导航（外部资源主题页，seeds 单源）挂本体组；实现为 TOPIC_MOUNT 显式映射（展示名随挂载，互引解析 base 仍按真实存放目录）；组内主页置顶规则扩展（模块导读/平台总览）。17 v0.24/20 v1.26（S1.4）同步；截图 smoke/req169/ 06/07 | 董奎 × 协作 Agent |