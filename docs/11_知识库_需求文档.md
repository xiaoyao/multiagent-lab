# 知识库需求文档（智能体平台）

> 状态：Draft v1.0
> 日期：2026-09-26
> 文档性质：**活文档**。知识库模块的唯一需求事实源；对话侧引用契约与全局导航等横切需求仍留在《01_智能体_需求文档_PRD.md》（v0.12 起 §3.6 收编为引用）。
> 关联：01 v0.10（对话侧引用 REQ-17/19a/19b/56、模型连接 REQ-45/46；原 v0.12，已重编号）、02 v0.11（技术栈与可插拔抽象）、12《知识库_方案设计.md》（设计事实源 + 开源方案对比）
> 会话分工：知识库模块（本文档）由智能体开发会话维护，与本体文档（03/04）同模式独立演进。

---

## 1. 定位与学习目标

| 项 | 内容 |
| --- | --- |
| 模块定位 | 知识库是主平台内的**全局资源**（非外部服务）：库列表/库详情两栏视图，文档入库→切分→向量化→检索→对话注入的完整闭环。 |
| 学习目标 | **LG-11（承接 01）**：掌握 RAG 最小闭环——文档加载/切分/Embedding/检索/上下文注入；v0.6 起基于开源组件搭建而非自研。 |
| 实现基调 | 开源优先（Q-18 已定）：向量存储与检索优先使用开源组件；自研仅保留 SQLite fallback 作学习对照。选型结论与对比分析见 12《知识库_方案设计.md》§5。 |
| 边界 | 简单 RAG：不做 PDF/OCR/表格抽取、Rerank/混合检索、GraphRAG 等进阶能力（需求池见 §4）；对话侧如何选择与引用知识库见 01（REQ-19a/19b/17/56）。 |

## 2. 需求列表

> 编号沿用 01 原 REQ 编号（可追溯），新增需求用 KB- 前缀。P1 为主（除注明）。

| 编号 | 名称 | 需求描述 | 优先级 |
| --- | --- | --- | --- |
| REQ-07 | 知识库界面（v0.7 同屏详情） | 知识库视图：左栏库列表（名称/状态徽标），右栏为选中库详情（文档/片段管理），同屏切换；支持「新建知识库」「重命名」「删除」 | P1 |
| REQ-50 | 知识库 CRUD | 新建/编辑/删除知识库：名称、简介；知识库为全局资源，可在任意对话引用 | P1 |
| REQ-51 | 内容接入 | 向知识库添加内容：支持**粘贴文本**、上传 `.txt` / `.md` 文件（UTF-8）；以"文档"为单位记录来源名 | P1 |
| REQ-52 | 文档切分 | 按固定长度 + 重叠（默认 500 字/重叠 50 字，可配置）的简单切分器切成片段（Eino 组件或轻量开源实现） | P1 |
| REQ-53 | 向量化与存储（开源优先，01 Q-18 / 12 D-KB1~3） | 用 embedding 连接把片段转向量，存入**开源向量存储**（首选本地轻量方案 Qdrant 容器；优先复用 eino-ext 现成 indexer/retriever 适配，实现时核对可用组件）；自研 SQLite 存向量方案降级为**无外部依赖 fallback / 学习对照** | P1 |
| REQ-54 | 文档/片段管理 | 知识库详情列出文档（可删除，级联清理片段与向量）；展示片段数、向量维度、更新时间 | P1 |
| REQ-55 | 索引状态可见 | 向量化进行中/成功/失败（失败显示原因）；支持对失败文档重建索引 | P1 |
| REQ-56 | 对话引用（**对话侧，保留 01 §3.6**） | 由 01 REQ-19a 选择知识库；提问时检索 TopK 片段（K 可配置，默认 4）注入上下文；交互语义归 01，检索与注入设计见 12 §3 | P1 |
| REQ-57 | 检索质量与试运行 | 可配置 TopK、相似度阈值；知识库详情提供"试运行检索"框，直接看召回片段与得分 | P1 |
| REQ-58 | 召回来源（**对话侧，保留 01 §3.6**） | 召回片段带来源（文档名/片段序号），回答过程可展开查看（01 REQ-17）；来源数据契约见 12 §3.4 | P1 |
| REQ-59 | 空库引导（**对话侧，保留 01 §3.6**） | 无向量连接或索引为空时，选择/提问给出明确引导，不静默用空知识作答（01 REQ-19b 联动） | P1 |
| REQ-107（v0.2 新增，D-KB4） | 知识库模块 IA 拆分「向量检索 RAG」「GraphRAG 检索」双子模块 | 同一模块页内左侧边栏顶部 tab 区分两子模块；共享 chunk 池，`kb.mode` 区分（默认 'rag'，老数据兼容）；GraphRAG 子模块复用本体侧 semantica worker :8093（`/graphrag/ingest` 抽 KG + `/query` 检索），KG 只落 semantica 侧；端点 `POST /api/kb/{id}/graphrag-search`；`retrieval` SSE 事件增 `mode`/`degraded`；worker 不可达降级不阻断；KG 与 embedding 模型一致性提示；设计见 12 v0.2 §3、02 v0.25 拍板——**⚠️ v0.3 注（D-O15）**：KG 源已切自研内置 backend（SQLite 三表自存 + REQ-98 LLM 抽取，graphrag 导入即建 KG、失败 degraded 不阻断），worker/:8093 反代口径作废 | P1（**已交付 c3cbe57**；KG 源切换 7d45dc0） |
| REQ-127（v0.6 新增；v0.7 实现回写） | **KG 图谱浏览与统计**：graphrag 库详情新增「图谱」视图——①实体搜索（名称/别名模糊匹配）；②点击实体展开**邻域图**（React Flow：关系边 + 邻接实体，可继续下钻）；③claims 列表带 **chunk 溯源**（点击查看原文片段）；④统计卡（实体数/关系数/claims 数、抽取方法分布 llm\|lightweight、degraded 文档数）；⑤文档删除时 KG 级联清理有确认提示 | P1 |
| REQ-128（v0.6 新增；v0.7 实现回写） | **KG 检索增强：实体聚焦与多跳**：graphrag-search 能力升级——①**实体聚焦查询**（entity 参数：从指定实体出发遍历，不再仅从命中片段出发）；②**跳数可配置**（1~2 跳，默认 1）；③关系类型过滤；④claims 结果带 chunk 原文定位（前端高亮溯源）；⑤retrieval SSE 事件携带命中实体/关系明细（对话侧三联卡渲染升级） | P1 |
| REQ-129（v0.6 新增；v0.8 实现回写） | **KG 抽取治理与人工反馈**：①库级抽取配置（抽取模型跟随默认或指定连接、提示词模板覆写）；②**实体消歧**（同名/别名合并 merge 接口 + 系统合并建议）；③**审核队列**：对抽取的关系/claims 人工 approve/reject，rejected 不参与检索；④质量面板：method 分布、孤儿实体数、高频关系类型 TopN | P2 |
| REQ-130（v0.6 新增；阶段二随触发条件评估） | **社区摘要与全局问答**（对标微软 GraphRAG global search）：社区检测（轻量 label propagation，学习尺度）→ 每社区 LLM 生成摘要（缓存，重建失效）→ **全局性问题**（「这个库整体在讲什么」）走社区摘要检索；UI：社区列表 + 摘要 + 成员实体｜✅ v0.9 已实现：迁移 015 kg_community 表（重建即全量替换 = 摘要缓存失效）+ internal/kg/community.go（确定性 label propagation、LLM 摘要主路径/骨架回退、社区数上限 12 长尾合并）+ 端点（POST /api/kg/{id}/communities/rebuild、GET /api/kg/{id}/communities、POST /api/kb/{id}/global-search 中文 2-gram 评分检索）+ 前端社区面板（列表/重建/全局问答） | P2 |
| KB-1（v0.1 新增） | 开源选型确认 | 向量存储/检索采用开源方案：eino-ext 现成适配优先 + Qdrant（P1 首选），自研 SQLiteRetriever 保留为无外部依赖 fallback 与学习对照；选型依据与替代方案对比（含 WeKnora 0.8.0 / RAGFlow 等）见 12 §5 | P1 |
| KB-2（v0.1 新增） | 进阶能力需求池（暂不实现） | 以下能力进入需求池，不排期：PDF/Word 等富格式解析与 OCR、表格抽取、父子分块/自适应分块、BM25 混合检索 + RRF 融合、Rerank 重排、查询改写、FAQ 索引、知识图谱（GraphRAG）（**v0.2 注：GraphRAG 已自池中升级为 P1 内置双子模块（REQ-107/D-KB4）并交付**）、多平台同步（飞书/Notion 等）——对标 WeKnora 0.8.0 / RAGFlow 0.27 已有能力（见 12 §5）；触发条件成熟时经评审升级 | 池 |
| KB-3（v0.1 新增） | 独立 RAG 服务集成（P2 备选路线） | 若未来需要进阶能力且不自研：可参照 OpenOntologies 双轨模式（01 REQ-100），将 WeKnora（MIT，含 MCP Server）或 RAGFlow 以**独立服务**接入主平台（Agent 挂载其 MCP 工具），与本文档 P1 组件级方案并行、数据不互通、可整体摘除；是否启动由主人拍板 | P2 |

## 3. 依赖契约（跨模块）

| 依赖 | 契约 | 归属 |
| --- | --- | --- |
| 模型连接（向量 embedding） | 知识库向量化使用一条 embedding 类型连接（REQ-46，可标记默认）；DeepSeek 不含 embedding，需配置 OpenAI 兼容向量服务 | 01 §3.4（模型连接） |
| 对话侧引用 | 对话配置条选择知识库（REQ-19a）、未配置向量连接的引导提示（REQ-19b）、召回来源可见（REQ-17）、检索注入与 `retrieval` 事件（REQ-56 语义 / 02 SSE 协议） | 01 §3.2（对话） |
| 平台技术栈 | 向量存储可插拔抽象（Store 接口）与部署形态（Qdrant 可选容器 / SQLite fallback） | 02 §3 / §6.9（指针→12） |
| 界面口径 | 知识库视图交互基线：左列表+右主区+库详情双子 Tab（原 prototype/05 原型已移除，口径归《17_产品_信息架构与界面设计》） | 17 号 |

## 4. 范围与非目标

| In Scope（P1） | Non-Goals（进入需求池 KB-2，不排期） |
| --- | --- |
| 知识库 CRUD、文本/txt/md 入库、切分、向量化（Qdrant / SQLite fallback）、TopK 检索、试运行、对话注入 | PDF/OCR/表格/音频解析、父子分块、混合检索与 Rerank、GraphRAG、Wiki 生成、多平台同步、权限与多租户、独立部署的知识库平台（WeKnora/RAGFlow 整体引入） |

## 5. 场景与验收

| 场景 | 验收标准（承接 01 验收第 2/6 条口径） |
| --- | --- |
| SC-6 建库并引用 | 新建知识库 → 导入文本 → 完成切分向量化（开源向量组件）；对话选择该知识库提问，回答引用召回片段与来源；检索试运行可见片段与得分 |
| 组件切换验收 | SQLite fallback 与 Qdrant 路径下，同一知识库的检索试运行结果一致（TopK/阈值相同口径）；Store 切换仅改配置 |
| SC-K1 依赖问答（v0.6） | 技术文档 graphrag 库问「A 依赖哪些组件」——经关系遍历给出完整依赖列表（多跳=2 可命中经中间实体的链路），claims 点击定位原文 | REQ-128，M16 一 |
| SC-K2 因果溯源（v0.6） | 故障案例库问「为什么出现 X」——答案中的结论带 claim 溯源，点击高亮原文片段 | REQ-128，M16 一 |
| SC-K3 关系网络梳理（v0.6） | 会议纪要/笔记导入后，图谱页搜索某人物/系统，展开其协作与依赖网络 | REQ-127，M16 一 |
| SC-K4 全局主题问答（v0.6） | 问「这个知识库整体在讲什么」——命中社区摘要并给出成员实体（local 与 global 两种检索并存） | REQ-130，M16 二 |
| SC-K5 检索对照学习（v0.6） | 同一问题分别用 RAG 库与 GraphRAG 库回答（复用对话对比模式 REQ-19e），对照召回差异——检索策略教学的核心场景 | REQ-107 + 19e，可演示 |

## 6. 开放问题

| 优先级 | 问题 | 影响 | 需要谁确认 |
| --- | --- | --- | --- |
| ~~KB-O1（P1）~~ **✅ 已关闭（v0.4）** | eino-ext 可用 indexer/retriever 组件清单——M6 实现首日核对后定稿对接方式 | ~~决定 Qdrant 走 eino-ext 适配还是直连 REST~~ **结论（2026-09-23 补记）：M6 交付采用自建 Qdrant REST client + SQLite fallback 双实现（`internal/kb/qdrant.go`/`vector.go`），`KB_VECTOR_BACKEND=qdrant\|sqlite` 配置切换（默认 qdrant，QDRANT_URL 缺失/不可达可退 sqlite）；eino-ext indexer/retriever 适配未采用。满足 §5"组件切换仅改配置"验收** | 已定稿 |
| KB-O2（P2） | KB-3 独立 RAG 服务集成（WeKnora/RAGFlow）是否启动 | 知识库能力上限 | 董奎。**触发条件：出现富格式文档（PDF/OCR）或 GraphRAG 进阶检索的真实使用需求且自研/internal/kg 不够用时**（D-KB2 口径）；最后评估 2026-09-23 |
| KB-O3（P2） | 向量连接默认维度与模型组合是否需要在库级锁定（当前隐含库内向量同维） | 换 embedding 模型的迁移成本 | 董奎（P2）。**触发条件：首次真实发生"KB 换 embedding 模型"操作时**（届时按 02 §13 风险行「切换时提示重建」处理）；最后评估 2026-09-23 |
| ~~KB-O4（P2）~~ **✅ 已关闭（v1.0）** | 社区摘要与全局问答（REQ-130）的 LLM 成本与更新时机：摘要按需生成还是重建时全量生成、库级 token 预算 | ~~全局问答的运行成本~~ **结论（2026-09-26 补记）：REQ-130 已随 M16 阶段二交付（v0.9）——摘要更新时机收敛为「前端手动重建触发、重建时全量生成（LLM 摘要+骨架回退，社区数上限 12 长尾合并）」，全局问答只读已有摘要；库级 token 预算未实现，实测出现成本困扰再入 KB-2 池评估** | 已定稿 |

## 7. 版本记录

> 后续每次变更请追加一行，不要覆盖历史。

| 版本 | 日期 | 变更 | 确认人 |
| --- | --- | --- | --- |
| v0.1 | 2026-09-21 | 首版：知识库需求自 01 v0.10（原 v0.12）§3.6 拆出独立成档——承接 REQ-07/50/51/53/54/56/57 与 LG-11/SC-6/Q-18；新增 KB-1（选型确认）、KB-2（进阶能力需求池）、KB-3（WeKnora/RAGFlow 独立服务集成备选，P2）；开源方案对比分析归档于 12 §5（WeKnora 0.8.0 结论：不替换 P1 组件级方案，列为 P2 集成候选） | 董奎 |
| v0.2 | 2026-09-23 | **REQ-107 落档 + 交付（D-KB4，02 v0.25 拍板；12 v0.2 头部早已引用本档 v0.2，本次补齐）**：①§2 新增 REQ-107（RAG/GraphRAG 双子模块 IA，P1 已交付 c3cbe57）；②KB-2 进阶池「知识图谱（GraphRAG）」条目标注已升级为 P1 内置双子模块（反转 KB-2，对齐 02 v0.25 ⑧）；③M14（02 §12）工程交付：008 迁移 kb.mode、入库触发 worker /graphrag/ingest（degraded 不阻断）、/api/kb/{id}/graphrag-search、retrieval 事件 mode/degraded、UI 双子页签 | 董奎 |
| v0.3 | 2026-09-23 | **D-O15 残留口径补注**：REQ-107 行补注——KG 源已切自研内置 backend（SQLite 三表自存 + REQ-98 LLM 抽取），worker/:8093 反代口径作废（7d45dc0） | 董奎 |
| v0.4 | 2026-09-23 | **开放问题治理（缺口分析全项落实）**：①KB-O1 关闭——M6 实际交付为自建 Qdrant REST client + SQLite fallback 双实现（KB_VECTOR_BACKEND 切换），eino-ext 适配未采用，满足 §5 组件切换验收；②KB-O2/O3 增补触发条件与最后评估日期（D-KB2 口径）。配合全局编号治理见《18_REQ编号注册表》 | 董奎 × 协作 Agent |
| v0.5 | 2026-09-23 | **整理轮**：§3 界面原型依赖行改指 17 号（prototype/05 已移除）。2026-09-23 文档整理轮（主人授权）：已完成需求细节压缩、过期/退役内容清除、prototype 原型移除——完整历史见 git 与本文档迭代记录；本轮不改变任何需求语义与验收口径。 | 董奎 × 协作 Agent |
| v0.6 | 2026-09-24 | **KG 产品规划完善（M16 立项落档，主人指示：完善知识图谱功能与使用场景，先文档后排期）**：①新增 REQ-127 图谱浏览与统计 / REQ-128 实体聚焦与多跳检索 / REQ-129 抽取治理与人工反馈 / REQ-130 社区摘要与全局问答（对标微软 GraphRAG global search）；②§5 增 KG 使用场景 SC-K1~K5（依赖问答/因果溯源/关系网络/全局问答/RAG 对照）；③KB-O4 登记社区摘要成本开放问题；编号经 18 号注册表分配（127~130）。排期见 02 §12 M16（阶段一 P1=127/128，阶段二 P2=129/130）；设计见 12 v0.9 | 董奎 × 协作 Agent |
| v0.7 | 2026-09-24 | **M16 阶段一（REQ-127/128）实现交付**：①REQ-127——新端点 GET /api/kg/{id}/stats（实体/关系/claims 计数 + 类型分布 + 文档覆盖 degraded_docs）、/entities（实体搜索）、/neighborhood（BFS 1~2 跳邻域 + claims chunk 溯源 doc/seq）；前端 graphrag 库详情新增图谱视图 KGGraphView（统计卡 + React Flow 邻域图 + claims 溯源列表）；②REQ-128——graphrag-search 增参 entity/hops(1~2)/relation_types，返回附命中路径 entities/relationships/claims 明细；retrieval SSE 事件携带实体/关系明细（对话侧过程可观测）；零依赖单测（聚焦检索 hops/过滤/明细 + store 统计/搜索/溯源）。冒烟：真实服务端点 degraded 路径 + 离线单测全绿；02 §12 M16 状态同步 | 董奎 × 协作 Agent |
| v0.8 | 2026-09-24 | **M16 阶段二（REQ-129）抽取治理与人工反馈交付**：①迁移 014——kg_relationship/kg_claim 增 status（默认 approved，存量兼容；rejected 不参与检索：KGSubjectsByChunks/KGNeighbors/KGClaims* 全部过滤）、knowledge_base 增 kg_conn_id/kg_prompt（库级抽取模型/提示词覆写，ExtractForDoc 按 kbConfig 解析，提示词覆写追加领域约束且 JSON 契约保留）；②治理端点四件——POST /review（approve/reject）、POST /merge（实体消歧合并：关系/claims 迁移 + 自环清理 + 决策留痕）、GET /quality（method 分布/孤儿实体/TopN 关系类型/rejected 计数）、GET /merge-suggestions（包含关系别名建议，仅提示不自动合并）；③前端治理面板（质量统计 + 审核队列批准恢复 + 合并建议执行）与 KB 配置卡抽取配置字段；④零依赖单测（status 过滤检索/合并迁移自环清理/质量统计）。冒烟：治理端点参数校验与空态真实服务验证 | 董奎 × 自动化执行者 |
| v0.9 | 2026-09-24 | **M16 阶段二（REQ-130）社区摘要与全局问答交付**：详见 REQ-130 行注记——label propagation 社区检测（确定性可单测）+ LLM 摘要（骨架回退，社区数上限 12 长尾合并）+ 全局问答端点（2-gram 评分，零 embedding 依赖）+ 前端社区面板（列表/重建/问答）。冒烟：零依赖单测（检测确定性/骨架回退/2-gram 评分）+ 真实服务 degraded 路径；02 §12 M16 状态同步 | 董奎 × 自动化执行者 |
| v1.0 | 2026-09-26 | **开放问题清账（文档滞后集中清账轮，无新开发）**：KB-O4 关闭——REQ-130 已随 M16 阶段二交付（v0.9），摘要更新时机=手动重建全量生成（骨架回退）、问答只读已有摘要；库级 token 预算未实现，成本困扰出现时再入 KB-2 池评估。知识同步：platform-knowledge/知识库/知识库模块.md「M16 排期中」→「已全量交付」（synced 2026-09-26）；17 v0.22 §3.1 同步 | 董奎 × 协作 Agent |