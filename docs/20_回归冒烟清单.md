# 20_回归冒烟清单

> 状态：v1.26 ｜ 2026-09-26 ｜ 维护人：董奎 + 协作 Agent
> 文档性质：**交付质量资产**。每个独立任务交付后按本清单跑一遍核心动线（AGENTS.md 纪律 #5 的落地载体）；目标：把"主人报障式"质量回路（02 v0.30/v0.33 两轮 bugfix 均由报障发现）转为交付前自检。前端改动用 headless Chrome（CDP）执行；后端/接口改动可用 curl 对照标注的 API。
> 用法：按改动面选模块（如只改模型连接 → 只跑 S6）；**标记 ★ 的为跨模块主链路，任何交付都建议全跑**。发现回归 → 修复后在本表对应行追加日期注记，不删历史。

## S1 顶部导航与全局 ★

| # | 动线 | 预期 |
| --- | --- | --- |
| 1.1 | 打开 `http://localhost:8080` | 六模块导航（智能体/项目/本体/知识库/技能）+ 设置可切换，无白屏/控制台报错 |
| 1.2 | 各模块来回切换 | 状态保持（左栏宽度 localStorage 记忆），无组件残留 |
| 1.3 | 无数据模块首屏 | 空态有引导（随 REQ-112 落地逐步从"留白"升级） |
| 1.4 | 导航「平台知识」→ L1 七组抽点（平台总览〔二级含产品设计〕/智能体〔含 DeepSeek Harness〕/项目/本体〔含本体学习外部资源导航〕/知识库/技能/设置；REQ-169 v1.26 更新） | 正文渲染无空页/无 frontmatter 残留；侧栏超长可滚动；「源文档地图」tag 与正文互引链接点击→右侧抽屉打开对应文档（抽屉默认关、可手动关、抽屉内互引可续点） |
| 1.5 | 供应商多实例与别名/改名（REQ-148，v1.7） | 同一供应商（同 BaseURL）可再次添加为独立实例并各自分组；编辑弹窗「显示别名」保存后列表/模型下拉显示「别名·模型名」且真名不变；「名称」改名按前缀批量重生成真名；新库老连接自动按 BaseURL 回填分组 |

## S2 智能体与对话 ★

| # | 动线 | 预期 |
| --- | --- | --- |
| 2.1 | 新建 Agent（名称/描述/提示词/模型） | 保存即出现在左栏；配置抽屉/侧边栏回显正确 |
| 2.2 | ★ 新建对话提问（配置好的模型连接） | 流式打字机输出；停止可用；消息与事件入库 |
| 2.3 | 重开历史对话 | 消息与过程事件完整还原 |
| 2.4 | 停用所用模型连接后提问 | 明确错误提示（不静默）；恢复启用后可继续 |
| 2.5 | 工具调用（如 current_time） | 过程时间线可见工具名/入参/结果/耗时 |
| 2.6 | 调试档冒烟（REQ-117/M17；REQ-149 补回放过滤，v1.8） | 调试下拉切 L1/L2 后提问：详细档见装配快照与分步用量；L2 另见模型输入全文；「过程展示」开「调试事件入库」→ model.step 落 run_events；历史/回放按当前级别过滤（简洁档隐藏调试细节并明示数量）；未开入库的会话回放显「未记录调试细节」警示 |
| 2.9 | dsh 推理后端（REQ-160/M24）：设置页「推理后端」面板应出现 deepseek-harness（dsh 在 PATH 时可用）；Agent 切 deepseek-harness 后发起对话 | run.started backend=deepseek-harness；dsh 侧缺凭据时 run.error 透出结构化 MISSING_CREDENTIAL（不静默）；配好凭据后全回复正常 |
| 2.8 | Docker 沙箱后端（M10/10a，v1.11；10b 生命周期+参数化 v1.12） | 设置 Agent 执行后端=docker（tool_approval=off）：发起对话 → 主平台日志可见容器 agt-{agentID} 启动 + manifest 下发；SSE 事件 run.started 带 backend=docker；回答流式输出且助手消息/事件落库；重开历史对话完整还原；二次运行容器复用（对账）；docker CLI 不在 PATH 时回退 ~/.docker/bin 等常见位置；**10b：SidePanel「运行后端」可选 docker 并配置沙箱内存/CPU（迁移 020）→ inspect 实测生效（256MiB/0.5CPU）；沙箱面板状态徽标（运行中/未运行）+ 启动/停止按钮（未配 SANDBOX_IMAGE 显式提示）** |
| 2.7 | ★ 对话对比模式（REQ-19e/f，v0.54 交付；REQ-143 跨智能体 v1.9；REQ-144 剖面 v1.10；REQ-150 系统化 v1.18） | 头部「对比」开关开启 → 2~4 列窗格（Splitter 分隔可拖宽）；**窗格头承载全量配置徽标（REQ-150）：模型/库/方案/技能——覆盖态品牌高亮显值名、继承态浅色，点击 Popover 可改（内显继承来源值）**+ 状态徽标（待提问/生成中/完成/出错/已停止）+ 窗格 Agent（logo+名称）+「采纳」写回对话配置；**共享输入区无配置 chips，仅摘要文案**（REQ-150）；窗格底部选智能体 +「不携带历史」开关 +「更多配置」（温度/提示词改写/剖面）；剖面可命名保存并在任意窗格应用（合并序 toast 明示）+「复制上一窗格」；同问发送 → 各窗格独立流式输出（跨 Agent/跨提示词各按其配置回答），窗格状态流转 待提问→生成中→完成；停止 → 整组终止已生成内容保留、窗格状态转「已停止」（游标收起）；刷新页面 → 对比开关与窗格覆盖配置持久化恢复；docker/外部 CLI 后端的窗格独立报错不阻断其余 |

## S3 项目与本地目录 ★

| # | 动线 | 预期 |
| --- | --- | --- |
| 3.1 | 新建项目绑定本地目录（含 `~/` 前缀） | 保存后 `GET /api/projects` 返回**展开后的绝对路径**（02 v0.33 回归点） |
| 3.2 | ★ 侧边栏文件视图 | 列出绑定目录内容；子目录导航/面包屑/刷新可用；文件预览 ≤1MB |
| 3.3 | Git 视图 | 非 git 目录给"不是 Git 仓库"；git 目录显示分支/提交历史/变更明细（02 v0.35 深度版） |
| 3.4 | 项目对话让模型"列出并读取目录里某个文件" | `list_files`/`read_file` 过程事件可见且结果正确；越界路径被拒绝 |
| 3.5 | 项目对话让模型保存文件 | `save_file` 落绑定目录；文件视图可见；artifact.saved 事件出现 |
| 3.6 | 本地目录检测分字段直连（REQ-133） | ①输入 Windows 盘符路径（如 `C:\Users\me\notes`）点检测 → 显示「路径格式合法 + 后端不可达（远程目录）」说明文案，**不出现「不存在/非目录」误报**；②POSIX 可达路径 → 存在/目录/Git 标签如实；③相对路径 → 「目录检测未通过」含原因；④「选目录」按钮渲染（同机部署唤起系统对话框回填，远程部署 toast 提示手输） |

## S4 本体模块 ★

| # | 动线 | 预期 |
| --- | --- | --- |
| 4.1 | 左侧边栏五栏切换 | 学习中心（默认）/构建/资产/运行/消费与审计可达，无空白 |
| 4.2 | 资产详情：Spec 编辑保存 → 图形编辑 Tab | 画布渲染（React Flow 节点/边）；保存经适配器回写 spec_json（02 v0.30 回归点：React Flow 样式缺失曾致白屏） |
| 4.3 | SPARQL 工作台选 running 方案执行 SELECT | 返回结果表（REQ-92 本体侧） |
| 4.5 | 可视化三态（M21/REQ-154，v1.14） | 资产详情「可视化」Tab 内 Segmented 三态：2D 结构（React Flow 默认）/ 三维浏览（canvas+scene-container 注入、点击聚焦飞入+邻居高亮、搜索/过滤器/复位）/ WebVOWL 对照（分发缺失或 TTL 导出失败时诚实降级警示）。**2026-09-26 修复注记（主人报障「三维视图初始化失败」）**：根因=vendor UMD 被 gitignore 且无补给脚本，新克隆缺文件→SPA fallback 回 HTML；修复=web/scripts/prepare-vendor.mjs 挂 dev/build 自动自 node_modules 补齐 + Graph3D 预挂 window.THREE；复验 8 断言全过（医学常识 102 概念/170 实例三维渲染，截图 smoke/viz3d-fix/） |
| 4.4 | 消费与审计第五栏 | KG 增强检索展示/决策溯源可打开；不依赖任何 Python 进程（03 验收 22） |
| 4.5 | 引擎缺失显异常与一键安装（REQ-146，v1.6） | 本地无 oxigraph 时：引擎导航显红「未安装」Tag + 分组页错误 Alert（含指引/上次安装失败回显）+ 方案卡「引擎缺失」标记；点「一键下载安装」→ 202 后轮询翻「可用」；装后方案可正常启动 |
| 4.6 | 方案停止态编辑（REQ-147，v1.6） | stopped/created/error 方案卡「编辑」可点：改名称与本体集合保存后卡片回显且 PUT 持久化；running/starting 的「编辑」置灰（Tooltip 先停止再编辑）；引擎/端口只读 |
| 4.7 | facade 第 5 工具 sparql_query（M20/REQ-151，v1.16） | 方案详情 Drawer「facade 与工具」页签：固定签名工具表 5 行（含 onto_sparql_query，「共 5 个」）；MCP tools/list 返回 5 工具；tools/call 变更查询（DELETE/INSERT/LOAD 等）回 QUERY_REJECTED 且不触达引擎；SELECT 正常返回表格（limit 截断带 total/truncated）；翻译透视页签留 sparql_query 记录；Agent 挂载方案对话可经 sparql_query 回答聚合类开放问题（03 验收 23 前半） |
| 4.8 | 百级大型种子本体（M20/REQ-153，v1.17） | 学习中心「示例本体库」列 7 个示例卡（含 med_common/gene_core）一键灌装成功；资产详情徽标 概念 102/104、实例 170/94；oxigraph 方案装载双本体 running 后工作台 SELECT 毫秒级返回（禁忌网/层级聚合/GROUP BY）；三维浏览 Tab 双本体渲染流畅（272/198 节点）且搜索定位/复位全景可用 |
| 4.9 | 前端体验 M22 一批（REQ-145 A1/A2/A4/B1，v1.19） | 资产详情「Spec 编辑」为 CodeMirror JSON 编辑器（语法高亮/行号/折叠；故意输错 JSON 行内出现 lint 错误标记；点「重新加载」内容恢复）；「图形编辑」选中实例 → 属性 JSON 小高度 CodeMirror，破坏 JSON 点「应用修改」出「属性不合法」内联提示不白屏；「添加实例」弹窗属性输入同为 CodeMirror；「消费与审计」决策审计补录→表格出现→溯源链抽屉 Timeline 打开；B1 后 AssetsPage/GraphEditor/AuditPage 主文件 ≤400 行 |
| 4.11 | 伴生本体 P1+P2a/P2b（M28/REQ-170，v1.22；curl+页面） | Agent 配置伴生开关（companion_ontology，默认 false）开启后跑一轮对话 → run 收尾日志出现伴生抽取且游标推进、GET /api/companion/candidates 出 pending 候选；POST confirm → GET status 的 graph 含候选三元组（labels 可见）；同主体同关系再确认 → 旧边 bot:invalidAt 失效（status 活动边不重复）；POST conversations/{id}/reset → 图空候选清；开关关的 Agent 对话零伴生动作（SSE 契约不变）；**P2 补充（v1.22）**：资产详情「伴生本体」页签（带「对话」徽标）渲染状态卡（引擎/待确认/游标/实体标签）与候选确认流——确认入图后状态卡「伴生引擎运行中」+labels 出现实体；facade sparql_query graph=companion+conversation_id 经 MCP tools/call 返回图内实体（带 graph 字段）；整体摘除按钮 DROP 图+清表 |
| 4.12 | SPARQL 工作台 v2（REQ-92 实现升级，v1.23） | 方案 Drawer「查询与挂载」：编辑器为 CodeMirror（SPARQL 语法高亮，无 Yasgui 裸表单）；「运行查询」SELECT 出 AntD 结果表（耗时/行数标签、分页、共 N 行）；「查询模板」五枚可选（含禁忌网/中心法则链教学查询）；ASK 出真值告警；故意写坏查询出可关闭错误告警卡（含引擎原文片段）；「导出 CSV」仅 SELECT 结果可用；查询刷新后从 localStorage 恢复 |
| 4.10 | 前端体验 M22 二批（REQ-145 A3/A5/B2/B3/B5，v1.20） | 资产详情「版本对比」出现「文本对照」页签：选两个不同版本后分栏展示两版 spec JSON 对照（新增行绿/删除行红高亮，行号与 v?（起始）/v?（目标）标题；相同版本时空态引导）；本体模块加载占位为 Skeleton 骨架屏（内容占位类）而过程进行中保留 Spin；「学习中心→工具链配置」刷新/删除 icon 按钮有 aria-label，「OntoChat 流程」会话删除按钮有 aria-label，「图形编辑」画布容器 role=application 带操作提示；「消费与审计→决策审计」表格横向滚动时操作列固定右侧；停 ontology-service 后 OntoChat 会话列表出现「会话列表加载失败」错误卡带重试按钮，服务恢复点重试列表恢复 |

## S5 知识库 ★

| # | 动线 | 预期 |
| --- | --- | --- |
| 5.1 | 新建 RAG 库 → 粘贴文本入库 → 索引 | 索引成功（Qdrant 未启时按 `KB_VECTOR_BACKEND` 降级 sqlite 并可见后端徽标） |
| 5.2 | 试运行检索 | TopK 片段 + 得分可见 |
| 5.3 | GraphRAG 库导入 → graphrag 检索 | KG 建立或 degraded 标记（不阻断）；检索返回实体/关系/claims |
| 5.4 | 对话挂库提问 | `retrieval` 事件带来源与 mode |

## S6 设置与模型连接

| # | 动线 | 预期 |
| --- | --- | --- |
| 6.1 | 添加提供商 → 选厂商预设（如智谱 GLM） | 名称/协议/BaseURL/建议模型自动填充（02 v0.34） |
| 6.2 | 预设创建后 | 该提供商行自动展开并挂自动发现面板（REQ-48 串联；接口未就绪给降级提示） |
| 6.3 | 千帆预置行存在性 | 「百度千帆（预置）」embeddings-v1 在列（无 embedding 连接的库）；停用 + 默认标记（迁移 011） |
| 6.4 | 填 Key → 测试连接 → 设为默认 | 掩码显示；测试回显成功/失败；默认标记生效 |
| 6.5 | 推理后端面板 | eino-adk 可用；外部 CLI 按 PATH 探测结果一致 |
| 6.6 | 添加提供商 → 协议选 anthropic（REQ-172） | BaseURL 标签/占位切「网关根地址」口径、类型锁定 chat（embedding 项禁用）、模型名占位 claude-*；提供商行显示 anthropic 徽标；anthropic+embedding 组合保存被后端 400 拒绝；选「Anthropic 官方」等预设自动填充协议 |

## S7 运行状态自检（每次交付前后）

| # | 检查 | 预期 |
| --- | --- | --- |
| 7.1 | `curl -sf http://localhost:8080/api/projects` 等核心 GET | 200；`/healthz`（8090/8091）按 run-dev 就绪 |
| 7.2 | 后端日志（data/*.log） | 无新增 panic/error |
| 7.3 | `go build ./... && go vet ./...`；前端 `tsc && vite build` | 全绿（交付前置） |

## 迭代记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v1.0 | 2026-09-23 | 首版：七节 27 条动线；沉淀 02 v0.30/v0.33/v0.34/v0.35 各轮报障为显式回归点（2.4/3.1/4.2/6.1/6.3） |
| v1.3 | 2026-09-24 | 恢复被并行提交覆盖的 S1.4（参考资料渲染检查）+ 新增 S2.6 调试档冒烟（REQ-117/M17）；1.3 空态口径更新为 REQ-112 已交付 || v1.5 | 2026-09-24 | 新增 S2.7 对话对比模式动线（REQ-19e/f，02 v0.54 交付）；恢复 v1.3 声称但行体丢失的 S2.6 调试档冒烟（本文档迭代行 v1.4 与头部版本曾有错位，一并在此说明） | 董奎 × 自动化执行者 |
| v1.6 | 2026-09-25 | 新增 S4.5 引擎缺失显异常与一键安装（REQ-146）、S4.6 方案停止态编辑（REQ-147）两条动线（824b063 交付） | 董奎 × 自动化执行者 |
| v1.7 | 2026-09-25 | 新增 S1.5 供应商多实例与别名/改名动线（REQ-148，a64178f 交付） | 董奎 × 自动化执行者 |
| v1.8 | 2026-09-25 | S2.6 调试档动线并入 REQ-149 回放过滤与入库联动检查（f03c1fe+5ee3ddd 交付） | 董奎 × 自动化执行者 |
| v1.9 | 2026-09-25 | S2.7 并入 REQ-143 跨智能体窗格动线（df25e82，M19 阶段一交付） | 董奎 × 自动化执行者 |
| v1.10 | 2026-09-25 | S2.7 并入 REQ-144 剖面/采纳/更多配置动线（f1fdc40，M19 阶段二交付；M19 对话对比扩展全项完成） | 董奎 × 自动化执行者 |
| v1.11 | 2026-09-25 | 新增 S2.8 Docker 沙箱后端动线（M10/10a，96210e6 交付） | 董奎 × 自动化执行者 |
| v1.12 | 2026-09-25 | S2.8 并入 10b 生命周期与资源限制参数化检查（9b0d45c 交付） | 董奎 × 自动化执行者 |
| v1.13 | 2026-09-25 | 增 S2.9 dsh 推理后端冒烟（REQ-160/M24 阶段二交付配套） | 董奎 × 文档会话 |
| v1.14 | 2026-09-25 | S1.4 分类数更新：参考资料中心新增「DeepSeek Harness」页（REQ-160/M24 配套学习页） | 董奎 × 文档会话 |
| v1.15 | 2026-09-25 | S1.4 平台知识页口径更新：模块更名（参考资料→平台知识，REQ-161）+ L1 模块组（10 组）与二级主题页抽点结构；外部资源主题页挂载（REQ-162②） | 董奎 × 文档会话 || v1.14 | 2026-09-26 | 新增 S4.5 可视化三态动线（M21 VIZ-1+VIZ-3，8d25e17 交付） | 董奎 × 自动化执行者 |
| v1.16 | 2026-09-26 | 新增 S4.7 facade 第 5 工具 sparql_query 动线（M20/REQ-151 交付：工具表 5 行/tools/list 5 工具/变更查询 QUERY_REJECTED/limit 诚实截断/透视留痕/Agent 聚合开放问题端到端） | 董奎 × 自动化执行者 |
| v1.17 | 2026-09-26 | 新增 S4.8 百级大型种子动线（M20/REQ-153 交付：双种子灌装/装载/SPARQL/三维浏览四段冒烟全过，03 v0.34） | 董奎 × 自动化执行者 |
| v1.18 | 2026-09-26 | S2.7 并入 REQ-150 对比界面系统化动线（M19 阶段三交付：窗格头全量徽标/共享区 chips 收起/状态徽标五态/停止→已停止/Splitter 拖宽/配置持久化；截图 smoke/req150/） | 董奎 × 自动化执行者 |
| v1.19 | 2026-09-26 | 新增 S4.9 前端体验 M22 一批动线（REQ-145 A1/A2/A4/B1 交付：Spec 编辑 CodeMirror 高亮+lint 行内标记+重载恢复/实例属性防崩内联提示/添加实例弹窗 CodeMirror/决策审计补录+溯源链抽屉/三大文件 ≤400 行；截图 smoke/req145/） | 董奎 × 自动化执行者 |
| v1.21 | 2026-09-26 | 新增 S4.11 伴生本体动线（M28/REQ-170 P1 交付：开关默认关/游标续抽/候选确认入图/矛盾失效化/整体摘除；后端 API 面 curl 冒烟，本机 oxigraph 0.5.11 真机集成测试覆盖） | 董奎 × 自动化执行者 |
| v1.23 | 2026-09-26 | 新增 S4.12 SPARQL 工作台 v2 动线（REQ-92 实现升级交付：CM6+AntD 自组装替换无样式 Yasgui——运行出表/模板/CSV/错误卡/持久化全过，本机 REQ-153-e2e 方案实测；03 v0.41/14 v0.22/15 v2.12） | 董奎 × 自动化执行者 |
| v1.22 | 2026-09-26 | S4.11 升级为 P1+P2 全链动线（M28 P2a+P2b 交付：伴生页签渲染/候选确认入图/引擎懒启动/facade graph=companion MCP 查询/整体摘除；截图 smoke/m28p2/） | 董奎 × 自动化执行者 |
| v1.20 | 2026-09-26 | 新增 S4.10 前端体验 M22 二批动线（REQ-145 A3/A5/B2/B3/B5 交付：版本文本对照分栏页签/Skeleton 内容占位/icon-only aria-label+画布 role/决策表操作列固定+scroll.x/会话列表错误卡重试闭环；截图 smoke/req145-b2/） | 董奎 × 自动化执行者 |
| v1.24 | 2026-09-26 | 新增 S6.6 Anthropic 协议动线（REQ-172/M29 交付：双协议表单/类型锁定/网关根地址口径/anthropic+embedding 拒绝；截图 smoke/req172/） | 董奎 × 协作 Agent |
| v1.25 | 2026-09-26 | S1.4 口径更新 + REQ-169 交付动线（平台知识页侧栏滚动+规划序分组/阅读抽屉默认关可关/正文与抽屉内互引链接打开文档——Splitter 吞非 Panel 子元素根因顺修；headless Chrome 四项实测过，截图 smoke/req169/；17 v0.23/18 v1.39） | 董奎 × 协作 Agent |
| v1.26 | 2026-09-26 | S1.4 组列表更新（REQ-169 二轮：L1 收敛七组 + 产品设计挂总览二级/DeepSeek Harness 挂智能体/本体学习外部资源导航挂本体，TOPIC_MOUNT 跨目录挂载；headless Chrome 结构断言过，截图 smoke/req169/ 06/07；17 v0.24） | 董奎 × 协作 Agent |
| v1.25 | 2026-09-26 | S4.5 追加三维视图修复注记（vendor UMD 分发缺失根因 + prepare-vendor 自愈脚本；复验截图 smoke/viz3d-fix/） | 董奎 × 协作 Agent |
