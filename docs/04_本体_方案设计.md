# eino-multiagent-lab · 本体模块方案设计（独立维护文档）

> 状态：Draft v0.27
> 日期：2026-09-26
> 负责人：董奎
> 文档性质：**活文档**，只维护本体模块的**设计方案**（需求与方案分离，D-O6）；需求事实源见《03_本体_需求文档.md》。每次变更在文末「迭代记录」追加留痕。
> **多途径构建收敛视图**：REQ-171 可落地方案（业界实践+理论论文线整合、六路径成熟度分级+两公共底座）见 [平台知识 · 26_本体_多途径构建平台可落地方案](../platform-knowledge/本体/26_本体_多途径构建平台可落地方案.md)；本文档继续作为全景架构事实源。
> 与其他文档关系：需求《03_本体_需求文档.md》（REQ-60~73 承接 + REQ-80~88 + NFR-O + 决策 D-O 系列均在此维护）；智能体侧《01_智能体_需求文档_PRD.md》《02_智能体_技术方案设计.md》——其本体章节自 v0.5 起冻结为快照，细节演进以本文档为准。
> 范围声明：本文档只写本体。Agent 智能体模块、知识库模块不属于本文档范围；对话侧只约定接口契约（§5）。

---

## 0. 模块定位（v0.2 更新：构建与运行解耦）

**一句话**：本体模块 = **构建平面 + 运行平面**两个解耦的子系统。构建平面负责本体的导入、创建、编辑、浏览（"本体仓库是唯一事实源"）；运行平面支持**同时启动多套本体运行方案**，每套方案可选择运行时类型、加载哪些本体、配置运行细节。Agent 对话经统一 MCP 契约挂载某个运行方案。

**总原则（v0.6 更新）**：各子系统**优先使用现成开源方案，避免重复造轮子**；自研保留运行方案管理器（编排层）与 MCP facade 路由（学习内容本身）——**OWL/RDF 解析不自研，走 Python rdflib sidecar**（O1 引入，同时承担 spec_json→TTL 导出与可选 pySHACL）；轻量内存图引擎**不自研**（D-O5 v0.4：P1 引擎用开源 Oxigraph；P2 若需内存图直接集成开源 Cayley fork，LG-12 自研学习目标取消）。

**七阶段工具链视角（v0.4 新增，D-O7）**：本体工程进一步组织为 S1 本体来源 → S2 编辑 → S3 校验 → S4 可视化 → S5 运行时引擎 → S6 服务化 → S7 对接 Agent 的流水线；每阶段候选主流开源工具可配置（需求 REQ-74~77），执行模式三态——builtin（内置直接用）/ guided（平台给指引+打卡，不拉进程）/ managed（仅 S5，Runtime Manager 自动编排）。阶段清单详见《07_本体_工程七阶段与开源工具链清单.md》，配置层设计见本文 §4.6。**该视角是对现有功能的重新组织 + 一个薄配置层，不引入新子系统。**

**五项操作方式需求（2026-09-20 主人提出）** 已落位为 REQ-80~88 并独立维护于《03_本体_需求文档.md》§1，此处不再重复。

---

## 1. 需求基线（速查）

**需求事实源：《03_本体_需求文档.md》**（v0.1：REQ-60~73 承接 + REQ-80~88 五项操作方式 + 非功能 NFR-O-1~5 + 决策 D-O1~D-O6 + 验收要点）。本文档不再重复维护需求条目，仅保留编号-主题速查：

| 编号 | 主题 | 编号 | 主题 |
| --- | --- | --- | --- |
| REQ-60 | 本体列表页 | REQ-80 | 导入即运行（多形态资产） |
| REQ-61/63/64 | JSON 编辑 / 校验 / 只读预览 | REQ-81 | 可视化浏览（开源嵌入，TBox+ABox） |
| REQ-62 | 内置 K8s 示例 | REQ-82 | LLM 辅助创建（生成-校验循环） |
| REQ-65/66/67 | 对话启用 / 查询 / 过程可见 | REQ-83 | fork 扩展（P2） |
| REQ-69/72 | OWL 有损导入 / 导入体验 | REQ-84/85/86 | 运行方案管理 / 配置 / 多方案 |
| REQ-70/73 | 连通降级 / 打开即用一致性 | REQ-87/88 | 构建-运行解耦 / 开源优先 |
| REQ-71 | 图形化编辑器 | NFR-O-1~5 | 一致性 / 隔离 / 性能 / 本地化 / 可观测 |

设计时如发现需求缺口，先补需求文档（REQ-74~79 预留段）再回头细化设计。

---

## 2. 总体架构（v0.2 重写：两平面 + 多运行方案）

```
┌────────────────── Browser（React SPA）────────────────────────────┐
│ 构建平面 UI                          │ 运行平面 UI                  │
│ · 本体列表页 /ontologies              │ · 运行方案列表 /runtimes      │
│ · 本体编辑页 /ontologies/:id          │   （状态/启停/健康）           │
│   （JSON / 图形化 / AI 创建）          │ · 运行方案配置页 /runtimes/:id │
│ · 可视化浏览页 /ontologies/:id/browse │   （类型/选本体/细节参数）      │
└──────┬────────────────────────────────┬───────────────────────────┘
       │ /api/ontologies*                │ /api/runtime-profiles*
       ▼                                ▼
┌─────────────────────┐        ┌─────────────────────────────────────┐
│ 构建平面 Build Plane  │        │ 运行平面 Runtime Plane                │
│ （主平台内 or 独立服务）│  分发   │ Runtime Manager（Go，mcp-go）         │
│ · 本体仓库（多形态资产）│───────▶│ · 运行方案 CRUD/编排/健康检查/日志      │
│ · 导入/校验/归一化     │        │   状态机 created→starting→running     │
│ · LLM 辅助创建        │        │              →stopped/error          │
│ · fork 扩展(P2)      │        │ ┌─────────┐ ┌──────────┐ ┌─────────┐ │
│ · 可视化浏览入口       │        │ │方案A     │ │方案B      │ │方案C     │ │
└─────────────────────┘        │ │memory-  │ │Oxigraph  │ │Fuseki   │ │
                               │ │graph:8091│ │:8092     │ │:8093    │ │
                               │ │K8s+FLUIDOS│ │HOCC(原始) │ │HOCC(推理)│ │
                               │ └─────────┘ └──────────┘ └─────────┘ │
                               │ 统一 MCP facade：每 running 方案        │
                               │ 暴露相同 4 个 onto_* 工具（:8090/mcp）  │
                               └─────────────────────────────────────┘
                                          ▲ MCP
                               ┌──────────┴───────────┐
                               │ 主平台 Eino 运行时     │
                               │ （Agent 对话挂载运行方案）│
                               └──────────────────────┘
```

### 2.1 核心数据流

1. **导入流（REQ-80）**：上传文件 → 格式识别 → 原始形态存仓库（OWL/TTL 原样）→ 归一化为 spec_json（可损，生成导入报告）→ 校验 → 入库。**一个本体资产 = 元数据 + 多形态文件**（original + spec_json）。
2. **LLM 创建流（REQ-82）**：描述领域 → Eino 结构化生成 spec_json 草稿 → 构建平面校验 → 错误回喂 LLM 修正（最多 N 轮）→ 用户预览确认 → 入库。
3. **分发流（REQ-87）**：运行方案启动时，从仓库取所选本体的**对应形态**——memory-graph 型取 spec_json；oxigraph/fuseki 型取 original OWL/TTL → 写入运行时数据目录/加载端点。
4. **查询流（REQ-65/66 继承）**：Agent 挂载运行方案 → 经统一 MCP facade 调 onto_* 工具（ontology_id 定位方案内本体）→ Manager 路由到对应运行时（内存图直查 / SPARQL 型转查询）→ 结果 + version 返回。
5. **浏览流（REQ-81）**：浏览页挂在运行方案上——TBox 结构经 WebVOWL（仓库文件直接转换）；实例关系经 LodLive（SPARQL 型方案直连端点）或 OntologyExplorer 资产（memory-graph 方案）。

### 2.2 代码结构（v0.2 演进）

```
ontology-service/                 # 构建平面（保留原服务形态，职责聚焦构建）
├── internal/
│   ├── spec/ graph/ graphcache/  # 保留：归一化、校验、引用完整性（图核心，P2 内存图引擎复用）
│   ├── repo/          # ★ 本体仓库：多形态资产存储（original + spec_json + 元数据）
│   ├── importer/      # spec/csv/graphml 业务格式解析（Go 薄层）；OWL/RDF 解析经 sidecar（不自研）
│   ├── llmcreate/     # ★ LLM 辅助创建（调主平台模型代理或自身配模型）
│   ├── rest/ mcp/ config/
└── migrations/

tools/rdf-sidecar/                # ★ Python rdflib sidecar（O1）：OWL/RDF→spec_json 解析 + spec_json→TTL 导出 + 可选 pySHACL（不自研，薄胶水）

runtime-manager/                  # ★ 运行平面（新进程，Go）
├── cmd/runtimemgr/main.go        # :8090（原端口角色转移至此）
├── internal/
│   ├── profile/       # 运行方案模型 + 状态机 + SQLite
│   ├── orchestrator/  # 生命周期编排：子进程/Docker 启停、健康检查、日志
│   ├── engines/       # 运行时类型适配器
│   │   ├── oxigraph.go      # ★ P1 唯一引擎：拉起 Oxigraph（子进程/容器）+ 数据装载
│   │   ├── fuseki.go        # 拉起 Fuseki（Java/Docker）+ 数据集创建/加载
│   │   └── memorygraph.go   # P2 可选：开源内存图引擎嵌入（Cayley fork，不自研；LG-12 已取消）
│   ├── facade/        # ★ 统一 MCP Server：路由 onto_* 到各方案
│   └── rest/          # 运行方案管理 REST
└── migrations/001_profile.sql

pkg/ontology/spec/                # 共享类型（三模块引用）
backend/                          # 主平台：MCP Client 挂"运行方案"、前端两类页面
```

---

## 3. 构建平面设计

### 3.1 本体仓库（多形态资产，REQ-87 事实源）

```sql
-- ontology-service/migrations/002_repo.sql（演进自 001）
CREATE TABLE ontology (
  id TEXT PRIMARY KEY,                    -- 如 onto_k8s_ops
  name TEXT NOT NULL,
  description TEXT DEFAULT '',
  version INTEGER NOT NULL DEFAULT 1,
  forked_from TEXT,                       -- REQ-83：fork 来源（P2）
  created_at DATETIME, updated_at DATETIME
);
CREATE TABLE ontology_artifact (          -- ★ 多形态资产：一个本体 N 个形态
  ontology_id TEXT REFERENCES ontology(id) ON DELETE CASCADE,
  format TEXT NOT NULL,                   -- spec_json | owl_rdfxml | turtle | csv | graphml
  content TEXT NOT NULL,                  -- 文件内容（大文件可存盘+路径）
  is_normalized INTEGER NOT NULL DEFAULT 0,  -- spec_json 归一化形态标记
  imported_at DATETIME,
  PRIMARY KEY (ontology_id, format)
);
```

要点：**original 形态不可变**（导入时存档，供 RDF 型运行时原样加载与溯源）；spec_json 是归一化工作形态（编辑/校验/内存图用）；查询统计（概念/关系/实例数）从 spec_json 计算。

### 3.2 导入（REQ-80，整合原 REQ-68/69/72）

流程：上传/粘贴 → 格式嗅探（扩展名 + 内容特征）→ ①原样存 original 形态 → ②解析为 spec_json（分格式解析器，规则沿用 v0.1 §4.3 表格：spec 无损/csv 两表/graphml/OWL-RDF 有损）→ ③校验 → ④入库（version=1）→ ⑤导入报告（统计 + warnings + **有损映射说明**）。

OWL/TTL 导入映射细则（v0.1 开放问题 O-1 已落地为 P1）：`rdfs:label`→name、`rdfs:subClassOf`→继承关系、`owl:NamedIndividual`+`rdf:type`→实例、对象属性断言→实例关系；annotation 属性→attributes（待细则定稿）；公理/推理语义丢弃并计入 warnings。**用 ontologies/ 下三个真实本体做验收集**。

### 3.3 LLM 辅助创建（REQ-82）

- **入口**：本体列表页「AI 创建」→ 填写领域描述（+可选：参考示例、规模要求）→ 生成 → 预览 → 确认入库或继续对话修改。
- **生成闭环（学习目标 LG-14）**：Eino `ChatModel` + 结构化输出（spec_json Schema 约束）→ 构建平面校验器 → 失败则把错误列表回喂 LLM 修正（最多 3 轮）→ 通过后交用户确认。**校验器与人工编辑共用同一套**（REQ-63）。
- **开源参考**：OWLAPY（Python，MIT，AI 辅助本体构建）验证了该模式；Go 生态无现成等价物，用 Eino 自实现生成循环（这是学习内容，不外包）。
- 产物直接走标准入库路径（version=1），与导入一致。
- **生成链路备选（v0.12 记录，D-O12：仅作设计参考，不排期实施）**：借鉴 OpenBKN BKN Lang 思路（Markdown+YAML DSL 对 LLM 更高容错）——LLM 产 Markdown 草稿 → 服务端 parser → spec_json → 校验循环回喂。spec_json 作为唯一运行时表示不变（D-O5）；仅在生成链路出现格式漂移问题时评估启用。来源：《本体研究资料/OpenBKN借鉴映射_eino-multiagent-lab_20260922.md》落点 1（OpenBKN 整体仅参考不落地，登记簿 v2.2）。

### 3.4 编辑与扩展（原 REQ-61/63/64/71 + REQ-83）

- **编辑双形态**（沿用 v0.1 §4.1）：P0 JSON 编辑器（校验就地标红 + 三栏只读预览）；P1 图形化（React Flow + RJSF + dagre，spec↔图 adapter，坐标存 localStorage）。
- **校验**（沿用 v0.1 §4.2）：JSON Schema 结构校验 + graph.Build 引用完整性，服务侧执行。
- **fork 扩展（REQ-83，P2）**：本体列表页「复制为新本体」→ 复制全部形态资产（forked_from 记录来源）→ 编辑后作为独立新本体保存；不做"引用继承"（保持简单）。
- **内置示例**（REQ-62）：seed 写入最小 K8s 运维本体（开放问题 O-4 定稿中）。

### 3.5 可视化浏览（REQ-81，开源方案评估结论）

> 依据：《开源本体可视化工具调研_report.md》（2026-09-18，覆盖 WebVOWL/OntoGraf/Ontology Playground/OntView/ontoink/LodLive 等 10+ 方案）。

**需求拆解**：TBox（类层次/对象属性结构浏览）+ ABox（实例间关系查询）两类诉求，开源方案没有单一工具两者都强：

| 层 | 选型 | 许可证 | 理由 | 反转条件 |
| --- | --- | --- | --- | --- |
| **TBox 结构浏览** | **WebVOWL**（自托管 Docker，OWL2VOWL 预转换） | MIT | VOWL 是 W3C 社区视觉标准；类层次/对象属性/数据属性语义丰富；在线版可直接拖文件验证 | 若嫌 Java 预处理重 → **Ontology Playground**（MIT，纯静态零后端，但仅 RDF/XML 输入、深度有限） |
| **ABox 实例关系查询**（SPARQL 型运行方案） | **LodLive** 直连方案 SPARQL 端点 | 开源 | 增量探索式实例关系浏览，天然匹配"查询对象之间关系"；浏览的就是运行中数据 | LodLive 活跃度低（🔴）→ 备选 ontoink widget 或下述复用方案 |
| **ABox 实例关系查询**（memory-graph 型运行方案） | **复用已有 OntologyExplorer 资产**（单文件 HTML，已实现 57 节点关系图/三类语义边/实例详情） | 自有资产 | 已交付可用，数据源对接 spec_json 即可；不重新造轮子 | — |
| **一体化备选** | **ontoink**（embeddable widget 模式） | MIT | 唯一同时覆盖类/属性/个体/SHACL/推理的开源方案（2026-07 活跃） | 若愿意引入 MkDocs/widget 集成成本 |

**集成方式**：本体详情页新增「浏览」入口——①结构视图：**ReactFlow 内置渲染（v0.9 现状）** 与 WebVOWL iframe（构建平面预生成 OWL2VOWL JSON，缓存按 version 失效）按 D-O9 对比择优、难以客观对比则双轨并存；②关系视图：按运行方案类型路由（SPARQL 型 → LodLive 指向方案端点；memory-graph 型 → OntologyExplorer 加载该本体 spec_json）。**浏览是运行方案的附属视图**（REQ-87 解耦原则的可视化体现）。

**D-O9 可视化对比择优（v0.9，2026-09-21 主人拍板）**：
- **现状**：S4 可视化已升级为 **ReactFlow 重构版**（`@xyflow/react` v12，随仓库 862df01 入库）——基于 spec_json 直渲的交互式图谱：概念/关系/实例节点、节点点击详情联动、MiniMap/Controls/图例；与 REQ-71 图形化编辑器同栈（Q-16），数据零转换。
- **对比维度**（ReactFlow vs WebVOWL 嵌入）：TBox 语义还原度（对象属性/数据属性/匿名公理——WebVOWL 语义更丰富）、交互体验（搜索/过滤/展开/大规模数据性能）、数据链路成本（spec_json 直渲 vs OWL2VOWL Java 预转换）、依赖与维护重量（ReactFlow 与现有栈同构）。**采用效果更优者；难以客观对比则双轨并存**——ReactFlow 作内置默认视图，WebVOWL 作「嵌入视图」入口（保留 OWL2VOWL 预转换缓存按 version 失效）。
- **v0.12 收口（D-O12，2026-09-22 主人拍板）**：对比任务不再挂起——**ReactFlow 内置默认定案**（现网实际使用形态）；WebVOWL 嵌入降为 **P3 可选增强项**（需求池，无排期），OWL2VOWL 预转换缓存机制仅在启用 P3 项时实现。
- **v0.27 注（2026-09-26）**：上述 P3 项已随 M21 VIZ-3 激活交付（8d25e17——WebVowlView「WebVOWL 对照」视图 + owl2vowl 预转换 + tools/fetch-webvowl.sh 自托管资产拉取；分发缺失/TTL 导出失败诚实降级，口径见 03 v0.32/14 v0.18）——「仅在启用 P3 项时实现」的条件已兑现。
- **v0.20 延伸（REQ-71 图形化编辑器 v1 交付，2026-09-23）**：同一 ReactFlow 栈从只读延伸到**编辑**——资产详情新增「图形编辑」Tab（与只读「可视化」Tab 双形态并存，正合 REQ-71 双形态要求）：概念添加/删除、属性面板（显示名/定义/父概念）、连线弹窗建关系或继承、关系与继承删除、删概念级联清理关系与实例；事实源是 spec 草稿、画布由其派生（拖拽坐标仅会话内保留，不写回 spec，维持 O-3 localStorage 口径排 O8）；保存复用既有 `PUT /api/ontologies/{id}/spec` 校验门控递增版本，零后端改动。实例增删 v1 不进画布（引导至 Spec 编辑/CSV 灌装）。
- **v0.21 延伸（REQ-71 v1.5 实例入画布，2026-09-23）**：画布节点扩为概念（蓝）/实例（紫）两类（自定义节点 `EditorInstanceNode`，概念徽标标注归属）；派生边四类——概念关系实线带标签、继承虚线、归属点线（实例→概念）、实例关系细实线（读自 `instance.relations`）；「添加实例」弹窗（概念下拉 + 属性 JSON 语法/类型校验、拒绝与概念同名）、实例属性面板（属性编辑/实例关系只读/删除级联清他实例引用）、「隐藏实例」开关（`rebuild(spec, showInstances)` 派生口径）；连线三分支按节点 kind 分派、拒绝概念/实例混连。事实源与保存链路同 v1 不变。
- **ABox 关系视图不变**：SPARQL 型 → LodLive；memory-graph 型 → OntologyExplorer。
- 上表保留为开源嵌入方案评估基线（WebVOWL/ontoink/LodLive/OntologyExplorer），供对比与反转时引用。

### 3.6 构建平面 REST（沿用 v0.1 §7.1，新增 fork/AI 路由）

`/api/ontologies` CRUD + `/validate` + `/seed-sample` + `/guide` + `/import` + `/export`（v0.1 已定义，不变）；新增：`POST /api/ontologies/{id}/fork`（P2）、`POST /api/ontologies/ai-draft`（LLM 草稿生成，见 §5 模型来源）、`GET /api/ontologies/{id}/artifacts`（形态清单）。

### 3.7 由知识库构建本体（v0.13 新增，D-O14/REQ-108；**已交付（c56ebd4，2026-09-23，02 v0.28/M15）**：三策略端点 + BuildFromKBFlow 流程页 + 示例 5 全部落地，实现注记见 02 v0.28 头注块——新包 ontobuild 编排、选择器 KG 统计 worker 回读、worker kb_kg.json 持久化、图缺失降级不再 500）

> **定位**：构建栏**第六路径**——以现有知识库（KB 模块，D-KB4 v0.25 拆为 RAG/GraphRAG 双子模块）为数据源，三种抽取策略生成 spec_json 草稿并落本体仓库。与 §3.3「LLM 辅助从零创建」（自然语言 → spec_json）正交互补——后者由「自然语言描述」驱动，本路径由「KB 文档池」驱动；与 §4.9 semantica 消费链路对偶——semantica 是「本体 → KG」消费方向，本路径是「KB → 本体」构建方向。**0 自研 KG 抽取**：复用本体侧 semantica worker `:8093` v0.6.8+ 的 `/graphrag/ingest`（已扩展接受任意 chunk 集合）+ `/graphrag/query` 输出 entities/relations/claims。

**三种抽取策略**（互斥或组合）：

| 策略 | 数据源 | 抽取实现 | 适合场景 | 状态 |
| --- | --- | --- | --- | --- |
| **A：chunk→LLM 抽 spec_json** | KB chunk 池（KB.mode='rag'） | 复用 §3.3 LLM 辅助从零创建（REQ-82）：拉 chunk 集合 → 喂 LLM + CQ 引导（REQ-90）→ spec_json 草稿 → 校验循环（最多 3 轮） → 预览入库 | 文档质量高、需要模型化抽象；学习者熟悉 LLM 驱动流程 | **可即时上线**（共用 `/api/ontology-llm/generate`） |
| **B：KG→直转 spec_json** | KB GraphRAG KG（KB.mode='graphrag'，semantica graph_store） | 主平台薄映射层 <300 行（entity→Concept / relation→Relation / claim→Attribute），不做抽取；用户可在 §3.4 编辑器二次调优 | 已有结构化 KG（如设备故障 KG）；想要严格语义资产；学习者想看「KG→本体」直接映射可读性 | **P2 后评估**（直转保真度依赖 semantica 抽取质量） |
| **C：混合**（KG 作初稿 + LLM 校验 + 补全） | 两种 KB 都可 | 策略 B 产 entity/relation 初稿 → 策略 A 喂 LLM 做校验 + 补全 concept definition / relation range/domain | 既要图谱语义覆盖又要模型完整性；大文档库 | **P2**（与策略 B 同步） |

**入口步骤**（前端流程页面，与现有 5 路径独立展示）：

```
[ KB 选择器 ] ─→ [ 抽取策略选择（A/B/C） ] ─→ [ CQ 引导模式 ] ─→ [ 生成草稿 ] ─→ [ 校验循环 ] ─→ [ 预览 ] ─→ [ 入库 ]
       │                   │                       │                  │
       │                   │                       │                  └─ spec_json 校验失败回喂模型修正（最多 3 轮，与 REQ-82 一致）
       │                   │                       └─ 自动生成 3~5 个能力问题 / 用户自定义 / 跳过（REQ-90 一致）
       │                   └─ 默认 A；KB.mode='graphrag' 时推荐 B 或 C
       └─ 必选；列已有 KB 列表 + KB.mode 徽标「RAG / GraphRAG」+ chunk 数 / KG 实体关系数预览
```

**新增 REST 端点**（薄层 4 个）：

- `GET /api/kbs/selectable-for-ontology-build`（列 KB + mode + 统计）
- `POST /api/ontologies/build-from-kb`（入参：kb_id + strategy + cq_mode；返回 spec_json 草稿 + 校验报告）
- `POST /api/semantica/chunks-to-kg`（v0.13 已扩展 `/graphrag/ingest`，薄封装主平台接口面）
- `POST /api/ontologies/kg-to-spec-json`（策略 B/C 用的薄映射层 <300 行；entity→Concept / relation→Relation / claim→Attribute）

**依赖与触发**：

- M6-KB1~3 KB 基础 RAG 已完成
- M14 KB 双子模块（向量检索 + GraphRAG 检索）已落地
- REQ-82 LLM 辅助创建已就绪
- v0.13 semantica worker `/graphrag/ingest` 接受 chunk 集合已实现
- 02 §12 M15 里程碑
- **最早可启动**：M14 + M6 KB 完成 + semantica worker chunk 接受已实现 → 策略 A 可上线；策略 B/C 随 P2 评估

**学习价值与示例本体**：

- 补齐「**文本 → 三种语义资产形态**」完整对照：RAG 片段（检索用，不持久化）/ KG（语义层，semantica）/ 本体（概念层，本仓库）—— 与本体侧「构建/运行/消费/审计」四环节形成「文本 → 不同形态资产」完整矩阵
- 示例本体：`seeds/learning/ontologies/` 增第 5 个示例「**设备故障知识库 → 设备故障本体**」（v0.13 增，P2 落地）——演示 chunk→spec_json 抽取流程 + 与 SKOS 分类样例对照
- 与现有 5 路径关系：独立构建路径；非 OntoChat 流式多轮；非自定义手写；与 semantica 流程「本体→消费 KG」方向相反

---

## 4. 运行平面设计（v0.2 全新）

### 4.1 运行方案模型（REQ-84/85/86）

```sql
-- runtime-manager/migrations/001_profile.sql
CREATE TABLE runtime_profile (
  id TEXT PRIMARY KEY,                    -- 如 rt_mem_k8s
  name TEXT NOT NULL,
  engine TEXT NOT NULL,                   -- memory_graph | oxigraph | fuseki
  ontology_ids TEXT NOT NULL DEFAULT '[]',-- 加载的本体集合（JSON 数组，仓库 id）
  config TEXT NOT NULL DEFAULT '{}',      -- 细节配置 JSON（端口/推理开关/可写/JVM 参数…）
  port INTEGER,
  status TEXT NOT NULL DEFAULT 'created', -- created|starting|running|stopped|error
  pid TEXT, last_error TEXT,
  created_at DATETIME, updated_at DATETIME
);
```

**状态机**：`created → starting → running ⇄ stopped`；任意态可进 `error`（启动失败/健康检查连续失败）；启停/配置变更经 Manager 编排，状态自动刷新（前端轮询或 SSE）。

### 4.2 运行时类型（engines 适配器）

| engine | 实现 | 加载形态 | 能力 | 学习点 | 交付 |
| --- | --- | --- | --- | --- | --- |
| `oxigraph` | Oxigraph 官方二进制/容器（Rust，MIT/Apache-2.0） | original TTL/OWL（自建本体经 spec_json→TTL 导出） | SPARQL 端点 + 图存储；无 OWL 推理 | RDF 三元组模型、SPARQL、标准部署 | **P1 唯一引擎（O2）** |
| `fuseki` | Apache Jena Fuseki（Java，Apache-2.0，Docker） | original TTL/OWL | SPARQL + **RDFS/OWL 推理**（可配置开启） | 推理语义、企业级本体服务形态 | P1 后期（O6） |
| `memory_graph` | **开源内存图引擎嵌入（不自研，D-O5 v0.4）**：Cayley fork（`NewMemoryGraph` Go 库嵌入）+ 薄语义适配层 | spec_json（经适配层转换装载） | 精确查询（4 工具语义），毫秒级 | 开源引擎集成、语义适配层设计 | **P2 可选（O7，不自研）** |

每个适配器实现统一接口：`Prepare(profile) → Start(profile) → HealthCheck() → Stop()`；数据装载方式按类型（oxigraph：`oxigraph-server` 数据目录 + bulk load；fuseki：创建 dataset + `/data` upload 或 assembler 配置；memory_graph：经语义适配层装载）。

**D-O5 v0.4 要点**：P1 唯一引擎 Oxigraph——onto_*→SPARQL 翻译层随之从原 O6 提前至 O2（本来就要做，只是提前）；自建本体（仅 spec_json 形态）经 O1 的 TTL 导出器获得 original 形态后装载；**TTL 导出器由 Python rdflib sidecar 实现（v0.6：解析与导出同源，Go 侧不再自研 RDF 相关代码）**；memory_graph 不自研（v0.6 定案），P2 若需直接集成开源 Cayley fork，原"spec_json 直载 + 版本自动生效"对照实验随 LG-12 取消而关闭。

### 4.3 Runtime Manager（编排层，自研边界内）

- **生命周期编排**：按 profile.config 启动对应 engine（子进程优先，Docker 可选）；端口分配与冲突检测；健康检查（HTTP 探测，间隔可配）；stdout/stderr 日志采集落盘（页面可看最近日志）。
- **分发**：启动前从构建平面仓库拉取所选本体的对应形态（HTTP 调 `/api/ontologies/{id}/artifacts`）；本体集合变更 → 提示需重启方案生效（v0.2 简化：不支持热加载，热加载进需求池）。
- **降级联动**：主平台挂载的方案停止/崩溃 → 该对话本体查询发 `ONTOLOGY_SERVICE_UNAVAILABLE`，不影响其他方案与普通对话（REQ-70 语义按方案隔离）。

### 4.4 统一 MCP facade（Agent 契约不变）

Manager 内嵌 mcp-go Server（`/mcp`，Streamable HTTP，沿用 Q-14，**v0.23 契约 4→5**）：`tools/list` 固定返回 5 个 onto_* 工具（4 签名不变，见 v0.1 §5.2；第 5 个 `sparql_query` REQ-151/D-O17——只读 SELECT 白名单词法校验 + 10s 超时 + limit 默认 50 上限 200，受控开放查询面）；`tools/call` 按 `ontology_id` 路由到所属 running 方案——memory_graph 直查内存图；oxigraph/fuseki 将工具语义翻译为 SPARQL（get_concept/get_instance→DESCRIBE/SELECT，list_instances→按 rdf:type 查，neighbors→属性路径查询）。**Agent 侧与主平台无需感知运行时差异**（facade 模式，学习点）。

- 挂载对象变更：对话挂载**运行方案**（conversation 配置存 `runtime_profile_id`），工具入参仍带 `ontology_id` 定位方案内本体（开放问题 O-6，默认按此实现）。
- 即时生效（REQ-73 演进，v0.6 对齐 REQ-87）：P1 全引擎（SPARQL 型）统一**显式重载生效**——本体在构建平面保存新版本后，方案页明确提示需重载，重载后按新版本返回（不静默）；"按版本自动生效"对照语义视 P2 引擎引入情况再评估。

### 4.5 运行平面 REST（新增）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET/POST | `/api/runtime-profiles` | 列表（含状态/健康/端点）/ 新建（engine + ontology_ids + config） |
| GET/PUT/DELETE | `/api/runtime-profiles/{id}` | 详情 / 编辑配置（变更后需重启生效）/ 删除（先停） |
| POST | `/api/runtime-profiles/{id}/start` \| `/stop` | 启动 / 停止 |
| POST | `/api/runtime-profiles/{id}/reload` | RDF 型方案重载本体数据 |
| GET | `/api/runtime-profiles/{id}/logs?tail=200` | 最近日志 |
| GET | `/healthz` | Manager 自身健康 + 各方案状态摘要 |

### 4.6 工具链配置层（REQ-74~77，v0.4 新增）

**定位**：在构建/运行两平面之上加一个**流程配置视图**——把 S1~S7 七阶段的工具选择持久化为 pipeline_profile。不引入新引擎：S5 段直接引用 runtime_profile（`stages.s5.tool` 落到 `runtime_profile_id`），一套引擎模型、两个视角（运行方案=运行视图，工具链=流程视图）。

> **v0.12 重定位（D-O11）**：随 REQ-104 构建路径分类落地，pipeline_profile 从"按阶段选工具"重定位为"**路径级工具链配置**"（学习中心 v1 先跑固定默认工具链，本层 P2 提供配置化能力）；guided 打卡机制归学习中心（`task:` 空间 P1 尾先行，`tool:` 空间随本层 P2）；七阶段模型（REQ-74）保留为学习中心的理论骨架。数据模型（pipeline_profile 表 / StageTool / tools.json）不变，语义视角调整。
>
> **✅ 已交付（2026-09-23，c55730b）**：004_pipeline.sql + internal/pipeline（tools.json 七阶段 24 工具 + 默认模板 + checklist 生成）+ /api/pipelines 八端点 + 学习中心「工具链配置」Tab（PipelinePane：候选卡片选中即保存 / guided 打卡 tool: key / 复制重置删除）。

```sql
-- ontology-service/migrations/003_pipeline.sql（P2）
CREATE TABLE pipeline_profile (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  ontology_id TEXT,                       -- 可选：预绑定本体（也可后选）
  runtime_profile_id TEXT,                -- S5 段引用已有运行方案（managed）
  stages TEXT NOT NULL DEFAULT '{}',      -- {"s1":{"tool":"builtin_import","mode":"builtin","params":{}} ... "s7":...}
  checklist TEXT NOT NULL DEFAULT '{}',   -- guided 阶段打卡状态 {stage: {step: done_at}}
  created_at DATETIME, updated_at DATETIME
);
```

**候选工具统一描述（数据驱动，REQ-77）**：

```go
type StageTool struct {
    ID       string   // builtin_import | protege_desktop | webvowl | fuseki | ...
    Stage    string   // s1..s7
    Name     string
    License  string
    Mode     string   // builtin | guided | managed
    Eats     []string // 输入形态: spec_json | original | endpoint
    Gives    []string // 输出形态（交接契约，见七阶段清单 §2）
    Guide    Guide    // guided 模式指引：安装命令/启动命令/数据交接点/入口链接
    Learning string   // 学习要点（配置页卡片展示）
    // P2 编排位：Execute(ctx, profile) error —— 预留不实现（D-O7）
}
```

候选清单存 `tools.json`（随版本维护），增删工具不改核心代码。

**执行模式三态**：builtin 直接可用；guided 由 REST 生成 checklist、前端打卡、状态存 pipeline_profile.checklist；managed 仅 S5（Runtime Manager 现有能力）。**交接契约**（spec_json/original/endpoint）是可插拔的技术基础——工具声明吃什么、吐什么形态即可入列。

**REST（P2）**：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET/POST | `/api/pipelines` | 工具链列表 / 创建（含默认工具链模板） |
| GET/PUT/DELETE | `/api/pipelines/{id}` | 详情 / 修改各阶段选择 / 删除 |
| POST | `/api/pipelines/{id}/clone` | 复制为新工具链 |
| GET | `/api/pipelines/{id}/checklist` | 按当前配置生成引导清单（guided 项聚合） |
| POST | `/api/pipelines/{id}/stages/{sid}/check` | guided 阶段打卡 |

前端：本体详情页新增「工具链」标签页——七阶段纵向步骤条，每阶段候选卡片（名称/许可/模式/形态/学习要点），S5 段内嵌运行方案选择入口（复用运行方案页能力）。

### 4.7 外部独立集成：OpenOntologies 路径（v0.7 新增）

> 依据：《open-ontologies借鉴与引入分析.md》v0.2 §8（2026-09-20 主人拍板：独立集成路径）；主平台对接点（oo-worker :8092、`/api/oo/*` 反代、`OO_MCP_URL`、M8.5 里程碑、验收 15a）以《02_智能体_技术方案设计.md》v0.8 §6.15 为准。

**定位**：open-ontologies（Rust 单二进制，MIT，Oxigraph 0.5 后端，pin 版本）经主平台 **oo-worker 独立托管**（单二进制 + 工作台 + MCP 暴露），与本体两平面**并行双轨、无接口依赖、可整体摘除（P2）**；能力：RDFS/OWL-RL 物化推理、SHACL 校验、不一致检查、变更影响分析（plan/blast radius）、数据装载（CSV/XLSX 等→RDF）、MCP server（39 个 onto_* 工具）。

- **与 D-O5 的关系**：P1 唯一引擎仍为 Oxigraph（不变）；open-ontologies **不作为两平面引擎适配器**（融入式方案搁置），不经 facade/翻译层。
- **数据边界**：该路径本体为 TTL 文件集（worker data-dir 自管），**不进本体仓库 spec_json 体系**；两轨数据 P1 不互通（互通见《03_本体_需求文档.md》REQ-78「P2 TTL 导出互通」）。
- **与七阶段工具链（D-O7）的关系**：仅在 §4.6 `tools.json` 候选清单登记 S3/S5 两条候选（见上），不改变 D-O7 执行模式定义。
- **待验证项**：①open-ontologies `serve` 的 MCP 传输形态（README 中 stdin/stdout 与 streamable HTTP 表述不一致；若为 stdio 由 oo-worker 转 HTTP）；②物化推理与重载成本实测。

---

### 4.8 学习增强设计（v0.8 新增：G1~G6 落地，承接 REQ-90~96）

> 学习内容包（REQ-90/91）为**内容资产**而非代码：统一存放 `seeds/learning/`，随版本维护、不进数据库 schema；REQ-92~96 为平台功能，落点在 ontology-service / runtimemgr / 前端。设计原则：复用既有机制（checklist、导入报告、spec_json 结构），不新增运行时服务。

#### 4.8.1 SPARQL 查询工作台与源码视图（REQ-92/93）

- **（v0.26 实现升级）工作台改为 CM6+AntD 自组装**：Yasgui 集成期其 CSS 未随包生效，界面杂乱；改 @uiw/react-codemirror + codemirror-lang-sparql 编辑器 + AntD 工具栏/结果表（模板/CSV/⌘⏎/耗时标签），执行 POST application/sparql-query 经原反代端点，后端零改动。下文 Yasgui 段为历史设计留档。
- **Yasgui 内嵌（前端组件，A 档）**：`@triply/yasgui`（MIT）作为 React 组件挂载于两处入口——①运行方案详情页「SPARQL」标签页；②本体详情页「查询」入口（自动关联包含该本体的运行方案）。默认 endpoint = `GET /proxy/ontology/runtime/{profile_id}/sparql`（复用 §5 双反向代理契约，浏览器不直连引擎端口）。Yasgui 自带多 tab、结果表格/图/原始响应三视图、查询历史（localStorage 本地存储即可，P1 不做服务端同步）。
- **源码视图**：`GET /api/ontologies/{id}/versions/{v}/original` → 前端只读渲染（CodeMirror 6，MIT），按扩展名选择 Turtle/OWL Functional/RDF-XML/JSON 模式；spec_json 同页提供格式化 JSON 视图。>1MB 文件提示下载查看。入口与工作台同页组织为「查询 & 源码」标签组。
- **自研边界**：均为前端组件集成 + 一个反代端点，后端零新服务。

#### 4.8.2 翻译透视与推理对照（REQ-94）

- **翻译透视**：facade（mcp-go handler）执行 onto_* 时记录调用日志 `{ts, tool, ontology_id, profile_id, sparql, took_ms, result_count}`，写入 runtimemgr 侧 SQLite；开关 `facade.trace_sparql`（pipeline_profile 可配，默认开）。展示位两处：①P1——运行方案调试面板 `GET /api/runtime/profiles/{id}/trace?limit=50`（时间倒序，点击展开 SQL 原文可复制）；②P2——对话时间线工具卡片展开透出（经主平台 SSE 元数据透传）。
- **推理对照（两段交付）**：
  - P1 通用版 = **跨方案对照**：方案详情页「推理对照」入口 → 同一 SPARQL 并行发往两个 profile（建议：Fuseki 带推理 + Oxigraph）→ 前端并排 diff（新增/消失行高亮）。O6 交付 Fuseki 后可用。
  - P2 增强版 = **单方案开关对照**：Fuseki 推理参数热切换（同 profile 两次执行）。统一 diff 算法按结果集行集合对比（行排序后集差）。
- **学习文案**：对照结果页固定渲染解释模板（"新增实例来自 `rdfs:subClassOf` 推断——X 的类型断言由 Y 的子类声明传导"），把 G3"可见性"落到可读性。

#### 4.8.3 学习内容包与任务卡（REQ-90/91，内容资产）

- **目录结构**：`eino-multiagent-lab/seeds/learning/` 下三类资产——①`methodology/`（建模方法论卡片，markdown 五模块：能力问题法 / 领域分析与概念抽取 / 复用既有词表与顶层本体 / 命名与 URI 规范 / 常见 OWL 建模模式）；②`examples/`（示例本体包 = spec_json + `README.md` 建模说明：背景、目标 CQ 列表、建模决策记录、局限与反例）；③`tasks/`（任务卡 JSON）。
- **任务卡结构（示例）**：
```json
{
  "id": "task_s3_inconsistency",
  "stage": "s3",
  "title": "故意制造一次不一致，看推理机怎么说",
  "goal": "理解逻辑一致性校验的价值与矛盾传导机制",
  "prereq": ["task_s1_first_ontology"],
  "steps": [
    "打开示例本体「设备故障知识」并复制到我的本体",
    "为某实例断言两个互斥类的 rdf:type",
    "对运行方案执行一致性检查（guided：Jena reasoner / oo validate）",
    "记录推理机输出的矛盾路径（哪两个类冲突、经哪条 subClassOf 传导）"
  ],
  "acceptance_question": "推理机给出的是哪两个类的冲突？矛盾为什么会沿子类关系传导？",
  "tools": ["jena_reasoner", "oo_validate"],
  "difficulty": 2
}
```
- **打卡机制**：复用 §4.6 pipeline_profile.checklist，key 空间分两组：`tool:<stage>:<tool_id>`（REQ-76 工具打卡）与 `task:<task_id>`（REQ-91 任务打卡），互不混用；进度视图前端按阶段聚合（随 O9 配置页交付）。
- **内置示例本体扩至 4 个**（覆盖四条构建路径）：
  | 示例 | 构建路径演示 | 教学侧重点 |
  | --- | --- | --- |
  | K8s 迷你运维（已有） | 手写 spec_json | 最小闭环、从零建模 |
  | 软件缺陷管理 | AI 生成（REQ-82） | CQ 驱动 + 生成-校验循环 |
  | 组织与人员 | fork/复用（REQ-83） | 对照 FOAF 术语理解重用 |
  | 设备故障知识 | 数据灌装（REQ-96） | SKOS 分类体系 + 实例规模化 |
- **AI 创建 CQ 引导**：REQ-82 生成页第一步加可选文本框"先列出 3~5 个能力问题"，内容并入生成上下文（prompt 侧改动，无新服务）。

#### 4.8.4 版本 diff（REQ-95）

- 端点：`GET /api/ontologies/{id}/diff?from={v1}&to={v2}`。服务端加载两版 spec_json，三集合结构化对比：concepts / relations / instances 按 id 对齐 → added / removed / changed（changed 比较 name、description、属性值）；附引用影响统计（changed 元素被 relations/instances 引用的计数）。
- 纯 Go 标准库实现（<1k 行），无新依赖；前端双栏 + 变更高亮渲染，支持导出 Markdown 对比报告。数据一致性依赖既有版本机制（REQ-64/65），无新增状态。

#### 4.8.5 主线数据灌装（REQ-96）

- **P2a 同名映射**：`POST /api/ontologies/{id}/ingest-csv`（multipart：csv + `{key_column, relation_columns, attribute_columns, skip_rows}`）→ Go `csv.Reader` 解析（同名映射无需 pandas/sidecar）→ 生成 instances 草稿 → 预览接口返回统计与 warnings（复用 REQ-72 导入报告口径）→ 确认后走 REQ-63 校验入库为新版本。
- **P2b 映射向导**：列绑定 UI + 类型转换规则（string/int/date）+ 多值分隔符 + 跳行规则；映射配置作为导入任务对象存储，执行引擎与 P2a 相同。✅ 已交付（2026-09-23）：资产栏「CSV 灌装」页签（CsvIngestPane）+ `type_rules`（扩至 int/number/date/bool，非法值降级原字符串 + warning）+ `multi_value_sep` + 映射配置存 `ingest_mapping_json` artifact（GET/PUT ingest-mapping；不 bump version、不进产物表、不随 fork 复制）。
- **与双轨关系**：oo-worker `load` 仍是 S1 段 guided 候选（数据留在双轨）；本节是**主线仓库内通路**（数据进 spec_json 体系），两者并存、面向不同学习目的。

---

### 4.9 Semantica 独立集成设计（v0.10 新增：D-O10，对标 §4.7 双轨模式；v0.12 设计事实源独立成档）

> 定位：**导航栏独立一栏**，不进两平面、不进 facade（同 §4.7 零侵入原则）。本体模块叙事由此扩展为"**构建 → 运行 → 消费 → 审计**"四环节：semantica（github.com/semantica-agi/semantica，MIT，Python）承担**消费**（KG 建库/GraphRAG）与**审计**（决策记录/PROV-O 溯源）两环节的学习载体与演示设施。
>
> **⚠️ 设计事实源已独立成档：《19_本体_semantica集成方案.md》**（v0.1）——含现状盘点（已实现接入架构图）/自研与开源边界（薄胶水层清单 vs semantica 库直调，防御式归一说明）/本地运行痛点分析（重依赖实测：venv 数 GB、import 慢、版本漂移）/支持方式六模式评估与推荐组合（本地 venv / **容器化推荐** / 远连服务端 / 0.7.0 瘦身迁移 / PyInstaller / 整体摘除）/部署矩阵/集成契约稳定面/待办。本节保留需求与复用策略口径，实现细节以 15 为准；后续演进只改 15，本节不再重复维护。
>
> **⛔ v0.18 D-O15 反转（2026-09-23 主人拍板）：semantica 不再作为运行时依赖接入**——主人实测 worker 可用性差（venv 数 GB/启动难）+ 重估确认核心学习链路零依赖 → 消费/审计改自研轻量替代（TTL 即 KG / LLM 抽 KG / KG 增强检索），semantica 转登记簿 🔭 对照参考；本节及《19_本体_semantica集成方案.md》转为**存档**（历史设计留痕，不再演进）；现行口径见 03 v0.15 §2.12 REQ-110。
>
> **✅ v0.19 实现交付（2026-09-23，commit 7d45dc0，03 v0.16）**——自研替代全量落地，与存档设计的对应关系：worker `POST /graphrag/ingest`（semantica 抽取）→ backend 内置 `kg.Extractor`（REQ-98 LLM 代理 method=`llm` + 规则回退 method=`lightweight`，SQLite 三表自存，claim.chunk_id 溯源）；worker `/graphrag/query`（GraphRAG 语义问答）→ `GraphragQuery` 三步检索（向量命中→KGSubjectsByChunks→KGNeighbors 一跳+claims，无命中回退纯向量标 degraded）；rdflib PROV-O 导出 → Go 原生 Turtle 模板（`renderProvTTL`，决策=prov:Activity、实体=prov:Entity、wasDerivedFrom 溯源，零 Python 依赖）；worker Explorer → ReactFlow/列表（D-O12③ 口径）；worker 反代 4 条路由 + `/api/semantica/chunks-to-kg` 退役，新 API 面 `/api/kg/{id}`、`/api/kg/{id}/rebuild`、`/api/audit/decisions(+/chain)`、`/api/audit/prov-export`；run-dev.sh 移除 worker 编排（验收 22 达成，冒烟全绿）。REQ-108 策略 B/C 数据源同步改自存 KG。

#### 4.9.1 部署与进程形态

- `tools/semantica-worker/`（对标 tools/rdf-sidecar）：Python venv（≥3.8）+ `pip install semantica`，**版本锁定 v0.6.8+**（v0.5.0 前有 12 项已披露漏洞修复：SQL 注入/XXE/eval 注入）；requirements.txt 钉版本，extras（neo4j 等）按需。
- 启动脚本拉起其 REST API（约定端口 **:8093**，纳入 §5 端口约定）；run-dev.sh 统一编排；健康检查 `GET /health`。
- 学习价值前置说明（首页引导素材）：13.1K stars 的"AI Agent 语义/决策层"——把碎片数据变成受本体治理（OWL/SHACL/SKOS）的 Context Graph，决策全链路 W3C PROV-O 可审计。

#### 4.9.2 前端：导航栏独立路由 + 左侧边栏功能全覆盖（REQ-99/102，v0.11 重写）

- 前端新增「Semantica」导航项（与 open-ontologies 界面同级、同为独立栏）。**页面布局：左侧边栏分组导航 + 右侧功能主区**（v0.11 依主人拍板由"三页签"升级，覆盖度分析结论：原三页签仅覆盖 semantica 完整能力面约 1/3）：

| 分组 | 侧边栏项 | 实现方式 | 优先级 |
| --- | --- | --- | --- |
| 学习引导 | 首页（三段式引导） | 自研薄页（已有） | P1 ✅ |
| 图谱 | 知识图谱（Sigma.js 画布/ForceAtlas2/Ego 模式/语义距离热力图） | Explorer 复用（现轻量自研版逐步替换） | P1 ✅ |
| 图谱 | 时间轴 Timeline（时序事件回放/图演化/时点快照） | Explorer 复用 | P1 |
| 图谱 | 实体消解（重复实体审查/合并，blocking+语义去重） | Explorer 复用 | P1 |
| 图谱 | 图谱分析（中心性/社区检测/链接预测/最短路径） | REST 薄页 | P2 |
| 推理与决策 | 推理引擎（前向链/Rete/Datalog/SPARQL，可解释推理路径；与 REQ-94 推理对照互链） | REST 薄页 | P1 |
| 推理与决策 | 决策审计（记录+因果链+先例搜索+影响分析+策略门） | 薄页（已有轻量版→补全） | P1 基础 / P2 完整 |
| 推理与决策 | 溯源 Lineage（PROV-O 链可视化） | Explorer 复用 | P2 |
| 推理与决策 | 操作登记 Registry（图变更审计日志） | Explorer 复用 | P2 |
| 本体治理 | 本体中心 Ontology Hub（SHACL Studio/可视化编辑/跨本体对齐/SKOS 浏览）——与构建平面本体互为参照 | Explorer 复用 | P1 |
| 本体治理 | 质量门（本体/KG 确定性质量检查，CI 友好）——与构建平面 OOPS!/SHACL 校验对照 | REST 薄页 | P2 |
| 数据与检索 | 数据摄取（TTL 导入 + 多源 ingest【文件/网页/数据库/流】+ NER/关系/事件抽取 + 冲突检测） | 薄页（现 ingest-ttl 扩展） | P1 TTL / P2 多源 |
| 数据与检索 | 语义检索（GraphRAG 向量+图混合问答独立页；embeddings 可视化）——**v0.13 起升级为 P1 并服务双消费场景**（本体侧 + 知识库侧 GraphRAG 子模块，见 04 §4.9.3 扩段、12 KB v0.2 D-KB4） | REST 薄页 | **P1** |
| 设置 | 存储与导出（polyglot 后端 RDF/LPG/向量切换、store migrate、导出 RDF/JSON/CSV/Parquet） | REST 薄页 | P2 |
| 设置 | MCP 与对话（挂载状态/stdio→HTTP 桥接状态/MCP 15 工具清单） | 自研薄页 | P2 |
| 设置 | Agent 记忆 Memories（AgentMemory 工作区；与智能体模块仅教学对照，不做生产互通） | Explorer 复用（可选显示） | P3 |

- **复用策略（零侵入不变）**：Explorer 原生 8 工作区（知识图谱/时间轴/决策/Registry/实体消解/Ontology Hub/Lineage/Memories）经反代/iframe 直接复用、**0 自研**；自研薄页仅两类——学习引导页与 Explorer 未覆盖的能力页，统一调 `/api/semantica/*` 反代（→ :8093），每页 <300 行。
- **首页三段式（内容资产，`seeds/learning/semantica/` 随版本维护）**：
  1. **功能**——它是什么（AI Agent 的语义层与决策智能层）、解决什么（黑盒决策不可审计）、不做什么（不解释 LLM 内部思维，只审计系统级决策链）；
  2. **原理**——三层架构（Input：ingest/parse/split/normalize → Semantic：semantic_extract/kg/ontology/reasoning → Storage：graph_store/vector_store/triplet_store）；PROV-O 溯源机制；record_decision 决策生命周期与因果链；**与两平面的关系图**（构建平面供给本体 TTL → semantica 消费建 KG → 审计链路回流展示）；
  3. **使用说明**——六步演练（对齐侧边栏分组）：选本体 → 导出 TTL（O1 sidecar 已有）→ 数据摄取建库 → 图谱浏览与推理引擎 → 语义检索问答 → 决策记录 + 溯源 + 操作登记查看。
- GraphRAG/embeddings 不并入构建平面（构建平面边界不变）。**v0.13 起例外（09-23 D-KB4 反转）**：知识库模块通过 `/api/semantica/*` 反代间接消费 semantica worker 的 GraphRAG/embeddings 能力（KG 实体/关系仍只落 semantica 侧，主平台不重存）——构建平面（runtime-manager / ontology-service）依然**零侵入**，仅 semantica worker 接口面扩展一行：`POST /graphrag/ingest` 增加 chunk 数组入参（详见 §4.9.3 扩段）。**学习语义边界**：本体侧 semantica 独立栏管「本体/决策/审计」闭环；知识库模块内的 GraphRAG tab 管「文档→知识图谱→检索问答」闭环——两个不合并。

#### 4.9.3 消费链路（REQ-100：TTL→KG；v0.13 增知识库侧消费入口）

- 路径：本体详情页「导出 TTL」（O1 已有）→ `tools/semantica-worker/ingest_ttl.py` 薄脚本（<200 行：调 semantica ingest + GraphBuilder，merge_entities/temporal 可配）→ KG 入其 graph_store；导入统计（实体/关系数、警告）回流独立栏首页展示。
- **教学对照（核心学习点）**：同一本体的两个消费面——运行平面 facade **精确查询**（SPARQL，REQ-86/92）vs semantica **GraphRAG 语义问答**（向量+图混合检索）；对照页说明各自适用场景（结构精确匹配 vs 语义近似与多跳）。

**v0.13 起新增知识库侧消费入口（D-KB4，2026-09-23 主人拍板）**：

- 路径：知识库模块创建 GraphRAG 子模块 KB（`kb.mode='graphrag'`）→ 用户上传文档 → 主平台切分 → chunk 入 SQLite + **异步**把 chunk 集合 POST 给 semantica worker `:8093 POST /graphrag/ingest`（v0.13 新接口契约；原 `/ingest/ttl` 入口不变）→ worker 抽 KG（实体/关系/事件）入其 graph_store → 用户提问 → 主平台 `POST /api/kbs/{id}/graphrag-search` 经 `/api/semantica/*` 反代 → worker `POST /graphrag/query` → GraphRAG 命中（实体/关系/claims）回主平台 → `retrieval` SSE 事件 `mode='graphrag'` → Agent 生成。
- **接口扩展（KB-O4）**：`POST /graphrag/ingest` 新接受 chunk 数组入参（与原 TTL 路径并列，分支由入参类型识别）：
  - TTL 路径：`POST {ttl_files: [...], merge_entities: bool, temporal: bool}`（v0.6 已实现，REQ-100）
  - Chunk 路径（v0.13 新增）：`POST {chunks: [{chunk_id, source_uri, content, embedding_dim?, doc_id}], embedding_model: str}`（用于知识库侧 GraphRAG 子模块）
  - 返回统一 `{entities_added, relations_added, events_added, warnings}`；失败重试 3 次仍败 → 主平台标记该 doc 为「KG 抽取失败，检索降级到 RAG 模式」。
- **零侵入校验**：构建平面（runtime-manager / ontology-service）依然 0 改造；主平台侧 0 自研 KG 抽取——只新增一个 HTTP 反代调用 + 一个 chunk 数组转换层（`<200 行`，与 ingest_ttl.py 同一作者）。
- **钉版本（KB-O5）**：semantica worker 主版本钉 v0.6.8+；主平台启动时 `GET /health` 返回的 worker_version 字段必须 ≥ v0.6.8，否则知识库 GraphRAG 子模块置为「服务版本过低」禁用状态 + 提示升级。版本变更须经知识库 + 本体双侧文档评审。
- **学习价值闭环**：本体侧三层——构建（O1 sidecar 导出）→ 运行（runtime-manager facade SPARQL 精确查询）→ 消费（semantica GraphRAG 语义问答）；知识库侧两层——chunk 入库（RAG）+ chunk→KG 入库（GraphRAG）→ 检索问答（RAG/GraphRAG 二选一）。两层对照让学习者直观看到「同一段文本的两条检索路径」与「本体驱动 vs 文档驱动」的本质差异。

#### 4.9.4 审计视图（REQ-101，P2）

- 演示流：Agent 对话（挂载 semantica MCP）回答本体问题 → record_decision 落决策记录（上下文/依据/结论）→ 决策审计页查看 PROV-O 链（事实→来源→推理路径）→ 与 REQ-94 推理对照页互链，构成"**推理可解释 + 决策可审计**"完整教学故事。
- 对话配置：主平台对话配置的 MCP 目标列表增加 semantica MCP server（外部 MCP 接入契约见智能体侧 REQ-97/98 文档），按对话勾选启用；semantica 侧 0 改造（其原生 MCP 能力）。

#### 4.9.5 与现有架构关系（零侵入确认）

| 项 | 现状 | 新增 |
| --- | --- | --- |
| 部署 | 无 | semantica-worker venv + 启动脚本（薄） |
| facade/两平面 | — | **零侵入**（不进翻译层、不进引擎清单） |
| 前端 | 无 | 导航项 + 首页三段式（内容资产）+ Explorer 反代（薄） |
| 消费 | TTL 导出已有（O1） | ingest 薄脚本（<200 行） |
| 审计 | 无 | P2，复用 Explorer UI，零自研 |

---

### 4.10 本体模块 IA 与学习中心（v0.12 新增：D-O11，REQ-104）

> 定位澄清（主人 2026-09-22）：本体模块 = **学习各种本体构建、运行方式的模块**。IA 由"三 Tab"升级为**左侧边栏四栏**，学习视角升为一级体验；现有页面主体降为五条构建路径之一。前端实施细节见《14_本体_前端改造方案.md》v0.3（F1~F5）。

**目标 IA**：

```
本体（顶部导航项不变）
└── 左侧边栏
    ├── 学习中心（默认页）        # 七阶段路径 + 任务卡打卡 + 构建/运行方式对照
    ├── 本体构建                  # **六路径**二级模块（状态徽标：可用/部分可用/引导先行）
    │   ├── 自定义构建           # 现有页面主体，保留 S1~S4；S5~S7 移交资产/运行
    │   ├── OntoChat 流程        # REQ-103 模式 A 载体（部分可用）
    │   ├── semantica 流程       # 入口卡跳转独立栏（D-O10 零侵入不破）
    │   ├── OntoExtend 流程      # 引导卡先行，工程化另行评估
    │   ├── Open Ontologies 流程 # 双轨引导 + 产物回流（REQ-78 P2 互通后顺畅）
    │   └── 由知识库构建本体     # **v0.13 新增第六路径，D-O14/REQ-108；KB 子模块（D-KB4）为数据源；三种抽取策略（chunk→LLM / KG→直转 / 混合），CQ 引导 + KB 选择器（mode 徽标「RAG / GraphRAG」）+ 独立流程页面；状态：部分可用**
    ├── 本体资产                  # 统一列表 + 详情工作区（Spec/校验/版本/产物/可视化/导出）
    └── 本体运行                  # 按运行方式分组：Oxigraph / Fuseki（O6）/ oo 引导页 / Cayley（P2）
```

**学习中心 v1（P1 尾，与 REQ-90/91 提前档同步交付）**：

- 七阶段学习路径主视图：S1→S7 路径条（REQ-74 模型为骨架），每阶段 = 方法论卡片入口（`seeds/learning/methodology/` 五模块精简版）+ 任务卡（REQ-91 结构，`task:` 打卡）+ 工具入口跳转（跳构建/资产/运行对应功能，不重复造功能）。
- 构建方式对照：**六路径**卡（v0.13 由知识库构建本体加入，D-O14），标注适用场景/学习要点/状态徽标；REQ-91 四示例本体映射对应路径。
- 运行方式对照：引擎差异卡（Oxigraph 无推理 / Fuseki 带推理 / oo 物化推理+39 工具），REQ-94 推理对照入口在此露出（随 O6 激活）。
- 打卡：复用 §4.6 checklist 机制，P1 尾落 `task:` 空间；进度按阶段聚合。
- 内容策略：P1 尾 = 方法论五模块精简版（每卡 300~500 字）+ 2 个示例建模说明；4 示例与深度版随 P2。

**学习中心 Tab 结构（v0.15 实际交付形态 + v0.16 增量）**：学习中心页 Tabs 共 5 个——①构建方式对照（六路径卡，v0.13）②运行方式对照（引擎卡 + 推理对照提示，O6 已激活）③示例本体库（4 示例一键灌装）④工具链配置（PipelinePane，v0.15 REQ-75 交付）⑤**外部资源**（**v0.16 新增，REQ-109**）——前端 LearnPage 以 **Vite `?raw` 构建期内联**直接导入 `seeds/learning/external-resources.md`（**单源维护：无后端端点、无 Go 侧拷贝**，dev 模式 `server.fs.allow` 放行仓库根；对比示例本体 internal/seed embed 拷贝双份模式，本机制单源更优），XMarkdown 渲染 `openLinksInNewTab`（与 ChatWindow 同栈），外链新窗口；**改 md + commit + 重新构建即生效，无需改代码**；md 面向学习者（一句话+链接），全局台账仍以 15 登记簿为准（维护者视角），不一致时以登记簿为准并回修 md。收录原则：可直接在线访问的平台（Ontology Playground 首条——微软官方本体学习 Playground，与本项目定位同构）/ 复用词表与顶层本体 / 官方规范与教程 / 典型开源项目；与 REQ-90 学习内容包同为 `seeds/learning/` 内容资产口径。

**路径分层实现原则**：已工程化路径给完整交互（自定义构建）；部分可用给"交互骨架+排期态"（OntoChat 流程，后端 REQ-103 模式 A 就绪前；**由知识库构建本体**——KB chunk→LLM 抽 spec_json 策略可即时上线、KG→直转策略 P2 后评估、混合策略 P2）；未工程化给引导卡（OntoExtend/oo 流程，含方法论、论文指引、手工路径、跳转）——**不做空壳交互**。

**与既有设计的关系**：§4.6 工具链层重定位为路径级配置（P2）；§4.8 学习件入口位置随四栏调整（SPARQL 工作台/源码视图在资产详情与方案详情，学习中心放跳转入口）；§4.9 semantica 独立栏零侵入不变（构建栏 semantica 流程仅为入口卡）；§3.7 由知识库构建本体（v0.13 新增，D-O14）与 §4.9 semantica 集成互为对偶——本路径为「KB→本体」构建方向，semantica 流程为「本体→KG」消费方向。

**本体侧边栏四栏→五栏（v0.19 D-O15/REQ-110 交付，commit 7d45dc0）**：Semantica 独立导航栏已撤销——原 SemanticaPage 骨架改造为「消费与审计」页（AuditPage，复用 sema-* 样式）挪入本体模块侧边栏第五栏，四 Tab：**KG 图谱**（实体按类型分组/关系列表/claim 表含 chunk_id 溯源 Tooltip）、**GraphRAG 试查**（选 KB 即查：向量命中→KG 一跳扩展，seed 1.0/邻居 0.6，KG 无命中降级纯向量并标注）、**决策审计**（决策表按 subject_kind 过滤/手动决策登记含 derived_from/溯源链 Timeline 抽屉/PROV-O TTL 导出下载）、**学习引导**（自研口径的教学三卡/检索三步/消费平面边界/演练步骤）；导航栏收敛一项（/semantica 重定向本体模块）；构建栏原 semantica 路径改「KG 消费流程」引导卡（入口跳转第五栏）。D-O11 四栏扩五栏完成。

---

## 5. 与主平台集成契约（v0.2 更新）

| 项 | 契约 |
| --- | --- |
| MCP Client | 主平台经 eino-ext `GetTools` 连 **Runtime Manager facade**（`ONTOLOGY_MCP_URL`）；断线重连不变 |
| 挂载 | 对话挂载运行方案（`runtime_profile_id`）；方案未 running → 禁选 + 提示（REQ-19c 语义平移） |
| 降级 | 所挂方案停止 → `ONTOLOGY_SERVICE_UNAVAILABLE`（带 profile_id）；其他方案与普通对话不受影响 |
| 指引注入 | `GET /api/runtime-profiles/{id}/guide?ontology_id=…`（Manager 代理构建平面 guide 并附加方案信息） |
| LLM 辅助创建（REQ-82） | 构建平面复用主平台模型连接：主平台提供 `/api/ontology-llm/generate` 代理（Eino 结构化生成），构建平面只传 prompt/schema 并接收草稿——**模型能力归主平台，校验归构建平面** |
| 反向代理 | `/api/ontologies*` → 构建平面；`/api/runtime-profiles*` → Runtime Manager（同源透传） |
| 环境变量 | `BUILD_SVC_URL`（默认 :8091）、`RUNTIME_MGR_URL`（默认 :8090）、`ONTOLOGY_MCP_URL`（facade）、`ONTOLOGY_DIAL_TIMEOUT` |

---

## 6. 开源组件清单与自研边界（REQ-88 落地）

| 子系统 | 方案 | 来源 | 自研？ |
| --- | --- | --- | --- |
| TBox 可视化 | WebVOWL（备选 Ontology Playground） | 开源 MIT | 仅做预转换与嵌入 |
| ABox 可视化 | LodLive（SPARQL 型）/ OntologyExplorer（memory 型） | 开源 / 自有资产 | 复用 |
| 运行时·SPARQL | Oxigraph | 开源 Rust | 无 |
| 运行时·推理 | Apache Jena Fuseki | 开源 Apache-2.0 | 无 |
| 运行时·轻量内存图 | Cayley fork（aperturerobotics，v0.15.0，Apache-2.0）+ 薄语义适配层 | Go 库嵌入 | **不自研，P2 可选直接集成**（D-O5 v0.4：LG-12 自研学习目标取消） |
| OWL/RDF 解析 | **rdflib sidecar**（Python，BSD-3）+ 导入报告胶水 | Python 子进程 | **不自研**（v0.6 定案；同时承担 spec_json→TTL 导出与可选 pySHACL，三职责一体化） |
| spec_json→TTL 导出 | rdflib（Turtle 序列化） | 同 sidecar | 不自研（O1 交付，随 sidecar） |
| 运行方案编排 | —（K8s 过重，无轻量开源） | — | **自研** Runtime Manager（学习内容） |
| MCP Server/Client | mcp-go + eino-ext | 开源 MIT | 无 |
| 一致性 MCP facade | — | — | 自研路由层（薄） |
| LLM 辅助创建 | 参考 OWLAPY 模式 | 思路参考 | Go/Eino 实现生成-校验循环（学习内容） |
| JSON Schema 校验 | santhosh-tekuri/jsonschema | 开源 | 无 |
| 前端图编辑 | React Flow + RJSF + dagre | 开源 | 仅 adapter |
| SPARQL 查询工作台 | Yasgui（@triply/yasgui，前端嵌入） | 开源 MIT | 仅配置 endpoint（v0.8，REQ-92） |
| 源码高亮视图 | CodeMirror 6（前端只读渲染） | 开源 MIT | 仅语言模式配置（v0.8，REQ-93） |
| 决策智能/溯源 | Semantica（pip，**v0.6.8+**，独立 worker :8093） | 开源 MIT | 独立栏集成，**零侵入两平面**（v0.10，D-O10：消费/审计两环节载体） |
| 推理/校验/灌装（双轨） | open-ontologies（**Rust 单二进制**，MIT，Oxigraph 0.5 后端，pin 版本） | 开源 MIT | 独立集成（oo-worker :8092 托管，**不进两平面**）：RDFS/OWL-RL 物化推理、SHACL 校验、不一致检查、变更影响分析（plan/blast radius）、数据装载（CSV/XLSX 等→RDF）、MCP server（39 个 onto_* 工具）（v0.7，§4.7） |

---

## 7. 里程碑（v0.2 重排）

| 步骤 | 内容 | 需求 |
| --- | --- | --- |
| **O1** | 构建平面基座：多形态仓库（002 迁移）+ 导入（原样存档 + 归一化 + 校验 + 报告；**OWL/RDF 解析经 Python rdflib sidecar**）+ 列表/编辑页（JSON 形态）+ **spec_json→TTL 导出器（rdflib sidecar 实现）** | REQ-80/60/61/63/64 |
| **O2** | Runtime Manager v1 + **Oxigraph 引擎（P1 唯一引擎）**：profile CRUD/状态机/启停/健康检查 + 引擎适配器（数据装载）+ 统一 MCP facade（4 工具 + **onto_*→SPARQL 翻译层**）+ 管理界面（列表/配置/日志） | REQ-84/85/86 |
| **O3** | 主平台接入：挂载运行方案 + guide 注入 + 降级 + SSE ontology.query；对话闭环验收 | REQ-65/66/67/70/73 |
| **O4** | 可视化浏览：WebVOWL 嵌入（TBox）+ OntologyExplorer 对接 spec_json（ABox）+ 浏览入口路由 | REQ-81 |
| **O5** | LLM 辅助创建：生成-校验循环 + 预览确认 + 入库 | REQ-82 |
| **O6** | Fuseki 运行时类型（推理对照，可配推理开关）+ LodLive 浏览路由——**v0.27 状态注（2026-09-26）**：Fuseki 引擎适配器（RDFS/OWL rules 推理启动）与推理开关透传已实现（manager knownEngines 含 fuseki / ReasoningRuntime.StartWithReasoning / RuntimePage 引擎卡「推理可配」透出）；「同查询并行 diff」对照界面与 Fuseki 真机验证未落地（FUSEKI_BIN 手动配置，REQ-146 诚实边界），REQ-94② 剩余口径见 03 v0.42 | REQ-86 扩展 |
| **O7（P2 可选）** | 轻量内存图引擎：**直接集成开源 Cayley fork（`NewMemoryGraph` 嵌入）+ 薄语义适配层，不自研**（D-O5 v0.4，LG-12 已取消） | D-O5 v0.4 |
| **O8（P2）** | fork 扩展；Claude/Cursor 直连 facade 复用；本体热加载；图形化编辑器（REQ-71） | REQ-83 等 |
| **O9（P2）** | 工具链配置层：pipeline CRUD + tools.json 候选清单 + 配置页（七阶段步骤条/候选卡片）+ 引导清单与打卡 | REQ-75/76/77 |
| **O10（P1 尾~P2）** | **学习增强包（v0.8；v0.12 调整）**：REQ-92/93 SPARQL 工作台+源码视图（P1 尾，随 O4 后插入）→ REQ-94 翻译透视（P1 尾）+ 推理对照（随 O6 Fuseki）→ **REQ-90/91 提前并入 O12（学习中心 v1，P1 尾）** → REQ-95 版本 diff + REQ-96 P2a 灌装（P2） | REQ-92~96（90/91 移 O12） |
| **O11（P1）** | **Semantica 独立集成（v0.10，D-O10）**：semantica-worker 部署（:8093，v0.6.8+）+ 导航栏独立路由 + 首页三段式学习引导（功能/原理/使用说明）+ 对话配置可选挂载（MCP 目标勾选）+ TTL→KG 消费链路（ingest 薄脚本）；审计/溯源视图（REQ-101）P2。**③stdio→HTTP 桥接未做**（对话挂载前置件） | REQ-99/100（101 P2） |
| **O12（P1 尾）** | **本体模块 IA 与学习中心（v0.12，D-O11）**：侧边栏四栏改版（学习中心默认页 / 构建五路径分类——自定义构建承载现有页面保留 S1~S4 / 资产统一 / 运行按引擎分组）+ 学习中心 v1（REQ-90/91 P1 尾档：方法论五模块精简版 + 2 示例建模说明 + 任务卡打卡）+ REQ-93 源码视图入口 / REQ-90 CQ 引导入口复核 | REQ-104（90/91 P1 尾档） |
| **O13（P2 前段）** | **由知识库构建本体——构建栏第六路径（v0.13，D-O14）**：①KB 选择器（GET /api/kbs/selectable-for-ontology-build，mode 徽标 RAG/GraphRAG + 统计）；②策略 A 入口端点 POST /api/ontologies/build-from-kb（kb_id + strategy='chunk-llm' + cq_mode，复用 /api/ontology-llm/generate + CQ 引导 REQ-90）；③策略 B/C 端点 POST /api/ontologies/kg-to-spec-json（<300 行薄映射层，entity→Concept / relation→Relation / claim→Attribute）；④前端独立流程页面（KB 选→策略选→CQ 引导→生成→校验→预览→入库）；⑤学习中心"构建方式对照"卡片六路径化；⑥示例本体第 5 个"设备故障知识库→设备故障本体"（seeds/learning/ontologies/）；⑦依赖——M6 KB + M14 KB 双子模块 + REQ-82 已就绪 + v0.13 semantica worker chunk 接受；与 §4.9 semantica 集成互为对偶（KB→本体构建 vs 本体→KG 消费） | REQ-108（策略 A 可即时上线；B/C 随 P2 评估） |

顺序建议：O1→O2→O3 跑通"导入→运行→对话"最小闭环（P1 引擎为 Oxigraph），再 O4/O5 并行；O4 后插入 O10 前段（工作台/源码视图/翻译透视，均为前端+薄层、成本低收益高）；O6 加推理对照；O7~O9 按学习节奏选做；O10 后段（diff/灌装）与 O9 并行推进；O11 与 O10 前段并行（独立栏零侵入两平面，互不阻塞），消费链路依赖 O1 的 TTL 导出器。**P1 收尾推进序（v0.12 建议口径）：O11③ semantica stdio→HTTP 桥接（打通对话挂载闭环，解锁 REQ-103 模式 B 依赖）→ O6 Fuseki（激活"运行方式对照"）→ O12 学习中心 v1 → 进 P2（REQ-95 → 96 → 103 模式 A → 90/91 补齐 → 75/76）**。

---

## 8. 风险与对策（v0.2 增补）

| 风险 | 对策 |
| --- | --- |
| 多进程编排复杂度（端口冲突/僵尸进程/重复启动） | Manager 单点编排 + pid 记录 + 启动幂等 + 健康检查自动标记 error；本地学习仅回环监听 |
| RDF 型方案的本体更新不同步（编辑后方案里还是旧数据） | memory_graph 版本校验自动生效；RDF 型明确"需重载"提示（不静默）；方案页显示各本体已加载 version |
| SPARQL 工具语义翻译失真（onto_* 语义 ≠ SPARQL 自然映射） | facade 层单测覆盖 4 工具 × 3 引擎的一致性用例；翻译失败返回明确错误 |
| WebVOWL 需 Java 预转换 | 构建平面按 version 缓存 OWL2VOWL JSON；预转换失败降级为"结构浏览暂不可用"，不影响其他功能 |
| LLM 生成质量差/格式漂移 | 结构化输出 + schema 校验 + 最多 3 轮修正循环；始终人工预览确认后入库（不自动保存） |
| 三进程架构学习成本（构建/运行/主平台） | run-dev.sh 一键拉起；接口契约文档化（本文 §5）；里程碑顺序保证先两进程再演进三进程 |
| 外部工具版本演进快 | open-ontologies 定位近期大改（变更管理/证书为主打），统一收在 oo-worker 适配层后并 pin 版本 |

---

## 9. 开放问题（v0.2 更新）

| ID | 问题 | 当前建议 |
| --- | --- | --- |
| O-1 | ~~OWL 有损导入优先级~~ | **已落地**：并入 REQ-80 为 P1（§3.2），映射细则待用三个真实本体验证后定稿 |
| O-2 | ~~运行时 Phase B/C 是否纳入~~ | **已被新架构吸收**：多运行方案类型即机制化（O6/O7 按学习节奏加） |
| O-3 | 图形化编辑坐标持久化 | 维持 localStorage 方案（REQ-71 整体排 O8） |
| O-4 | 内置 K8s 示例本体字段定稿 | 从 ontology_data.json 裁剪最小闭环（10~15 概念 + 3~5 关系 + 若干实例） |
| O-5 | CSV/GraphML 真实来源 | 无来源则 graphml 降为可选；csv 保留为 OWL 转换中间格式 |
| O-6 | **对话挂载对象：运行方案 vs 本体？** | 建议**挂运行方案**（conversation 存 profile_id，工具入参仍带 ontology_id）——与"多方案并存"语义一致；若挂本体则需自动路由到包含它的方案，语义更隐晦 |
| O-7 | ABox 浏览在 SPARQL 型方案下用 LodLive 还是 ontoink widget？ | LodLive 交互形态最贴合但活跃度低；建议 O6 时两个都试跑半天再定，memory-graph 型先复用 OntologyExplorer 不阻塞 |
| O-8 | 运行方案编排方式：子进程 vs Docker Compose？ | 本地学习默认**子进程**（零 Docker 依赖，Oxigraph 单二进制即可）；Fuseki 走 Docker 可选项 |
| O-9 | 构建平面是否独立进程？ | v0.2 先随主平台/独立服务演进均可（接口已按 §5 解耦）；O1 实现时若主平台耦合过重再拆 |
| O-10 | 工具链配置的入口整合：配置页独立路由还是并入本体详情页？ | 建议**本体详情页「工具链」标签页**（§4.6）——工具链强绑定本体上下文；若后续出现"跨本体通用工具链"需求再升独立页 |

---

## 10. 参考资料（本体专属）

- 《开源本体可视化工具调研_report.md》《本体运行时与MCP服务开源方案调研_report.md》《开源本体构建与运行工具链调研_report.md》（项目根目录）：§3.5/§4.2/§6 选型依据
- WebVOWL：https://github.com/VisualDataWeb/WebVOWL · Ontology Playground：https://github.com/microsoft/Ontology-Playground · ontoink：https://github.com/ISE-FIZKarlsruhe/ontoink · LodLive：http://lodlive.it
- Oxigraph：https://oxigraph.org/ · Apache Jena Fuseki：https://jena.apache.org/documentation/fuseki2/
- mark3labs/mcp-go：https://github.com/mark3labs/mcp-go · eino-ext MCP：https://github.com/cloudwego/eino-ext/tree/main/components/tool/mcp
- React Flow：https://reactflow.dev/ · RJSF：https://github.com/rjsf-team/react-jsonschema-form · dagre：https://github.com/dagrejs/dagre
- Open Ontologies（契约参考）：https://github.com/fabio-rovai/open-ontologies
- OWLAPY（LLM 辅助构建模式参考）：https://github.com/AlessandroLorenzi/owlapy
- 《Kubernetes 云平台智能运维本体构建实现方案》+ ontologies/ 资产：示例本体与导入验收集

---

## 11. 迭代记录

| 版本 | 日期 | 变更 | 负责人 |
| --- | --- | --- | --- |
| v0.1 | 2026-09-20 | 自主方案 v0.5 抽出独立维护：承接 REQ-60~73、Q-13~16、技术方案 §5.2/§6.10~6.11/§8.2~8.3/§10/M9；新增外部资产盘点、运行时演进路线、里程碑 O1~O8、开放问题 O-1~O-5 | 董奎 |
| v0.2 | 2026-09-20 | **架构重设计：构建/运行解耦 + 多运行方案**。按主人五项操作方式需求新增 REQ-80~88：导入即运行（REQ-80）、可视化浏览（REQ-81，含 WebVOWL/LodLive/OntologyExplorer 分层选型）、LLM 辅助创建（REQ-82，生成-校验循环）、fork 扩展（REQ-83，P2）、运行方案管理界面与多方案并存（REQ-84~86）、解耦与开源优先原则（REQ-87/88）。架构改为构建平面（本体仓库多形态资产）+ 运行平面（Runtime Manager 编排 memory_graph/oxigraph/fuseki 三类运行时 + 统一 MCP facade）；里程碑重排 O1~O8；开放问题 O-1/O-2 落地，新增 O-6~O-9；Q-13"单一本体服务"语义演进为"多运行方案"（决策方向不变：REST+MCP 双协议、独立演化） | 董奎 |
| v0.3 | 2026-09-20 | **文档重组：需求与方案分离**。本文档由 docs/ontology/00 上提至 docs/ 并更名《04_本体_方案设计.md》（与智能体方案同级）；需求独立为《03_本体_需求文档.md》（REQ-60~73 承接 + REQ-80~88 + NFR-O-1~5 + 决策 D-O1~D-O6 + 验收要点 9 条），本文档自此只维护设计：§0 五项落位表与 §1 需求基线改为指向需求文档，头部文档引用同步智能体文档改名（01_智能体_需求文档_PRD / 02_智能体_技术方案设计） | 董奎 |
| v0.4 | 2026-09-20 | **新增工具链配置层设计（REQ-74~77，D-O7）**：§0 增七阶段工具链视角（S1 来源→S2 编辑→S3 校验→S4 可视化→S5 运行→S6 服务化→S7 对接 Agent；builtin/guided/managed 三态执行）；新增 §4.6 pipeline_profile 模型 + StageTool 数据驱动接口（tools.json）+ REST + 前端步骤条草案——S5 段复用 runtime_profile 不建第二套引擎模型；里程碑加 O9（P2）；开放问题加 O-10（入口整合）；候选清单明细独立维护于《07_本体_工程七阶段与开源工具链清单.md》 | 董奎 |
| v0.5 | 2026-09-20 | **D-O5 变更：memory_graph 延后为 P2 可选学习项（O7），P1 唯一引擎 = Oxigraph**（主人质疑自研必要性，分析确认后拍板）。§0 总原则更新；§2.2 代码结构 engines 调整；§4.2 引擎表加"交付"列（oxigraph P1/fuseki O6/memory_graph P2）+ D-O5 要点（SPARQL 翻译层提前至 O2、自建本体经 TTL 导出器装载）；§6 自研边界表更新（内存图降级 + 新增 spec_json→TTL 导出器薄层）；§7 里程碑重排（O1 加导出器、O2 换 Oxigraph+翻译层、O6=Fuseki、O7=内存图可选）；需求侧变更见《03_本体_需求文档.md》v0.3 | 董奎 |
| v0.6 | 2026-09-20 | **两处"避免自研"定案（主人拍板）**：①OWL/RDF 解析不自研——新增 Python rdflib sidecar 子项目（tools/rdf-sidecar，O1 引入：解析 + TTL 导出 + 可选 pySHACL 三职责一体）；②memory_graph 不自研——P2 直接集成开源 Cayley fork（NewMemoryGraph 嵌入）+ 薄语义适配层，LG-12 取消、对照实验关闭。§0 总原则/§2.2 代码结构/§4.2 引擎表+D-O5 v0.4 要点/§5 即时生效对齐 REQ-87/§6 自研边界表（内存图与解析改"不自研"、rdflib 入清单）/§7 O1+O7 更新；需求侧见《03_本体_需求文档.md》v0.4；配套分析见《08_本体_开源复用与自研边界分析.md》v0.2 | 董奎 |
| v0.7 | 2026-09-20 | **§4.7 外部独立集成 OpenOntologies 路径落档**（承接《09_本体_open-ontologies集成修订建议.md》，该文档已并入本文档/03 后删除）：oo-worker :8092 托管（Oxigraph 后端）与两平面双轨并行、不进 facade/翻译层，RDFS/OWL-RL 推理、SHACL、变更影响分析、CSV/XLSX 灌装、39 工具 MCP | 董奎 |
| v0.8 | 2026-09-21 | **学习增强设计落地（D-O8：主人拍板"能做多详细就多详细，分阶段施行"，采纳《13_本体_学习视角缺口分析.md》G1~G9）**：新增 §4.8——4.8.1 Yasgui SPARQL 工作台 + CodeMirror 源码视图（REQ-92/93，前端集成零后端）；4.8.2 翻译透视（facade 调用日志 + trace 开关/调试面板）与推理对照（跨方案 diff → 单方案开关，含学习文案模板）（REQ-94）；4.8.3 学习内容包 `seeds/learning/`（方法论卡片五模块/任务卡 JSON 结构/示例本体扩至 4 个覆盖四条构建路径/CQ 引导）（REQ-90/91）；4.8.4 版本 diff（spec_json 三集合结构化对比）（REQ-95）；4.8.5 主线数据灌装（P2a 同名映射 → P2b 映射向导）（REQ-96）。§6 清单加 Yasgui/CodeMirror；§7 里程碑加 O10（P1 尾~P2）与顺序建议更新；需求侧见《03_本体_需求文档.md》v0.5（REQ-90~96 + REQ-78/79 登记 + D-O8） | 董奎 |
| v0.9 | 2026-09-21 | **前端改造方案融合（D-O9，主人拍板）**：§3.5 可视化口径更新——ReactFlow 重构版（@xyflow/react v12，随仓库 862df01 入库）为 S4 内置渲染，与 WebVOWL 嵌入按效果对比择优、难以客观对比则双轨并存（ReactFlow 内置默认 / WebVOWL 嵌入入口），ABox 关系视图路由不变；§4.5 `/healthz` 登记引擎预检 P3 待办（engines 段：二进制/容器就绪、版本、端口占用），本迭代不开发；前端 IA 三 Tab 改造（构建/运行正交）实施细节见《14_本体_前端改造方案.md》；需求侧见《03_本体_需求文档.md》v0.6（REQ-81 调整 + D-O9） | 董奎 |
| v0.10 | 2026-09-21 | **Semantica 独立集成设计落地（D-O10，主人拍板）**：新增 §4.9——4.9.1 semantica-worker 部署（tools/semantica-worker，venv+pip，:8093，锁 v0.6.8+）；4.9.2 导航栏独立路由（对标 §4.7）+ 首页三段式学习引导（功能/原理/使用说明，内容资产 seeds/learning/semantica/）+ 对话配置 MCP 目标可选挂载；4.9.3 消费链路（TTL→ingest→KG，ingest 薄脚本，与 facade 精确查询形成"精确 vs 语义"教学对照）；4.9.4 审计视图（record_decision + PROV-O 链，P2，复用 Explorer UI）；4.9.5 零侵入确认表。§6 清单加 Semantica 行；§7 里程碑加 O11（P1）与顺序建议更新；本体叙事扩展为"构建→运行→消费→审计"四环节闭环；需求侧见《03_本体_需求文档.md》v0.7（REQ-99~101 + D-O10） | 董奎 |
| v0.11 | 2026-09-21 | **§4.9.2 重写：左侧边栏功能全覆盖（REQ-102，主人第二次拍板）**——对照 semantica README 完整能力面做覆盖度分析（原三页签仅覆盖约 1/3），布局升级为**左侧边栏六组十六项**（学习引导/图谱×4/推理与决策×4/本体治理×2/数据与检索×2/设置×3）；复用策略明确：Explorer 原生 8 工作区反代复用 0 自研 + REST 薄页（<300 行）；使用说明五步演练扩为六步；首页三段式与零侵入定位不变；需求侧见 03 v0.8（REQ-102），前端回写见 02 v0.18 | 董奎 |
| v0.12 | 2026-09-22 | **IA 与学习中心设计落档 + 三项收口（D-O11/D-O12，主人拍板）**：新增 §4.10——侧边栏四栏 IA（学习中心默认页/构建五路径分类含自定义构建承载现有页 S1~S4/资产统一/运行按引擎分组）、学习中心 v1 设计（七阶段路径主视图/构建方式对照/运行方式对照/打卡与内容策略）、路径分层实现原则（完整交互/交互骨架+排期态/引导卡，不做空壳）；§4.6 重定位注记（路径级工具配置、打卡归学习中心）；§3.5 D-O9 收口（ReactFlow 内置默认定案，WebVOWL 嵌入降 P3）；§3.3 加 OpenBKN 生成链路备选记录（仅参考不排期，D-O12）；§7 里程碑加 O12、O10 调整（90/91 移入）、P1 收尾推进序建议（O11③ 桥接→O6→O12→P2 序列）；需求侧见 03 v0.9（REQ-103/104 + D-O11/D-O12），前端实施见 14 v0.3 | 董奎 |
| v0.12 | 2026-09-22 | **§4.9 Semantica 接入方案独立成档（设计事实源移交）**：详细设计（现状盘点/自研与开源边界/本地运行痛点分析/支持方式六模式评估/部署矩阵/集成契约稳定面）移交《19_本体_semantica集成方案.md》v0.1（对标 11/12 知识库拆分模式）；本节收编为对接契约 + 指针；演进只改 15 | 董奎 |
| v0.13 | 2026-09-23 | **由知识库构建本体——构建栏第六路径立项（主人拍板，D-O14，方案 A 落档）**：KB 模块 D-KB4 v0.25 拆双子模块就绪后，新增构建栏第六路径；新增 §3.7（三种抽取策略 A chunk→LLM / B KG→直转 / C 混合；入口步骤：KB 选择器→策略选→CQ 引导→生成→校验→预览→入库；新增 4 个 REST 端点）；§4.10 树形增第 6 子项 + 路径分层实现原则 + 学习中心"构建方式对照"卡片六路径化；§7 里程碑增 O13（P2 前段）；§4.9.5 与现有架构关系加 KB→本体对偶；需求侧见 03 v0.10（REQ-108 + D-O14），02 v0.26（M15），12 KB v0.3（§3.5 KB 作为本体构建数据源） | 董奎 |
| v0.14 | 2026-09-23 | **REQ-96 主线数据灌装全量交付（P2a+P2b）**：§4.8.5 P2b 落地——ingestConfig 增 type_rules（int/number/date/bool，非法值降级原字符串 + warning）与 multi_value_sep（关系列多值拆分）；映射配置存 `ingest_mapping_json` artifact（GET/PUT ingest-mapping，不 bump version、不进产物表、不随 fork 复制）；前端资产栏增「CSV 灌装」页签（CsvIngestPane：上传→列绑定→预览→校验入库）；冒烟全通过（类型归一/多值拆分/校验门控/版本递增/映射复用）；实施细节见 14 v0.9 | 智能体开发助理 |
| v0.15 | 2026-09-23 | **P2 序收官（REQ-90/91 补齐 + REQ-75/76 交付）**：§4.6 工具链配置层落地——004_pipeline.sql / tools.json（七阶段 24 工具，REQ-77 数据驱动）/ 默认工具链模板 / guided checklist 生成 / REST 八端点 / 学习中心「工具链配置」Tab；REQ-90/91 全量（方法论深度版 + 4 示例建模说明 + 4 示例本体齐）；**§7 P2 序（95→96→103 模式 A→90/91→75/76）全部完成**；需求侧见 03 v0.12、前端见 14 v0.10 | 智能体开发助理 |
| v0.16 | 2026-09-23 | **学习中心外部资源导航（REQ-109，主人指示立项）**：主人引入 microsoft/Ontology-Playground（微软官方本体学习 Playground，MIT，React19+Cytoscape.js 纯静态站，与本项目定位同构）→ §4.10 增「学习中心 Tab 结构」注记：第 5 个 Tab「外部资源」——**Vite `?raw` 构建期内联**导入 `seeds/learning/external-resources.md`（单源零拷贝零后端改动，vite.config `server.fs.allow` 放行仓库根 + 新增 src/vite-env.d.ts）+ LearnPage XMarkdown `openLinksInNewTab` 渲染；md 面向学习者、全局台账以 15 登记簿为准；收录四节（在线平台首条 Ontology Playground / 复用词表 / 官方规范 / 典型开源项目）；需求侧见 03 v0.13 §2.11，登记簿见 15 v2.3 | 董奎 |
| v0.17 | 2026-09-23 | **§3.7 由知识库构建本体交付（D-O14/REQ-108，02 v0.28/M15，c56ebd4）**：§3.7 节头补「已交付」注记——策略 A chunk→LLM（chat.GenerateStructured + CQ auto/custom/skip + 修复轮 adopt-if-fewer-errors）/ B KG→薄映射（entity→Concept/IS_A→parents/HAS→「具有」，rounds=0）/ C 混合；KB 选择器 KG 统计从 worker `/graphrag/kg` 按库回读（kb_kg.json 权威，doc.graphrag 非落库不可作源——实现期修正）；worker semantica 库缺失时 ingest 降级不 500；示例 5 = device_fault.kb.md 语料 + README（与 failure.json 对照）；冒烟含 worker 重启回读、三策略、错误路径、入库闭环、headless UI 断言；需求侧 03 v0.14 §2.10 已交付注记 | 董奎 |
| v0.18 | 2026-09-23 | **D-O15 去-semantica 化立项（反转 D-O10，REQ-110，P2）**：主人反馈 worker 可用性差 → §4.9 加反转注记（本节转存档）：消费/审计改自研轻量——TTL 即 KG（Oxigraph）/ LLM 抽 KG（REQ-98 代理，lightweight 方法位）/ KG 增强检索（向量+SQLite 一跳，主人定「前期够教学即可后期扩展」）/ 审计 PROV-O P2 / ReactFlow / MCP 契约保留删预设；§4.10 加 IA 预告：SemanticaPage 改造复用挪本体侧边栏第五栏「消费与审计」；需求侧 03 v0.15 §2.12，集成方案档 15 v0.3 转存档，登记簿 15 v2.4，12 v0.6；实现移交智能体开发助理 | 董奎 |
| v0.19 | 2026-09-23 | **D-O15/REQ-110 实现交付（commit 7d45dc0，03 v0.16）**：§4.9 存档注记后加实现交付对照（worker 抽取/查询/PROV-O/Explorer 四能力 → 自研映射 + 新 API 面 + 退役路由清单）；§4.10 五栏预告落为交付注记（AuditPage 四 Tab 细节；修复 v0.18 重复段落）；配套 02 v0.31、12 v0.7、14 v0.15；验收 22 达成 | 董奎 |
| v0.20 | 2026-09-23 | **REQ-71 图形化编辑器 v1 交付（03 v0.17）**：D-O12 段落加 v0.20 延伸注记——ReactFlow 栈只读→编辑，资产详情「图形编辑」Tab（概念增删/属性面板/连线建关系或继承/删除与级联），spec 草稿为事实源、画布派生，坐标不持久化（O-3 口径），保存复用 PUT spec 校验门控零后端改动；同轮 03 v0.17 完成 D-O15 文档清扫（D-O10 反转标注/诉求 #8/验收 16~18/REQ-102 存档） | 董奎 |
| v0.21 | 2026-09-23 | **REQ-71 v1.5 实例入画布交付（03 v0.19）**：D-O12 段落加 v0.21 延伸注记——节点扩概念/实例双类、派生边四类（关系/继承/归属/实例关系）、「添加实例」弹窗（概念+属性 JSON 校验）、实例属性面板、「隐藏实例」开关，连线三分支拒绝混连；事实源与 PUT spec 保存链路不变。冒烟排障沉淀：CDP 窗口遮挡 → 页面不产渲染帧 → ResizeObserver 不交付 → ReactFlow 节点无 measured 内联 hidden、边不渲染——headless=new 产帧即恢复，属环境伪缺陷非应用 bug；后续 UI 冒烟统一用 headless=new | 董奎 |
| v0.22 | 2026-09-25 | **REQ-146/147 设计落点（随 824b063 交付，03 v0.26 需求侧）**：①引擎自检——engine 包新增 EngineStatus 结构与 StatusProbe 可选接口（适配器实现 Probe()，未实现按未注册呈现）+ FindExecutable 共享助手（含分隔符候选 Stat+执行位、裸名走 PATH）；oxigraph 候选序=显式 OXIGRAPH_BIN→PATH(oxigraph_server/oxigraph)→data/bin→tools/bin，与 run-dev.sh 搜索语义一致；②一键安装——Manager.StartInstall 异步任务（installMu 防并发）+ downloadTo（temp+rename 原子、minBytes 防错误页、非 windows chmod 0755），资产映射 ReleaseAsset(goos/goarch) pin v0.5.11；oxigraph.Start 改 resolveBinary 动态解析——安装落 data/bin 后 Start 即命中，无需重启 runtimed；fuseki 一键安装不做（JDK+分发包解压，诚实边界）；③方案停止态编辑——后端既有 PUT（running 先停语义）不改动，前端补编辑入口（stopped/created/error），端口回传保留、config 不传由 orDefault 保留，引擎/端口只读 | 董奎 × 自动化执行者 |
| v0.23 | 2026-09-26 | **REQ-151 sparql_query 设计落点（§4.4 契约 4→5，03 v0.33 需求侧）**：facade 包新增 sparql.go——①词法级只读校验 vetReadonlySelect：扫描器跳过字符串字面量（含 '''/""" 长串与转义）/IRI `<…>`/注释后产出裸词 token，PREFIX/BASE 任意次序前导声明后首关键词须 SELECT；UPDATE/INSERT/DELETE/LOAD/CLEAR/CREATE/DROP/MOVE/COPY/ADD 十变更关键字全禁（字面量/IRI 内同形词不误伤，`?delete` 变量与自定义前缀名误伤为已知取舍=受控查询面教学点）；②limit 默认 50/上限 200（clampLimit），toTable 表格投影列名字母序稳定、缺键补空、total+truncated 诚实标注；③per-call 10s 超时（context 传播，低于 HTTP client 15s）；④翻译透视走既有 exec（trace 落库 REQ-94 不变）；⑤tools/call 校验顺序=参数→白名单→路由（被拒查询不触达引擎）；⑥guide 注入文案同步（ontology-service）；前端 facade 工具表 + 「共 5 个」。测试：纯函数 3 组 28 例 + httptest 桩 SPARQL 集成测试；真机全链（REQ-146 安装 oxigraph → running 方案 → MCP tools/list 5 工具/变更查询全拒/聚合开放问题） | 董奎 × 自动化执行者 |
| v0.24 | 2026-09-26 | **REQ-153 大型种子内容包设计口径（03 v0.34 需求侧）**：seed embed 双百级本体（med_common 102c/170i 禁忌关系网、gene_core 104c/94i 中心法则链）——建模取舍：①具体病种/症状/药物/检查挂实例层（实例关系断言只能连实例，知识对才可被 SPARQL 命中），概念层纯分类；②实例类型挂最近分类（无推理时 rdf:type 直查，上层走 rdf:type/rdfs:subClassOf* 属性路径——sparql_query 教学点）；③GO 三树留概念层，注释以属性承载（类目/个体分离），参与过程/行使功能/定位于组分仅 schema 无断言（有意且已文档化）；seed 单测 3 例含百级规模断言 | 董奎 × 自动化执行者 |
| v0.26 | 2026-09-26 | **REQ-92 工作台 v2 设计落点（03 v0.41 需求侧）**：§4.8.1 执行面不变（反代端点/POST 契约/localStorage 持久化），呈现层 Yasgui→CM6+AntD 自组装；SELECT 表格化/ASK 真值/CONSTRUCT Turtle 三形态 + 变更查询前端拦截（诚实指向 facade 受控白名单）；查询模板含 REQ-153 种子教学查询；依赖变更登记 15 号 v2.12 | 董奎 × 自动化执行者 |
| v0.25 | 2026-09-26 | **REQ-171 立项指针（03 v0.39）**：头部加「多途径构建收敛视图」指针——REQ-171 可落地方案入 platform-knowledge/本体/26（业界实践+理论论文线整合；六路径成熟度分级兑现+两公共底座 qualitygate/导入审查+生成必过门禁铁律；新建仅 1 包级模块+1 importer 增强、零新表；P1 API 先行无前端依赖）；本文档继续作为全景架构事实源；18 v1.34 | 董奎 × 文档会话 |
| v0.27 | 2026-09-26 | **文档滞后集中清账（无新开发，03 v0.42 同轮）**：①§3.5 D-O12 段补 v0.27 注——WebVOWL 嵌入 P3 项已随 M21 VIZ-3 激活交付（8d25e17：WebVowlView 对照视图 + owl2vowl 预转换 + tools/fetch-webvowl.sh；诚实降级口径 03 v0.32/14 v0.18）；②§7 O6 行状态注——Fuseki 适配器与推理开关透传已实现，「同查询并行 diff」对照界面与 Fuseki 真机验证未落地（REQ-94② 剩余口径见 03 v0.42） | 董奎 × 协作 Agent |
