# eino-multiagent-lab · 本体模块需求文档

> 状态：Draft v0.42
> 日期：2026-09-26
> 负责人：董奎
> 文档性质：**活文档**，本体模块的**唯一需求事实源**。需求变更在文末「迭代记录」留痕；已确认需求不删除，标注 `（已变更/已废弃，见 vX.X）`。
> 文档关系：设计方案见《04_本体_方案设计.md》（需求与方案分离：本文档回答"做什么/做到什么程度"，方案回答"怎么做"）；智能体侧需求见《01_智能体_需求文档_PRD.md》——对话对接需求（启用开关、挂载运行方案、对接契约）横跨两模块，边界见 §2.5。
> 编号体系：延续 REQ-60~73（承接自智能体 PRD，冻结快照）+ REQ-74~77（工具链配置）+ REQ-78~79（外部集成：双轨互通/本体对齐）+ REQ-80~88（本模块需求）+ REQ-90~96（学习增强，2026-09-21 缺口分析采纳）+ REQ-99~102（semantica 独立集成与消费/审计闭环，2026-09-21 拍板）+ REQ-103~104（对话伴生本体生长 + 界面信息架构与学习中心，2026-09-22 拍板）+ ~~REQ-105~106（预留占位）~~（**v0.20 撤销**——REQ-105/106 已归智能体 PRD 模型管理预设实占；全局编号以《18_REQ编号注册表》为唯一权威，本体侧新需求自 REQ-113 起避开 01 横切段 111~119，经注册表分配）+ REQ-107（知识库模块 IA 拆分 RAG/GraphRAG 双子模块，D-KB4，2026-09-23）+ REQ-108（由知识库构建本体——构建栏第六路径，D-O14，2026-09-23 主人拍板）+ REQ-109（学习中心外部资源导航，2026-09-23 主人引入 Ontology-Playground 时指示立项）+ REQ-110（去-semantica 化与消费/审计轻量自研，D-O15，2026-09-23 主人拍板反转 D-O10）**。决策编号 Q-13~16 承接智能体 PRD，新决策 D-O1 起。

---

## 0. 背景与目标

eino-multiagent-lab 是学习型 Web 平台，功能分**智能体**与**本体**两类。本体模块目标：支持**导入、创建、编辑、浏览**多个本体，并以后台**多套运行方案**的形式运行本体，供智能体对话结构化查询。

两条架构级原则（主人 2026-09-20 拍板）：

1. **构建与运行解耦**：本体仓库是唯一事实源；运行平面支持同时启动多套本体运行方案，方案停止/删除不影响仓库资产。
2. **开源优先**：各子系统优先使用现成开源方案，避免重复造轮子；自研边界由方案文档 §6 明确。

---

## 1. 用户操作方式总览（六项诉求 → 需求映射）

主人原始诉求与需求编号对照：

| # | 操作方式 | 需求编号 | 优先级 |
| --- | --- | --- | --- |
| 1 | 支持导入外部本体，支持直接加载运行从外部导入的本体 | REQ-80（+REQ-72 体验、REQ-69 OWL） | P1 |
| 2 | 本体可视化浏览查看：交互友好地查询本体对象之间关系（直接评估开源方案） | REQ-81 | P1 |
| 3 | 从零开始由大模型辅助创建本体 | REQ-82 | P1 |
| 4 | 基于已有本体编辑扩展为新的本体 | REQ-83 | **P2** |
| 5 | 本体运行方式管理界面：查看所有后台运行方案状态、配置方案加载哪些本体、配置运行细节 | REQ-84 / REQ-85 / REQ-86 | P1 |
| 6 | 本体工程全流程拆为七阶段，每阶段可选主流开源工具；平台提供可配置页面指定从 0 到对接 agent 各阶段的工具（2026-09-20 新增） | REQ-74~77 | REQ-74 P1，REQ-75/76 P2，REQ-77 P3 |
| 7 | 学习视角缺口补齐（2026-09-21 拍板"能做多详细就多详细，分阶段施行"，采纳《13_本体_学习视角缺口分析.md》G1~G9）：建模方法论引导、学习任务卡、SPARQL 工作台、源码视图、翻译透视与推理对照、版本 diff、主线数据灌装 | REQ-90~96（另 REQ-78/79 正式登记、OOPS! 以 tools.json 数据条目登记） | 分三阶段，见 §4 |
| 8 | semantica 作为参考借鉴——**（已变更，D-O15 反转 D-O10）**：不接入运行时（独立栏与 worker 退役），消费/审计保留改**轻量自研**（第五栏「消费与审计」） | REQ-99③（MCP 契约）、REQ-110 | REQ-110 P2 已交付 |
| 9 | 界面信息架构与学习中心（D-O11）：本体模块**左侧边栏四栏→五栏**（学习中心默认页/构建/资产/运行/消费与审计）；构建路径分类见 17 §2.2 | REQ-104（+D-O15 第五栏） | REQ-104 P1 尾 |
| 10 | **由知识库构建本体**（D-O14 方案 A）：构建栏第六路径，三种抽取策略（chunk→LLM 抽 / KG 直转 / 混合）；KG 源已切自研 internal/kg（D-O15 注） | REQ-108（+REQ-107 前置） | REQ-108 P2 前段 |

---

## 2. 功能需求

### 2.1 本体资产管理（构建平面 · 仓库）

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| REQ-60 | **本体列表页（类知识库形态）**：列出全部本体（名称、简介、概念/关系/实例统计、版本、更新时间）；支持查询搜索、新建（名称+简介）、重命名、删除（二次确认）；本体为全局资源，可被任意对话的运行方案加载 | P1 |
| REQ-80 | **导入外部本体并直接加载运行**：①支持上传/粘贴外部文件，格式分层：`spec`（自家格式，无损）/ `csv`（nodes+edges 两表）/ `graphml`（通用图交换）/ **OWL-RDF/Turtle（有损导入，P1）**；②导入产生**多形态资产**——原始文件原样存档 + 归一化 spec_json + 导入报告（统计/warnings/有损映射说明）；③导入成功的本体立即可被任意运行方案加载运行 | P1 |
| REQ-69 | **OWL/RDF 有损导入映射**（REQ-80 的 OWL 分支细则）：`rdfs:label`→名称、`rdfs:subClassOf`→继承关系、`owl:NamedIndividual`+`rdf:type`→实例、对象属性断言→实例关系；公理/推理语义丢弃并计入导入报告；映射细则用 ontologies/ 三个真实本体（K8s/FLUIDOS/HOCC）验证后定稿 | P1（v0.5 时为 P2，随 REQ-80 提级） |
| REQ-72 | **导入体验**：导入前预览解析结果（概念/关系/实例数量统计、映射规则、无法识别条目的 warnings）；导入失败给出可读错误；导入成功的新本体 version=1，立即可在列表/运行方案配置中使用 | P1 |
| REQ-62 | **内置示例本体**：一个最小 K8s 运维本体（Pod/Node/Service 概念、belongsTo/exposes 关系、若干实例），seed 方式提供，便于上手 | P1 |
| REQ-83 | **基于已有本体编辑扩展为新本体（fork）**：本体列表页「复制为新本体」→ 复制全部形态资产（记录 fork 来源）→ 编辑扩展后作为独立新本体保存｜✅ 已实现（AssetsPage「复制为新本体」：全形态资产复制 + forked_from 溯源、版本重置 v1，见 v0.22 清账） | **P2** |
| — | **导出**：本体可导出为 spec_json（备份/分享）；RDF 型本体可导出原始形态；**从零创建的本体（仅有 spec_json）可经 spec_json→Turtle 转换导出 RDF 形态**，使其可在 SPARQL 型运行方案中加载运行 | P1（转换器随 O1 交付，由 Python rdflib sidecar 实现——v0.4） |

### 2.2 本体创建与编辑

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| REQ-61 | **JSON 编辑器**：本体内容用约定 JSON 表达三类要素 `concepts`/`relations`/`instances`；页面提供结构化 JSON 编辑器（带格式校验） | P1 |
| REQ-63 | **保存校验**：JSON Schema 结构校验 + 引用完整性（引用了未定义概念/关系时给出错误），不允许保存坏本体；校验在构建平面服务侧完成 | P1 |
| REQ-64 | **只读预览**：编辑页支持概念/关系/实例分栏只读预览，不只是原始 JSON | P1 |
| REQ-71 | **图形化编辑器（P1 增强）**：React Flow 画布（节点=概念/实例、连线=关系）+ RJSF 属性表单，与 JSON 编辑器双形态并存；保存经适配器转回 spec_json，校验不变｜✅ 已交付：v1（图形编辑 Tab）+ v1.5 实例入画布（概念/实例双类节点、归属边、实例面板）——细节见 04 §3.4 / 14 | **P1** |
| REQ-82 | **LLM 辅助从零创建**：①入口「AI 创建」：自然语言描述领域（可附参考示例/规模要求）→ 生成本体草稿；②生成走**结构化输出 + 服务端校验循环**（校验失败错误回喂模型修正，最多 3 轮），**草稿必须经用户预览确认后才入库**，不自动保存；③模型能力经主平台代理提供（模型能力归主平台，校验归构建平面） | P1 |

### 2.3 本体可视化浏览

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| REQ-81 | **交互式可视化浏览**（v0.6 调整口径）：①本体详情页提供浏览入口，支持查看**类层次与对象属性结构**（TBox）和**实例间关系**（ABox）两类视图；②结构视图实现**对比择优（D-O9）**——ReactFlow 重构版（@xyflow/react v12，spec_json 直渲交互式图谱，主人本地重构已随仓库入库）与 WebVOWL 嵌入按实际效果对比，采用效果更优者；**难以客观对比则双轨并存**（ReactFlow 作内置默认，WebVOWL 作嵌入视图入口）；关系视图仍按运行方案类型路由 LodLive/OntologyExplorer 资产，集成方式见方案文档 §3.5；③浏览对象是"运行中的本体"，与运行方案联动 | P1 |

### 2.4 本体运行方案（运行平面）

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| REQ-84 | **运行方案管理界面（列表）**：列出所有后台运行方案——名称、运行时类型、已加载本体、状态（running/stopped/error 等）、端口/端点、健康信息；支持启动/停止/删除（二次确认）；状态自动刷新；可查看方案最近日志 | P1 |
| REQ-85 | **运行方案配置**：新建/编辑运行方案——①选择**运行时类型**（P1 支持 SPARQL 型 Oxigraph，Fuseki 随后交付；轻量内存图为 P2 可选，**直接集成开源引擎、不自研**（D-O5 v0.4）——引擎清单见方案 §4.2）；②勾选加载的**本体集合**（来自构建平面仓库）；③配置**运行细节**（监听端口、推理开关、SPARQL 端点可写性、资源参数等）；配置变更后需重启方案生效，页面明确提示 | P1 |
| REQ-86 | **多套运行方案并存**：后台可同时运行 ≥2 套方案（不同类型/不同本体集合/不同端口），互不干扰；不同对话可分别挂载不同方案 | P1 |
| REQ-87 | **构建/运行解耦原则**（v0.3 调整）：本体仓库为唯一事实源；运行方案是仓库内容的"部署视图"——同一本体可被多套方案加载；方案停止/删除不影响仓库资产；本体在仓库更新版本后需在方案页**显式重载生效**（页面提示，不静默）；P2 可选引擎引入后可再评估"按版本自动生效"对照语义 | P1（架构原则） |
| REQ-88 | **开源优先原则**（v0.4 调整）：每个子系统先评估开源方案再决定自研；自研仅限业务编排与路由薄层（运行方案编排层、MCP facade 路由）；**OWL/RDF 解析不自研（Python rdflib sidecar）、内存图引擎不自研（P2 直接集成开源）**——清单由方案文档 §6 维护 | P1（原则） |

### 2.5 对话集成（与智能体模块的边界）

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| REQ-65 | **对话启用本体**：对话配置选择某个**运行方案**（而非直接选本体）；启用后该方案的 MCP 查询工具挂给 Agent，并注入本体使用指引（含 `ontology_id` 上下文） | P1 |
| REQ-66 | **本体查询能力**（经统一 MCP 契约提供，精确匹配优先、不做逻辑推理）：按名称/标识查概念或实例、查某实例的关系（邻居）、列出某概念下所有实例；**该契约对所有运行时类型一致**（SPARQL 型经 facade 翻译，非 SPARQL 型直查，差异由 facade 屏蔽） | P1 |
| REQ-67 | **查询过程可见**：工具调用在对话时间线可见，便于理解 Agent 如何利用结构化知识 | P1 |
| REQ-70 | **连通与生效**：①运行方案管理器可探测各方案健康状态，方案不可达/停止时有明确降级提示、不影响其他方案与普通对话；②本体管理页保存新版本后，按 REQ-87 语义分类型生效；③MCP 连接具备初始化与断线重连 | P1 |
| REQ-73 | **打开即用一致性**：编辑页保存或导入产生的新版本，在运行方案与对话侧按 REQ-70/REQ-87 语义即时可见或明确提示重载，无需重启平台 | P1 |
| — | **边界说明**：对话侧 UI（启用开关、下拉、状态提示 REQ-19b/c/d）与主平台对接实现（MCP Client 接入、双反向代理、模型代理端点——智能体 PRD v0.6 的 REQ-97/98）由**智能体侧文档维护**；本模块负责保证契约能力：统一 MCP facade、按方案 guide 端点、降级事件语义、REST API 可反代 | — |

### 2.6 工具链配置（七阶段流水线，2026-09-20 新增）

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| REQ-74 | **七阶段工具链模型**：将本体工程定义为七个阶段——S1 本体来源 → S2 编辑管理 → S3 校验 → S4 可视化 → S5 运行时引擎 → S6 服务化接口 → S7 对接 Agent；每阶段定义输入/输出**交接形态**（spec_json / original / endpoint 三形态契约）；平台现有功能（导入、编辑、校验、Explorer、运行方案、facade）映射为各阶段的**内置默认工具**，功能不变、组织升级。阶段划分与候选清单详见《07_本体_工程七阶段与开源工具链清单.md》 | P1（叙事与映射；v0.9 起**保留为学习中心理论骨架**，D-O11） |
| REQ-75 | **工具链配置页**（v0.9 重定位，D-O11）：学习者为本体创建工具链配置（pipeline_profile）——**按构建路径**（REQ-104）配置各阶段工具与参数，路径级而非仅按阶段孤立选择；支持复制为新工具链、重置为默认（默认工具链每阶段预置 builtin 项，打开即可用）；配置可命名管理。学习中心 v1 先跑固定默认工具链，本页随 P2 提供配置化能力 | ✅ 已交付（2026-09-23，c55730b：/api/pipelines 八端点 + 学习中心「工具链配置」Tab；tools.json 七阶段 24 工具数据驱动） |
| REQ-76 | **引导执行模式**（v0.9 重定位，D-O11）：非自动编排阶段的工具（guided 模式），平台生成执行指引（安装/启动命令、数据交接点、入口链接），学习者逐项打卡完成并记录状态；**打卡机制归学习中心**（checklist 双 key 空间：`task:` 任务打卡 P1 尾随学习中心先行、`tool:` 工具打卡随本条 P2）；运行段（S5）沿用 Runtime Manager 自动编排（managed 模式） | ✅ 已交付（2026-09-23，c55730b：guided 打卡清单 tool:<stage>:<tool_id> key 空间，与 task: 分列） |
| REQ-77 | **候选清单开放性**：各阶段候选工具清单数据驱动维护（增删工具不改核心代码）；新增工具需声明许可、执行模式（builtin/guided/managed）、输入输出形态与学习要点 | P3 |

> **工具链候选登记（v0.5，随 open-ontologies 独立集成引入）**：S1 段增候选「实例数据快速灌装」（open-ontologies `load`：CSV/XLSX/JSON→RDF，guided）；S3 段增候选「语义校验/推理」（open-ontologies `validate`/`reason`，guided）。两条均为 `tools.json` 数据条目（REQ-77 口径），**不新增执行模式**；设计登记见《04_本体_方案设计.md》§4.6/§4.7。

### 2.7 学习增强（v0.5 新增：2026-09-21 缺口分析 G1~G6 采纳，D-O8）

主人拍板"能做多详细就多详细，分阶段施行"——教学层走需求化路线，工程层与内容体系按下列规格落地：

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| REQ-90 | **建模方法论引导（学习内容包·方法论侧）**：①新增**学习内容包**内容资产（`seeds/learning/`，随版本维护、不进代码）——建模方法论卡片五模块：能力问题法（competency questions，先列"本体要回答什么问题"再动手）、领域分析与概念抽取、复用既有词表与顶层本体（FOAF/SKOS/schema.org 及 BFO 类顶层本体的定位）、命名与 URI 规范、常见 OWL 建模模式；②**每个内置示例本体附建模说明**：背景与目标 CQ 列表、建模决策记录（"为什么这样分层/为什么用这个属性"）、局限与反例；③**AI 创建 CQ 引导**（REQ-82 入口扩展）：生成第一步提供可选"先列 3~5 个能力问题"输入，内容并入生成上下文，让学习者体会"问题驱动建模" | **P1 尾**（方法论五模块**精简版** + 2 个示例建模说明，随学习中心 v1 交付——v0.9 提前，D-O11；深度版与 4 示例说明仍 P2） | ✅ 全量交付（2026-09-23，b20a070：方法论五卡深度版 + 4 示例建模说明 README 全覆盖） |
| REQ-91 | **学习任务卡与示例体系（学习内容包·任务侧）**：①任务卡数据结构——阶段/标题/目标/前置任务/步骤清单/验收问题/关联工具/难度；②每阶段 ≥1 个动手任务（如 S3"故意制造一次不一致，看推理机报什么"、S7"让 Agent 用 neighbors 回答跨概念问题"），任务打卡与 REQ-76 工具打卡分列（checklist 分 `task:`/`tool:` 两个 key 空间）；③**内置示例本体扩至 4 个**：K8s 迷你运维（已有，手写路径）、软件缺陷管理（AI 生成路径演示）、组织与人员（复用路径演示，对照 FOAF 术语）、设备故障知识（SKOS 分类+数据灌装路径演示）；④学习进度视图按阶段聚合任务完成度（随 REQ-75 配置页交付） | **P1 尾**（任务卡打卡机制随学习中心 v1 先行——v0.9 提前，D-O11；4 示例本体与进度完整视图仍 P2） | ✅ 全量交付（2026-09-23，b20a070：4 示例本体已齐（k8s/defects/orgs/failure）+ 进度聚合随 O12） |
| REQ-92（✅ 已交付；v0.41 实现升级） | **SPARQL 查询工作台**：运行方案详情 Drawer「查询与挂载」页签内嵌工作台——endpoint 指向所选 running 方案 SPARQL 端点（经平台反代）；学习者可直接编写 SELECT/ASK/DESCRIBE/CONSTRUCT。原内嵌 Yasgui（多 tab/历史）→ **v0.40 改开源组件自组装（CM6+AntD）**：Yasgui 自带样式未随包生效致界面杂乱，重构为 @uiw/react-codemirror + codemirror-lang-sparql（语法高亮/关键字补全）+ AntD 工具栏与结果表（查询模板/重置/CSV 导出/⌘Ctrl+Enter 运行/耗时与行数标签/错误告警卡/查询 localStorage 持久化）；执行走既有 api.runSparql（POST application/sparql-query） | P1 |
| REQ-93 | **本体源码只读视图**：本体详情页可查看 original 形态（Turtle/OWL/RDF-XML）与 spec_json 的格式化只读视图（语法高亮）；大文件（>1MB）提示下载；入口与 REQ-92 同页组织 | P1 |
| REQ-94 | **翻译透视与推理对照**：①**翻译透视**——facade 执行 onto_* 时记录"工具调用→翻译后 SPARQL→耗时→结果数"调用日志（开关默认开），运行方案调试面板可查看（P1）；对话时间线工具卡片展开透出（P2，经主平台）；②**推理对照**——同一 SPARQL 对"带推理方案（Fuseki）与不带推理方案（Oxigraph）"并行执行并 diff 结果（新增实例清单高亮），结果页附解释文案模板（"新增实例来自 subClassOf 推断"）；Fuseki 交付后支持单方案推理开关对照 | P1（翻译透视）/随 Fuseki（推理对照——引擎基座已实现，「同查询并行 diff」界面未落地，见 v0.42） |
| REQ-95 | **版本 diff**：本体详情页选择两个版本 → spec_json 结构化对比——概念/关系/实例三集合按 id 对齐的 added/removed/changed（changed 比较名称/描述/属性值）+ 引用影响统计（变更元素被关系/实例引用的计数）；结果双栏高亮渲染，可导出对比报告 | P2 |
| REQ-96 | **主线数据灌装**：①P2a 同名映射模式——上传 CSV（表头=关系/属性名，指定主键列）→ 自动生成实例草稿 → 预览统计与 warnings（复用 REQ-72 报告口径）→ 校验入库为新版本；②P2b 映射向导——列绑定、类型转换（string/int/date）、多值分隔符、跳行规则；与双轨 oo `load` 并存（本条走主线仓库，数据进 spec_json 体系） | ✅ 已交付（P2a+P2b，2026-09-23；资产栏「CSV 灌装」页签 + ingest-mapping 映射配置存取；类型转换扩至 int/number/date/bool） |

> REQ-78/79 正式登记（原"预留"）：**REQ-78 双轨 TTL 互通（P2）**——主线构建平面经 spec_json→TTL 导出器（rdflib sidecar）产出 Turtle，可装载进 open-ontologies 双轨做推理/灌装对照实验（与 §4.7 呼应）；**REQ-79 本体对齐/合并（P2 远期）**——多本体重用与对齐工具（`owl:imports` 管理、等价类映射建议）。二者进需求池排期，不阻塞 P1。

### 2.8 semantica 独立集成与消费/审计闭环（v0.7 新增，D-O10）

主人拍板（2026-09-21）：semantica（github.com/semantica-agi/semantica，MIT，Python，AI Agent 语义层/决策智能层）作为参考借鉴，**与 Open Ontologies 同模式在导航栏单独加界面引入**（P1）；本体模块叙事在"构建、运行"基础上**增加"消费、审计"两环节，形成完整闭环**。不融入两平面（Python 全家桶与"1 核心+7 薄层"自研边界冲突、其无本体工程能力），零侵入集成。

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| REQ-99 | ~~semantica 独立集成栏~~（⛔ 退役，D-O15；设计存档《19_本体_semantica集成方案》）——存续语义：③通用 MCP 挂载契约保留（归 01 REQ-123）+ 界面 IA 演进为五栏（REQ-104/D-O11） | 归档 |
| REQ-100 | ~~本体消费链路（TTL→KG→GraphRAG 检索）~~（⛔ 随 D-O15 存档）——由 REQ-110 自研承接：TTL 即 KG + KG 增强检索 + `/api/kg/{id}`/rebuild | 归档 |
| REQ-101 | ~~审计/溯源学习视图~~（📌 提前交付）——由 REQ-110 承接：SQLite 决策审计 + `/api/audit/decisions(+/chain)` + PROV-O 导出，第五栏「消费与审计」可见 | 交付 |

### 2.9 界面信息架构与对话伴生本体生长（v0.9 新增，D-O11/D-O12）

主人拍板（2026-09-22）：本体模块定位澄清为**学习各种本体构建、运行方式的模块**；界面改为左侧边栏四栏 IA（REQ-104），对话式构建路线立项（REQ-103）。前端实施细节见《14_本体_前端改造方案.md》v0.3。

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| REQ-104 | **本体模块界面信息架构与学习中心**：①左侧边栏四栏——**学习中心**（默认页：七阶段学习路径 S1→S7、方法论卡片、任务卡打卡进度、构建方式五路径对照、运行方式引擎对照）、**本体构建**（二级模块=构建路径分类，见下）、**本体资产**（已构建本体统一管理：列表+详情工作区）、**本体运行**（二级模块=运行方式分组：Oxigraph/Fuseki/Open Ontologies 引导页/Cayley P2）；②现有页面主体降为「自定义构建」路径，保留七阶段前 4 步（S1~S4），S5 运行时移交运行模块、S6/S7 服务化与对接说明移交资产详情/方案文档；③构建路径五条分层标注状态——自定义构建（可用）、OntoChat 流程（部分可用，REQ-103 载体）、semantica 流程（入口卡跳转独立栏，D-O10 零侵入不破）、OntoExtend 流程（引导卡先行，工程化另行评估）、Open Ontologies 流程（引导+产物回流，REQ-78 互通后顺畅）；未工程化路径显示引导卡、不做空壳交互；④学习中心 v1 内容（P1 尾）=REQ-90 方法论五模块精简版+2 个示例建模说明+任务卡打卡（`task:` key 空间，先跑固定默认工具链、不等 REQ-75 配置页）；4 示例与深度版随 P2 补齐 | P1 尾 |
| REQ-103 | **对话伴生本体生长（OntoChat 流程工程化载体）**：**模式 A（先行，P2 前段）**——REQ-82 生成链路多轮化：对话式 CQ 引导（先列能力问题）→ 逐轮补全概念/关系 → spec_json 草稿 → 校验循环 → 预览入库；复用 `/api/ontology-llm/generate` 扩展会话上下文，无新服务；**模式 B（需求池，将来可选增量）**——对话伴生沉淀：长对话中自动抽取概念/关系候选入草稿池，依赖 semantica 桥接与对话记忆成熟后评估 | 模式 A P2 前段；模式 B 需求池 |

### 2.10 由知识库构建本体（v0.10 新增，D-O14，2026-09-23 主人拍板）

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| REQ-108 | **由知识库构建本体**（构建栏**第六路径**，D-KB5）：①入口位置——「本体构建」栏内与现有 5 路径并列（自定义构建 / OntoChat / semantica / OntoExtend / Open Ontologies），列为**第六路径**「**由知识库构建本体**」（v0.10，方案 A），状态分层「**引导卡先行 + 部分可用**」；②三种抽取策略（互斥或组合）——**策略 A：KB chunk 池 → LLM 抽 spec_json**（复用 REQ-82 能力，指定 KB → 主平台拉 chunk 集合 → 喂 LLM + CQ 引导 → spec_json 草稿）；**策略 B：KB GraphRAG 子模块的 KG → 直接映射 spec_json**（v0.13 已扩展的 semantica `/graphrag/ingest` 接受任意 chunk 集合，对应 `/graphrag/query` 输出 entities/relations/claims；主平台做 entity→Concept/relation→Relation 的薄映射层 <300 行）；**策略 C：混合**——KG 作初稿 + LLM 校验 + 补全；③关键入口步骤——选 KB（必选，列已有 KB 列表 + KB mode 徽标「RAG / GraphRAG」+ chunk 数 / KG 实体关系数）→ 选抽取策略（默认 A）→ 选 CQ 引导模式（与 REQ-90 一致：自动生成 3~5 个能力问题 / 用户自定义 / 跳过）→ 生成草稿 → spec_json 校验循环 → 用户预览 → 入本体仓库；④UI 入口——「本体构建」栏内路径列表第 6 项（独立卡片 + KG 徽标「数据驱动」+ 状态徽标「部分可用」），点击进入独立流程页面（不与现有 5 路径并列展示共用页面）；⑤依赖——M6-KB1~3 KB 基础 RAG 已就绪 + REQ-82 LLM 辅助创建已实现 + M14 KB 双子模块拆分已落地 + ~~v0.13 semantica worker 接受 chunk 集合已实现~~（**D-O15 后 KG 源切自研 internal/kg**，02 v0.31）+ `/api/ontology-llm/generate` 已实现；⑥**与现有 5 路径的关系**——独立构建路径，非 OntoChat 流式多轮、非自定义手写；与 semantica 流程的"本体→消费 KG"方向相反（这里是"KB→本体"构建方向）；⑦教学价值——补齐"**文本→三种语义资产形态**"完整对照：RAG 片段（检索用）/ KG（语义层）/ 本体（概念层），与本体侧"构建/运行/消费"三层形成"文本→不同形态资产"完整矩阵；⑧示例本体——`seeds/learning/ontologies/` 增第 5 个示例「**设备故障知识库 → 设备故障本体**」（演示 chunk→spec_json 抽取流程 + 与 SKOS 分类样例对照）。**已交付（c56ebd4，2026-09-23，02 v0.28/M15）**：①~⑧ 全项——4 端点 + BuildFromKBFlow 独立流程页 + S1 第 5 项 + 六路径「部分可用」；示例 5 落地为 `seeds/learning/examples/device_fault.kb.md` 语料 + README（`failure.json` 本身即目标本体「设备故障知识」，第 5 示例补 KB 侧语料与操作路径，两相对照即学习点）；策略 A/C 需 chat 默认连接，B/C 需 worker | P2 前段（与 OntoChat 模式 A 共用 `/api/ontology-llm/generate` 扩展，最早随 M6 KB 模块完成后立项） |

> REQ-75/76 重定位注记（v0.9，D-O11）：原"七阶段每阶段选工具"的口径与新「构建路径」概念重叠，REQ-75 重定位为**路径级工具链配置**（pipeline_profile 数据模型不变），REQ-76 打卡机制归**学习中心**（checklist 双 key 空间：`task:<task_id>` 任务打卡随学习中心 v1 先行，`tool:<stage>:<tool_id>` 工具打卡随 REQ-75 P2）；REQ-74 七阶段模型保留为学习中心的理论骨架。
> REQ-108 设计细节——参考《04_本体_方案设计.md》v0.13 §3.7（新章节）三种抽取策略 + KB 选择器 + CQ 引导；《12_知识库_方案设计.md》v0.3 §3.5（KB 作为本体构建数据源）；02 §12 M15 里程碑。

### 2.11 学习中心外部资源导航（v0.13 新增，2026-09-23 主人指示立项）

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| REQ-109 | **学习中心外部资源导航**：①触发——主人 2026-09-23 引入 [microsoft/Ontology-Playground](https://github.com/microsoft/Ontology-Playground)（微软官方本体学习 Playground，MIT，React19+Cytoscape.js 纯静态站，与本项目定位同构）时指示：在本体学习模块增加**相关内容链接页面**，加载代码仓库维护的 md 文档，把可直接访问的平台与典型开源项目维护起来；②内容资产——`seeds/learning/external-resources.md`（**随版本维护、单源、不进代码逻辑**，与 REQ-90 学习内容包同口径）：四节结构——可直接访问的在线平台（Ontology Playground / WebProtégé 在线版 / Fabric IQ 文档）/ 复用词表与顶层本体（FOAF/SKOS/schema.org/BFO）/ 官方规范与教程（OWL 2 Primer / RDF 1.1 / SPARQL 1.1）/ 典型开源项目（Protégé / ROBOT / rdflib / Oxigraph / Fuseki / Yasgui / Cayley / OOPS! / semantica 等，与 15 登记簿同源精选）；③加载机制（v0.13 实施定案：**Vite `?raw` 构建期内联，单源零拷贝、零后端改动**）——前端 LearnPage 直接 raw import 该 md（dev 模式 `server.fs.allow` 放行仓库根），不引入后端端点（与"自研收缩 1 核心+薄层"哲学一致；对比示例本体 internal/seed embed 拷贝双份模式，本机制单源更优）；④前端——学习中心新增「**外部资源**」Tab（第 5 个 Tab，XMarkdown 渲染 `openLinksInNewTab`，外链新窗口打开）；⑤维护约定——md 面向学习者（一句话+链接），全局台账仍以 15 登记簿为准（维护者视角，含状态/借鉴价值/活跃度），不一致时以登记簿为准并回修 md | P1（增量，随学习中心 O12 已交付形态追加，改动小） |

> REQ-109 设计细节——见 04 v0.14 §4.10 学习中心注记；登记簿新条目见 15 v2.3（Ontology-Playground 🔭 对照参考）。

### 2.12 去-semantica 化与消费/审计轻量自研（v0.15 新增，D-O15，2026-09-23 主人拍板）

| REQ-110 | **去-semantica 化与消费/审计轻量自研**（D-O15 反转 D-O10）：①触发——主人 2026-09-23 反馈「semantica 集成后可用性太差，semantica-worker 很难正常启动，直接继承这个方案方向有问题」→ 重估确认：安装重量结构性无解（torch 系 venv 数 GB、0.7.0 瘦身未上 PyPI）、防御式归一在为上游文档/实现漂移打补丁、依赖从独立栏扩散至 KB GraphRAG 与 REQ-108 策略 B/C；**核心学习链路（构建六路径/运行/KB RAG/策略 A）零依赖 semantica**；②原则——**不接入 semantica 运行时依赖，参考其能力自研轻量替代**；semantica 转 15 登记簿 🔭 对照参考（课程讲工业级平台形态+链接在线材料，不进代码）；③能力替代映射——**TTL→KG**：Oxigraph 直接装载 TTL（TTL 即 KG，SPARQL 查询遍历，零新增）；**KG 抽取**：REQ-98 LLM 能力代理结构化抽取（复用 REQ-82/90 CQ 引导链路，启用 ontobuild `lightweight` 方法位，输出 entities/relations 而非 spec_json）；**GraphRAG 语义问答→KG 增强检索**：向量命中（既有）→ SQLite 图表一跳扩展 → 上下文拼装——**前期够教学即可，后期再扩展**（主人定；诚实标注与微软工业级 GraphRAG 的差距，作为教学对照点）；**决策审计/PROV-O**：SQLite 决策表+溯源链父子引用+rdflib 导出 PROV-O TTL（P2 内再排期）；**Explorer 可视化**：ReactFlow（D-O12③ 已定案）/列表时间线；**MCP 挂载**：REQ-99③ 通用契约保留，删 semantica 预设；④页面处置——SemanticaPage **改造复用**（骨架保留、改挂自研后端、内容改为消费/审计教学叙事）+ **挪到本体模块下**（本体侧边栏第五栏「消费与审计」，D-O11 四栏扩五栏；导航栏收敛一项——主人征询后采纳「挪」方向，IA 细节实现期与主人对齐）；⑤worker 处置——`tools/semantica-worker/` 归档休眠（代码保留、不进启动链路），run-dev.sh 移除编排，后端反代与探活降级为可选；⑥依赖调整——M14 graphrag 模式 KG 源切自研 LLM 抽取（v0.4 ⑥ worker 降级路径的轻量抽取转正为主路径）；REQ-108 策略 B/C 数据源从 worker `/graphrag/kg` 回读改为自存 KG；⑦实现移交——智能体开发助理（2026-09-23 主人指定） | P2 |
| REQ-146 | **本体引擎自检与一键安装**：①运行列表引擎状态自检——运行平面新增 `GET /api/engines`：按引擎（oxigraph/fuseki）返回 {registered/installed/binary/searched/version/hint/installable}，oxigraph 按「环境 OXIGRAPH_BIN → PATH → data/bin → tools/bin」候选序探测可执行文件；②**引擎缺失显异常**——本体运行页引擎导航与方案列表在本地无引擎启动程序时显示红色「未安装/异常」徽标 + 顶部预检 Alert（替代静默不可启动）；③**一键下载安装入口**——`POST /api/engines/oxigraph/install`（异步任务 202 + 轮询自检）：按 GOOS/GOARCH 映射官方 release 资产（pin v0.5.11：darwin x64/arm64、linux gnu x64/arm64、windows msvc x64/arm64），流式下载至 data/bin/oxigraph（temp+rename 原子落盘，防主平台反代 3s 响应头超时），完成后 oxigraph 适配器动态解析即时生效（无需重启 runtimed）；fuseki 依赖 JDK+分发包解压，仅提供手动指引（诚实边界，不做自动安装）；④安装失败给平台映射说明 + 手动命令。**✅ 已交付（824b063，2026-09-25）**：①~④ 全项——/api/engines 自检 + 一键安装（异步任务）+ 前端异常态与安装入口 + httptest 单测；沙箱 CDN 受限致真实下载未在本机走通（机制经单测与桩二进制动态解析验证），真机可用 | P1 |
| REQ-147 | **运行方案停止态编辑（更换加载本体）**：①已创建方案在 stopped/created/error 状态下支持「编辑」——Modal 改名称与本体集合（多选自构建平面仓库，同新建向导口径），提交走既有 PUT /api/runtime-profiles/{id}（后端本已支持，缺口在前端无入口；running/starting 置灰提示「先停止再编辑」）；②编辑保存后明确提示「下次启动按新本体集合装载」（REQ-87 显式重载语义不破坏）；③端口/引擎不可改（端口影响 facade 挂载对话的稳定性；引擎换型=删建，学习尺度从简） | P1 |

> REQ-110 设计细节——见 04 v0.18 §4.9 D-O15 反转注记；《19_本体_semantica集成方案.md》v0.3 转存档；登记簿 15 v2.4（semantica ✅→🔭）；12 KB v0.6（graphrag KG 源切换预告）。
>
> **v0.16 实现交付（2026-09-23，commit 7d45dc0）**——②③⑤⑥ 全部落地：SQLite KG 三表 `kg_entity/kg_relationship/kg_claim`（claim.chunk_id 溯源）+ `onto_decision` 审计表（migration 010）；KG 抽取 = REQ-98 LLM 代理（method=`llm`）+ 规则回退（method=`lightweight`，「A 是 B/的 B」句式），graphrag 模式导入自动建 KG、失败不阻断（M14 degraded 语义保留）；KG 增强检索三步（向量命中→KGSubjectsByChunks→KGNeighbors 一跳+claims，seed 1.0/邻居 0.6，无命中回退纯向量）；审计 API（决策 CRUD/溯源链/PROV-O 导出——Go 原生 Turtle 模板，不依赖 rdflib，达成零 venv 口径）；API：`GET /api/kg/{id}`、`POST /api/kg/{id}/rebuild`、`GET/POST /api/audit/decisions`、`GET /api/audit/decisions/{id}/chain`、`GET /api/audit/prov-export`；前端第五栏「消费与审计」四 Tab（KG 图谱/GraphRAG 试查/决策审计/学习引导），详见 04 v0.19 §4.10、14 v0.15。**验收 22 达成**（run-dev.sh 一条命令启动零 Python venv 依赖，冒烟全绿：隔离栈 API + UI CDP 6/6）。

---

| REQ-151（v0.27 新增；**✅ 已交付 2026-09-26，见 v0.33**，M20/P1） | **facade 第 5 工具 `sparql_query`**：只读 SELECT 白名单（禁 UPDATE/INSERT/DELETE/LOAD 等变更关键字）、查询超时与结果行数上限、必带 ontology_id；入参 {query, ontology_id, limit?}，结果以表格式返回；翻译透视（REQ-94）照常记录。**交付口径**：词法级校验（跳过字符串字面量/IRI/注释后，PREFIX/BASE 前导声明后首关键词须为 SELECT，全文裸词禁变更关键字——字面量/IRI 内同形词不误伤）；超时 10s/次；limit 默认 50 上限 200，结果带 total/truncated 诚实标注；guide 注入文案同步 5 工具口径 | 见 §5 D-O17 |
| REQ-152（v0.27 新增，待开发，M20/P2） | **外部 MCP 客户端直连验证包**：facade 直连 Claude/Cursor 的配置文档（端点/传输/工具说明）+ 实测记录 + 学习中心外部资源条目；"外部 IDE 查平台本体"演示 | 见 §5 D-O17 ② |
| REQ-153（v0.27 新增，✅ 已交付 2026-09-26，M20/P2 内容包） | **大型种子本体双份（百级概念）**：①医学常识片段——症状/疾病/药物/检查/禁忌关系网（自建教学本体，公开医学常识，避免授权风险）；②生物信息基因领域——基因/转录本/蛋白/通路/染色体位置（命名对齐 HGNC、结构参考 GO，公开资源字段）；随 seeds/learning 交付并附建模说明；兼作 NFR-O-3「100+ 概念不卡死」的性能验证载体 | REQ-91 示例体系扩展（第 6/7 示例） |

### 2.13 智能体运行时动态薄本体（v0.31 新增，REQ-170，2026-09-26 立项）

**需求**：为智能体构建动态薄本体——报告轻量档 × D-O15 自研栈旁路落地：伴生 worker（Run/Resume 收尾触发 + message 表游标续抽，只读）+ LLM 结构化抽取（REQ-98 代理复用）+ 候选人工确认流（REQ-82 草稿必审）+ Oxigraph 会话图（named graph 隔离，种子 5 骨架类，失效化而非删除）+ PROV-O 审计。**约束**：对话主链路 0 改动、装配仅配置增量、开关默认关、产物可整体摘除（低侵入三原则）。**边界**：D-O19 第三来源（对话伴生）——资产栏独立页签展示，不入第五栏 KG 检索区；P2 检索接入依赖 REQ-151/127~130。**方案**：[平台知识 · 动态薄本体可行方案](../platform-knowledge/智能体/动态薄本体_可行方案.md)（v2.0，含项目实现分析）。**状态**：🚧 **P1 薄本体最小闭环已交付（2026-09-26，M28 首项；主人指令授权开发）**——迁移 021（agent.companion_ontology 开关默认关 + companion_candidate 候选表 + companion_cursor 游标表）；backend/internal/companion 旁路包（worker 游标续抽 → chat.GenerateStructured 抽取 → 候选 pending 落库；伴生图引擎=独立 Oxigraph 实例 data/companion-graph/ :9199 懒启动，复用 REQ-146 二进制候选序；SPARQL 纯函数层：种子 5 类幂等预置入全局默认图/实体 slug 同名归并/关系边=bot:Relation 实例节点/矛盾旧边 bot:invalidAt 失效化/DROP GRAPH 整体摘除）；API 先行五端点（candidates 列表 / confirm 入图+失效化 / reject / status / reset）；Run/Resume 收尾非阻塞触发（handlers_runs.go 两处，开关关零动作）。**坑**：oxigraph UPDATE 端点与查询分离（/update vs /query）；跨 run 残留引擎进程指向已清数据目录会 IO 错。**P1 验收**：开关默认关=零伴生动作 ✓；confirm 后 named graph 三元组含 bot:extractedFrom/prov:wasGeneratedBy 溯源 ✓；同主体同关系名再确认→旧边 invalidAt ✓；reset 后图空+表清 ✓；对话 SSE 契约 0 改动 ✓。**P2 进展（2026-09-26 同日交付 P2a+P2b）**：①REQ-151 graph 参数扩展——facade sparql_query 增可选 graph=companion + conversation_id，转发伴生引擎 /query 并以 SPARQL 协议标准参数 default-graph-uri 限定会话图（**零查询改写**，实测 oxigraph 0.5.11 支持）；SELECT 白名单/透视/limit 照旧，端点经 COMPANION_GRAPH_ENDPOINT 可配；guide 注入文案同步（ontology-service rest.go）；单测 5 例（成功路径带 graph 溯源/缺 conversation_id/非法 graph/引擎失联 COMPANION_GRAPH_UNAVAILABLE/变更查询仍拒）。②前端「伴生本体」页签（资产栏第九 Tab，带「对话」来源徽标）——api/companion.ts 独立模块（client.ts 含并行 WIP 按文件避让）+ CompanionPane（会话选择/管线状态卡 引擎-游标-pending-实体标签/候选三桶确认流 confirm 入图·reject/整体摘除 Popconfirm；失败走 LoadErrorAlert 可重试，三态规范对齐）；**真机全链验证**：注入候选→页签确认入图→伴生引擎懒启动（:9199）→labels 回显→facade companion 查询返回图内实体（含 graph 字段）——「对话→候选→确认→agent 可查」动态价值链打通；**坑：backend 进程 cwd=backend/，引擎二进制候选序需含 ../data/bin**。**P2 剩余**：Agent 配置页开关 UI（AgentSidePanel 含主人 M27 WIP，待其提交后补）+ 3d-force 成长可视化 + KG 检索源并入；LLM 真实抽取 E2E 随对话自然覆盖。

### 2.14 本体多途径构建平台可落地方案（v0.39 新增，REQ-171，2026-09-26 立项）

**需求**：以「支持多种途径构建本体的平台」为目标，整合业界开源实践（[平台知识 · 23](../platform-knowledge/本体/23_本体_开源实现方案借鉴研究.md) 18 方案）与理论论文线（research/本体构建理论论文线综述_20260926.md，35 篇四方向），重新收敛输出系统性工程路线。**方案主张**：架构不推翻——六条构建路径成熟度分级兑现（①②⑤已交付深化、③④P2 兑现、⑥P2 评估）+ 两个公共底座（资产质量门禁 qualitygate / 导入审查与合并）+ 设计铁律（LLM 生成必过门禁并自修复 ≤3 轮）；🆕 新建仅 1 包级模块（qualitygate）+ 1 importer 增强，质量报告复用多形态资产存储（零新表）；P1 无前端依赖 API 先行。**方案**：[平台知识 · 26_本体_多途径构建平台可落地方案](../platform-knowledge/本体/26_本体_多途径构建平台可落地方案.md)。**状态**：✅ 已立项（2026-09-26 主人拍板，方案 §11 四项决策点全部接受：P1 三子项整体立项 / 质量门禁检查项首批 8~12 项范围确认 / ROBOT Template 纳入 P2 评估 / M-O14 里程碑段启用）；P1 先行段挂 M-O14，开发排期待定（铁律：排期拍板前不写实现代码）。

## 3. 非功能需求

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| NFR-O-1 | **一致性**：本体保存后按 REQ-87 分类型生效；同一本体在同一运行方案内查询结果与仓库版本一致（带 version 校验） | P1 |
| NFR-O-2 | **故障隔离**：单个运行方案停止/崩溃只影响挂载它的对话；其他方案、普通对话、知识库不受影响，且有明确提示 | P1 |
| NFR-O-3 | **性能**（v0.3 调整）：SPARQL 型方案在本地学习数据量下查询毫秒级（Oxigraph 能力范围）；运行方案启动超时上限（建议 30s，超时报 error 并可看日志）；浏览页加载 100+ 概念本体不卡死（开源方案能力范围） | P1 |
| NFR-O-4 | **本地化约束**（继承平台定位）：全部服务回环监听、无鉴权、单机运行；运行方案编排以子进程为主，Docker 可选 | P1 |
| NFR-O-6 | **前端体验与组件标准化**（REQ-145，v1.13 治理改号，原误记 REQ-131）：页面组件避免手搓、优先复用已装开源组件（CodeMirror/Yasgui/React Flow/AntD 6）；统一三态（空/加载/错误）、无障碍基础、大表虚拟化、单文件 ≤400 行；**避免手搓优先复用**——详单见 14 号「前端体验优化与组件标准化待办」节｜✅ 一批+二批已交付（一批 2026-09-26：A1/A2 CodeMirror JSON 编辑、A4 claim 表 virtual+分页统一、B1 三大文件拆分全 ≤400 行；二批 2026-09-26：A3 版本文本对照（react-diff-viewer-continued，15 v2.11）/A5 Skeleton 规范/B2 无障碍 aria+画布 role/B3 scroll.x+操作列 fixed/B5 LoadErrorAlert 可重试错误卡；B4 样式 token 化 P3 与 C 类（KnowledgePage/ChatWindow 拆分、分页规范）待后续评估——14 v0.21） | P2 |
| NFR-O-5 | **可观测**：方案启停/健康检查/装载事件有日志；方案页可看最近 200 行运行日志｜✅ 已实现（runtime-manager 引擎 stdout 落 LogDir/{id}.log + GET /api/runtime-profiles/{id}/logs?tail= 默认 200 + 方案详情 Drawer「日志」Tab tail 可调，见 v0.22 清账） | P2 |

---

## 4. 优先级视图

| 档 | 内容 |
| --- | --- |
| **P1（本迭代）** | 仓库与多形态导入（REQ-60/80/69/72/62）、spec_json→TTL 导出（从零创建本体可入 RDF 型方案）、JSON 编辑与校验预览（REQ-61/63/64）、LLM 辅助创建（REQ-82）、可视化浏览（REQ-81）、运行方案管理/配置/多方案（REQ-84/85/86，引擎 Oxigraph 优先 + Fuseki）、解耦与开源优先原则（REQ-87/88）、对话集成（REQ-65/66/67/70/73）、七阶段模型映射（REQ-74）、**查询下钻与透视（REQ-92/93：SPARQL 工作台、源码视图；REQ-94 翻译透视；推理对照随 Fuseki）**、**semantica 独立集成与消费链路（REQ-99/100：独立栏+首页三段式引导+对话可选挂载+TTL→KG）——已交付后随 D-O15 转第五栏自研（v0.18 注）**、**界面信息架构与学习中心 v1（REQ-104：侧边栏四栏+构建路径分类+学习中心；REQ-90/91 内容包提前随行——方法论五模块精简版+2 示例建模说明+任务卡打卡）** |
| **P2（v0.22 清账：逐项以代码证据核查；已实现 ✅，真实剩余见行尾）** | 前端体验优化与组件标准化（NFR-O-6/REQ-145，详单见 14 待办节）｜ ~~fork 扩展（REQ-83）~~✅、~~方案日志面板（NFR-O-5）~~✅、~~REQ-71 适配器细节增强~~✅（随 v1.5 实例入画布交付）、~~版本 diff（REQ-95）~~✅（VersionDiff 组件）、~~主线数据灌装（REQ-96，P2a）~~✅（CsvIngestPane）、~~工具链配置页与引导执行（REQ-75/76）~~✅（学习中心 PipelinePane）、~~对话伴生本体生长模式 A（REQ-103 OntoChat 多轮 CQ 引导）~~✅（OntoChatFlow）、~~审计/溯源学习视图（REQ-101）~~✅（随 REQ-110 提前交付）、**轻量内存图引擎（可选，直接集成开源 Cayley fork——LG-12 自研学习目标已取消，D-O5 v0.4）**、**对话伴生本体生长模式 A（REQ-103：OntoChat 流程多轮 CQ 引导生成，P2 前段）**、**学习内容包深化（REQ-90/91 后半：4 示例本体+方法论深度版——精简版已随 P1 尾学习中心 v1 交付）、版本 diff（REQ-95）、主线数据灌装（REQ-96，P2a 先行）、双轨 TTL 互通（REQ-78）、审计/溯源学习视图（REQ-101——已随 REQ-110 提前交付，v0.18 注）、semantica 功能全覆盖第二批（REQ-102：图谱分析/语义检索独立页/决策智能完整版/质量门/多源摄取/存储与导出/MCP 与对话页；Agent 记忆 P3）**——**（已存档，见 v0.17：随 D-O15 semantica 退役，能力面由 REQ-110 轻量自研按需承接，不再排期）**、**由知识库构建本体（REQ-108：构建栏第六路径，P2 前段；三种抽取策略 chunk→LLM 抽 spec_json / KG→spec_json 直转 / 混合；共用 REQ-82 LLM 辅助创建能力；最早随 M6 KB 模块完成后立项）**、**去-semantica 化与消费/审计轻量自研（REQ-110，D-O15：TTL 即 KG / LLM 抽 KG / KG 增强检索前期够教学 / 页面改造复用挪本体侧边栏第五栏「消费与审计」/ worker 归档休眠）**）。
**P2 真实剩余（v0.22 收敛，v0.42 清账）**：OWL 映射细则增强（文档细则，待具体规格落档）、双轨 TTL 互通（REQ-78，触发条件：oo-worker 实际启用——当前未满足）、轻量内存图引擎（Cayley，可选）；~~学习内容包深化后半（方法论深度版，随内容排期）~~（✅ REQ-90/91 已 v0.12 全量交付 b20a070：方法论五卡深度版+4 示例建模说明，本条系 v0.22 清账残留、v0.42 移除）；REQ-108 由知识库构建本体已随 M15 交付（02 v0.28）。 |
| **P3（有余力）** | 本体热加载（免重启更新 RDF 型方案）✅ 已实现（POST /api/runtime-profiles/{id}/reload + 方案卡片「重载」按钮，v0.22 清账补记）、外部 MCP 客户端（Claude/Cursor）直连复用验证、本体与知识库联动（实例挂文档）、候选清单开放性工具注册机制（REQ-77）、**本体对齐/合并（REQ-79 远期）**、**引擎可用性预检——Runtime Manager `/healthz` 增补 engines 段（D-O9，2026-09-21 主人拍板：仅登记待办、本迭代不开发；落地前前端按 last_error 关键词映射友好提示）**、~~WebVOWL 嵌入视图~~（✅ 已随 M21 VIZ-3 交付 8d25e17——WebVowlView 对照视图 + owl2vowl 预转换，见 v0.32；D-O12 收口注同步 04 v0.27）、**对话伴生本体生长模式 B（REQ-103：对话伴生沉淀；v0.18 口径修正——原「依赖 semantica 桥接成熟」前提随 D-O15 失效，改依赖 LLM 抽 KG 能力（已具备）+ 对话记忆成熟，保留需求池）** |

---

## 5. 决策记录

| 编号 | 决策 | 结论 | 状态 |
| --- | --- | --- | --- |
| Q-13 | 本体查询放进程内还是独立服务？ | 独立本体服务（REST 管理构建 + MCP 查询运行），主平台经 MCP Client 接入 | ✅ 已定（智能体 PRD v0.4 拍板） |
| Q-14 | MCP 工具契约 | 固定 4 个只读 onto_* 工具（get_concept / get_instance / list_instances / neighbors），必带 ontology_id，tools/list 不随本体内容变化。**✅ 已定（v0.27 修订为 5 工具，见 D-O17）**：新增第 5 个只读工具 `sparql_query`（SELECT-only 白名单 + 超时 + 行数上限），解锁 Agent 开放性问题查询面——"受控 4 工具 vs 任意查询"的取舍成为教学点 | ✅ 已定（修订） |
| Q-15 | 导入格式分层 | spec 无损 / csv / graphml / OWL-RDF 有损；外部真实资产为 OWL/TTL → 有损导入 P1 | ✅ 已定（v0.2 提级） |
| Q-16 | 前端图编辑库 | React Flow + RJSF + dagre | ✅ 已定 |
| D-O1 | 构建与运行的关系 | **解耦**：构建平面管仓库（多形态资产），运行平面管运行方案；仓库是唯一事实源 | ✅ 已定（2026-09-20，五项诉求产物） |
| D-O2 | 运行方案数量 | 支持多套并存，每套独立选择运行时类型/本体集合/细节配置 | ✅ 已定（同上） |
| D-O3 | 自研 vs 开源（v0.4 调整） | 各子系统开源优先；P1 自研仅运行方案编排层、MCP facade 路由；OWL/RDF 解析不自研（Python rdflib sidecar）；内存图引擎不自研（P2 直接集成开源） | ✅ 已定（2026-09-20，两次拍板收敛） |
| D-O4 | 对话挂载对象 | 挂**运行方案**（存 runtime_profile_id，工具入参仍带 ontology_id）；备选"按本体挂载自动路由"已评估、暂不采用 | ✅ 已定（如需调整见方案 O-6） |
| D-O5 | 运行时类型清单（v0.4 调整） | **oxigraph 为 P1 唯一引擎**（配 spec_json→TTL 导出器，使自建本体可装载）；fuseki 随后（推理对照）；memory_graph **不自研，P2 若需直接集成开源引擎（Cayley fork 候选）**——2026-09-20 两次拍板：先延后（v0.3），后确定避免自研（v0.4）；LG-12 手写图引擎学习目标取消，学习点转为开源引擎集成与语义适配 | ✅ 已定（v0.4，2026-09-20） |
| D-O6 | 需求与方案文档关系 | 需求与设计分离：本档为需求事实源，方案档只维护设计；需求变更先改本档 | ✅ 已定（2026-09-20） |
| D-O7 | 工具链可配置的执行深度 | **引导执行为主**：内置工具（builtin）直接用，外部工具（guided）平台生成指引+打卡不拉进程，仅运行段自动编排（managed，沿用 Runtime Manager）；阶段划分采用七阶段细粒度。备选"全流程自动编排"因阶段×工具适配器组合爆炸暂缓，架构预留 execute() 扩展位 | ✅ 已定（2026-09-20） |
| D-O8 | 学习视角缺口补齐路线（G1~G9） | **全量采纳、分三阶段施行**（2026-09-21 主人拍板"能做多详细就多详细，分阶段施行"）：①教学层走需求化路线（G1→REQ-90、G2→REQ-91，内容资产随 seed 交付）；②下钻与透视进 P1（G3/G4→REQ-92/93/94）；③演化与灌装进 P2（G5/G6→REQ-95/96）；④进阶进需求池（G9→REQ-78、G7→REQ-79，G8 OOPS! 以 tools.json 数据条目登记，零代码）。边界不变：RAG/embedding 归知识库模块，不扩入本体需求 | ✅ 已定（2026-09-21） |
| D-O10 | semantica 集成路线 | **独立一栏接入（对标 Open Ontologies 独立集成模式）+ 消费/审计闭环**（2026-09-21 主人拍板）：不融入两平面（Python 全家桶与"1 核心+7 薄层"自研边界冲突、无本体工程能力），导航栏单独界面引入（P1）+ 模块首页功能/原理/使用说明三段式学习引导 + 对话配置可选挂载（MCP 目标勾选）；本体叙事扩展为"构建→运行→消费→审计"四环节（REQ-99/100 P1、REQ-101 P2）；安全要求锁 v0.6.8+（v0.5 前有 12 项已披露漏洞修复） | ⛔ 已反转（v0.15 D-O15，2026-09-23 主人拍板：worker 可用性差→不接入 semantica，消费/审计转轻量自研 REQ-110；见 D-O15 行） |
| D-O9 | 可视化择优与引擎预检（2026-09-21 主人拍板） | ①**可视化对比择优**：TBox 结构视图在 ReactFlow 重构版（@xyflow/react v12，已随仓库入库）与 WebVOWL 嵌入之间按实际效果对比，采用更优者；难以客观对比则双轨并存（ReactFlow 内置默认 / WebVOWL 嵌入入口）——替代原「TBox→WebVOWL」单选结论；②**引擎预检延后 P3**：Runtime Manager `/healthz` 增补 engines 段仅登记待办、本迭代不开发，前端按 last_error 映射友好提示兜底。**v0.9 收口（D-O12）：①对比任务不再挂起——ReactFlow 内置默认定案，WebVOWL 嵌入降为 P3 可选增强** | ✅ 已定（2026-09-21；①已收口 2026-09-22） |
| D-O11 | 本体模块 IA 与学习中心（2026-09-22 主人拍板） | **左侧边栏四栏**（学习中心默认页/本体构建五路径分类/本体资产统一管理/本体运行按运行方式分组）——定位澄清：本体模块 = 学习各种本体构建、运行方式的模块；现有页面降为「自定义构建」流程并保留七阶段前 4 步（S1~S4），S5~S7 移交资产/运行模块；构建路径分层：已工程化完整交互/部分可用交互骨架+排期态/未工程化引导卡，不做空壳；REQ-75/76 重定位（路径级工具配置/打卡归学习中心），REQ-74 保留为学习中心理论骨架；REQ-90/91 提前 P1 尾随学习中心 v1 | ✅ 已定（2026-09-22） |
| D-O12 | 三项长期悬挂项收口（2026-09-22 主人拍板） | ①**REQ-103 对话伴生本体生长**：先采用模式 A（REQ-82 生成链路多轮化，P2 前段），模式 B（对话伴生沉淀）为将来可选增量、进需求池；②**OpenBKN**：仅作参考开源项目记录、不落地实施（登记簿 🔭，04 §3.3 留生成链路备选设计记录）；③**WebVOWL 对比收口**：ReactFlow 内置默认定案（D-O9 对比任务不再挂起），WebVOWL 嵌入降为 P3 可选增强 | ✅ 已定（2026-09-22） |
| D-O14 | 由知识库构建本体——构建栏第六路径（2026-09-23 主人拍板） | **方案 A**：本体模块左侧边栏仍四栏（D-O11 IA 不变）；「本体构建」栏内构建路径从 5 扩为 6，新增第 6 条「**由知识库构建本体**」（REQ-108）；入口在路径列表第 6 项（独立卡片 + 状态徽标「部分可用」），点击进入独立流程页面；三种抽取策略——A：KB chunk 池 → LLM 抽 spec_json（复用 REQ-82）；B：KB GraphRAG 子模块的 KG → 直接映射 spec_json（v0.13 已扩展的 semantica `/graphrag/ingest` 接受任意 chunk 集合，主平台做 entity→Concept 薄映射层 <300 行）；C：混合（KG 作初稿 + LLM 校验 + 补全）；选 KB（必选，列 KB + mode 徽标「RAG / GraphRAG」）→ 选策略（默认 A）→ 选 CQ 引导模式 → 生成草稿 → 校验循环 → 预览入库；与现有 5 路径关系——独立构建路径，与 OntoChat（流式多轮）、自定义构建（手写）、semantica 流程（消费 KG 方向相反）均不重叠；与 semantica 独立栏（同 D-O10 模式）：KB→本体构建闭环与本体→KG 消费闭环形成"文本→不同形态资产"完整矩阵；依赖——M6-KB1~3 + REQ-82 已就绪 + M14 KB 双子模块 + v0.13 semantica worker chunk 接受 + 02 §12 M15 里程碑 + 04 §3.7 设计 + 12 KB v0.3 §3.5 KB 作为本体构建数据源；示例本体——`seeds/learning/ontologies/` 增第 5 个示例「设备故障知识库 → 设备故障本体」（P2） | ✅ 已定（2026-09-23） |
| D-O15 | 去-semantica 化——不接入 semantica，参考其能力自研轻量替代（2026-09-23 主人拍板，反转 D-O10） | ①**动机**：semantica-worker 可用性差（主人实测「很难正常启动」）——安装重量结构性无解（torch 系 venv 数 GB、0.7.0 瘦身未上 PyPI）、防御式归一在为上游文档/实现漂移打补丁、依赖从独立栏扩散至 KB GraphRAG 与 REQ-108 策略 B/C；重估确认核心学习链路零依赖 semantica，叙事价值（消费/审计教学）可由轻量自研等价实现（semantica 抽取本质同为 transformer/LLM 调用）；②**定案**：见 REQ-110 映射（TTL 即 KG / LLM 抽 KG / KG 增强检索前期够教学 / 审计 P2 / ReactFlow / MCP 契约保留预设删除）；SemanticaPage 改造复用并**挪本体侧边栏第五栏「消费与审计」**；worker 归档休眠；③**哲学一致性**：D-O5 边界是重能力不自研（图数据库/OWL 解析/GraphRAG 框架仍不自研），其前提「集成成本合理」已不成立，换轻量路线正是同一判断逻辑；semantica 转登记簿 🔭 对照参考 | ✅ 已实现（2026-09-23 v0.16，commit 7d45dc0；验收 22 达成——run-dev.sh 一条命令启动零 Python venv 依赖，构建六路径/KB 双模式/消费审计页全可用） |
| D-O16 | 产品规划治理三拍板（2026-09-23 主人认可缺口分析并授权全项落实） | ①**OO 导航归位**：顶部导航维持五项（OO 独立导航从未实装），OpenOntologies 双轨能力由本体模块构建栏「OO 流程」+ 运行栏「OO 引导页」两入口承载（智能体 PRD REQ-99/100 随之修订，REQ-111 转正登记；双轨可摘除原则不变）；②**第五栏「消费与审计」IA 收口**：维持 D-O15 约定（五栏 IA 不变），栏内信息组织细节在后续迭代中按 17 号 P4「诚实标注」原则演进，无未决冲突；③**文档编号治理**：《15_本体_semantica集成方案》改号 19（存档），全局 REQ 编号以《18_REQ编号注册表》为唯一权威（REQ-105/106 归智能体 PRD，本体侧新需求经注册表分配）。详见 17 v0.2 与 18 号 | ✅ 已定（2026-09-23） |
| D-O17 | **本体使用深化：facade 契约扩容 sparql_query + 大型种子本体**（2026-09-25 主人认可方向分析并授权落档） | ①**REQ-151 `sparql_query` 工具（P1/M20）**——Q-14 契约 4→5：只读 SELECT 白名单（禁 UPDATE/INSERT/DELETE）、查询超时与结果行数上限、必带 ontology_id；动机：4 固定工具答不了开放性问题（如"哪些概念没有注释"），query 通道已存在仅缺工具面；教学价值=受控查询面 vs 任意查询面的契约取舍（LG-12/13）。②**REQ-152 外部 MCP 客户端直连验证包（P2/M20）**——facade 本身即独立 MCP Server，Claude/Cursor 直连仅差配置文档与实测；"外部 IDE 查平台本体"= 使用本体的最直观外部证明。③**REQ-153 大型种子本体双份（P2/M20 内容包）**——医学常识片段（症状/疾病/药物/检查关系网）+ 生物信息基因领域（基因/转录本/蛋白/通路/染色体位置，命名对齐 HGNC、结构参考 GO）；百级概念规模兼作 NFR-O-3 性能验证载体；数据源用公开资源（HGNC/GO/HPO，15 号登记），自建教学本体避免授权风险 | ✅ 已定（2026-09-25） |

| D-O18 | **本体可视化路线修订：三维/沉浸只读浏览新增（REQ-154，M21）**（2026-09-25 主人指出二维可视化无法满足复杂本体，授权调研落档） | ①**编辑态不变**——GraphEditor（React Flow）仍是唯一编辑视图（REQ-71）；②**新增只读「三维浏览」Tab**——3d-force-graph（three.js WebGL，MIT）基线：点击节点相机聚焦飞入 + 邻居高亮 + 详情侧栏 + 搜索过滤 + 标签 LOD；spec_json 直映射（概念/实例双类着色）；③**WebVOWL 对照激活**（原 P3 提级为 VIZ-3）——本体语义原生视觉语言与 3D 并存对照，OWL2VOWL 预转换缓存随行；④G6 5/sigma 作 PoC 候选（VIZ-4，结论回写 15 号）；⑤大图性能目标：千级节点流畅（覆盖并 10 倍余量于 NFR-O-3）。调研详见 research/本体可视化方案调研_20260925.md 。**排期注（2026-09-25 主人指示）**：本期开发 VIZ-1 三维浏览（3d-force-graph，含点击聚焦/邻居高亮/搜索过滤基础交互）+ VIZ-3 WebVOWL 对照激活；VIZ-2 交互增强（路径高亮等进阶）与 VIZ-4 G6 5/sigma PoC 转入候选池随需领取——**前置已满足**（M10 阶段 10a 沙箱闭环已交付 96210e6） | ✅ 已定（2026-09-25） |
| D-O19 | **「消费与审计」第五栏定位厘清与 KG 边界**（2026-09-25 主人指出第五栏含知识图谱内容属混淆，授权梳理） | **定位定案**：第五栏 = **本体消费侧观测台**——展示与审计的对象是「本体 TTL 装载来源的 KG」与「onto_* 消费决策」；知识库侧文本抽取 KG（REQ-107/M16 自研）的展示归知识库 GraphRAG 子模块。**边界规则**：①第五栏 KG 检索区数据源限定本体 TTL 装载链路（检索结果带来源徽标 TTL/文本，改造入 REQ-163）；②知识库侧 KG 管理与检索展示不变（两处受众不同：本体侧看"本体被消费成什么样"，KB 侧看"文档抽取成什么图"）；③不删除能力只厘清边界——能力全集不变，仅界面数据源与文案按来源区分。**附**：开发细节（来源徽标/过滤实现）见 02 §12 M26 | ✅ 已定（2026-09-25） |

---

## 6. 与智能体 PRD 的编号关系

- **REQ-60~73**：源自智能体 PRD（v0.5 定稿后冻结为快照）。本文档为其**唯一后续维护点**——修改需求只改本文档，智能体 PRD 的冻结快照不再动。
- **REQ-74~77**：本文档 2026-09-20 新增（工具链配置，第六项诉求）；**REQ-78~79**：2026-09-21 正式登记（双轨 TTL 互通 / 本体对齐，见 §2.7 尾注记）。
- **REQ-80~88**：本文档 2026-09-20 新增（五项操作方式 + 两原则），智能体 PRD 已认可该编号归属本体文档。
- **REQ-19b/c/d、REQ-97/98**：对话侧对接需求，归智能体 PRD 维护；本模块在 §2.5 只承诺本体侧契约能力。
- **Q-13~16**：承接智能体 PRD 已拍板决策；D-O1 起为本体模块自有决策。

---

## 7. 验收要点

| # | 验收项 | 对应需求 |
| --- | --- | --- |
| 1 | 导入一份外部本体文件（spec/csv 或真实 OWL/TTL）→ 预览统计与 warnings → 确认入库 → 列表可见、统计正确 → 能被某运行方案勾选加载并查询到 | REQ-80/72/69/85 |
| 2 | 导入的 OWL/TTL 本体在 Oxigraph 方案中原样加载成功，SPARQL 查询可命中（原始形态未被破坏） | REQ-80/87 |
| 3 | 本体列表页完成新建/搜索/重命名/删除（二次确认）；非法 JSON 在保存时被拦截并给出可读错误 | REQ-60/63 |
| 4 | 「AI 创建」输入一段领域描述 → 生成草稿 → 预览确认（或要求修正）→ 入库后可编辑、可运行（经 spec_json→TTL 导出装载） | REQ-82 |
| 5 | 本体详情页浏览：TBox 结构图可交互（展开/过滤/搜索）；ABox 关系查询可从某实例出发查看邻居 | REQ-81 |
| 6 | 同时创建并启动 2 套运行方案（不同本体集合/端口），状态页均显示 running；分别被两个对话挂载，各自查询正确且互不影响；Fuseki 交付后可加"不同引擎类型"对照 | REQ-84/85/86 |
| 7 | 停止一个运行方案：挂载它的对话得到明确降级提示，其他方案与普通对话不受影响；重启方案后对话恢复 | REQ-70/84 |
| 8 | 管理页保存本体新版本：运行方案页面明确提示需重载，重载后查询按新版本返回（P1 全 SPARQL 型统一语义；"自动生效"对照视 P2 引擎引入情况评估） | REQ-87/73 |
| 9 | P2（延后）：fork 一个本体为副本，编辑后保存为独立新本体，fork 来源可追溯 | REQ-83 |
| 10 | P2（延后）：创建工具链配置——S1 选 Protégé（guided）、S5 选 Fuseki（managed）、其余默认 builtin → 页面生成引导清单 → 打卡完成 S1 → S5 方案自动拉起并可被对话挂载 | REQ-74~76 |
| 11 | 打开 SPARQL 工作台选某运行方案 → 执行 SELECT 返回结果表；本体详情页打开 TTL 源码视图显示原文（语法高亮、只读） | REQ-92/93 |
| 12 | 对话触发一次 onto_* 查询 → 运行方案调试面板可见对应翻译后 SPARQL 与耗时；推理对照入口对两个方案同查询并排 diff 展示（Fuseki 交付后） | REQ-94 |
| 13 | P2（延后）：上传 CSV 同名映射 → 预览实例统计 → 校验入库 → list_instances 命中新数据且版本 +1 | REQ-96 |
| 14 | P2（延后）：选择本体两个版本查看 diff，概念/关系/实例的增删改统计正确 | REQ-95 |
| 15 | P2（延后）：学习内容包随 seed 就绪（4 个示例本体 + 方法论卡片 + 任务卡），任务打卡后进度视图反映完成状态 | REQ-90/91 |
| 16 | ~~Semantica 独立栏~~（已存档 v0.17，随 D-O15 退役；替代验收见 22） | REQ-99 |
| 17 | ~~semantica MCP 挂载消费闭环~~（已存档 v0.17；由自研 KG 增强检索承接，验收见 22） | REQ-99/100 |
| 18 | ~~Semantica 独立栏六组功能~~（已存档 v0.17，REQ-102 不再排期） | REQ-102 |
| 19 | 本体模块左侧边栏四栏可见（学习中心默认页/构建/资产/运行）；构建栏**六路径**完整呈现且状态徽标正确（可用/部分可用/引导先行）——自定义构建（可用）/OntoChat 流程（部分可用，REQ-103）/semantica 流程（入口卡跳转独立栏）/OntoExtend 流程（引导先行）/Open Ontologies 流程（引导+产物回流）/**由知识库构建本体（部分可用，D-O14，REQ-108）**；未工程化路径为引导卡而非空壳交互 | REQ-104/108 |
| 20 | 「自定义构建」仅保留 S1~S4 步骤；资产详情修改保存后有"已保存新版本；运行方案需在其页面显式重载后生效"固定提示；运行栏引擎分组与运行方案页一致 | REQ-104/87 |
| 21 | 学习中心第 5 个 Tab「外部资源」可打开：渲染 `seeds/learning/external-resources.md` 内容（四节：在线平台/复用词表/官方规范/典型开源项目），外链新窗口打开；编辑该 md 并 commit 后刷新页面即生效（无需改代码） | REQ-109 |
| 22 | 主平台一条命令启动（run-dev.sh）后，**不依赖任何 Python venv/额外进程**：构建六路径（含 KB→本体三策略）、KB RAG/GraphRAG 双模式、消费与审计页（挪本体侧边栏后）全部可用；`tools/semantica-worker/` 不在启动链路（归档休眠）——**✅ 已达成（v0.16 实测：隔离栈 API 冒烟 + UI CDP 6/6 全绿；PROV-O 导出为 Go 原生 Turtle 模板，无 rdflib 依赖）** | REQ-110 |

---

## 8. 迭代记录

| 版本 | 日期 | 变更 | 负责人 |
| --- | --- | --- | --- |
| v0.1 | 2026-09-20 | **需求与方案分离**：从《04_本体_方案设计.md》v0.2 抽出需求独立成档——承接 REQ-60~73 冻结需求 + REQ-80~88 五项操作方式需求，补齐每条验收要点；新增非功能需求 NFR-O-1~5、决策记录 D-O1~D-O6、优先级视图（P1/P2/P3）、与智能体 PRD 的编号关系说明；方案文档自此只维护设计 | 董奎 |
| v0.2 | 2026-09-20 | **新增工具链配置需求（第六项诉求）**：新增 §2.6 REQ-74~77——七阶段工具链模型（S1 来源→S2 编辑→S3 校验→S4 可视化→S5 运行→S6 服务化→S7 对接 Agent，三形态交接契约）、工具链配置页（REQ-75）、引导执行模式（REQ-76）、候选清单开放性（REQ-77）；决策 D-O7（引导执行为主 + 七阶段细粒度，主人拍板）；§1 补第六项诉求映射、§4 优先级视图更新、验收要点加第 10 条、编号关系更新（REQ-78~79 预留） | 董奎 |
| v0.3 | 2026-09-20 | **D-O5 变更：memory_graph 延后为 P2 可选学习项，P1 引擎 = Oxigraph**（主人质疑自研必要性 → 分析确认开源更可行，主人拍板）。REQ-85 引擎清单调整；REQ-87 版本生效语义改为 P1 统一"显式重载"（内存图自动生效对照延后）；REQ-88 自研清单去掉内存图引擎必做性；导出需求补 **spec_json→Turtle 转换器**（自建本体装载路径，P1）；NFR-O-3 性能表述调整；D-O3 同步；验收 2/4/6/8 条更新；优先级视图 P2 加内存图引擎 | 董奎 |
| v0.4 | 2026-09-20 | **两处"避免自研"定案（主人拍板）**：①OWL/RDF 解析不自研——Python rdflib sidecar 方案定案（原"受限解析器"路线否决，导出转换器明确由 sidecar 实现）；②memory_graph 不自研——P2 若需直接集成开源引擎（Cayley fork 候选），LG-12 自研学习目标取消。REQ-85/87/88 更新；D-O3/D-O5 升 v0.4；验收第 8 条微调；优先级视图 P2 表述更新。配套分析见《08_本体_开源复用与自研边界分析.md》v0.2 | 董奎 |
| v0.5 | 2026-09-21 | **学习增强需求落地（主人拍板"能做多详细就多详细，分阶段施行"，采纳《13_本体_学习视角缺口分析.md》G1~G9）**：新增 §2.7 REQ-90~96——建模方法论引导（90）、学习任务卡与示例体系（91）、SPARQL 查询工作台 Yasgui（92）、源码只读视图（93）、翻译透视与推理对照（94）、版本 diff（95）、主线数据灌装（96）；REQ-78/79 由"预留"正式登记为需求池（双轨 TTL 互通/本体对齐，承接 09 建议与 §4.7 双轨语境）；决策 D-O8（全量采纳、分三阶段施行）；§1 补第七项诉求映射、优先级视图三档更新、验收要点加 11~15 条 | 董奎 |
| v0.6 | 2026-09-21 | **前端改造方案融合 + D-O9（主人拍板）**：REQ-81 可视化口径更新——ReactFlow 重构版（@xyflow/react v12，已随仓库 862df01 入库）与 WebVOWL 嵌入按效果对比择优、难以客观对比则双轨并存（ReactFlow 内置默认 / WebVOWL 嵌入入口），ABox 关系视图路由不变；引擎预检（Runtime Manager /healthz 补 engines 段）登记 P3 待办、本迭代不开发；新增决策 D-O9、优先级视图 P3 档同步、头部状态行更新；前端 IA 改造（构建/运行正交三 Tab）实施细节见《14_本体_前端改造方案.md》v0.2 | 董奎 |
| v0.7 | 2026-09-21 | **semantica 独立集成与消费/审计闭环（主人拍板，D-O10）**：新增 §2.8 REQ-99~101——semantica 独立集成栏（导航栏单独界面，对标 Open Ontologies 独立集成；首页功能/原理/使用说明三段式学习引导；对话配置 MCP 目标可选挂载，REQ-99，P1）、本体消费链路（TTL→ingest→KG，与 facade 精确查询形成"精确 vs 语义"教学对照，REQ-100，P1）、审计/溯源学习视图（record_decision + PROV-O，复用 Explorer UI，REQ-101，P2）；D-O10 决策（不融入两平面、零侵入独立栏、锁 v0.6.8+）；§1 补第八项诉求映射、优先级视图更新、验收要点加 16/17、编号体系更新 | 董奎 |
| v0.8 | 2026-09-21 | **semantica 独立栏功能全覆盖（主人第二次拍板，D-O10 补充）**：对照 semantica README 完整能力面（Context Graph/决策智能/本体治理/确定性推理/多源摄取/实体消解/图谱分析/时间轴/Registry/PROV-O/向量层/多形态存储/MCP/Explorer 8 工作区）做覆盖度分析——原方案仅覆盖约 1/3；新增 REQ-102：独立栏升级为**左侧边栏分组导航**（学习引导/图谱/推理与决策/本体治理/数据与检索/设置六组），复用策略=Explorer 原生工作区反代复用优先（0 自研）+REST 薄页扩展（每页<300 行）；P1=侧边栏框架+Explorer 复用组+推理引擎+Ontology Hub，P2=图谱分析/语义检索/决策智能完整版/质量门/多源摄取/存储导出，P3=Agent 记忆；验收加 18；方案侧见 04 v0.11 §4.9.2、前端回写见 02 v0.18 | 董奎 |
| v0.9 | 2026-09-22 | **IA 与学习中心需求落地 + 三项收口（D-O11/D-O12，主人拍板）**：定位澄清——本体模块=学习各种本体构建、运行方式的模块；新增 §2.9 REQ-104（侧边栏四栏 IA+构建五路径分层+学习中心 v1 内容）与 REQ-103（对话伴生本体生长：模式 A 先行 P2 前段/模式 B 需求池）；REQ-90/91 提前 P1 尾（方法论五模块精简版+2 示例+任务卡随学习中心 v1）；REQ-75/76 重定位（路径级工具配置/打卡归学习中心、`task:`/`tool:` 双 key 空间），REQ-74 保留为学习中心理论骨架；D-O9 可视化对比由 D-O12 收口（ReactFlow 内置默认定案、WebVOWL 嵌入降 P3）；OpenBKN 定为仅参考不落地（登记簿 v2.2）；§1 补第九项诉求映射、优先级三档更新、验收补 18（v0.8 声称未落表，本次补齐）+加 19/20；前端实施见 14 v0.3、设计侧见 04 v0.12 §4.10 | 董奎 |
| v0.10 | 2026-09-23 | **由知识库构建本体路径立项（主人拍板，D-O14，方案 A 落档）**：KB 模块 D-KB4 v0.25 拆双子模块就绪后，新增构建栏**第六路径**「由知识库构建本体」（REQ-108）；新增 §2.10 三种抽取策略（KB chunk→LLM 抽 spec_json / KB GraphRAG KG→直转 spec_json / 混合）+ CQ 引导 + KB 选择器（mode 徽标「RAG / GraphRAG」）+ 入口独立流程页面 + 示例本体第 5 个「设备故障知识库→设备故障本体」；§1 补第十项诉求映射、§4 P2 加 REQ-108、§5 决策记录加 D-O14（方案 A：本体侧边栏仍四栏、构建栏五路径扩六路径）、§7 验收 19 改为六路径；编号体系补 REQ-105~106 占位 + REQ-107/108；依赖——M6-KB1~3 + REQ-82 已就绪 + M14 KB 双子模块 + v0.13 semantica worker chunk 接受 + 02 §12 M15 + 04 v0.13 §3.7 + 12 KB v0.3 §3.5 | 董奎 |
| v0.11 | 2026-09-23 | **REQ-96 主线数据灌装全量交付（P2a+P2b）**：REQ-96 行标 ✅（P2a 同名映射已有 + P2b 映射向导本轮交付：列绑定/类型转换扩至 int-number-date-bool/多值分隔符/跳行规则/映射配置存取）；实施见 04 v0.14 §4.8.5、14 v0.9；验收 13 口径可执行（资产栏「CSV 灌装」页签全流程） | 智能体开发助理 |
| v0.12 | 2026-09-23 | **P2 序收官（REQ-90/91 补齐 + REQ-75/76 交付）**：REQ-90/91 标 ✅ 全量（方法论五卡深度版 + 4 示例建模说明 + 4 示例本体齐）；REQ-75/76 标 ✅（pipeline_profile 路径级配置 + guided 打卡 tool: key 空间，tools.json 数据驱动 24 工具，/api/pipelines 八端点 + 学习中心「工具链配置」Tab）；**04 §7 P2 序（95→96→103 模式 A→90/91→75/76）全部完成**；设计侧见 04 v0.15、前端见 14 v0.10 | 智能体开发助理 |
| v0.13 | 2026-09-23 | **学习中心外部资源导航（主人指示立项，REQ-109）**：主人引入 microsoft/Ontology-Playground（微软官方本体学习 Playground，MIT，纯静态站，与本项目定位同构）→ ①新增 §2.11 REQ-109：`seeds/learning/external-resources.md` 内容资产（四节：在线平台/复用词表/官方规范/典型开源项目，随版本维护单源不进代码，REQ-90 同口径）+ 加载机制定案 **Vite `?raw` 构建期内联**（单源零拷贝零后端改动，dev 模式 fs.allow 放行仓库根；优于 internal/seed embed 拷贝双份模式）+ 学习中心第 5 个 Tab「外部资源」（XMarkdown `openLinksInNewTab` 渲染，外链新窗口）；②§7 增验收 21；③编号体系补 REQ-109；④15 登记簿 v2.3 登记 Ontology-Playground（🔭 对照参考，在线可直接访问、导航首条）；⑤04 v0.16 §4.10 学习中心注记同步 | 董奎 |
| v0.14 | 2026-09-23 | **REQ-108 由知识库构建本体交付（D-O14，02 v0.28/M15，c56ebd4）**：§2.10 REQ-108 行补「已交付」注记——4 端点（KB 选择器 KG 统计从 worker 按库回读 / build-from-kb 三策略 / kg-to-spec-json 薄映射 / chunks-to-kg 薄封装）+ BuildFromKBFlow 独立流程页（四步：KB 选→策略与 CQ→生成与预览→入库）+ BuildPage 第 6 路径与 S1 来源第 5 项；示例 5 落地为 `seeds/learning/examples/device_fault.kb.md` 语料 + README（与内置 failure.json「设备故障知识」本体对照）；状态「引导卡先行」→「部分可用」 | 董奎 |
| v0.15 | 2026-09-23 | **去-semantica 化立项（主人拍板反转 D-O10，D-O15/REQ-110，P2）**：主人反馈「semantica 集成后可用性太差，semantica-worker 很难正常启动，方向有问题」→ 重估（依赖面盘点：核心链路零依赖；痛点：venv 数 GB/0.7.0 未上 PyPI/防御式归一）→ ①新增 §2.12 REQ-110：不接入 semantica、参考其能力自研轻量替代——TTL 即 KG（Oxigraph）/ KG 抽取切 REQ-98 LLM 代理（lightweight 方法位）/ KG 增强检索（向量+一跳扩展，主人定「前期够教学即可后期扩展」）/ 审计+PROV-O P2 / ReactFlow / MCP 通用契约保留；②页面：SemanticaPage 改造复用 + 挪本体侧边栏第五栏「消费与审计」（D-O11 四栏扩五栏）；③worker 归档休眠；④§4 P2 补 REQ-110、§5 加 D-O15、§7 增验收 22；⑤登记簿 15 v2.4（semantica ✅→🔭）、15 集成方案 v0.3 转存档、04 v0.18 §4.9 反转注记、12 v0.6 KG 源切换预告；实现移交智能体开发助理（主人指定） | 董奎 |
| v0.16 | 2026-09-23 | **REQ-110 实现交付（D-O15 落地，commit 7d45dc0）**：§2.12 加实现交付注记（SQLite KG 三表+onto_decision 审计表/LLM 抽取+规则回退/三步 KG 增强检索/PROV-O Go 原生导出/新 API 面/kg+audit 端点）；§5 D-O15 状态 ✅ 已定→✅ 已实现；§7 验收 22 达成标注（run-dev.sh 一条命令零 Python venv，冒烟全绿）；前端第五栏「消费与审计」见 04 v0.19 §4.10 + 14 v0.15；配套 02 v0.31（§6.9 KG 源口径）、04 v0.19、12 v0.7、14 v0.15；另修复云盘同步坍缩的代码树（7b4de80 重放为 d4634c3） | 董奎 |
| v0.17 | 2026-09-23 | **D-O15 文档清扫（反转标注补全）**：§1 诉求映射 #8 改写为去-semantica 口径（REQ-99~101 → REQ-99③+REQ-110）；§5 D-O10 状态 ✅ 已定→⛔ 已反转（指向 D-O15）；§4 P2 视图 REQ-102 加存档注（随 semantica 退役、能力面由 REQ-110 按需承接）；§7 验收 16/17/18 划线存档（替代口径指向验收 22）。需求实体不删除，仅状态标注——符合文档性质约定。**另：REQ-71 图形化编辑器 v1 交付（资产详情「图形编辑」Tab，同轮回写）**——画布编辑概念/关系/继承写回 spec_json，验收细节见条目注记与 04 v0.20 | 董奎 |
| v0.18 | 2026-09-23 | **D-O15 文档清扫第二轮（主人指示，残留口径补注）**：§2.8 REQ-99/100/101 三行补 D-O15 处置注（独立栏退役→第五栏承接／TTL 即 KG 自研／审计提前交付、Explorer 口径作废）；§4 P1 行 REQ-99/100 补反转注、P2 行 REQ-101 补交付注；§4 P3 REQ-103 模式 B 依赖口径修正（保留需求池）；配套 02 v0.32、11 v0.3、12 v0.8、16 v0.2 | 董奎 |
| v0.19 | 2026-09-23 | **REQ-71 v1.5 实例入画布交付（续 v1 同轮回写）**：REQ-71 条目补 v1.5 交付注——实例节点（紫色系 `onto-flow-node-inst`）+ 归属虚线/实例关系细边、「添加实例」弹窗（概念选择 + 属性 JSON 语法校验、拒绝与概念同名）、实例属性面板（属性编辑/实例关系只读列表/删除级联清理他实例引用）、「隐藏实例」开关（切回纯概念视图）；连线规则三分支（概念关系/继承/实例关系，拒绝跨层混连）；冒烟 6/6（实例/归属边/实例关系边渲染、加赵六、面板回填、保存递增版本、开关切换）；排障注记：CDP 冒烟遇「全应用 ReactFlow 边不渲染」为窗口遮挡无渲染帧所致（ResizeObserver 不交付 → 节点无 measured → 内联 hidden），headless=new 产帧即恢复，应用无恙 | 董奎 |
| v0.20 | 2026-09-23 | **产品规划治理（D-O16，缺口分析全项落实）**：①头部编号体系撤销"REQ-105~106 预留占位"（已归智能体 PRD 实占；全局编号以《18_REQ编号注册表》为唯一权威，G-1 裁决本体学习增强 90~96 保留）；②§1 诉求 #10 依赖行补 D-O15 注（semantica worker 依赖作废，KG 源已切自研 internal/kg）；③§5 新增 D-O16（OO 导航归位五项导航 + 第五栏 IA 收口 + 文档编号治理）；④《15_本体_semantica集成方案》改号 19。配套：01 v0.21 / 02 v0.36 / 17 v0.2 / 18 新建 / 20 新建 | 董奎 × 协作 Agent |
| v0.21 | 2026-09-23 | **整理压缩轮**：§1 诉求 8/9/10 长行压缩；REQ-71 已交付注记压缩；REQ-99/100/101 存档三行压缩为摘要；§7 验收 16/17/18 存档条目压缩。2026-09-23 文档整理轮（主人授权）：已完成需求细节压缩、过期/退役内容清除、prototype 原型移除——完整历史见 git 与本文档迭代记录；本轮不改变任何需求语义与验收口径。 | 董奎 × 协作 Agent |
| v0.22 | 2026-09-24 | **P2 存量池全面清账（逐项代码证据核查，无新开发）**：取证确认已实现——REQ-83 fork（AssetsPage doFork，forked_from 溯源）、NFR-O-5 方案日志（runtime-manager /logs?tail=200 + Drawer 日志 Tab）、REQ-71 适配器增强（随 v1.5）、REQ-95 版本 diff（VersionDiff）、REQ-96 CSV 灌装（CsvIngestPane）、REQ-75/76 工具链（PipelinePane）、REQ-103 模式 A（OntoChatFlow）；§3 阶段表 REQ-83/NFR-O-5 行标注 ✅，P2 档案行收敛为真实剩余（OWL 映射细则增强 / REQ-78 未触发 / Cayley 可选 / 方法论深度版随内容排期）。冒烟：logs 端点契约验证（create→logs→delete）；另取证本体热加载已实现（/reload + 重载按钮）随 P3 行补记。AGENTS.md 存量池同步收敛 | 董奎 × 自动化执行者 |
| v0.23 | 2026-09-24 | **前端体验优化立项（NFR-O-6/REQ-131，只立项待统一开发）**：2026-09-24 全页扫描（本体 15 个 tsx 共 7551 行 + 全站抽查）识别 12 项——A 类已装开源库未充分使用（Spec 编辑器 TextArea→CodeMirror JSON、GraphEditor 属性编辑防崩、VersionDiff 文本 diff、大表虚拟化、Skeleton）、B 类通用习惯（大文件拆分/无障碍/列宽/样式 token 化/错误卡）、C 类跨模块（KnowledgePage/ChatWindow 拆分、分页规范）；详单落 14 号待办节；登记表 18 v1.12 分配 REQ-131 | 董奎 × 协作 Agent || v0.24 | 2026-09-24 | **REQ 编号治理**：NFR-O-6 前端体验优化的需求编号 REQ-131 → **REQ-145**（18 号 v1.13：v1.12 分配与 01 侧「Agent 对外 MCP 服务化」冲突，本表为唯一需求事实源同步改注；需求语义与详单不变，仍待统一开发） | 董奎 × 自动化执行者 |
| v0.26 | 2026-09-24 | **REQ-146/147 实现交付（824b063）**：①REQ-146 引擎自检与一键安装——运行平面 GET /api/engines（oxigraph 候选序 OXIGRAPH_BIN→PATH→data/bin→tools/bin，version best-effort；fuseki 未注册也呈现+手动指引）+ POST /api/engines/oxigraph/install（202 异步，GOOS/GOARCH 映射官方 release 资产 pin v0.5.11，downloadTo temp+rename 原子落盘+最小体积校验）；oxigraph Start 改动态解析（装后即时生效免重启 runtimed）；主平台反代 /api/engines*；前端运行页导航缺失红 Tag（安装中橙）+ 分组页预检 Alert + 一键安装按钮（失败回显）+ 方案卡缺失标记；②REQ-147 停止态编辑——方案卡「编辑」（stopped/created/error 可用，running/starting 置灰）Modal 改名称与本体集合走既有 PUT（端口回传保留/config 后端保留）；引擎与端口只读；③测试：资产映射/FindExecutable/downloadTo httptest 9 例 + 全量绿；冒烟：缺失态→安装态→可用态全链 + 编辑弹窗双本体持久化 + 截图；**附带核验**：并行会话报的「c48599c 工具执行环回归」在 HEAD b5de3ac 复测未复现（tool.call→tool.result→二轮续跑→completed，疑为其提交前中途 WIP 态），见 02 v0.55 | 董奎 × 自动化执行者 |
| 23 | 对话挂载 running 方案后，Agent 可经 sparql_query 回答 4 工具无法覆盖的开放问题（如「哪些概念没有任何注释」）；医学常识/基因领域两个百级种子本体装载后浏览/查询不卡顿（NFR-O-3 规模真实化）。**前半 ✅ 达成（2026-09-26，REQ-151 交付）**：Agent（eino-adk + GLM 连接）对话实测 tool.call(source=ontology:facade)→sparql_query→oxigraph 聚合执行（GROUP BY/COUNT 实例计数）→tool.result 表格→回答正确；后半 ✅ 达成（2026-09-26，REQ-153 交付）：med_common（102 概念/170 实例）+ gene_core（104 概念/94 实例）灌装→oxigraph 方案装载→SPARQL 实测（禁忌网/层级聚合/中心法则链/染色体 GROUP BY 全命中，1.3~1.7ms）；Graph3D 三维浏览 272 节点流畅——**验收 23 全达成** | REQ-151/153 |
| 24 | 资产详情「可视化」区新增三维浏览 Tab：百级概念本体加载流畅；点击节点相机聚焦 + 邻居高亮 + 属性侧栏；WebVOWL 对照视图可切换（REQ-154 阶段交付逐项验收）。**✅ 全达成（2026-09-26）**：百级流畅经 REQ-153 双大型种子实测（med_common 272 节点/gene_core 198 节点渲染流畅、搜索定位 TP53/复位全景正常；WebVOWL 降级路径 M21 已实测） | REQ-154 |
| v0.27 | 2026-09-25 | **本体使用深化立项（M20，D-O17；主人认可方向分析并授权落档）**：①REQ-151 facade 第 5 工具 sparql_query（Q-14 契约 4→5 修订，SELECT 白名单/超时/行数上限）；②REQ-152 外部 MCP 客户端直连验证包（Claude/Cursor）；③REQ-153 大型种子本体双份（医学常识片段 + 生物信息基因领域，百级概念，兼作 NFR-O-3 性能载体，数据源 HGNC/GO/HPO 已登记 15 号）；验收 23 增补；排期见 02 §12 M20 | 董奎 × 文档会话 |
| v0.28 | 2026-09-25 | **本体可视化 3.0 立项（REQ-154/M21，D-O18；主人指出二维无法满足复杂本体、要求调研三维/伪三维+点击聚焦方案）**：research/本体可视化方案调研_20260925.md——九候选比选（3d-force-graph 基线/WebVOWL 对照激活/G6 5·sigma PoC/Cytoscape·Neo4j·桌面·自研不引入）+ 点击聚焦交互规范 + 数据映射与风险；编辑态 React Flow 不变；15 号 +3 库登记；18 v1.19 分配 REQ-154；排期 M21（VIZ-1~4 阶段化） | 董奎 × 文档会话 |
| v0.29 | 2026-09-25 | **M21 排期细化 + D-O18 去重**（主人指示：先排 3d-force-graph 与 WebVOWL，其他候选）：本期开发 VIZ-1 三维浏览 + VIZ-3 WebVOWL 对照激活；VIZ-2 交互增强/VIZ-4 G6 5·sigma PoC 转候选池；前置已满足（10a 已交付 96210e6）；并行写入致 D-O18 重复行已去重 | 董奎 × 文档会话 |
| v0.30 | 2026-09-25 | **体验批次立项（REQ-162/163，D-O19；主人提出学习中心七阶段方块化/外部资源迁移/第五栏 KG 混淆）**：①REQ-162 学习中心七阶段方块+箭头化、外部资源 Tab 迁平台知识（REQ-109 挪位）；②D-O19 第五栏定位厘清——KG 检索限定本体 TTL 装载来源（来源徽标），KB 抽取 KG 归知识库侧；登记表 18 v1.25 分配 REQ-162/163 | 董奎 × 文档会话 |
| v0.31 | 2026-09-26 | **REQ-170 立项（智能体运行时动态薄本体，方案 only；§2.13）**：报告轻量档 × D-O15 自研栈旁路落地（伴生 worker+候选确认+Oxigraph 会话图+种子 5 骨架类+失效化）；对话主链路 0 改动、开关默认关；D-O19 第三来源边界；方案迁 platform-knowledge/智能体/动态薄本体_可行方案.md（v2.0 含项目实现分析，子课题资料 01/02 同迁）；18 v1.28 | 董奎 × 文档会话 |
| v0.32 | 2026-09-26 | **M21 VIZ-1+VIZ-3 实现交付（8d25e17，REQ-154）**：①VIZ-1 三维浏览——Graph3D 新组件（3d-force-graph 1.80 UMD 本地分发 + three 0.186）：spec_json→{nodes,links} 映射（概念球体按顶层根着色/实例八面体继承概念色/继承暗线/关系粒子流向线/实例归属边），点击聚焦飞入+邻居高亮其余淡出、侧栏详情（概念=定义/父/实例数；实例=所属/属性表）、搜索定位（精确>前缀>包含）、类型过滤器、复位全景、标签悬停；②VIZ-3 WebVOWL 对照——WebVowlView 组件（平台 TTL 导出→owl2vowl 预转换→webvowl 渲染，public/vendor 静态注入 + tools/fetch-webvowl.sh 获取脚本）；分发缺失/TTL 导出失败诚实降级警示（本机 xcrun 损坏致降级路径实测生效）；③接入：资产详情「可视化」Tab Segmented 三态（2D 结构/三维浏览/WebVOWL 对照），编辑仍归 GraphEditor（REQ-71）；④**关键坑**：3d-force-graph 1.80 起为 class 模式必须 `new ForceGraph3D(el)`——普通调用 classMode=false 不执行 initStatic，容器静默空白无报错（排查耗多轮）；⑤验收 24 基础达成（百级流畅待大型种子本体验证=REQ-153 联动），演示截图 4 张 | 董奎 × 自动化执行者 |
| v0.33 | 2026-09-26 | **REQ-151 实现交付（M20 首项，Q-14 契约 4→5 落地）**：①facade 第 5 工具 `sparql_query`（runtime-manager/internal/facade/sparql.go）——词法级只读 SELECT 校验（跳过字符串字面量/IRI/注释后校验，PREFIX/BASE 任意次序前导；UPDATE/INSERT/DELETE/LOAD/CLEAR/CREATE/DROP/MOVE/COPY/ADD 十关键字全禁，字面量内同形词不误伤）；超时 10s；limit 默认 50/上限 200，表格投影（列名字母序稳定/缺键补空/total+truncated 诚实标注）；翻译透视照常落 trace；②guide 注入文案同步（ontology-service rest.go：sparql_query 用法与只读边界）；③前端 RuntimePage facade 与工具页签补第 5 行 + 「共 4 个」→「共 5 个」；④测试：纯函数单测 3 组 28 例（白名单 21/limit 6/表格投影）+ httptest 桩 SPARQL 端点集成测试（正常截断/拒绝不触达下游/未挂载路由失败/透视落库）；⑤冒烟：本机经 REQ-146 一键安装 oxigraph v0.5.11（macOS 首次走通）→ running 方案真机实测——tools/list 5 工具、DELETE/INSERT/LOAD/ASK 全拒（QUERY_REJECTED 不触达引擎）、SELECT 概念列表（limit=3 截断 total=6 truncated=true）、GROUP BY/COUNT 聚合开放问题、透视 4 条落库；Agent 端到端达成验收 23 前半（tool.call→sparql_query→聚合→正确回答）；前端 facade 工具表/透视页签截图留证；**环境注**：本机 python3 shim 因 xcrun 缺失损坏，方案启动的 sidecar 导出经 SIDECAR_PYTHON=/usr/local/bin/python3.12 + pip rdflib 绕过（不改平台代码，run-dev.sh 既有 env 口径）；DeepSeek 预置 Key 401（M24 已知边界），E2E 用 GLM flashx 连接 | 董奎 × 自动化执行者 |
| v0.34 | 2026-09-26 | **REQ-153 实现交付（M20 第 3 项，百级大型种子本体双份）**：①`ontology-service/internal/seed/examples/` 新增 med_common.json（医学常识（家庭常见病）：102 概念=疾病/症状/药物/检查四大分类树，170 实例=具体病种/症状/药物/检查，实例层承载知识关系网 9 关系：表现为/治疗/缓解/禁忌于/可引起/典型检查/并发/需鉴别/相互作用，禁忌网经典药对：美托洛尔×哮喘/阿司匹林×胃溃疡/华法林×阿司匹林/氯吡格雷×奥美拉唑）+ gene_core.json（基因与中心法则：104 概念=基因/转录本/蛋白质/通路/染色体位置五实体树 + GO 风格生物过程/分子功能/细胞组分三功能树，94 实例=26 基因（HGNC 符号 TP53/BRCA1/EGFR…）/6 转录本（RefSeq）/17 蛋白/13 通路/25 染色体带，中心法则链 转录为→翻译为→编码 + 参与通路/调控/相互作用/定位于，EGFR 与 ACTB 共处 7p11.2 共定位教学点）；②建模说明 seeds/learning/examples/med_common.README.md + gene_core.README.md（背景/CQ/决策记录/局限，含「分类树在概念层、知识网在实例层」两层口径与 GO 注释以属性承载的取舍）；③seed 单测 learning_test.go 3 例（列表含新种子/加载过校验+百级规模断言/非法 key 拒绝）；④前端 LearnPage 示例库提示文案 4→7 示例（REQ-91 ③ + REQ-153）；⑤冒烟：go build/test 全过 + web build + 本机运行栈（SIDECAR_PYTHON 绕 xcrun）灌装→oxigraph 方案装载双本体→SPARQL 四查询全命中（禁忌于胃溃疡=布洛芬/阿司匹林；subClassOf* 感染性疾病分支计数=4；TP53→TP53-201→p53蛋白 链；7 号染色体带 GROUP BY，1.3~1.7ms）→Graph3D 双本体三维浏览流畅（272/198 节点）+ TP53 定位聚焦侧栏属性——验收 23 全达成/24 百级达成；顺修验收 24 重复行；15 号 HGNC/GO/HPO 状态翻「已用作」 | 董奎 × 自动化执行者 |
| v0.35 | 2026-09-26 | **M22 一批交付（NFR-O-6/REQ-145 P1 四项：A1/A2/A4/B1，前端体验优化首交付）**：①A1 Spec 编辑器 `Input.TextArea`→CodeMirror JSON（JsonEditor 共享组件：json() 语法高亮 + jsonParseLinter 行内错误标记 + 行号/折叠；@codemirror/lang-json+lint 新依赖登记 15 号 v2.10）；②A2 GraphEditor 实例属性两处（选中面板 96px/添加实例弹窗 110px）换 JsonEditor，apply 路径 try/catch 防崩与内联错误提示保留（概念定义为纯文本不经 JSON 化）；③A4 消费与审计 claim 溯源表开 AntD 6 Table virtual（scroll.y 400）+ 决策/claim 表 pageSize 统一 10；④B1 三大文件拆分：AssetsPage 835→329（assets/ 三件）+ GraphEditor 966→353（graph-editor/ 五件：model 派生规则/nodes/panels/modals/useEditorActions）+ AuditPage 977→141（audit/ 四页签），全部 ≤400 行；**坑沉淀：@uiw/react-codemirror 4.25 value 受控同步在打字锁挂起期间吞外部值更新（重新加载/切换本体后编辑器不重置）——JsonEditor 改显式所有权同步（onCreateEditor 持 view，外部值显式变更时 dispatch 整体替换）**；冒烟 S1.1/S4.1/S4.2/S4.4 全过 + 截图 smoke/req145/；02 v0.70 §12 M22/14 v0.20/20 v1.19 S4.9/18 v1.31/15 v2.10 | 董奎 × 自动化执行者 |
| v0.37 | 2026-09-26 | **REQ-170 P1 薄本体最小闭环交付（M28 首项，主人指令授权伴生本体开发）**：①迁移 021_companion_thin_ontology（agent.companion_ontology 开关默认关 / companion_candidate 候选表 / companion_cursor 会话游标）；②backend/internal/companion 旁路包——worker（Run/Resume 收尾触发+message 游标续抽，chat.GenerateStructured 结构化抽取复用 REQ-98）+ 伴生图引擎（独立 Oxigraph 实例 data/companion-graph/ :9199 懒启动，二进制候选序复用 REQ-146）+ SPARQL 纯函数层（种子 5 骨架类幂等预置入全局默认图、实体 slug 同名归并、关系边=bot:Relation 实例节点、矛盾旧边 bot:invalidAt 失效化、DROP GRAPH 摘除）；③API 先行：GET candidates / POST confirm+失效化 / POST reject / GET status / POST conversations/{id}/reset 五端点（server.go 路由）；④handlers_runs.go Run/Resume 收尾非阻塞触发（低侵入：开关关零动作、goroutine 失败仅日志、SSE 契约 0 改动）；⑤测试：零依赖单测 4 组（游标增量定位/候选转换/SPARQL 生成/存储回环）+ 真机冒烟（本机 oxigraph 0.5.11 独立端口 9198：懒启动→schema→概念/关系入图→失效化→摘除全链）；**坑沉淀：oxigraph UPDATE 走 /update 与 /query 端点分离；测试残留引擎进程指向已清 TempDir 报 IO error——固定冒烟目录+前置 pkill**；E2E（真实 LLM 抽取）待 dev 栈下次重启实测（工作树含主人 M27/A3/B5 未提交改动，本轮不重启）；02 v0.72 §12 M28 行/20 v1.21 S4.11/18 v1.33/AGENTS 状态节 | 董奎 × 自动化执行者 |
| v0.38 | 2026-09-26 | **REQ-170 P2a+P2b 交付（伴生图查询接入 + 前端伴生页签）**：①facade sparql_query graph=companion 转发伴生引擎（default-graph-uri 协议参数零改写限定会话图；COMPANION_GRAPH_ENDPOINT 可配；SELECT 白名单/透视照旧；单测 5 例）+ guide 注入文案同步；②资产栏「伴生本体」页签（CompanionPane：会话选择/状态卡/候选确认流/整体摘除，LoadErrorAlert 三态，api/companion.ts 独立避让 client.ts WIP）；**真机全链**：注入候选→确认入图→引擎懒启动 :9199→labels→facade 查询返回实体——动态价值链打通；坑：backend cwd=backend/ 引擎候选序补 ../data/bin；02 v0.73/20 v1.22/18 v1.34 | 董奎 × 自动化执行者 |
| v0.36 | 2026-09-26 | **M22 二批交付（NFR-O-6/REQ-145 P2 五项：A3/A5/B2/B3/B5）**：①A3 版本文本对照——ontology-service 新增 `GET /api/ontologies/{id}/versions/{version}/spec` 快照原文端点（复用 repo.GetVersionSpec；httptest 三例）+ VersionDiff「文本对照」页签（两版 spec 归一格式化→react-diff-viewer-continued 4.4 分栏，15 号 v2.11 登记）；②A5 Skeleton 规范（内容占位 Skeleton/过程进行中 Spin，本体模块 6 处落地）；③B2 无障碍（icon-only 按钮 aria-label 补齐 + GraphEditor 画布 role）；④B3 决策表操作列 fixed + TraceTable/CSV 草稿表 scroll.x；⑤B5 共享 LoadErrorAlert 可重试错误卡（KnowledgePage 侧栏/OntoChat 会话列表/RuntimePage）+ 停服实测重试闭环；B4 token 化（P3）与 C 类待后续评估；冒烟 S4.10 新增 + S1.1/S4.9 回归全过（截图 smoke/req145-b2/）；02 v0.71 §12 M22/14 v0.21/20 v1.20 S4.10/18 v1.32/15 v2.11 | 董奎 × 自动化执行者 |
| v0.41 | 2026-09-26 | **REQ-92 实现升级：SPARQL 工作台 v2（CM6+AntD 自组装，主人口头授权「用开源片段组件优化」）**：①根因——@triply/yasgui 4.2 的 yasgui.min.css 从未导入，YASQE/YASR 以无样式裸表单渲染（杂乱简陋的直接原因）；②重构——新组件 SparqlWorkbench（编辑器 @uiw/react-codemirror + codemirror-lang-sparql ^2.1.0 MIT 语法高亮 + 自组 20 词关键字/常用词补全；工具栏 AntD：运行查询/⌘Ctrl+Enter/查询模板五枚含 REQ-153 种子教学查询/重置/导出 CSV/耗时与行数 Tag）；SELECT→AntD Table（列=vars 动态、分页、共 N 行），ASK→真值告警，CONSTRUCT/DESCRIBE→Turtle 文本（runSparql 增 accept 覆盖），变更查询前端拦截提示走 facade 受控面；查询 localStorage 持久化（persistenceId=onto-detail-{profileId}）；③依赖——+codemirror-lang-sparql +@codemirror/autocomplete（转直连） −@triply/yasgui（卸载，15 号 v2.12）；④api.runSparql 既有零调用方法首次启用；后端零改动；⑤冒烟：tsc/vite 过；本机 REQ-153-e2e running 方案实测——默认模板 20 行表（7ms）、禁忌网查询恢复运行命中布洛芬/阿司匹林（7ms/2 行）、错误路径 Alert 正常；截图 artifacts | 董奎 × 自动化执行者 |
| v0.39 | 2026-09-26 | **REQ-171 立项（本体多途径构建平台可落地方案，方案 only；§2.14）**：整合 docs/23 18 方案 + 理论论文线（research/本体构建理论论文线综述）收敛——架构不推翻、六路径成熟度分级兑现、两公共底座（qualitygate/导入审查）、LLM 生成必过门禁铁律；新建仅 1 包级模块+1 importer 增强、零新表；P1 API 先行无前端依赖；方案 platform-knowledge/本体/26；18 v1.35 | 董奎 × 文档会话 |
| v0.40 | 2026-09-26 | **REQ-171 转正（主人拍板）**：方案 §11 四项决策点全部接受——P1 三子项（质量门禁 qualitygate+LLM 自修复循环+LOV 词表搜索薄层）整体立项、检查项首批 8~12 项范围确认、ROBOT Template 纳入 P2 评估、M-O14 里程碑段启用；§2.14 状态「方案 only」→「✅ 已立项」，P1 先行段挂 M-O14、开发排期待定；方案 26 v1.1；18 v1.30 | 董奎 |
| v0.42 | 2026-09-26 | **文档滞后集中清账（无新开发，逐项以代码/交付记录为证）**：①§4 P2 真实剩余移除「学习内容包深化后半（方法论深度版）」——REQ-90/91 已 v0.12 全量交付（b20a070），池条目系 v0.22 清账残留；②REQ-94② 推理对照状态回写（纪律 6 以实现为准）——引擎基座已实现（runtime-manager knownEngines 含 fuseki、ReasoningRuntime.StartWithReasoning 推理开关透传、RuntimePage 推理开关与 FUSEKI_BIN 手动指引透出），而「同查询并行 diff 高亮」对照界面未落地（现口径=同本体建两套方案一开一关人工对照，RuntimePage 配置文案即此指引），REQ-94 行状态同步；③§4 P3 WebVOWL 行标注已随 M21 VIZ-3 交付（8d25e17，见 v0.32；04 v0.27 同步） | 董奎 × 协作 Agent |
