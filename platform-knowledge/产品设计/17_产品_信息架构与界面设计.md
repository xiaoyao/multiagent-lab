---
module: 产品设计
topic: 界面口径事实源
desc: 平台北极星与产品原则、五栏/导航 IA、构建路径分类学、用户旅程（REQ-111/112）
synced: 2026-09-26
---

# 17_产品_信息架构与界面设计

> **版本：** v0.22（2026-09-26）｜**作者：** 董奎 × 智能体（运维本体开发助理）
> **定位：** 产品设计层——回答「界面给什么能力、能力如何组织、用户如何走」。需求（01/03/11）说 what 与验收，本文档说 how-to-organize，14/12/代码说 how-to-build。**界面口径以本文档为事实源；与 01/03/11 冲突时以需求文档为准并回提本文档修订。**
> **起草说明：** §2~§6 以收拢既有设计为主（01 §3、D-O11/REQ-104、14 §4/§5、REQ-07/107）；§3.2/§3.3（构建路径分类学与统一收敛）、§2.2（模块角色矩阵）、§7~§8（旅程与动线）为本轮新增规划；v0.2 起REQ-111/112 已拍板转正（回写 01 §3.9/§3.1，D-O16/G-3）；本档为界面口径事实源，需求事实源仍在 01/03/11。

## 0. 产品核心目标与设计原则

**平台级定位（v0.2 裁决，双主线）**：本产品是「多智能体工程 + 本体工程」**双主线学习平台**——A 线对应 01 §1.2 学习目标 LG-1~16（多智能体应用工程化），B 线为本体工程全流程（03）；知识库/技能为两线共用的能力扩展，项目/设置为工作空间与配置支撑。下述核心目标表述**限定于本体主线（B 线）的模块内叙事**：

**产品核心目标（B 线·本体）：本体的构建、运行、使用。** 平台一切模块围绕这个目标分工——有的模块生产本体，有的模块让本体跑起来，有的模块消费本体，其余模块为这三段提供工作空间、能力扩展与配置支撑。界面组织的评判标准只有一条：**是否让开发者学习者更快走通「构建→运行→使用」的完整闭环，并理解每一步为什么这样做。**

产品原则（界面决策依据，成文防止隐性语境丢失）：

| # | 原则 | 含义 | 已有体现 |
| --- | --- | --- | --- |
| P1 | 学习动线优先于工具效率 | 默认页是学习中心而非工作台；先看骨架与示例，再动手 | REQ-104 学习中心为默认页 |
| P2 | 渐进披露 | 每层界面只暴露当前阶段需要的能力；进阶配置收进抽屉/详情页，不铺满首屏 | 七阶段路径条、构建路径分层徽标 |
| P3 | 示例先行 | 每个能力都有可运行的示例本体/任务卡，先看效果再理解原理 | REQ-91 四示例本体 + 任务卡 |
| P4 | 诚实标注 | 未工程化的能力显示引导卡而非空壳交互；教学简化与工业级差距明示 | 构建路径状态徽标、GraphRAG「前期够教学」标注 |
| P5 | 试错安全 | 鼓励故意犯错观察系统反应；所有破坏性操作二次确认 | REQ-91「故意制造不一致看推理机报什么」、删除二次确认 |
| P6 | 过程可观测 | Agent/工具/检索的中间过程对学习者可见，不黑箱 | REQ-13/17/18 过程时间线 |

## 1. 全局信息架构

### 1.1 顶部导航（现状与收敛）

**当前实现**（TopNav）：智能体 / 项目 / 本体 / 知识库 / 技能 / **平台知识**（REQ-116/161，2026-09-24 新增、2026-09-25 自「参考资料」更名——模块化定位/原理/资料的学习入口）六项 + 设置页。**01 REQ-99 原规划**为六模块（含 OpenOntologies 独立导航，未实现）。

**收敛决议（REQ-111，已拍板转正——2026-09-23 D-O16/G-3，01 REQ-99 已修订）**：顶部导航**维持五项**；OpenOntologies 不设独立导航——其双轨能力（01 REQ-100）由本体模块两处既有入口承载：构建栏「Open Ontologies 流程」（引导+产物回流）与运行栏「Open Ontologies 引导页」（独立托管工作台入口）。理由：本体是产品核心主线，OO 只是构建/运行的一种方式，独立导航会割裂「六路径对照」的教学叙事；双轨可摘除原则不变，仅导航归位。设置保持独立页（连接配置：模型/向量/embedding）。**实现注**：现状 TopNav 即五项，本决议使文档与实现对齐。

### 1.2 模块角色矩阵

| 模块 | 构建（本体从哪来） | 运行（本体怎么活） | 使用（本体怎么被消费） |
| --- | --- | --- | --- |
| **本体**（核心枢纽） | ★ 六条构建路径（§3.2） | ★ 运行方案管理：Oxigraph/Fuseki/OO 引导/Cayley 占位 | 资产形态出口：spec_json/TTL/MCP（onto_*） |
| **知识库** | ★ 由 KB 构建本体（REQ-108 三策略） | GraphRAG 的 KG 即本体资产 TTL 装载（TTL 即 KG） | RAG/GraphRAG 检索增强对话 |
| **智能体** | OntoChat 参照的对话基础设施 | 消费运行方案的最终场景 | ★ 对话挂载 running 方案（REQ-19c）、onto_* 过程可见（REQ-18） |
| **项目** | 工作空间：目录绑定/文件/Git 视图（REQ-101/102） | — | — |
| **技能** | — | — | 智能体能力扩展（提示词/工具包装） |
| **设置** | — | — | 连接配置（模型/向量/embedding），使用的前置 |

### 1.3 布局范式

- **统一两栏范式**：各模块「左列表 + 右主区」（01 REQ-99 v0.7 定稿，对标简化版 Coze 工作台）。
- **本体模块特例**：本体视图由本体模块独立实现，经主平台**反代同源嵌入**（01 REQ-08a）；其内部为「左侧边栏五栏 + 右主区」（§3.1）。
- **对话详情区**：右侧对话窗口 + 可收起 SidePanel（activity bar 小图标切换文件/Git/配置视图，REQ-102/103）。

### 1.3 界面视觉基线口径（v0.2 补）

本文档承载**信息架构与动线口径**（结构层）；**视觉与交互细节**的基线为**运行实例本身 + 各交付的 headless 截图**（随迭代记录/PR 归档）——原 `prototype/05` 交互原型已与实现严重脱节，2026-09-23 整理轮移除（历史见 git），本档自此为界面口径唯一事实源。暂不制作线框图墙——待界面形态稳定后评估补「页面缩略图墙」一节（触发条件：模块首屏因 REQ-111/112 改造定型）。

### 1.4 知识三层职责（v0.4 补，回答"平台知识与 docs/research 什么关系"）

| 层 | 位置 | 回答的问题 | 读者 |
| --- | --- | --- | --- |
| 决策与规格层 | `docs/`（01/02/03/04/11 + 17 本档） | 是什么、为什么、验收口径 | 协作开发者 / AI（权威事实源，活文档） |
| 立项依据层 | `research/` | 当初为什么选它不选它 | 偶尔考古（只增不改，已归档） |
| 学习视图层 | `platform-knowledge/`（2026-09-25 自 seeds/learning/reference/ 迁入） | 模块怎么理解、怎么用、该去读哪篇 | 学习者 / 快速上手者（页面渲染，REQ-116/161） |

**裁决：reference 是模块知识库的「学习层 + 索引层」，不做 docs 的全量镜像**——镜像会重新制造第二事实源与漂移（prototype 前车之鉴）。三机制保证它既指导学习又不失真：①每篇头部 frontmatter 源指针（req/docs/decisions/synced）＝模块知识地图；②固定骨架（产品定位/设计原理/相关资料）＝渲染与写作契约；③AGENTS.md 纪律 7＝语义级变更强制同步（渲染检查在 20 号 S1.4）。页面内不直接渲染 docs 全文：受众、结构、耦合均不合适；未来若做 docs 只读文件服务属独立增量。

## 2. 本体模块（核心资产枢纽）

### 2.1 侧边栏五栏（收拢 D-O11/REQ-104 + D-O15）

`sidebarKey: learn | build | assets | runtime | audit`：

| 栏 | 定位 | 关键能力 |
| --- | --- | --- |
| **学习中心**（默认页） | 学习动线主入口 | 七阶段学习路径条（S1→S7 理论骨架 REQ-74；REQ-162 改造为一排方块+箭头步骤条）+ 方法论卡（REQ-90）+ 任务卡打卡（`task:`/`tool:` 双 key，REQ-76）+ 构建方式对照（同示例 × 六路径）+ 运行方式对照 + 外部资源导航（REQ-109；**REQ-162 迁移至平台知识模块**——原学习中心第 5 Tab 藏得过深）+ 工具链配置（REQ-75） |
| **本体构建** | 六路径生成本体 | `buildPath: custom | ontochat | kg | ontoextend | oo` + 第 6 条「由知识库构建」（§3.2）；分层状态徽标；引导卡不做空壳 |
| **本体资产** | 全部已构建本体统一管理 | 列表 + 详情工作区 Tabs：Spec 编辑 / 校验 / 版本（源码视图 REQ-93）/ 产物 / 可视化（ReactFlow）/三维浏览（REQ-154）/ TTL 导出 |
| **本体运行** | 按运行方式分组的方案管理 | 引擎分组：Oxigraph / Fuseki（推理对照 REQ-94）/ Open Ontologies 引导页 / Cayley 占位；方案向导 + 详情 + 端点 |
| **消费与审计**（第五栏，D-O15；**D-O19 边界厘清**） | 本体消费侧观测台（KG 检索限定本体 TTL 装载来源，带来源徽标；KB 抽取 KG 归知识库侧） | KG 增强检索展示（向量命中→一跳扩展）、SQLite 决策表溯源、PROV-O 导出、ReactFlow 图 |

### 2.2 构建路径分类学（六条路径的分类、规划与统一）

**分类维度**：本体从哪来（来源）× 怎么生成（交互形态）。六条路径各占一格，互不重叠：

| # | 路径 | 来源 | 交互形态 | 产物 | 状态 | 关键需求 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | **自定义构建** | 人类手工 | 表单向导（S1~S4 四步） | spec_json | ✅ 可用（教学基准路径） | REQ-61~64 |
| 2 | **OntoChat 流程** | AI 对话生成 | 多轮对话流（CQ 引导→草稿→校验循环） | spec_json | ◐ 部分可用 | REQ-103 模式 A |
| 3 | **由知识库构建** | 知识资产转化 | 表单向导（选库→选策略→CQ 模式→草稿→校验） | spec_json | ◐ 部分可用 | REQ-108 三策略（A：chunk→LLM 抽取 / B：KG→直转 / C：混合） |
| 4 | **Open Ontologies 流程** | 外部本体引入 | 引导卡 + 独立工作台 + 产物回流 | TTL→导入 | ◐ 引导卡（P1 手工导入） | REQ-78/100 |
| 5 | **OntoExtend 流程** | 方法论参照 | 引导卡（方法论+论文指引） | —（不产本体） | ○ 引导卡，工程化入需求池 | 登记簿 🔭 |
| 6 | ~~semantica 流程~~ | ~~KG 消费~~ | — | — | ✕ 已退役（D-O15）：KG 消费方向由第五栏继承，KG→本体逆向由路径 3 策略 B 吸收 | REQ-99/110 |

**规划原则**：路径间按「来源」正交——同一本体可以用任意路径生成，也可以多路径交叉验证（同一示例本体 × 六路径对照卡是学习中心的核心教具）。新路径立项先回答「来源是否与既有路径重叠」（D-O14 拍板先例）。

### 2.3 最终统一：差异只在生成段，收敛在资产段

**所有路径殊途同归**——生成方式不同，生成之后完全相同：

```
六条路径（各自生成/引导方式）          统一收敛层
┌─────────────────────┐
│ 1 手工向导            │──┐
│ 2 OntoChat 对话流      │──┤
│ 3 由 KB 构建（三策略）  │──┼──→ 本体资产（spec_json 主形态 + TTL 导出）
│ 4 OO 产物回流          │──┤      ↓ 统一能力（路径无关）
│ 5 OntoExtend 引导      │──┘      校验（S3，含推理对照）→ 可视化（ReactFlow）
└─────────────────────┘      → 版本/源码视图 → 运行方案挂载 → MCP onto_*
                                → 对话消费（REQ-19c）→ 消费与审计（第五栏）
```

这是教学价值的关键设计：**学习者沿任意一条路径走通后，后半程（资产→运行→使用→审计）的技能完全迁移**。学习中心的七阶段理论骨架（REQ-74）即这条统一链路的概念化表达。

## 3. 知识库模块（数据源与语义场景）

### 3.1 IA（收拢 REQ-07/107）

左栏库列表（名称/RAG·GraphRAG 模式徽标）+ 右栏详情；详情页内双子模块 Tab（REQ-107/D-KB4）：

| 子模块 | 能力 |
| --- | --- |
| 向量检索 RAG | 文档接入（粘贴/.txt/.md）→ 切分 → 向量化（Qdrant 主/SQLite fallback）→ 索引状态 → 试运行检索（TopK/阈值可见） |
| GraphRAG 检索 | graphrag 导入即建 KG（自研：REQ-98 LLM 抽取 + SQLite 三表）→ KG 增强检索（degraded 不阻断）；**M16 已全量交付（11 v0.9/REQ-127~130，2026-09-24）**：图谱浏览与统计视图、实体聚焦与多跳检索、抽取治理与人工审核、社区摘要与全局问答 |

### 3.2 与本体的双向关系

- **KB → 本体**（构建方向）：REQ-108——KB 作为本体构建数据源，入口在构建栏第 6 路径。
- **本体 → KB**（消费方向）：TTL 即 KG（D-O15）——本体资产 TTL 直接装载为 GraphRAG 检索的 KG 源，SPARQL 一跳扩展。
- 两方向合起来构成「文本 → 不同形态资产」的完整矩阵（D-O14 语）。

## 4. 智能体模块（使用的最终场景）

### 4.1 IA（收拢 01 §3）

- **左栏**：两级折叠树（Agent → 其名下对话）；项目树同构（项目 → 对话）。
- **右栏对话详情**：消息气泡区分角色（REQ-11）+ 流式输出（REQ-12）+ 过程时间线（工具调用/Agent 委派/技能注入 REQ-13、本体查询 REQ-18、知识召回 REQ-17）+ 对比模式（REQ-19e/f 多窗格，✅ 已实现 v0.15；✅ REQ-150 系统化 2026-09-26：头部「对比」开关 + 2~4 列窗格（Splitter 分隔可拖宽）+ 窗格头承载 全量配置徽标（模型/库/方案/技能，覆盖态高亮显值名/继承态浅色）+ 状态徽标（待提问/生成中/完成/出错/已停止）+ Agent logo/名称与采纳；共享输入区 chips 收起为摘要不参与配置）。
- **输入区工具条**：三枚 chip（知识库/本体/技能，REQ-19a/c/g）——对话级开关，配置随对话保存；本体 chip 选 running 运行方案（REQ-19c），不可达降级提示（REQ-19d）。
- **Agent 配置**：SidePanel 配置视图（REQ-103 智能体侧），弹窗仅保留新建；配置视图按**四分类分区**组织（基本 / 模型与参数 / 能力 / 对外服务，REQ-132/131 v0.7），详见 §4.3。

### 4.2 本体消费入口（核心目标「使用」段）

对话挂载 running 方案 → Agent 经 MCP 调用 onto_* 工具 → 入参/结果出现在过程时间线（REQ-18）→ 第五栏审计可回看。**这是全产品「本体被使用」的唯一对话入口，IA 上必须保证：从任一对话 3 次点击内可达「选方案」**（现状：chip→单选列表，满足）。

### 4.3 Agent 创建与配置分级（REQ-132/131，v0.7 新增；✅ 已排期 M18）

**SidePanel 配置视图——四分类分区**（面板内页签/锚点切换；沿用项目侧 ProjectSidePanel 的 activity bar 视觉范式，配置项收纳后主区不再长表单滚动）：

| 分区 | 内容 | 关联需求 |
| --- | --- | --- |
| ① 基本 | 名称、描述（多智能体委派路由依据）、头像颜色、系统提示词、审批策略 | REQ-20/21/22/26、REQ-14 |
| ② 模型与参数 | 模型连接覆盖（空=跟随全局默认）、温度、最大 tokens、最大迭代、执行后端、推理后端 | REQ-42/123、REQ-25、REQ-122、D-O13 |
| ③ 能力 | 工具白名单（事件卡 source 徽标口径：builtin/skill/mcp）、技能勾选（带注入预览）、MCP servers 多行编辑 | REQ-24、REQ-121/123 |
| ④ 对外服务 | REQ-131，**默认关闭**：暴露开关、工具名（默认 agent_{id}）、Token（生成/重置/掩码显示）、统一端点展示（/mcp，Streamable HTTP）+ 复制、调用示例说明 | REQ-131 |

- 对外服务开启后：对话头部徽标提示「对外服务中」；REQ-117 装配快照含 serve 段；关闭即从 /mcp 工具表撤出。
- 安全口径（界面层）：Token 仅创建/重置时完整显示；tool_approval≠off 的 Agent 在「对外服务」分区提示「server 会话内需审批工具将被默认拒绝」。

**创建弹窗 AgentModal——分级创建**：

- **基本必填区（创建即可用）**：名称（必填）、描述（建议填写）、系统提示词、模型连接（留空=跟随全局默认）——填写即可创建。
- **高级配置（Collapse 默认收起）**：采样参数与最大迭代、执行/推理后端、工具白名单、技能、MCP servers、审批策略、对外服务。
- **默认值口径**：创建即可对话——跟随全局模型 + inprocess + 审批 off + 无技能/MCP + 对外服务关；创建后一律经 SidePanel 修改（§4.1「弹窗仅保留新建」口径不变）。
- **v0.8 增补（REQ-133/134；134 已排期 M18）**：配置界面（AgentModal/AgentSidePanel/项目配置）组件化重构——一律复用 pro-components/AntD 既有组件，不手搓（REQ-134）；本地目录配置口径见下条。
- **v0.9 修订（REQ-133 主场景，主人指正）**：实际异常场景=**后端 Linux 部署 + Web 在 Windows 打开 + 目录为 Windows 本机目录**——界面状态展示与后端 validate-dir 分字段结果直连（路径格式/存在/是目录/后端可达），前端不按 POSIX 规则推断 Windows 盘符路径；选目录以手输+校验反馈为主（远程部署下浏览器取不到绝对路径、后端弹不了用户侧系统对话框），**同机部署才走点击唤起系统文件管理器**；本地伴生组件/目录挂载共享为可选增强。技术方案见 02 v0.49 §6.17。
- **v0.10 实现注记（REQ-133 交付）**：①分字段直连落地为共享组件 `DirCheckResult`（新建项目弹窗与项目配置侧边栏共用）——字段渲染顺序：路径格式合法（红/绿 Tag）→ 后端不可达（蓝 Tag + 远程目录说明文案，存在性未知不渲染存在/目录 Tag）→ 存在/目录/Git 信息（仅后端可达时）；格式非法整体渲染「目录检测未通过」错误卡（含后端原因文案）；②「选目录」按钮置于「检测」右侧（title 说明仅同机部署可用），系统目录对话框成功后自动回填并触发检测，失败 toast 提示手输。
- **v0.11 增补（REQ-135~140 已交付；141/142 排期 M19）**：①对话窗口配置入口整合——顶部「配置按钮」与「配置图标」重复，去掉按钮，配置入口改为右侧边栏收放按钮（REQ-135）；对话级分级配置开关控制运行过程（展示粒度/审批/细节可见性，对话级>Agent 级>全局），过程时间线折叠分组降噪；②对话自动命名：首轮问题经默认大模型提炼为对话名，异步不阻塞（REQ-136）；③非内置后端智能体沿用其原 logo，未配置回退默认（REQ-137）；④平台主标题「多智能体学习平台」→「**智能体构建平台**」（REQ-138，品牌 AgentLab 不变；v0.12 主人实时指示修正定名）；⑤学习中心卡片深度版收起保留、内容加详更直观（REQ-139）；⑥内部方案文档链接支持点击只读查看（REQ-140）。
- **v0.12 实现注记（REQ-138 交付）**：主标题定名「**智能体构建平台**」（主人实时指示，修正 v0.11 立项文本的「多智能体构建平台」）——已交付（9bf8ca2）：TopNav brand-sub、index.html 页面标题、参考资料 overview.md 定位行、README 标题（顺带补齐 AgentLab 品牌）；headless 渲染断言通过。
- **v0.13 增补（REQ-143/144，已排期 M19）**：对话对比扩展——窗格头「智能体」选择器（跨 Agent 对比，名称+logo 标识；默认共享对话历史，窗格级可关）与窗格「更多配置」剖面区（推理参数/系统提示词改写/技能开关；剖面对话级命名保存，「复制上一窗格配置」，可选「采纳窗格配置」继续对话）；交互增量，不动对比布局与停止语义。
- **v0.17 增补（REQ-155~158 立项，方案 only）**：本体开源方案吸收落地——①构建/资产/运行三栏多方案**并存**呈现：路径内方案卡片并列+来源徽标+落地栏标记（分组结构化，一目了然）；②资产详情新增「质量卡」页签（门禁 pass/fail+检查项明细，REQ-156）；③本体导入审查向导（三策略选择+冲突清单+前缀协调+diff 预览，REQ-157）；④运行栏「方案生命周期面板」（plan/apply/drift 状态视图，Phase3，REQ-155）；⑤参考资料页「技术研究」专栏+本体栏入口（REQ-158，内容单源 external-resources §六 18 方案卡片）。
- **v0.21 实现注记（M10 10b 交付，9b0d45c 2026-09-25 代码先行）**：SidePanel「模型与参数」分区的**执行后端**自禁用 Input 点亮为 Select（inprocess/docker，REQ-122）；选 docker 后展示**沙箱资源字段**（内存/CPU，迁移 020 `sandbox_memory`/`sandbox_cpus`，空/0=默认 512m/1CPU）+ **SandboxPanel 卡片**（10s 轮询状态徽标「容器运行中/未运行」+ 启动/停止按钮；未配 SANDBOX_IMAGE 显式 400 提示，非 docker 后端拒绝启动）。诚实边界：容器实机动线需 Docker 环境（10a 同）；未配置 SANDBOX_IMAGE（如本机无 Docker）时面板显「沙箱后端未启用」信息提示卡，不阻断其余配置。

## 5. 支撑模块

- **项目**：左栏项目树 + 右侧 SidePanel（文件/Git/配置三视图，REQ-101/102/103）；对话产物写入绑定目录。
- **技能**：左栏筛选（全部/已挂载/未挂载）+ 右栏列表（REQ-08）；会话级开关由对话 chip 承载（REQ-19g）。
- **设置**：模型/向量/embedding 连接配置；知识库、本体运行的前置依赖，空配置时各模块引导跳转（REQ-19b 范式）。

## 6. 页面能力清单全景

| 页面 | 顶层归属 | 核心能力 | 承接需求 |
| --- | --- | --- | --- |
| AgentsPage | 智能体 | Agent/对话折叠树、对话窗口、SidePanel | 01 REQ-01~23 |
| ProjectsPage | 项目 | 项目树、目录绑定、SidePanel 三视图 | 01 REQ-03/04/101/102/103 |
| OntologyModule（五栏 7 页） | 本体 | Learn/Build×6 路径/Assets/Runtime/Audit | 03 REQ-60~110 |
| KnowledgePage | 知识库 | 库列表、RAG/GraphRAG 双 Tab、试运行 | 11 REQ-07/50~59/107 |
| SkillsPage | 技能 | 技能 CRUD、挂载筛选 | 01 REQ-08 |
| SettingsPage | 设置 | 连接配置 | 01 REQ-2x 连接类 |

## 7. 核心用户旅程（三条动线）

**J1 首次上手（目标 15 分钟建立全局感）**：打开平台 → 学习中心（默认页）看七阶段骨架 → 选示例本体 k8s-ops → 资产详情看 Spec/可视化 → 运行栏确认默认方案 → 回学习中心做第一张任务卡打卡。引导要点：无任何数据时各模块空态显示引导卡（REQ-112）。

**J2 构建→运行→使用（完整闭环，B 线主动线）**：构建栏任选一路径（推荐由 KB 构建看 AI 生成）→ 草稿 → 校验循环（故意留一次不一致看推理报错，P5）→ 入资产 → 运行方案（Oxigraph）→ 对话 chip 挂方案 → 提问看 onto_* 过程时间线 → 第五栏复盘消费记录与 PROV-O 溯源。

**J3 知识资产闭环（KB 与本体双向）**：建 KB 传文档 → RAG 试运行看召回 → GraphRAG 导入建 KG → 构建栏第 6 路径用策略 B 从 KG 直转本体草稿 → 校验入库 → 对话同时挂 KB+本体 → 对照两种召回质量的差异。

**J4 智能体工程主线（A 线，v0.2 补——对应 01 SC-1/3/5/7/10 与 LG-1~16）**：设置页从厂商预设建模型连接（REQ-105/106，填 Key 启用）→ 建 Agent（提示词/工具/技能挂载）→ 直聊观察 ReAct 与工具事件（LG-1/2/4）→ 建 Agent 级覆盖与 MCP（REQ-123，P2）→ 创建项目配置多 Agent 协作，观察委派/切换事件（LG-6）→ 开对比模式同问不同模型/知识库（SC-10，LG-3/8）→ 设置页看使用统计复盘 token（REQ-49）。**与 B 线交汇点**：对话 chip 挂 running 方案（§4.2，L2）。

### 7.1 学习目标 → 产品触点映射（A 线，v0.2 补）

> 01 §1.2 LG 表只给了"对应功能落点"；本表把 A 线学习目标映射到**可操作的界面触点与旅程步骤**，补齐"学习成果如何被产品承载"的缺口。B 线（LG-12/14）触点即学习中心任务卡体系（03 REQ-90/91），此处不重复。

| LG | 学习目标（摘要） | 产品触点 | 旅程 |
| --- | --- | --- | --- |
| LG-1/2 | Eino 抽象 / ReAct 循环与事件流 | 对话窗口过程时间线（工具/子Agent/技能事件卡） | J4 |
| LG-3 | 会话记忆与持久化 | 折叠树多对话 + 历史还原 | J4 |
| LG-4 | 自定义 Tool | Agent 配置工具勾选 + 工具事件卡 | J4 |
| LG-5 | Middleware/Callback | 使用统计（REQ-49）/ 耗时展示 | J4 |
| LG-6 | 多 Agent 协作 | 项目配置协作模式 + 委派事件时间线 | J4 |
| LG-7 | 确定性编排 | 项目工作流模式（P1，REQ-33） | J4 延伸 |
| LG-8 | Interrupt/Resume | 停止/恢复（REQ-14，P2 随池） | — |
| LG-9 | SSE 流式链路 | 打字机 + 事件渲染 | J4 |
| LG-10 | 配置驱动动态装配 | Agent/项目/连接配置即改即生效 | J4 全程 |
| LG-11 | RAG 最小闭环 | 知识库双子模块 + 对话召回卡 | J3/J4 |
| LG-13 | MCP 服务化 | Agent MCP servers 配置 + 本体 facade | J2/J4 |
| LG-15 | 沙箱化 | 执行后端选择 + 沙箱面板（REQ-122；M10 10a/10b 已交付 96210e6/9b0d45c，2026-09-25） | — |
| LG-16 | 技能化封装 | 技能 CRUD + 挂载 + 会话 chip（REQ-120/121） | J4 |

## 8. 跨模块动线契约

| # | 动线 | 契约 | 需求锚点 |
| --- | --- | --- | --- |
| L1 | KB → 本体构建 | 构建栏第 6 路径：选库（带模式徽标）→ 策略 → CQ 模式 → 草稿进入统一校验循环 | REQ-108 |
| L2 | 本体运行 → 对话挂载 | 仅 running 方案可挂；不可达降级不阻断对话 | REQ-19c/d |
| L3 | 本体资产 → KB GraphRAG | TTL 即 KG，TTL 直接装载为 KG 源 | REQ-110/D-O15 |
| L4 | 对话过程 → 审计 | onto_* 调用入 SQLite 决策表，第五栏可回看/导出 PROV-O | REQ-18/110 |
| L5 | 嵌入架构 | 本体视图独立实现、主平台反代同源嵌入；跨模块跳转用路由不跨 iframe | 01 REQ-08a |

## 9. 新增需求（REQ-111~115 已拍板转正，2026-09-23 回写 01 §3.9——D-O16/G-3）

| 编号 | 名称 | 需求描述 | 优先级 |
| --- | --- | --- | --- |
| REQ-111（已转正） | 全局导航收敛与模块首屏规范 | ①顶部导航维持五项，OpenOntologies 不设独立导航，双轨能力由本体构建路径 4 + 运行栏引导页承载（修订 01 REQ-99 六模块规划，REQ-100 双轨原则不变）；②各模块首屏空态统一引导卡规范——无数据时不留白，引导至学习中心/对应创建动作，全产品唯一 onboarding 主线是学习中心任务卡 | P2 |
| REQ-112（已转正） | 模块空态与首用引导 | 各模块首用引导矩阵：智能体无 Agent → 引导建第一个 Agent（附示例配置）；本体无资产 → 引导学习中心示例；知识库无库 → 引导建库+示例文档；对话 chip 无可用项 → 已有置灰+Tooltip（REQ-19b/d 范式推广） | P2 |
| REQ-115（已转正） | 三态与错误文案规范 | 全产品统一空态/加载态/错误态的组件与文案规范；错误文案三要素——发生了什么/可能原因/下一步动作（REQ-16/19b/19d/59 升级为全站规范） | P2 |

### 9.1 空态承载组件与实现注记（v0.6 补，REQ-112/111② 已实现）

- **空态统一组件 `EmptyGuide`**（`web/src/components/EmptyGuide.tsx`）：标题 + 编号引导要点（≤3 条）+ 主行动按钮 + 可选脚注。全站模块首用空态统一走本组件，禁止留白或仅一行灰字。
- **已接入**：智能体左栏（无 Agent → 三步建第一个智能体：命名/提示词 → 模型留空跟随默认 → 勾选工具）、项目左栏（无项目 → 协作模式/目录绑定授权提示/成员配置）、知识库左栏（无库 → 类型选择/导入/对话召回三步）。本体模块默认页即学习中心（自带引导，P1）；项目右栏空态已有 Result 引导卡；对话 chip 置灰+Tooltip 沿用 REQ-19b/d。
- **文案口径**：行动按钮统一「＋ 动词短语」；要点用学习者视角的动作句（不是功能罗列）；脚注放前置依赖说明（如「嵌入模型使用设置页默认向量连接」）。

### 9.2 三态与错误文案规范（v0.6 补，REQ-115）

| 态 | 承载 | 规范 |
| --- | --- | --- |
| 空态 | `EmptyGuide`（左栏）/ `Welcome`（对话首屏）/ `Result`（右主区大空态） | 不留白；引导动作直达创建/学习入口 |
| 加载态 | `Spin`（局部）/ Table `loading` / Button `loading` | 禁整页白屏；数据到达前显示骨架或 Spin，不用定长 sleep 冒充加载 |
| 错误态 | 轻提示 `showToast(err)` / 块级 `Alert(warning|error)` | 文案三要素：**发生了什么**（如「知识库检索失败」）＋ **可能原因**（如「嵌入模型连接停用/网络不可达」）＋ **下一步动作**（如「到设置-模型连接启用后重试」）；禁止静默吞错，接口未就绪给降级提示并保留手动路径（run.warning 范式推广） |

## 10. 开放问题（✅ 全部关闭，2026-09-23 缺口分析全项落实轮）

1. ~~**REQ-111 OpenOntologies 导航归位**~~ → **已拍板采纳**（五项导航 + OO 并入本体两入口）；01 REQ-99/REQ-100 已修订，登记为 D-O16/18 号注册表 G-3。
2. ~~**15 号文档重号**~~ → **已解决**：《15_本体_semantica集成方案》改号 19（存档）；全局编号治理见《18_REQ编号注册表》（含 REQ-90~96 冲突裁决：本体保留、01 侧迁移 120~126）。
3. ~~**第五栏审计 IA 细节**~~ → **已收口**：维持 D-O15 五栏约定，栏内信息组织按 P4「诚实标注」原则随后续迭代演进（D-O16 ②）。

## 11. 迭代记录

| 版本 | 日期 | 变更 | 作者 |
| --- | --- | --- | --- |
| v0.1 | 2026-09-23 | 首版起草：北极星与产品原则 P1~P6 成文；全局 IA（导航收敛建议+模块角色矩阵）；本体五栏收拢；构建路径六条分类学与「差异在生成段、收敛在资产段」统一模型；KB/智能体/支撑模块 IA 收拢；页面清单全景；三条用户旅程；五条跨模块动线契约；新增 REQ-111/112（起草态）；开放问题 3 项 | 董奎 × 智能体 |
| v0.2 | 2026-09-23 | **缺口分析全项落实**：①§0 增平台级双主线定位裁决（A 线智能体 LG-1~16 / B 线本体——原北极星限定为 B 线模块内叙事），补 J4 智能体工程旅程与 §7.1 A 线学习触点映射表；②REQ-111/112 转正（回写 01 §3.9；01 REQ-99 六模块口径同步修订）；③§10 三个开放问题全部关闭（OO 导航/15 重号改 19/第五栏收口→D-O16）；④§1.3 界面视觉基线口径：线框以 prototype/05 交互原型 + 运行实例截图为准（headless 截图随交付归档），本文档暂不做线框墙，待界面稳定后评估补「页面缩略图墙」 | 董奎 × 协作 Agent |
| v0.3 | 2026-09-23 | **整理轮**：§1.3 视觉基线改口径——prototype/05 原型移除（与实现脱节），本档为界面口径唯一事实源，基线=运行实例+交付截图。2026-09-23 文档整理轮（主人授权）：已完成需求细节压缩、过期/退役内容清除、prototype 原型移除——完整历史见 git 与本文档迭代记录；本轮不改变任何需求语义与验收口径。 | 董奎 × 协作 Agent |
| v0.4 | 2026-09-24 | **知识分层设计落地**：新增 §1.4 知识三层职责（决策规格层/立项依据层/学习视图层），裁决 reference 为模块知识库的学习层+索引层（非全量镜像）；配套——7 篇 reference md 加 frontmatter 源指针、页面文末渲染「源文档地图」、AGENTS.md 新增纪律 7（语义级变更强制同步）、20 号冒烟清单 S1.4 渲染检查 | 董奎 × 协作 Agent |
| v0.5 | 2026-09-24 | KB 模块 KG 增强（M16/REQ-127~130）IA 口径同步：GraphRAG 子模块能力规划标注（浏览/多跳/治理/全局问答）；跳过 v0.4 空号 | 董奎 × 协作 Agent |
| v0.6 | 2026-09-24 | **REQ-112/111② 空态引导 + REQ-115 三态规范交付**：新增 §9.1 空态承载组件 EmptyGuide 实现注记（智能体/项目/知识库三左栏已接入）与 §9.2 三态与错误文案规范（错误文案三要素）；§9 标题更新为 REQ-111~115 | 董奎 × 协作 Agent |
| v0.7 | 2026-09-24 | **REQ-132/131 配置页产品规划（方案 only）**：新增 §4.3 Agent 创建与配置分级——SidePanel 四分类分区（基本/模型与参数/能力/对外服务）+ AgentModal 分级创建（基本必填即建、高级配置默认收起）；§4.1 Agent 配置行同步指针；需求 01 v0.28、技术 02 v0.47 §6.13、注册表 18 v1.5 | 董奎 × 协作 Agent |
| v0.8 | 2026-09-24 | **「北极星」表述修正 + REQ-133/134 立项注记（方案 only）**：§0 标题与正文、§1 收敛决议、§4.2 标题、§7 J2 等处「北极星」统一改为「产品核心目标/产品核心主线/B 线主动线」（主人指正表述不当；迭代表历史行保留原貌）；§4.3 尾增补配置界面组件化与本地目录选择器口径（REQ-134/133）；需求 01 v0.29、技术 02 v0.48 §6.17、注册表 18 v1.6 | 董奎 × 协作 Agent |
| v0.9 | 2026-09-24 | **REQ-133 主场景修订注记**（主人补充：后端 Linux 部署 + Web 在 Windows 打开 + 目录为 Windows 本机目录）：§4.3 增补——状态展示与 validate-dir 分字段结果直连（不按 POSIX 规则推断 Windows 盘符路径）；选目录以手输+校验反馈为主，同机部署才走系统文件管理器，伴生组件/挂载共享可选；需求 01 v0.30、技术 02 v0.49 §6.17、注册表 18 v1.7 | 董奎 × 协作 Agent |
| v0.10 | 2026-09-24 | **REQ-133 实现注记**：§4.3 增补——分字段直连落地为共享组件 DirCheckResult（新建弹窗/配置侧边栏共用；字段顺序 格式→后端可达→存在/目录/Git，格式非法渲染「目录检测未通过」错误卡）；「选目录」按钮置于检测右侧，同机系统对话框回填自动检测、失败 toast 提示手输；需求 01 v0.31、技术 02 v0.50 §6.17、注册表 18 v1.8、冒烟 20 S3.6 | 董奎 × 协作 Agent |
| v0.11 | 2026-09-24 | **REQ-135~140 立项注记（方案 only）**：对话窗口配置入口整合（去重复配置按钮→右侧边栏收放）+ 对话级分级配置开关 + 时间线折叠降噪（135）；对话自动命名（136）；非内置后端智能体原 logo（137）；主标题→「多智能体构建平台」（138）；学习中心卡片内容加详（139）；内部方案文档点击只读查看（140）；141/142 为调研/研究待办；需求 01 v0.32、技术 02 v0.51 §6.18、注册表 18 v1.9 | 董奎 × 协作 Agent |
| v0.12 | 2026-09-24 | **REQ-138 口径修正 + 实现注记（主人实时指示）**：主标题定名「**智能体构建平台**」（修正 v0.11 立项文本的「多智能体构建平台」）；已交付（9bf8ca2）——TopNav brand-sub / index.html 页面标题 / 参考资料 overview.md 定位行 / README 标题（顺带补齐 AgentLab 品牌），headless 渲染断言通过；需求 01 v0.33、注册表 18 v1.10 | 董奎 × 协作 Agent |
| v0.13 | 2026-09-24 | **REQ-143/144 立项注记（方案 only）**：对话对比扩展——跨智能体窗格（窗格头 Agent 选择器+logo，默认共享历史/窗格可关）与配置剖面（更多配置折叠区+对话级保存+采纳转正可选）；需求 01 v0.35、技术方案 02 v0.52、注册表 18 v1.11 | 董奎 × 协作 Agent |
| v0.14 | 2026-09-24 | 方案 only 全量排期状态同步：§4.3 与各立项注记改「已排期」（M18/M19） | 董奎 × 协作 Agent || v0.15 | 2026-09-24 | **REQ-19e/19f 对话对比模式交付注记**：§4 右栏对话详情行标注已实现——「对比」开关（快捷切换条）+ 2~4 列横向等分窗格 + 窗格头覆盖徽标（模型/库/方案 · 继承或覆盖名）+ 窗格底部三项独立配置选择器（空=继承，Q-6 不追溯）+ 共用输入框与组级停止；J4 的「开对比模式同问不同模型」环节自此可演示（SC-10）；需求/方案侧 01 v0.37 / 02 v0.54 | 董奎 × 自动化执行者 |
| v0.15 | 2026-09-25 | REQ-150 对比界面系统优化立项注记（待开发）：对比模式下共享输入区 chips 收起为摘要、配置承载迁各窗格头（含技能新增覆盖项 + 继承/覆盖状态显性化）、窗格视觉系统化；详见 01 v0.39 §3.2.1 | 董奎 × 文档会话 |
| v0.16 | 2026-09-25 | 资产详情可视化区口径补三维浏览 Tab（REQ-154/M21 立项，只读沉浸浏览与 2D 并列切换） | 董奎 × 文档会话 |
| v0.17 | 2026-09-25 | REQ-155~158 立项注记（本体开源方案吸收落地，方案 only）：多方案并存呈现原则（方案卡片+来源徽标+落地栏标记）、资产详情「质量卡」页签、导入审查向导、方案生命周期面板、参考资料「技术研究」专栏 | 董奎 × 协作 Agent |
| v0.19 | 2026-09-25 | **平台知识结构基座交付（REQ-161，主人指令「整理项目文档」）**：本文档迁 platform-knowledge/产品设计/（docs/ 留开发辅助活文档）；导航更名参考资料→平台知识（TopNav/页面侧栏，PageKey 不变）；学习导读 8 篇自 seeds/learning/reference/ 迁入 platform-knowledge/ 各模块目录；页面目录驱动二级主题树；文档互引相对路径化（点击→右侧 Drawer）；前端交互完善与构建验证主人本地开发 | 董奎 × 文档会话 |
| v0.18 | 2026-09-25 | **参考资料→平台知识升级立项注记（REQ-161/M25，待开发）**：①更名——模块名「参考资料」→「平台知识」（学习视图层定位，17 §1.4；导航 label 同步）；②左栏二级主题树——L1 模块 / L2 主题页（可收缩，AntD SubMenu 原生）；③首批内容矩阵——智能体 5 题（含 Eino ADK/dsh 适配器/沙箱）/ 本体 5 题（含开源项目调研集）/ 知识库 4 题（RAG/GraphRAG/选型对比）/ 项目·技能·设置·总览保留扩充；内容源=research/ 调研 + docs/ 模块档 | 董奎 × 文档会话 |
| v0.19 | 2026-09-25 | **REQ-162/163 立项注记**：学习中心七阶段改方块+箭头步骤条、外部资源迁平台知识（REQ-162）；第五栏消费与审计 D-O19 边界厘清（KG 检索限定本体 TTL 装载来源徽标） | 董奎 × 文档会话 |
| v0.20 | 2026-09-26 | **REQ-150 对比界面系统优化交付注记（M19 阶段三）**：§4.1 右栏对话详情行更新——对比窗格头承载全量配置徽标（模型/库/方案/技能，覆盖/继承显性化）+ 状态徽标五态 + Splitter 可拖宽；共享输入区 chips 收起为摘要；需求 01 v0.49 / 方案 02 v0.68 | 董奎 × 自动化执行者 |
| v0.21 | 2026-09-26 | **M10 10b 交付回写**（9b0d45c，2026-09-25 代码先行；回写领取）——§7.1 LG-15 行去「M10 未激活」→ 10a/10b 已交付；§4.3 增 v0.21 实现注记：SidePanel「模型与参数」执行后端 Select（inprocess/docker）点亮 + docker 态沙箱资源字段（内存/CPU，迁移 020）+ SandboxPanel 卡片（状态徽标 10s 轮询/启停/未配 SANDBOX_IMAGE 显式提示）；需求 01 v0.50 / 方案 02 v0.69 | 董奎 × 自动化执行者 |
| v0.22 | 2026-09-26 | **文档滞后清账**：§3.1 GraphRAG 行「M16 规划中（11 v0.6）」→「M16 已全量交付（11 v0.9/REQ-127~130，2026-09-24）」——图谱浏览统计/多跳检索/抽取治理/社区摘要与全局问答均已上线；知识同步：platform-knowledge/知识库/知识库模块.md 同步（11 v1.0 KB-O4 关闭轮，synced 2026-09-26） | 董奎 × 协作 Agent |
| v0.23 | 2026-09-26 | **平台知识页侧栏组织规划 + 互引阅读修复交付（REQ-161/169，主人四点反馈）**：①侧栏菜单区自身滚动（分组全展开超出视口可滚）；②L1 分组改规划序——平台总览置顶 → 五业务模块按导航栏顺序 → 平台级专题（产品设计/DeepSeek Harness/外部资源）→ 设置殿后；组内「模块导读」置顶、专题按文档编号升序、展示标题去编号前缀；③右侧阅读抽屉默认关/点击互引才展开/可手动关；④互引点击全链修复——根因：DocViewerModal 误作 Splitter 直接子元素被吞成空白面板（「右侧默认空白栏」与「链接无反应」共同根因），移出 Splitter；抽屉内 XMarkdown 补 openLinksInNewTab（默认渲染器对中文 href 做 encodeURI 致后端按字面路径找不到文件）+ resolveRef 百分号编码防御解码 + 抽屉内互引可续点跨目录跳转 + 抽屉剥 frontmatter 头；headless Chrome 四项实测过（截图 smoke/req169/） | 董奎 × 协作 Agent |
| v0.24 | 2026-09-26 | **平台知识页 L1 收敛七组 + 三处跨目录挂载（REQ-169 二轮，主人指定）**：L1 由十组收敛为七组（平台总览/智能体/项目/本体/知识库/技能/设置）；产品设计（17 号）挂平台总览二级、DeepSeek Harness 挂智能体组、外部资源导航（本体学习外部资源导航，seeds 单源）挂本体组——TOPIC_MOUNT 显式映射，互引解析 base 仍按真实存放目录不受挂载影响；组内主页置顶规则扩展（模块导读/平台总览）；headless Chrome 结构断言+挂载页渲染实测过（截图 smoke/req169/ 06/07） | 董奎 × 协作 Agent |